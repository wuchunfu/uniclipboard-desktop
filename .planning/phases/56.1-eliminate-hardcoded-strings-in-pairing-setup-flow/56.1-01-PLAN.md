---
phase: 56.1-eliminate-hardcoded-strings-in-pairing-setup-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/crates/uc-core/src/network/daemon_api_strings.rs
  - src-tauri/crates/uc-core/src/network/mod.rs
autonomous: true
requirements:
  - PH561-01
  - PH561-02

must_haves:
  truths:
    - 'daemon_api_strings.rs exists in uc-core/src/network/ and compiles'
    - 'All five constant submodules (ws_topic, ws_event, pairing_stage, pairing_busy_reason, pairing_error_code) are defined'
    - 'Value assertion tests pass for every constant'
    - 'Module is re-exported from network/mod.rs'
  artifacts:
    - path: 'src-tauri/crates/uc-core/src/network/daemon_api_strings.rs'
      provides: 'Shared daemon wire-protocol string constants'
      contains: 'pub mod ws_topic'
    - path: 'src-tauri/crates/uc-core/src/network/mod.rs'
      provides: 'Re-export of daemon_api_strings'
      contains: 'pub mod daemon_api_strings'
  key_links:
    - from: 'src-tauri/crates/uc-core/src/network/daemon_api_strings.rs'
      to: 'uc-daemon and uc-daemon-client crates'
      via: 'pub const imports'
      pattern: 'pub const .+: &str'
---

<objective>
Define all daemon wire-protocol string constants in a new uc-core module with value assertion tests.

Purpose: Create the single source of truth for WS topic names, WS event types, pairing stage labels, pairing busy reasons, and HTTP error codes so that uc-daemon (server) and uc-daemon-client (consumer) can import from one location instead of defining strings independently.
Output: `daemon_api_strings.rs` with 5 submodules and exhaustive value assertion tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-CONTEXT.md
@.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-RESEARCH.md

<interfaces>
<!-- Existing pattern to follow -->

From src-tauri/crates/uc-core/src/network/protocol_ids.rs:

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ProtocolId {
    Pairing,
    PairingStream,
    Business,
    FileTransfer,
}

#[cfg(test)]
mod tests {
    use super::ProtocolId;

    #[test]
    fn protocol_id_strings_match_expected_values() {
        assert_eq!(ProtocolId::Pairing.as_str(), "/uc-pairing/1.0.0");
        // ...
    }
}
```

From src-tauri/crates/uc-core/src/network/mod.rs:

```rust
pub mod connection_policy;
pub mod events;
pub mod paired_device;
pub mod pairing_state_machine;
pub mod protocol;
pub mod protocol_ids;
// daemon_api_strings will be added here
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create daemon_api_strings.rs with all constant submodules and value assertion tests</name>
  <files>src-tauri/crates/uc-core/src/network/daemon_api_strings.rs, src-tauri/crates/uc-core/src/network/mod.rs</files>
  <read_first>
    - src-tauri/crates/uc-core/src/network/protocol_ids.rs (style precedent for wire-protocol constants and value assertion tests)
    - src-tauri/crates/uc-core/src/network/mod.rs (where to add re-export)
    - src-tauri/crates/uc-daemon/src/api/ws.rs (lines 26-50: source of truth for WS topic and event type values)
    - src-tauri/crates/uc-daemon/src/pairing/host.rs (inline string literals for pairing stages, busy reasons, emit calls)
    - src-tauri/crates/uc-daemon/src/api/server.rs (lines 108-150: error code strings)
    - src-tauri/crates/uc-daemon/src/api/routes.rs (acknowledged() calls with state labels, error code strings)
  </read_first>
  <action>
Create `src-tauri/crates/uc-core/src/network/daemon_api_strings.rs` with the following five `pub mod` submodules containing `pub const &str` values (per D-01, D-02, D-03):

**`pub mod ws_topic`** — 8 constants:

- `STATUS: &str = "status"`
- `PEERS: &str = "peers"`
- `PAIRED_DEVICES: &str = "paired-devices"`
- `PAIRING: &str = "pairing"`
- `PAIRING_SESSION: &str = "pairing/session"`
- `PAIRING_VERIFICATION: &str = "pairing/verification"`
- `SETUP: &str = "setup"`
- `SPACE_ACCESS: &str = "space-access"`

**`pub mod ws_event`** — 16 constants:

- `STATUS_SNAPSHOT: &str = "status.snapshot"`
- `STATUS_UPDATED: &str = "status.updated"`
- `PEERS_SNAPSHOT: &str = "peers.snapshot"`
- `PEERS_CHANGED: &str = "peers.changed"`
- `PEERS_NAME_UPDATED: &str = "peers.name_updated"`
- `PEERS_CONNECTION_CHANGED: &str = "peers.connection_changed"`
- `PAIRED_DEVICES_SNAPSHOT: &str = "paired-devices.snapshot"`
- `PAIRED_DEVICES_CHANGED: &str = "paired-devices.changed"`
- `PAIRING_SNAPSHOT: &str = "pairing.snapshot"`
- `PAIRING_UPDATED: &str = "pairing.updated"`
- `PAIRING_VERIFICATION_REQUIRED: &str = "pairing.verification_required"`
- `PAIRING_COMPLETE: &str = "pairing.complete"`
- `PAIRING_FAILED: &str = "pairing.failed"`
- `SETUP_STATE_CHANGED: &str = "setup.state_changed"`
- `SETUP_SPACE_ACCESS_COMPLETED: &str = "setup.space_access_completed"`
- `SPACE_ACCESS_SNAPSHOT: &str = "space_access.snapshot"`
- `SPACE_ACCESS_STATE_CHANGED: &str = "space_access.state_changed"`

**`pub mod pairing_stage`** — 5 constants:

- `REQUEST: &str = "request"`
- `VERIFICATION: &str = "verification"`
- `VERIFYING: &str = "verifying"`
- `COMPLETE: &str = "complete"`
- `FAILED: &str = "failed"`

**`pub mod pairing_busy_reason`** — 3 constants:

- `HOST_NOT_DISCOVERABLE: &str = "host_not_discoverable"`
- `NO_LOCAL_PAIRING_PARTICIPANT_READY: &str = "no_local_pairing_participant_ready"`
- `BUSY: &str = "busy"`

**`pub mod pairing_error_code`** — 7 constants:

- `ACTIVE_SESSION_EXISTS: &str = "active_session_exists"`
- `HOST_NOT_DISCOVERABLE: &str = "host_not_discoverable"`
- `NO_LOCAL_PARTICIPANT: &str = "no_local_participant"`
- `SESSION_NOT_FOUND: &str = "session_not_found"`
- `INTERNAL: &str = "internal"`
- `BAD_REQUEST: &str = "bad_request"`
- `RUNTIME_UNAVAILABLE: &str = "runtime_unavailable"`

Add a `#[cfg(test)] mod tests` block at the bottom with value assertion tests for EVERY constant (per D-07). Follow the `protocol_ids.rs` test style — one `assert_eq!` per constant. Group assertions by submodule in separate test functions:

- `fn ws_topic_values_match()`
- `fn ws_event_values_match()`
- `fn pairing_stage_values_match()`
- `fn pairing_busy_reason_values_match()`
- `fn pairing_error_code_values_match()`

Add `/// Daemon wire-protocol string constants shared between uc-daemon (server) and uc-daemon-client (consumer).` doc comment at the top of the file.

Then edit `src-tauri/crates/uc-core/src/network/mod.rs` to add `pub mod daemon_api_strings;` after the `pub mod protocol_ids;` line.
</action>
<verify>
<automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo test -p uc-core --lib network::daemon_api_strings 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria> - src-tauri/crates/uc-core/src/network/daemon_api_strings.rs exists - daemon_api_strings.rs contains `pub mod ws_topic` - daemon_api_strings.rs contains `pub mod ws_event` - daemon_api_strings.rs contains `pub mod pairing_stage` - daemon_api_strings.rs contains `pub mod pairing_busy_reason` - daemon_api_strings.rs contains `pub mod pairing_error_code` - daemon_api_strings.rs contains `assert_eq!(ws_topic::STATUS, "status")` - daemon_api_strings.rs contains `assert_eq!(pairing_stage::REQUEST, "request")` - network/mod.rs contains `pub mod daemon_api_strings;` - `cd src-tauri && cargo test -p uc-core --lib network::daemon_api_strings` exits 0
</acceptance_criteria>
<done>All 5 constant submodules defined with correct values, value assertion tests pass, module re-exported from network/mod.rs</done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo check -p uc-core` passes
- `cd src-tauri && cargo test -p uc-core --lib network::daemon_api_strings` passes with all assertions green
- `grep -c "pub const" src-tauri/crates/uc-core/src/network/daemon_api_strings.rs` returns 39+ (8+16+5+3+7)
</verification>

<success_criteria>

- daemon_api_strings.rs exists with 5 submodules and 39 constants
- All value assertion tests pass
- Module is importable from uc-core::network::daemon_api_strings
  </success_criteria>

<output>
After completion, create `.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-01-SUMMARY.md`
</output>
