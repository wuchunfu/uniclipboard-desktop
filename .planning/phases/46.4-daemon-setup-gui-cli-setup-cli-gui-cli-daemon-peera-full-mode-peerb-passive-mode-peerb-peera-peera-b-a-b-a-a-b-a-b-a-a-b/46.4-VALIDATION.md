---
phase: 46.4
slug: daemon-setup-gui-cli-setup-cli-gui-cli-daemon-peera-full-mode-peerb-passive-mode-peerb-peera-peera-b-a-b-a-a-b-a-b-a-a-b
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-21
---

# Phase 46.4 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property               | Value                                                                                                                                                                                                                            |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Framework**          | Rust `cargo test`                                                                                                                                                                                                                |
| **Config file**        | `src-tauri/Cargo.toml`                                                                                                                                                                                                           |
| **Quick run command**  | `cd src-tauri && cargo test -p uc-cli`                                                                                                                                                                                           |
| **Full suite command** | `cd src-tauri && cargo test -p uc-daemon pairing_api -- --test-threads=1 && cargo test -p uc-daemon pairing_host -- --test-threads=1 && cargo test -p uc-app setup_flow_integration_test -- --nocapture && cargo test -p uc-cli` |
| **Estimated runtime**  | ~120 seconds                                                                                                                                                                                                                     |

---

## Sampling Rate

- **After every task commit:** Run `cd src-tauri && cargo test -p uc-cli`
- **After every plan wave:** Run `cd src-tauri && cargo test -p uc-daemon pairing_api -- --test-threads=1 && cargo test -p uc-daemon pairing_host -- --test-threads=1 && cargo test -p uc-app setup_flow_integration_test -- --nocapture && cargo test -p uc-cli`
- **Before `$gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 180 seconds

---

## Per-Task Verification Map

| Task ID    | Plan | Wave | Requirement                                  | Test Type   | Automated Command                                                                 | File Exists | Status     |
| ---------- | ---- | ---- | -------------------------------------------- | ----------- | --------------------------------------------------------------------------------- | ----------- | ---------- |
| 46.4-01-01 | 01   | 1    | daemon-owned setup transport                 | integration | `cd src-tauri && cargo test -p uc-daemon pairing_api -- --test-threads=1`         | ❌ W0       | ⬜ pending |
| 46.4-01-02 | 01   | 1    | setup state/read-model contract              | integration | `cd src-tauri && cargo test -p uc-daemon http_api -- --test-threads=1`            | ❌ W0       | ⬜ pending |
| 46.4-02-01 | 02   | 2    | CLI setup command parsing/output             | unit        | `cd src-tauri && cargo test -p uc-cli`                                            | ✅          | ⬜ pending |
| 46.4-02-02 | 02   | 2    | interactive host/join flow adapter           | integration | `cd src-tauri && cargo test -p uc-cli`                                            | ✅          | ⬜ pending |
| 46.4-03-01 | 03   | 3    | setup retry + completion behavior            | integration | `cd src-tauri && cargo test -p uc-app setup_flow_integration_test -- --nocapture` | ✅          | ⬜ pending |
| 46.4-03-02 | 03   | 3    | daemon pairing invariants after setup wiring | integration | `cd src-tauri && cargo test -p uc-daemon pairing_host -- --test-threads=1`        | ✅          | ⬜ pending |

_Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky_

---

## Wave 0 Requirements

- [ ] `src-tauri/crates/uc-daemon/tests/setup_api.rs` — daemon setup route coverage for state/query/mutation/error cases
- [ ] `src-tauri/crates/uc-cli/tests/setup_cli.rs` or equivalent command tests — CLI setup parsing/rendering/exit-code coverage
- [ ] Shared helper/fixture coverage for dual-profile `peerA` / `peerB` runtime setup if current tests cannot express profile-scoped runs cleanly

---

## Manual-Only Verifications

| Behavior                                                       | Requirement                   | Why Manual                                                                                        | Test Instructions                                                                                                                                                                                                                                                               |
| -------------------------------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Two-terminal same-machine `peerA` host / `peerB` join succeeds | operator workflow deliverable | interactive UX, daemon lifecycle, and profile separation must be proven end-to-end                | Terminal A: `UC_PROFILE=peerA UC_CLIPBOARD_MODE=full ... setup host`; Terminal B: `UC_PROFILE=peerB UC_CLIPBOARD_MODE=passive ... setup join`; complete discovery, request, confirm, passphrase submit, join success, then verify both `setup status` and paired-device queries |
| Wrong passphrase then retry succeeds                           | negative-path acceptance      | passphrase prompt timing and retry behavior are interactive and span daemon + setup orchestration | Repeat the same flow, enter a wrong passphrase once on `peerB`, verify return to passphrase-entry state, then submit correct passphrase and complete setup                                                                                                                      |
| Reset allows rerun on the same machine                         | repeatability contract        | reset semantics and residual local state are environment-sensitive                                | After successful setup, run the phase-defined reset path on both profiles, rerun the exact host/join flow, and verify second success without manual file/database edits                                                                                                         |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 180s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
