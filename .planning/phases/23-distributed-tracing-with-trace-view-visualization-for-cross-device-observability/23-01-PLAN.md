---
phase: 23-distributed-tracing-with-trace-view-visualization-for-cross-device-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/crates/uc-observability/src/seq/layer.rs
  - src-tauri/crates/uc-observability/src/seq/mod.rs
  - src-tauri/crates/uc-observability/src/lib.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/tracing.rs
  - docker-compose.seq.yml
autonomous: true
requirements: []

must_haves:
  truths:
    - 'Every CLEF event sent to Seq includes device_id field'
    - 'device_id is read from device_id.txt at tracing initialization time'
    - 'Seq is accessible from LAN devices for cross-device testing'
    - 'Older peer messages without origin_flow_id are handled gracefully'
  artifacts:
    - path: 'src-tauri/crates/uc-observability/src/seq/layer.rs'
      provides: 'device_id injection into CLEF events'
      contains: 'device_id: Option<String>'
    - path: 'src-tauri/crates/uc-observability/src/seq/mod.rs'
      provides: 'build_seq_layer accepts device_id parameter'
      contains: 'device_id: Option<&str>'
    - path: 'src-tauri/crates/uc-tauri/src/bootstrap/tracing.rs'
      provides: 'Early device_id resolution from device_id.txt'
      contains: 'resolve_device_id_for_seq'
    - path: 'docker-compose.seq.yml'
      provides: 'LAN-accessible Seq instance'
      contains: '0.0.0.0:5341'
  key_links:
    - from: 'tracing.rs'
      to: 'seq/layer.rs'
      via: 'build_seq_layer call with device_id'
      pattern: 'build_seq_layer.*device_id'
    - from: 'seq/layer.rs'
      to: 'CLEF output'
      via: 'format_clef_event'
      pattern: 'device_id.*serialize_entry'
---

<objective>
Inject device_id as static field into every CLEF event sent to Seq, enabling cross-device log correlation.

Purpose: Every Seq event from any device will carry the device_id field, allowing queries like "show me all logs from device X" or "show me all flow".
Output: device events across all devices_id appears in all Seq CLEF events, Seq accessible from LAN for cross-device testing.
</objective>

<context>
@src-tauri/crates/uc-observability/src/seq/layer.rs
@src-tauri/crates/uc-observability/src/seq/mod.rs
@src-tauri/crates/uc-tauri/src/bootstrap/tracing.rs
@docker-compose.seq.yml

# Key interfaces from prior phase (22)

# build_seq_layer(profile: &LogProfile) -> Option<(impl Layer<S> + Send + Sync, SeqGuard)>

# SeqLayer::on_event() calls format_clef_event(event, &ctx)

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add device_id field to SeqLayer struct</name>
  <files>src-tauri/crates/uc-observability/src/seq/layer.rs</files>
  <action>
    1. Add `device_id: Option<String>` field to `SeqLayer` struct
    2. Update `SeqLayer::new(tx, device_id)` constructor to accept and store device_id
    3. Modify `on_event()` to pass device_id to `format_clef_event()`
  </action>
  <verify>
    <automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo check -p uc-observability 2>&1 | head -30</automated>
  </verify>
  <done>SeqLayer struct has device_id field, constructor accepts it, passed to format_clef_event</done>
</task>

<task type="auto">
  <name>Task 2: Modify format_clef_event to accept and inject device_id</name>
  <files>src-tauri/crates/uc-observability/src/seq/layer.rs</files>
  <action>
    1. Change `format_clef_event(event, ctx)` signature to `format_clef_event(event, ctx, device_id: Option<&str>)`
    2. In the CLEF JSON serialization, after @m (message) field and before span fields, inject device_id if present:
       ```rust
       if let Some(did) = device_id {
           map.serialize_entry("device_id", did).ok()?;
       }
       ```
    3. Ensure field appears in every CLEF event regardless of span context
  </action>
  <verify>
    <automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo check -p uc-observability 2>&1 | head -30</automated>
  </verify>
  <done>format_clef_event injects device_id into CLEF output, appears before span fields</done>
</task>

<task type="auto">
  <name>Task 3: Update build_seq_layer to accept device_id parameter</name>
  <files>src-tauri/crates/uc-observability/src/seq/mod.rs</files>
  <action>
    1. Change `build_seq_layer(profile: &LogProfile)` to `build_seq_layer(profile: &LogProfile, device_id: Option<&str>)`
    2. Pass device_id to `SeqLayer::new(tx, device_id.map(String::from))`
    3. Update lib.rs re-export if needed
  </action>
  <verify>
    <automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo check -p uc-observability 2>&1 | head -30</automated>
  </verify>
  <done>build_seq_layer accepts device_id parameter, passes to SeqLayer</done>
</task>

<task type="auto">
  <name>Task 4: Resolve device_id early in tracing.rs init</name>
  <files>src-tauri/crates/uc-tauri/src/bootstrap/tracing.rs</files>
  <action>
    1. Add helper function `resolve_device_id_for_seq(config_dir: &Path) -> Option<String>`:
       - Read from `{config_dir}/device_id.txt`
       - Trim whitespace, filter empty strings
       - Return None if file doesn't exist (first launch graceful degradation)
    2. In `init_tracing_subscriber()`, after getting config_dir from AppPaths, call the helper:
       `let device_id = resolve_device_id_for_seq(&paths.config_dir);`
    3. Pass device_id to `build_seq_layer(&profile, device_id.as_deref())`
    4. Ensure AppPaths exposes config_dir (add field if missing)
  </action>
  <verify>
    <automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo check -p uc-tauri 2>&1 | head -30</automated>
  </verify>
  <done>device_id resolved from device_id.txt at tracing init time, passed to SeqLayer</done>
</task>

<task type="auto">
  <name>Task 5: Update docker-compose.seq.yml for LAN access</name>
  <files>docker-compose.seq.yml</files>
  <action>
    1. Change port binding from '5341:80' to expose on all interfaces
    2. Add `SEQ_FIRSTRUN_ADMINPASSWORD` environment variable (set to a reasonable default like "uniclipboard")
    3. Add comment warning this is development-only configuration
  </action>
  <verify>
    <automated>cat docker-compose.seq.yml | grep -E "(ports|SEQ_FIRSTRUN|0.0.0.0)"</automated>
  </verify>
  <done>Seq bound to 0.0.0.0:5341, password protected, comment added</done>
</task>

</tasks>

<verification>
All tasks compile successfully. device_id appears in CLEF events when device_id.txt exists.
</verification>

<success_criteria>

- SeqLayer stores and injects device_id into every CLEF event
- device_id resolved from device_id.txt at tracing initialization
- docker-compose.seq.yml accessible from LAN devices for cross-device testing
- No compilation errors
  </success_criteria>

<output>
After completion, create `.planning/phases/23-distributed-tracing-with-trace-view-visualization-for-cross-device-observability/23-01-SUMMARY.md`
</output>
