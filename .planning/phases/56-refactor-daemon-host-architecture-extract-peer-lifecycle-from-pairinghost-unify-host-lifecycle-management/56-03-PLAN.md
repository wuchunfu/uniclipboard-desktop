---
phase: 56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management
plan: 03
type: execute
wave: 3
depends_on:
  - 56-01
  - 56-02
files_modified:
  - src-tauri/crates/uc-daemon/src/app.rs
  - src-tauri/crates/uc-daemon/src/main.rs
  - src-tauri/crates/uc-daemon/src/pairing/host.rs
  - src-tauri/crates/uc-daemon/tests/pairing_host.rs
autonomous: true
requirements:
  - PH56-03
  - PH56-04
must_haves:
  truths:
    - 'DaemonApp starts and stops PairingHost, PeerMonitor, and existing background components through one shared services list.'
    - 'DaemonApp no longer hardcodes pairing-host construction or dedicated pairing shutdown bookkeeping.'
    - 'HTTP routes still have typed access to DaemonPairingHost control methods after lifecycle unification.'
  artifacts:
    - path: 'src-tauri/crates/uc-daemon/src/app.rs'
      provides: 'Unified service lifecycle management'
    - path: 'src-tauri/crates/uc-daemon/src/main.rs'
      provides: 'Typed pairing host plus PeerMonitor assembly into one service list'
    - path: 'src-tauri/crates/uc-daemon/src/pairing/host.rs'
      provides: 'DaemonService implementation for the pairing host'
  key_links:
    - from: 'src-tauri/crates/uc-daemon/src/main.rs'
      to: 'src-tauri/crates/uc-daemon/src/app.rs'
      via: 'Vec<Arc<dyn DaemonService>>'
      pattern: 'Vec<Arc<dyn DaemonService>>'
    - from: 'src-tauri/crates/uc-daemon/src/main.rs'
      to: 'src-tauri/crates/uc-daemon/src/api/server.rs'
      via: 'DaemonApiState::with_pairing_host(Arc::clone(&pairing_host))'
      pattern: 'with_pairing_host'
    - from: 'src-tauri/crates/uc-daemon/src/pairing/host.rs'
      to: 'src-tauri/crates/uc-daemon/src/service.rs'
      via: 'impl DaemonService for DaemonPairingHost'
      pattern: 'impl DaemonService for DaemonPairingHost'
---

<objective>
Unify daemon lifecycle management so `DaemonApp` treats pairing host, peer monitor, and existing background components as one service list.

Purpose: Finish the structural refactor by removing pairing-specific lifecycle boilerplate while preserving typed API access to pairing commands.
Output: `DaemonPairingHost` as a `DaemonService`, simplified `DaemonApp`, and `main.rs` assembling typed host access plus unified services.
</objective>

<execution_context>
Use the repository GSD execution workflow already loaded by the orchestrator.
Validation contract: @.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-VALIDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-CONTEXT.md
@.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-RESEARCH.md
@.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-VALIDATION.md
@.planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-01-SUMMARY.md
@.planning/phases/45-daemon-api-foundation-add-local-http-and-websocket-transport-with-read-only-runtime-queries/45-02-SUMMARY.md
@src-tauri/crates/uc-daemon/src/app.rs
@src-tauri/crates/uc-daemon/src/main.rs
@src-tauri/crates/uc-daemon/src/api/server.rs
@src-tauri/crates/uc-daemon/src/pairing/host.rs
@src-tauri/crates/uc-daemon/src/peers/monitor.rs

<interfaces>
From `src-tauri/crates/uc-daemon/src/api/server.rs`:

```rust
pub fn with_pairing_host(mut self, pairing_host: Arc<DaemonPairingHost>) -> Self;
pub fn pairing_host(&self) -> Option<Arc<DaemonPairingHost>>;
pub async fn run_http_server(
    state: DaemonApiState,
    cancel: CancellationToken,
) -> anyhow::Result<()>;
```

From `src-tauri/crates/uc-daemon/src/pairing/host.rs`:

```rust
pub fn new(...) -> Self;
pub async fn accept_pairing(&self, session_id: &str) -> Result<(), DaemonPairingHostError>;
pub async fn reject_pairing(&self, session_id: &str) -> Result<(), DaemonPairingHostError>;
pub async fn cancel_pairing(&self, session_id: &str) -> Result<(), DaemonPairingHostError>;
pub async fn register_gui_participant(
    &self,
    enabled: bool,
    lease_ttl_ms: Option<u64>,
) -> Result<(), DaemonPairingHostError>;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make DaemonPairingHost a DaemonService without changing its control API</name>
  <files>
src-tauri/crates/uc-daemon/src/pairing/host.rs
src-tauri/crates/uc-daemon/tests/pairing_host.rs
  </files>
  <read_first>
- src-tauri/crates/uc-daemon/src/pairing/host.rs
- src-tauri/crates/uc-daemon/src/service.rs
- src-tauri/crates/uc-daemon/tests/pairing_host.rs
- .planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-01-SUMMARY.md
  </read_first>
  <behavior>
- Test 1: `DaemonPairingHost` implements `DaemonService` with `name()`, `start(CancellationToken)`, `stop()`, and `health_check()`.
- Test 2: starting the host still uses the existing one-time `pairing_action_rx` take pattern and preserves the "already running" guard.
- Test 3: typed pairing control methods (`accept_pairing`, `reject_pairing`, `cancel_pairing`, `register_gui_participant`) remain available for the API layer.
  </behavior>
  <action>Implement `DaemonService` for `DaemonPairingHost` in `src-tauri/crates/uc-daemon/src/pairing/host.rs`. Move the current public lifecycle entrypoint behind `start(CancellationToken)` so the host can be launched from the shared service list, but keep all typed pairing control methods public and unchanged. Preserve the single-start guard by reusing the existing `pairing_action_rx: Mutex<Option<_>>` take path and keep the existing host tests green. If a helper is still needed internally, make it private and have `start()` call it; do not leave a second public lifecycle surface that lets `DaemonApp` bypass the service trait.</action>
  <acceptance_criteria>
- `rg -n "impl DaemonService for DaemonPairingHost" src-tauri/crates/uc-daemon/src/pairing/host.rs` returns one match.
- `rg -n "name\\(&self\\) -> &str" src-tauri/crates/uc-daemon/src/pairing/host.rs` returns a match under the service impl.
- `rg -n "daemon pairing host already running" src-tauri/crates/uc-daemon/src/pairing/host.rs` still returns one match.
- `rg -n "pub async fn accept_pairing|pub async fn reject_pairing|pub async fn cancel_pairing|pub async fn register_gui_participant" src-tauri/crates/uc-daemon/src/pairing/host.rs` returns four matches.
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-daemon daemon_pairing_host_starts_non_discoverable_in_headless_mode -- --test-threads=1 && cargo test -p uc-daemon daemon_pairing_host_survives_client_disconnect -- --test-threads=1</automated>
  </verify>
  <done>`DaemonPairingHost` can be lifecycle-managed through `DaemonService` while routes still call its typed pairing commands directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire DaemonApp and main.rs to use one unified services list</name>
  <files>
src-tauri/crates/uc-daemon/src/app.rs
src-tauri/crates/uc-daemon/src/main.rs
  </files>
  <read_first>
- src-tauri/crates/uc-daemon/src/app.rs
- src-tauri/crates/uc-daemon/src/main.rs
- src-tauri/crates/uc-daemon/src/api/server.rs
- src-tauri/crates/uc-daemon/src/pairing/host.rs
- src-tauri/crates/uc-daemon/src/peers/monitor.rs
- .planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-RESEARCH.md
  </read_first>
  <behavior>
- Test 1: `DaemonApp` stores `services: Vec<Arc<dyn DaemonService>>` and no longer stores pairing-specific constructor inputs.
- Test 2: `main.rs` builds typed `Arc<DaemonPairingHost>` and `Arc<PeerMonitor>`, adds them to the unified services list, and still injects the typed pairing host into `DaemonApiState`.
- Test 3: `DaemonApp::run()` no longer has dedicated `pairing_handle` or `completed_pairing_handle` bookkeeping; all long-lived components start and stop through the same service loop.
  </behavior>
  <action>Refactor `DaemonApp` so its struct fields hold only `services: Vec<Arc<dyn DaemonService>>`, `runtime`, `state`, `socket_path`, and shutdown token. Change `DaemonApp::new()` accordingly. Build `DaemonApiState` outside the constructor path in `main.rs`: create `Arc<DaemonPairingHost>`, create `Arc<PeerMonitor>`, assemble `services: Vec<Arc<dyn DaemonService>>` in deterministic order with `pairing_host` and `peer_monitor` before `PeerDiscoveryWorker`, then call `with_pairing_host(Arc::clone(&pairing_host))`, `with_setup(...)`, and `with_space_access(...)` on the API state before passing it into `DaemonApp::run(api_state)` or an equivalent run-time argument. In `app.rs`, replace the dedicated pairing-host spawn/shutdown path with the generic service spawn/shutdown loop and keep reverse-order stopping. Do not look the pairing host up from generic trait objects; typed API access must come from the `main.rs` assembly path.</action>
  <acceptance_criteria>
- `rg -n "services: Vec<Arc<dyn DaemonService>>" src-tauri/crates/uc-daemon/src/app.rs src-tauri/crates/uc-daemon/src/main.rs` returns matches in both files.
- `rg -n "pairing_handle|completed_pairing_handle|pairing_orchestrator: Arc<PairingOrchestrator>|pairing_action_rx: mpsc::Receiver<PairingAction>|space_access_orchestrator: Arc<SpaceAccessOrchestrator>|key_slot_store: Arc<dyn KeySlotStore>" src-tauri/crates/uc-daemon/src/app.rs` returns no matches.
- `rg -n "PeerMonitor" src-tauri/crates/uc-daemon/src/main.rs` returns at least one match.
- `rg -n "with_pairing_host\\(Arc::clone\\(&pairing_host\\)\\)" src-tauri/crates/uc-daemon/src/main.rs` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo check -p uc-daemon && cargo test -p uc-daemon daemon_pairing_host_starts_non_discoverable_in_headless_mode -- --test-threads=1 && cargo test -p uc-daemon daemon_pairing_host_survives_client_disconnect -- --test-threads=1</automated>
  </verify>
  <done>`DaemonApp` manages one service list, pairing-host lifecycle boilerplate is gone, and the HTTP layer still receives typed pairing-host access from `main.rs` assembly.</done>
</task>

</tasks>

<verification>
Run the task-level commands, then confirm `rg -n "pairing_handle|completed_pairing_handle" src-tauri/crates/uc-daemon/src/app.rs` returns no matches.
</verification>

<success_criteria>
`DaemonApp` no longer knows how to build or special-case the pairing host, all daemon long-lived components start from one service list, and typed pairing API control remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-03-SUMMARY.md`
</output>
