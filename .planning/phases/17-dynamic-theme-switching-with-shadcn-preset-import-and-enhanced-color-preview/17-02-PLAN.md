---
phase: 17-dynamic-theme-switching-with-shadcn-preset-import-and-enhanced-color-preview
plan: '02'
type: execute
wave: 2
depends_on:
  - '17-01'
files_modified:
  - src/components/setting/AppearanceSection.tsx
  - src/components/setting/__tests__/AppearanceSection.test.tsx
  - src/constants/theme.ts
  - src/lib/theme-engine.ts
autonomous: true
requirements:
  - P17-04
  - P17-07
must_haves:
  truths:
    - 'Appearance preset picker uses multi-dot swatches (3-4 dots) sourced from runtime preset metadata.'
    - 'Selecting a preset still persists via updateGeneralSetting({ theme_color }).'
    - 'Selected-state visuals remain clear and default selection fallback still works.'
    - 'Component tests cover swatch rendering, selection interaction, and update contract.'
  artifacts:
    - path: 'src/components/setting/AppearanceSection.tsx'
      provides: 'Theme swatch grid with multi-dot preview rendering and persisted selection interaction'
      contains: 'theme_color'
    - path: 'src/components/setting/__tests__/AppearanceSection.test.tsx'
      provides: 'Appearance swatch rendering and click-contract tests'
      contains: 'updateGeneralSetting'
  key_links:
    - from: 'src/components/setting/AppearanceSection.tsx'
      to: 'src/constants/theme.ts'
      via: 'built-in theme options and preview metadata'
      pattern: 'THEME_COLORS'
    - from: 'src/components/setting/AppearanceSection.tsx'
      to: 'src/hooks/useSetting.ts'
      via: 'persist selected preset via updateGeneralSetting'
      pattern: 'updateGeneralSetting'
---

<objective>
Upgrade Appearance -> Theme Color picker to multi-dot preset previews backed by the new runtime preset metadata, with regression tests for interaction contracts.

Purpose: Deliver user-visible preset discovery improvements without changing persistence semantics or introducing hover-preview side effects.
Output: Updated AppearanceSection UI and comprehensive component tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dynamic-theme-switching-with-shadcn-preset-import-and-enhanced-color-preview/17-CONTEXT.md
@.planning/phases/17-dynamic-theme-switching-with-shadcn-preset-import-and-enhanced-color-preview/17-RESEARCH.md
@.planning/phases/17-dynamic-theme-switching-with-shadcn-preset-import-and-enhanced-color-preview/17-VALIDATION.md
@.planning/phases/17-dynamic-theme-switching-with-shadcn-preset-import-and-enhanced-color-preview/17-01-SUMMARY.md

<interfaces>
From `src/components/setting/AppearanceSection.tsx` current theme color interaction:

```tsx
{
  THEME_COLORS.map(item => (
    <div key={item.name} onClick={() => void handleThemeColorChange(item.name)}>
      <div style={{ backgroundColor: item.color }} />
    </div>
  ))
}
```

From `src/hooks/useSetting.ts` contract:

```ts
const { setting, updateGeneralSetting } = useSetting()
await updateGeneralSetting({ theme_color: newThemeColor })
```

From Plan 01 expected metadata:

```ts
interface ThemeColorOption {
  name: string
  dots: string[] // 3-4 preview colors
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement multi-dot preset swatches in AppearanceSection</name>
  <files>
    src/components/setting/AppearanceSection.tsx
    src/constants/theme.ts
    src/lib/theme-engine.ts
  </files>
  <behavior>
    - Theme swatch tile renders 3-4 compact color dots instead of one solid circle.
    - Dot colors come from preset metadata (no component-level hardcoded colors).
    - Clicking swatch persists selected preset with `updateGeneralSetting({ theme_color: presetName })`.
    - Existing selected marker/default fallback behavior remains intact.
    - No hover-based global preview behavior is introduced.
  </behavior>
  <action>
    1. Update `AppearanceSection.tsx` theme color tile rendering:
       - Replace single-circle style block with a compact row/cluster of 3-4 dots.
       - Keep existing layout footprint and accessibility (click target remains tile-level).
       - Keep current selected indicator and border behavior.

    2. Ensure swatch data comes from centralized preset metadata:
       - If needed, extend `ThemeColorOption` shape to expose preview dots.
       - Do not duplicate token values inside component.

    3. Confirm default selection fallback still works when `setting.general.theme_color` is unset (default preset highlighted).

  </action>
  <verify>
    <automated>bun run test --run src/components/setting/__tests__/AppearanceSection.test.tsx</automated>
  </verify>
  <done>
    - Theme color picker is multi-dot preview based.
    - Behavior remains click-to-apply only.
    - Persistence path remains unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add AppearanceSection regression tests for swatch rendering and persistence contract</name>
  <files>
    src/components/setting/__tests__/AppearanceSection.test.tsx
  </files>
  <behavior>
    - Each theme option renders 3-4 dot elements.
    - Clicking a non-selected theme calls `updateGeneralSetting` with correct payload.
    - Default preset is treated as selected when `theme_color` is null.
    - Selected explicit preset is highlighted correctly.
  </behavior>
  <action>
    1. Create `src/components/setting/__tests__/AppearanceSection.test.tsx`:
       - Use `SettingContext.Provider` with mock `updateGeneralSetting`.
       - Mock `react-i18next` similarly to existing component tests.

    2. Add test cases:
       - Renders theme mode section and theme color swatch list.
       - Each swatch has expected number of dot children (3-4).
       - Click swatch -> `updateGeneralSetting({ theme_color: <name> })`.
       - Null `theme_color` highlights default preset.
       - Explicit `theme_color` highlights the chosen preset.

    3. Keep assertions resilient to future preset count changes by checking relative behavior (e.g., at least one swatch with 3+ dots) where appropriate.

  </action>
  <verify>
    <automated>bun run test --run src/components/setting/__tests__/AppearanceSection.test.tsx</automated>
  </verify>
  <done>
    - AppearanceSection has dedicated regression tests.
    - Swatch rendering and persistence contract are guarded by CI-friendly tests.
  </done>
</task>

</tasks>

<verification>
- `bun run test --run src/components/setting/__tests__/AppearanceSection.test.tsx` passes.
- `bun run test --run` passes.
- `bun run build` passes.
</verification>
