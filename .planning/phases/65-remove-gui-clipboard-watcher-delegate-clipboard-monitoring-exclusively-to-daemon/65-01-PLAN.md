---
phase: 65-remove-gui-clipboard-watcher-delegate-clipboard-monitoring-exclusively-to-daemon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/crates/uc-platform/tests/runtime_test.rs
  - src-tauri/crates/uc-platform/tests/watcher_control_test.rs
  - src-tauri/crates/uc-platform/src/adapters/in_memory_watcher_control.rs
  - src-tauri/crates/uc-platform/src/adapters/mod.rs
  - src-tauri/crates/uc-platform/src/usecases/start_clipboard_watcher.rs
  - src-tauri/crates/uc-platform/src/usecases/mod.rs
  - src-tauri/crates/uc-platform/src/runtime/event_bus.rs
  - src-tauri/crates/uc-platform/src/runtime/runtime.rs
  - src-tauri/crates/uc-platform/src/runtime/mod.rs
  - src-tauri/crates/uc-platform/src/ipc/command.rs
  - src-tauri/crates/uc-platform/src/ipc/event.rs
  - src-tauri/crates/uc-platform/src/ipc/mod.rs
  - src-tauri/crates/uc-platform/src/ports/watcher_control.rs
  - src-tauri/crates/uc-platform/src/ports/command_executor.rs
  - src-tauri/crates/uc-platform/src/ports/clipboard_runtime.rs
  - src-tauri/crates/uc-platform/src/ports/mod.rs
  - src-tauri/crates/uc-platform/src/lib.rs
  - src-tauri/crates/uc-core/src/ports/start_clipboard_watcher.rs
  - src-tauri/crates/uc-core/src/ports/mod.rs
  - src-tauri/crates/uc-app/src/usecases/mod.rs
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-13
  - D-15

must_haves:
  truths:
    - 'PlatformRuntime struct no longer exists in uc-platform crate'
    - 'WatcherControlPort trait no longer exists in uc-platform ports'
    - 'StartClipboardWatcherPort trait no longer exists in uc-core ports'
    - 'uc-platform/ipc/ module entirely deleted'
    - 'uc-platform/runtime/ module entirely deleted'
    - 'ClipboardWatcher and LocalClipboard in uc-platform/clipboard/ are preserved'
    - 'ClipboardChangeHandler trait in uc-core/ports/ is preserved'
  artifacts:
    - path: 'src-tauri/crates/uc-platform/src/lib.rs'
      provides: 'Module declarations without runtime or ipc'
      contains: 'pub mod clipboard'
    - path: 'src-tauri/crates/uc-core/src/ports/mod.rs'
      provides: 'Port declarations without StartClipboardWatcherPort'
      contains: 'pub use clipboard_change_handler::ClipboardChangeHandler'
  key_links:
    - from: 'src-tauri/crates/uc-platform/src/lib.rs'
      to: 'uc-platform/clipboard/'
      via: 'pub mod clipboard preserved'
      pattern: 'pub mod clipboard'
---

<objective>
Delete all GUI clipboard watcher infrastructure from uc-platform and the StartClipboardWatcherPort from uc-core. This removes PlatformRuntime, event bus, IPC modules, WatcherControlPort, InMemoryWatcherControl, StartClipboardWatcher use case, ClipboardRuntimePort, and PlatformCommandExecutorPort.

Purpose: Remove dead code that was made obsolete by Phase 57 (daemon clipboard watcher integration). The GUI now runs in Passive mode exclusively -- daemon is the sole clipboard observer.
Output: uc-platform crate without runtime/ipc modules, uc-core without StartClipboardWatcherPort, uc-app without StartClipboardWatcher re-exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-remove-gui-clipboard-watcher-delegate-clipboard-monitoring-exclusively-to-daemon/65-CONTEXT.md
@.planning/phases/65-remove-gui-clipboard-watcher-delegate-clipboard-monitoring-exclusively-to-daemon/65-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete uc-platform runtime, ipc, watcher, and executor infrastructure</name>
  <files>
    src-tauri/crates/uc-platform/tests/runtime_test.rs,
    src-tauri/crates/uc-platform/tests/watcher_control_test.rs,
    src-tauri/crates/uc-platform/src/adapters/in_memory_watcher_control.rs,
    src-tauri/crates/uc-platform/src/adapters/mod.rs,
    src-tauri/crates/uc-platform/src/usecases/start_clipboard_watcher.rs,
    src-tauri/crates/uc-platform/src/usecases/mod.rs,
    src-tauri/crates/uc-platform/src/runtime/event_bus.rs,
    src-tauri/crates/uc-platform/src/runtime/runtime.rs,
    src-tauri/crates/uc-platform/src/runtime/mod.rs,
    src-tauri/crates/uc-platform/src/ipc/command.rs,
    src-tauri/crates/uc-platform/src/ipc/event.rs,
    src-tauri/crates/uc-platform/src/ipc/mod.rs,
    src-tauri/crates/uc-platform/src/ports/watcher_control.rs,
    src-tauri/crates/uc-platform/src/ports/command_executor.rs,
    src-tauri/crates/uc-platform/src/ports/clipboard_runtime.rs,
    src-tauri/crates/uc-platform/src/ports/mod.rs,
    src-tauri/crates/uc-platform/src/lib.rs
  </files>
  <read_first>
    src-tauri/crates/uc-platform/src/lib.rs,
    src-tauri/crates/uc-platform/src/ports/mod.rs,
    src-tauri/crates/uc-platform/src/adapters/mod.rs,
    src-tauri/crates/uc-platform/src/usecases/mod.rs,
    src-tauri/crates/uc-platform/src/runtime/mod.rs,
    src-tauri/crates/uc-platform/src/ipc/mod.rs
  </read_first>
  <action>
Per D-01, D-03, D-04, D-05, D-06, D-07, D-13:

**Step 1 — Delete test files (no compilation impact):**

- Delete `src-tauri/crates/uc-platform/tests/runtime_test.rs` entirely
- Delete `src-tauri/crates/uc-platform/tests/watcher_control_test.rs` entirely

**Step 2 — Delete implementation files in uc-platform:**

- Delete `src-tauri/crates/uc-platform/src/adapters/in_memory_watcher_control.rs`
- Update `src-tauri/crates/uc-platform/src/adapters/mod.rs`:
  - Remove `pub mod in_memory_watcher_control;` line
  - Remove `pub use in_memory_watcher_control::InMemoryWatcherControl;` line
- Delete `src-tauri/crates/uc-platform/src/usecases/start_clipboard_watcher.rs`
- Update `src-tauri/crates/uc-platform/src/usecases/mod.rs`:
  - Remove `mod start_clipboard_watcher;` line
  - Remove entire `pub use start_clipboard_watcher::{ClipboardIntegrationMode, StartClipboardWatcher, StartClipboardWatcherError};` line
  - NOTE: `ClipboardIntegrationMode` is defined in `uc-core`, not in the file being deleted. Verify that `start_clipboard_watcher.rs` re-exports it from uc-core. If so, add `pub use uc_core::clipboard::ClipboardIntegrationMode;` directly to usecases/mod.rs if any code in uc-platform uses it, OR verify no uc-platform code references it through this path.
  - Keep `mod apply_autostart;` and its `pub use` line
  - The module should retain only: `mod apply_autostart; pub use apply_autostart::ApplyAutostartSetting;`

**Step 3 — Delete runtime module entirely:**

- Delete `src-tauri/crates/uc-platform/src/runtime/event_bus.rs`
- Delete `src-tauri/crates/uc-platform/src/runtime/runtime.rs`
- Delete `src-tauri/crates/uc-platform/src/runtime/mod.rs`
- (Or delete the entire `runtime/` directory)

**Step 4 — Delete ipc module entirely:**

- Delete `src-tauri/crates/uc-platform/src/ipc/command.rs`
- Delete `src-tauri/crates/uc-platform/src/ipc/event.rs`
- Delete `src-tauri/crates/uc-platform/src/ipc/mod.rs`
- (Or delete the entire `ipc/` directory)

**Step 5 — Delete port files and update ports/mod.rs:**

- Delete `src-tauri/crates/uc-platform/src/ports/watcher_control.rs`
- Delete `src-tauri/crates/uc-platform/src/ports/command_executor.rs`
- Delete `src-tauri/crates/uc-platform/src/ports/clipboard_runtime.rs` (zero external callers, depends on deleted PlatformEvent)
- Update `src-tauri/crates/uc-platform/src/ports/mod.rs`:
  - Remove `pub mod clipboard_runtime;`
  - Remove `pub mod command_executor;`
  - Remove `pub mod watcher_control;`
  - Remove `pub use clipboard_runtime::ClipboardRuntimePort;`
  - Remove `pub use command_executor::PlatformCommandExecutorPort;`
  - Remove `pub use watcher_control::{WatcherControlError, WatcherControlPort};`
  - Keep: `pub mod app_dirs;`, `pub mod app_event_handler;`, `pub mod autostart;`, `pub mod identity_store;`, `pub mod observability;`, `pub mod ui_port;` and their re-exports

**Step 6 — Update uc-platform lib.rs:**

- Remove `pub mod ipc;` line
- Remove `pub mod runtime;` line
- Keep all other module declarations (especially `pub mod clipboard;`)

**Step 7 — Run `cd src-tauri && cargo check -p uc-platform`**

- Fix any compilation errors from remaining uc-platform code that references deleted items
- The `usecases/mod.rs` may need to re-export `ClipboardIntegrationMode` if anything in uc-platform references it through the old path. Check if `ClipboardIntegrationMode` is defined in `start_clipboard_watcher.rs` or in `uc-core`. If it is re-exported from uc-core in `start_clipboard_watcher.rs`, downstream code should import from `uc_core` directly.
  </action>
  <verify>
  <automated>cd /Volumes/ExternalSSD/myprojects/uniclipboard-desktop/src-tauri && cargo check -p uc-platform 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria> - `src-tauri/crates/uc-platform/src/runtime/` directory does not exist - `src-tauri/crates/uc-platform/src/ipc/` directory does not exist - `src-tauri/crates/uc-platform/src/ports/watcher_control.rs` does not exist - `src-tauri/crates/uc-platform/src/ports/command_executor.rs` does not exist - `src-tauri/crates/uc-platform/src/ports/clipboard_runtime.rs` does not exist - `src-tauri/crates/uc-platform/src/adapters/in_memory_watcher_control.rs` does not exist - `src-tauri/crates/uc-platform/src/usecases/start_clipboard_watcher.rs` does not exist - `src-tauri/crates/uc-platform/tests/runtime_test.rs` does not exist - `src-tauri/crates/uc-platform/tests/watcher_control_test.rs` does not exist - `src-tauri/crates/uc-platform/src/lib.rs` does NOT contain `pub mod ipc` or `pub mod runtime` - `src-tauri/crates/uc-platform/src/lib.rs` DOES contain `pub mod clipboard` - `src-tauri/crates/uc-platform/src/ports/mod.rs` does NOT contain `watcher_control` or `command_executor` or `clipboard_runtime` - `cargo check -p uc-platform` exits 0
  </acceptance_criteria>
  <done>All PlatformRuntime, WatcherControl, IPC, and executor infrastructure deleted from uc-platform. Only clipboard module (ClipboardWatcher, LocalClipboard) and unrelated adapters/ports preserved. uc-platform compiles cleanly.</done>
  </task>

<task type="auto">
  <name>Task 2: Delete StartClipboardWatcherPort from uc-core and remove re-exports from uc-app</name>
  <files>
    src-tauri/crates/uc-core/src/ports/start_clipboard_watcher.rs,
    src-tauri/crates/uc-core/src/ports/mod.rs,
    src-tauri/crates/uc-app/src/usecases/mod.rs
  </files>
  <read_first>
    src-tauri/crates/uc-core/src/ports/start_clipboard_watcher.rs,
    src-tauri/crates/uc-core/src/ports/mod.rs,
    src-tauri/crates/uc-app/src/usecases/mod.rs
  </read_first>
  <action>
Per D-08:

**Step 1 — Delete the trait file:**

- Delete `src-tauri/crates/uc-core/src/ports/start_clipboard_watcher.rs`

**Step 2 — Update uc-core ports/mod.rs:**

- Remove `pub mod start_clipboard_watcher;` line
- Remove `pub use start_clipboard_watcher::{StartClipboardWatcherError, StartClipboardWatcherPort};` line
- Keep ALL other module declarations and re-exports unchanged

**Step 3 — Update uc-app usecases/mod.rs:**

- Remove line 60: `pub use uc_core::ports::{StartClipboardWatcherError, StartClipboardWatcherPort};`
- Keep ALL other re-exports unchanged. Specifically keep:
  - `pub use app_lifecycle::{ ... }` block (line 37-42)
  - All other `pub use` lines

**Step 4 — Verify compilation:**

- Run `cd src-tauri && cargo check -p uc-core -p uc-app`
- If errors appear from uc-app referencing StartClipboardWatcherPort (e.g., in app_lifecycle/mod.rs), those will be fixed in Plan 02. At this stage, only verify uc-core compiles. Run `cargo check -p uc-core` first.
  </action>
  <verify>
  <automated>cd /Volumes/ExternalSSD/myprojects/uniclipboard-desktop/src-tauri && cargo check -p uc-core 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria> - `src-tauri/crates/uc-core/src/ports/start_clipboard_watcher.rs` does not exist - `src-tauri/crates/uc-core/src/ports/mod.rs` does NOT contain `start_clipboard_watcher` - `src-tauri/crates/uc-app/src/usecases/mod.rs` does NOT contain `StartClipboardWatcherPort` or `StartClipboardWatcherError` - `src-tauri/crates/uc-app/src/usecases/mod.rs` DOES contain `pub use app_lifecycle::` - `cargo check -p uc-core` exits 0
  </acceptance_criteria>
  <done>StartClipboardWatcherPort trait deleted from uc-core. Re-exports removed from uc-app. uc-core compiles cleanly. ClipboardChangeHandler trait in uc-core preserved per D-15.</done>
  </task>

</tasks>

<verification>
After both tasks:
1. `cd src-tauri && cargo check -p uc-platform -p uc-core` must exit 0
2. No file in `src-tauri/crates/uc-platform/src/` contains `PlatformRuntime`, `WatcherControlPort`, or `PlatformCommand`
3. No file in `src-tauri/crates/uc-core/src/` contains `StartClipboardWatcherPort`
4. `src-tauri/crates/uc-platform/src/clipboard/` directory still exists and is unmodified
</verification>

<success_criteria>

- uc-platform runtime/ and ipc/ modules fully deleted
- WatcherControlPort, PlatformCommandExecutorPort, ClipboardRuntimePort deleted from uc-platform ports
- InMemoryWatcherControl deleted from uc-platform adapters
- StartClipboardWatcher use case deleted from uc-platform usecases
- StartClipboardWatcherPort deleted from uc-core ports
- Re-exports removed from uc-app usecases/mod.rs
- Both uc-platform and uc-core pass `cargo check`
- clipboard_rs dependency kept in uc-platform (per D-13)
- ClipboardChangeHandler trait kept in uc-core (per D-15)
  </success_criteria>

<output>
After completion, create `.planning/phases/65-remove-gui-clipboard-watcher-delegate-clipboard-monitoring-exclusively-to-daemon/65-01-SUMMARY.md`
</output>
