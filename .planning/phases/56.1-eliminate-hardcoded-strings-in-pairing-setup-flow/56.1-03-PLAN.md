---
phase: 56.1-eliminate-hardcoded-strings-in-pairing-setup-flow
plan: 03
type: execute
wave: 2
depends_on:
  - 56.1-01
files_modified:
  - src-tauri/crates/uc-daemon-client/src/ws_bridge.rs
autonomous: true
requirements:
  - PH561-05

must_haves:
  truths:
    - 'ws_bridge.rs topic_name() function returns uc-core constants instead of inline strings'
    - 'ws_bridge.rs map_daemon_ws_event() match arms use uc-core constants instead of inline strings'
    - 'ws_bridge.rs pairing stage match arms use uc-core constants'
    - 'All existing ws_bridge tests pass'
  artifacts:
    - path: 'src-tauri/crates/uc-daemon-client/src/ws_bridge.rs'
      provides: 'WS bridge using shared constants for event dispatch'
      contains: 'use uc_core::network::daemon_api_strings'
  key_links:
    - from: 'src-tauri/crates/uc-daemon-client/src/ws_bridge.rs'
      to: 'uc-core::network::daemon_api_strings'
      via: 'use import'
      pattern: 'use uc_core::network::daemon_api_strings'
---

<objective>
Replace all hardcoded string literals in uc-daemon-client (consumer side) with imports from uc-core::network::daemon_api_strings.

Purpose: Eliminate the client-side copy of all wire-protocol strings so topic subscriptions and event type dispatch cannot silently drift from the server-side definitions.
Output: ws_bridge.rs using shared constants for topic_name() and map_daemon_ws_event() with zero remaining inline wire-protocol string literals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-CONTEXT.md
@.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-RESEARCH.md
@.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ws_bridge.rs topic_name() and map_daemon_ws_event() inline strings with uc-core imports</name>
  <files>src-tauri/crates/uc-daemon-client/src/ws_bridge.rs</files>
  <read_first>
    - src-tauri/crates/uc-core/src/network/daemon_api_strings.rs (the constants to import)
    - src-tauri/crates/uc-daemon-client/src/ws_bridge.rs (full file — contains topic_name() at ~line 763 and map_daemon_ws_event() at ~line 510)
  </read_first>
  <action>
1. Add import at the top of ws_bridge.rs: `use uc_core::network::daemon_api_strings::{ws_topic, ws_event, pairing_stage};`

2. Replace `topic_name()` function body (around line 763-771):
   - `"pairing"` -> `ws_topic::PAIRING`
   - `"peers"` -> `ws_topic::PEERS`
   - `"paired-devices"` -> `ws_topic::PAIRED_DEVICES`
   - `"setup"` -> `ws_topic::SETUP`
   - `"space-access"` -> `ws_topic::SPACE_ACCESS`

3. Replace `map_daemon_ws_event()` event_type match arms (around line 515-743):
   - `"pairing.updated"` -> `ws_event::PAIRING_UPDATED`
   - `"pairing.verification_required"` -> `ws_event::PAIRING_VERIFICATION_REQUIRED`
   - `"pairing.complete"` -> `ws_event::PAIRING_COMPLETE`
   - `"pairing.failed"` -> `ws_event::PAIRING_FAILED`
   - `"peers.changed"` -> `ws_event::PEERS_CHANGED`
   - `"peers.name_updated"` -> `ws_event::PEERS_NAME_UPDATED`
   - `"peers.connection_changed"` -> `ws_event::PEERS_CONNECTION_CHANGED`
   - `"paired-devices.changed"` -> `ws_event::PAIRED_DEVICES_CHANGED`
   - `"setup.state_changed"` -> `ws_event::SETUP_STATE_CHANGED`
   - `"setup.space_access_completed"` -> `ws_event::SETUP_SPACE_ACCESS_COMPLETED`
   - `"space_access.state_changed"` -> `ws_event::SPACE_ACCESS_STATE_CHANGED`
   - `"space_access.snapshot"` -> `ws_event::SPACE_ACCESS_SNAPSHOT`

4. Inside the `"pairing.verification_required"` handler, replace the nested `payload.kind.as_str()` match arms:
   - `"verification"` -> `pairing_stage::VERIFICATION`
   - `"verifying" | "request"` -> `pairing_stage::VERIFYING | pairing_stage::REQUEST`
   - `"complete"` -> `pairing_stage::COMPLETE`
   - `"failed"` -> `pairing_stage::FAILED`

5. After all replacements, grep the file for remaining hardcoded topic/event/stage strings to verify completeness. Exclude strings in log messages, comments, and test assertions that reference human-readable descriptions.
   </action>
   <verify>
   <automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo test -p uc-daemon-client 2>&1 | tail -20</automated>
   </verify>
   <acceptance_criteria> - ws_bridge.rs contains `use uc_core::network::daemon_api_strings::` - ws_bridge.rs topic_name() contains `ws_topic::PAIRING` (not bare `"pairing"`) - ws_bridge.rs map_daemon_ws_event() contains `ws_event::PAIRING_UPDATED` (not bare `"pairing.updated"`) - ws_bridge.rs contains `pairing_stage::VERIFICATION` in the nested kind match - ws_bridge.rs does NOT contain bare `"pairing.updated"` as a match arm pattern - ws_bridge.rs does NOT contain bare `"peers.changed"` as a match arm pattern - ws_bridge.rs does NOT contain bare `"space-access"` in topic_name() return values - `cd src-tauri && cargo test -p uc-daemon-client` exits 0
   </acceptance_criteria>
   <done>ws_bridge.rs uses shared constants for all topic names, event types, and pairing stage labels; zero remaining inline wire-protocol strings</done>
   </task>

</tasks>

<verification>
- `cd src-tauri && cargo check -p uc-daemon-client` passes
- `cd src-tauri && cargo test -p uc-daemon-client` passes
- `grep -n '"pairing\.updated"\|"peers\.changed"\|"setup\.state_changed"\|"paired-devices"\|"space-access"' src-tauri/crates/uc-daemon-client/src/ws_bridge.rs | grep -v '//\|#\[' | wc -l` returns 0
- Phase gate: `cd src-tauri && cargo test -p uc-core -p uc-daemon -p uc-daemon-client` all green
</verification>

<success_criteria>

- ws_bridge.rs imports from daemon_api_strings
- Zero remaining hardcoded wire-protocol strings in ws_bridge.rs production code
- All existing uc-daemon-client tests pass
- Full phase test suite green: `cd src-tauri && cargo test -p uc-core -p uc-daemon -p uc-daemon-client`
  </success_criteria>

<output>
After completion, create `.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-03-SUMMARY.md`
</output>
