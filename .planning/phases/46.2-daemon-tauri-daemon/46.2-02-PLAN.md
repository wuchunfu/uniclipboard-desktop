---
phase: 46.2-daemon-tauri-daemon
plan: 02
type: execute
wave: 2
depends_on: ['46.2-01']
files_modified:
  - .planning/REQUIREMENTS.md
  - src-tauri/crates/uc-tauri/src/commands/pairing.rs
  - src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/realtime_runtime.rs
  - src-tauri/src/main.rs
  - src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs
autonomous: true
requirements: ['R46.2-1', 'R46.2-2', 'R46.2-4', 'R46.2-7']
user_setup: []
must_haves:
  truths:
    - 'Phase requirement IDs R46.2-1 through R46.2-8 are formally defined in REQUIREMENTS.md and traceable to Phase 46.2.'
    - 'All GUI pairing commands and setup-facing pairing entrypoints use the daemon pairing client; uc-tauri no longer owns PairingOrchestrator state or legacy pairing loops.'
    - 'Realtime pairing updates flow only through daemon websocket -> DaemonWsBridge -> existing frontend event contract.'
    - 'Shell-side regression tests prove the daemon-backed pairing bridge is stable enough for frontend parity work to build on.'
  artifacts:
    - path: '.planning/REQUIREMENTS.md'
      provides: 'Formal R46.2-1..R46.2-8 definitions and Phase 46.2 traceability rows aligned with roadmap and plans.'
      min_lines: 160
    - path: 'src-tauri/crates/uc-tauri/src/commands/pairing.rs'
      provides: 'Tauri pairing commands implemented as daemon-client shims with no PairingOrchestrator ownership.'
      min_lines: 200
    - path: 'src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs'
      provides: 'Tauri daemon pairing client that targets the stable daemon /pairing/* surface.'
      min_lines: 120
    - path: 'src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs'
      provides: 'Single realtime bridge that translates daemon pairing events into existing frontend contracts.'
      min_lines: 160
  key_links:
    - from: '.planning/ROADMAP.md'
      to: '.planning/REQUIREMENTS.md'
      via: 'Phase 46.2 requirement IDs declared in the roadmap are mirrored as formal requirement definitions and traceability rows.'
      pattern: "R46\\.2-1|R46\\.2-2|R46\\.2-3|R46\\.2-4|R46\\.2-5|R46\\.2-6|R46\\.2-7|R46\\.2-8"
    - from: 'src-tauri/crates/uc-tauri/src/commands/pairing.rs'
      to: 'src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs'
      via: 'TauriDaemonPairingClient replaces direct PairingOrchestrator usage in pairing commands.'
      pattern: 'TauriDaemonPairingClient::new|pairing_client'
    - from: 'src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs'
      to: 'src-tauri/crates/uc-daemon/src/api/routes.rs'
      via: 'Tauri daemon client targets daemon /pairing/initiate|accept|reject|cancel|gui/lease and /pairing/sessions/:id/verify routes.'
      pattern: '/pairing/initiate|/pairing/accept|/pairing/reject|/pairing/cancel|/pairing/gui/lease|/pairing/sessions/'
    - from: 'src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs'
      to: 'src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs'
      via: 'DaemonWsBridge pairing event mapping is guarded by shell-side regression tests.'
      pattern: 'p2p-pairing-verification|daemon://realtime|pairing'
---

<objective>
完成 46.2 的 shell-side 硬切换与文档补洞：补齐正式 requirements/traceability，同时把 uc-tauri 和前端配对流完全收敛为 daemon client + DaemonWsBridge 的薄桥接实现，不改变现有用户可见阶段语义。

Purpose: 在 daemon contract 已固定的前提下，删除 Tauri pairing host 残留，让 GUI 只消费 daemon pairing surface，并把 phase 级 requirement 合同补齐到 REQUIREMENTS.md。
Output: 更新后的 REQUIREMENTS.md、daemon-backed Tauri pairing bridge、保持原契约的前端 pairing flow，以及对应 regression tests。
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46.2-daemon-tauri-daemon/46.2-CONTEXT.md
@.planning/phases/46.2-daemon-tauri-daemon/46.2-RESEARCH.md
@.planning/phases/46.2-daemon-tauri-daemon/46.2-VALIDATION.md
@.planning/phases/46.2-daemon-tauri-daemon/46.2-VERIFICATION.md
@.planning/phases/46.2-daemon-tauri-daemon/46.2-01-PLAN.md

@src-tauri/crates/uc-tauri/src/commands/pairing.rs
@src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs
@src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs
@src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
@src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs
@src-tauri/crates/uc-tauri/src/bootstrap/realtime_runtime.rs
@src-tauri/src/main.rs
@src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 补齐 Phase 46.2 requirements contract 与 traceability</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/46.2-daemon-tauri-daemon/46.2-RESEARCH.md
    - .planning/phases/46.2-daemon-tauri-daemon/46.2-VERIFICATION.md
    - .planning/phases/46.2-daemon-tauri-daemon/46.2-01-PLAN.md
    - .planning/phases/46.2-daemon-tauri-daemon/46.2-02-PLAN.md
  </read_first>
  <action>
    1. 在 `.planning/REQUIREMENTS.md` 的 Phase 46 / 46.1 相邻位置新增 `### Daemon Pairing Hard Cutover` 小节，正式定义并标记为完成的 `R46.2-1` 到 `R46.2-8` 八条 requirement，文案以 `ROADMAP.md` 和 `46.2-RESEARCH.md` 为准，不得再让这些 ID 只存在于 roadmap/plan 中。
    2. 在 `.planning/REQUIREMENTS.md` 的 `## Traceability` 表中新增 `R46.2-1` 到 `R46.2-8` 八行，`Phase` 列全部写成 `46.2`，`Status` 列全部写成 `Complete`，并保持 Phase 46 / 46.1 / 46.2 的顺序连续。
    3. 更新 `.planning/REQUIREMENTS.md` 底部 `_Last updated:` 行，明确写出 `Phase 46.2`，让后续 verifier 可以通过 grep 直接看到这次 gap closure 文档补齐已经发生。
  </action>
  <verify>
    <automated>
      rg "\\*\\*R46\\.2-1\\*\\*|\\*\\*R46\\.2-8\\*\\*" .planning/REQUIREMENTS.md -n
      rg "R46\\.2-1\\s*\\|\\s*46\\.2\\s*\\|\\s*Complete|R46\\.2-8\\s*\\|\\s*46\\.2\\s*\\|\\s*Complete" .planning/REQUIREMENTS.md -n
      rg "_Last updated: .*Phase 46\\.2" .planning/REQUIREMENTS.md -n
    </automated>
  </verify>
  <acceptance_criteria>
    - `rg "\\*\\*R46\\.2-1\\*\\*" .planning/REQUIREMENTS.md -n` 返回正式 requirement 定义。
    - `rg "\\*\\*R46\\.2-8\\*\\*" .planning/REQUIREMENTS.md -n` 返回正式 requirement 定义。
    - `rg "R46\\.2-1\\s*\\|\\s*46\\.2\\s*\\|\\s*Complete" .planning/REQUIREMENTS.md -n` 返回 traceability 行。
    - `rg "R46\\.2-8\\s*\\|\\s*46\\.2\\s*\\|\\s*Complete" .planning/REQUIREMENTS.md -n` 返回 traceability 行。
    - `rg "_Last updated: .*Phase 46\\.2" .planning/REQUIREMENTS.md -n` 返回更新后的底部说明。
  </acceptance_criteria>
  <done>
    - `R46.2-1` 到 `R46.2-8` 在 REQUIREMENTS.md 中成为正式 requirement，而不是悬空 roadmap 引用。
    - Phase 46.2 的 traceability 与 verification / plans 保持一致。
  </done>
</task>

<task type="auto">
  <name>Task 2: 将 uc-tauri pairing surface 硬切到 daemon client 与单一路径 realtime bridge</name>
  <files>
    src-tauri/crates/uc-tauri/src/commands/pairing.rs,
    src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs,
    src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs,
    src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs,
    src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs,
    src-tauri/crates/uc-tauri/src/bootstrap/realtime_runtime.rs,
    src-tauri/src/main.rs,
    src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs
  </files>
  <read_first>
    - src-tauri/crates/uc-tauri/src/commands/pairing.rs
    - src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs
    - src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs
    - src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
    - src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs
    - src-tauri/crates/uc-tauri/src/bootstrap/realtime_runtime.rs
    - src-tauri/src/main.rs
    - src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs
    - .planning/phases/46.2-daemon-tauri-daemon/46.2-01-PLAN.md
    - .planning/phases/46.2-daemon-tauri-daemon/46.2-VALIDATION.md
  </read_first>
  <action>
    1. 在 `src-tauri/crates/uc-tauri/src/commands/pairing.rs` 中，把 `initiate_p2p_pairing`、`accept_p2p_pairing`、`reject_p2p_pairing`、`verify_p2p_pairing_pin` 等配对命令统一改成 daemon client shim：
       - 保持命令名与前端契约不变。
       - 使用 `TauriDaemonPairingClient` 调用 daemon `/pairing/*` surface，不再直接依赖 `PairingOrchestrator` 或本地 pairing host state。
       - 错误通过既有 command error / response.error 路径回给前端，不额外引入新的事件名。
    2. 在 `src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs` 中，固定 daemon pairing client 的目标路径：`/pairing/initiate`、`/pairing/accept`、`/pairing/reject`、`/pairing/cancel`、`/pairing/gui/lease`、`/pairing/sessions/:id/verify`，并保留 daemon 返回的错误 metadata 给命令层映射。
    3. 在 `src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs`、`src-tauri/crates/uc-tauri/src/bootstrap/realtime_runtime.rs`、`src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs`、`src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs` 和 `src-tauri/src/main.rs` 中，删除或改造所有 legacy pairing host 路径：
       - 移除 `PairingOrchestrator` state ownership 与 legacy pairing loop/backoff wiring。
       - 保持 pairing realtime 只通过 DaemonWsBridge 转成现有前端事件契约。
       - setup 相关桥接仍然消费 daemon pairing API / realtime，不重新引入第二套 host。
    4. 在 `src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs` 中补齐 cutover regression，证明 daemon bridge 对 pairing realtime contract 的 shell-side 映射稳定成立。
  </action>
  <verify>
    <automated>
      cd src-tauri &amp;&amp; cargo test -p uc-tauri daemon_ws_bridge -- --test-threads=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `rg "PairingOrchestrator" src-tauri/crates/uc-tauri src-tauri/src/main.rs -n` 无任何匹配结果。
    - `rg "TauriDaemonPairingClient::new|pairing_client" src-tauri/crates/uc-tauri/src/commands/pairing.rs -n` 返回 pairing 命令中的 daemon client 调用位置。
    - `rg "/pairing/initiate|/pairing/accept|/pairing/reject|/pairing/cancel|/pairing/gui/lease|/pairing/sessions/" src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs -n` 返回 daemon pairing 路径定义或调用位置。
    - `rg "run_pairing_event_loop|pairing_events_subscribe_backoff_ms|manage\\(pairing_orchestrator\\)" src-tauri/crates/uc-tauri src-tauri/src/main.rs -n` 无任何匹配结果。
    - `rg "p2p-pairing-verification|daemon://realtime" src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs -n` 返回 bridge 与 shell-side regression 的连接点。
    - `cd src-tauri &amp;&amp; cargo test -p uc-tauri daemon_ws_bridge -- --test-threads=1` 退出码为 0。
  </acceptance_criteria>
  <done>
    - uc-tauri pairing commands、setup bridge 与 runtime wiring 变成 daemon-only bridge，而不是业务 host。
    - shell 层只保留 daemon client + realtime bridge 两种 pairing integration points。
  </done>
</task>

</tasks>

<verification>
- `rg "\\*\\*R46\\.2-1\\*\\*|\\*\\*R46\\.2-8\\*\\*" .planning/REQUIREMENTS.md -n` 与 `rg "R46\\.2-1\\s*\\|\\s*46\\.2\\s*\\|\\s*Complete|R46\\.2-8\\s*\\|\\s*46\\.2\\s*\\|\\s*Complete" .planning/REQUIREMENTS.md -n` 通过，证明 46.2 requirement 合同与 traceability 已补齐。
- `cd src-tauri && cargo test -p uc-tauri daemon_ws_bridge -- --test-threads=1` 通过，证明 daemon realtime bridge 仍然稳定。
- `rg "PairingOrchestrator" src-tauri/crates/uc-tauri src-tauri/src/main.rs -n` 无匹配，证明 Tauri pairing host 残留已被移除。
</verification>

<success_criteria>

- Phase 46.2 的 formal requirements 和 traceability 文档完整落地。
- uc-tauri shell 收敛到 daemon client + DaemonWsBridge 的薄桥实现，不再持有 pairing host。
- shell-side bridge regression 可以自动验证，为下一步 frontend parity plan 提供稳定基础。
  </success_criteria>

<output>
完成后创建 `.planning/phases/46.2-daemon-tauri-daemon/46.2-02-SUMMARY.md`，总结 requirements 补齐与 Tauri shell bridge cutover 结果。
</output>
