---
phase: 28-support-link-content-type-mime-link-and-url-detected-plain-text
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - src/api/clipboardItems.ts
  - src/components/clipboard/ClipboardPreview.tsx
  - src/components/clipboard/ClipboardItemRow.tsx
  - src/components/clipboard/ClipboardContent.tsx
  - src/components/device/DeviceSettingsPanel.tsx
autonomous: true
requirements:
  - LINK-05
  - LINK-06
  - LINK-07

must_haves:
  truths:
    - 'Link entries display the first URL as clickable text with ExternalLink icon in list view'
    - "Multi-URL link entries (text/uri-list) show '+N more' badge in list view"
    - 'Link detail panel shows all URLs with domains and character count'
    - "Link toggle in DeviceSettingsPanel is interactive (not 'Coming Soon')"
    - 'transformProjectionToResponse correctly identifies link entries from projections'
  artifacts:
    - path: 'src/api/clipboardItems.ts'
      provides: 'Updated ClipboardLinkItem interface with urls and domains arrays, link detection in transformProjectionToResponse'
      contains: 'urls: string[]'
    - path: 'src/components/clipboard/ClipboardPreview.tsx'
      provides: 'Multi-URL detail view with domain display'
      contains: 'domains'
    - path: 'src/components/device/DeviceSettingsPanel.tsx'
      provides: 'Link toggle as editable'
      contains: 'link.*editable'
  key_links:
    - from: 'src/api/clipboardItems.ts'
      to: 'src/components/clipboard/ClipboardContent.tsx'
      via: 'ClipboardLinkItem type used in contentByType mapping'
      pattern: 'ClipboardLinkItem'
    - from: 'src/api/clipboardItems.ts'
      to: 'src/components/clipboard/ClipboardPreview.tsx'
      via: 'ClipboardLinkItem imported for link detail rendering'
      pattern: 'ClipboardLinkItem'
---

<objective>
Frontend link display and sync toggle activation.

Purpose: Update the frontend to receive and display multi-URL link data from the backend, show links with proper UI treatment (clickable URLs, domains, +N more badge), and activate the link sync toggle in device settings.

Output: Updated ClipboardLinkItem type, link-aware transformProjectionToResponse, enhanced ClipboardPreview for links, activated link toggle.
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-support-link-content-type-mime-link-and-url-detected-plain-text/28-CONTEXT.md
@.planning/phases/28-support-link-content-type-mime-link-and-url-detected-plain-text/28-RESEARCH.md
@.planning/phases/28-support-link-content-type-mime-link-and-url-detected-plain-text/28-01-SUMMARY.md

<interfaces>
<!-- Backend contract after Plan 01 -->

ClipboardLinkItemDto (from backend get_clipboard_item):

```json
{
  "urls": ["https://github.com", "https://example.com"],
  "domains": ["github.com", "example.com"]
}
```

ClipboardItemDto.link field is now Option<ClipboardLinkItemDto> (was Option<serde_json::Value>).

ClipboardEntryProjection.content_type for links:

- "text/uri-list" for MIME-based links
- "text/plain" for URL-detected plain text (still reports text/plain in projection MIME, but backend classify_snapshot returns Link)

Note: The projection path (get_clipboard_entries -> transformProjectionToResponse) does NOT go through backend DTO mapping. The frontend transform must detect links itself using content_type and preview text.

From src/components/clipboard/ClipboardContent.tsx:

```typescript
export interface DisplayClipboardItem {
  id: string
  type: 'text' | 'image' | 'link' | 'code' | 'file' | 'unknown'
  content: ClipboardTextItem | ClipboardImageItem | ClipboardLinkItem | ...
  // ... other fields
}
```

From src/components/device/DeviceSettingsPanel.tsx:

```typescript
const contentTypeEntries = [
  { field: 'text', i18nKey: 'syncText', status: 'editable' },
  { field: 'image', i18nKey: 'syncImage', status: 'editable' },
  { field: 'file', i18nKey: 'syncFile', status: 'coming_soon' },
  { field: 'link', i18nKey: 'syncLink', status: 'coming_soon' }, // -> change to 'editable'
  // ...
]
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update frontend types, transformProjectionToResponse, and link display components</name>
  <files>src/api/clipboardItems.ts, src/components/clipboard/ClipboardPreview.tsx, src/components/clipboard/ClipboardItemRow.tsx</files>
  <action>
1. In `src/api/clipboardItems.ts`:
   - Update `ClipboardLinkItem` interface from `{ url: string }` to `{ urls: string[], domains: string[] }`.
   - Add a helper function `isLinkType(contentType: string, preview: string): boolean` that returns true if:
     a. contentType is "text/uri-list", OR
     b. contentType starts with "text/plain" AND preview (trimmed) looks like a single URL (starts with http://, https://, ftp://, ftps://, mailto: and contains no whitespace)
   - Note: This is a lightweight frontend heuristic. The authoritative classification happens in the backend. This is only needed for the projection path.
   - Update `transformProjectionToResponse`:
     - Before the existing `isImage` check, add link detection using `isLinkType(entry.content_type, entry.preview)`.
     - When link detected: set `link: { urls: [entry.preview.trim()], domains: [extractDomainFromUrl(entry.preview.trim())] }` for text/plain URLs. For text/uri-list, parse lines (skip empty and # lines) into urls array and extract domains.
     - Add a local `extractDomainFromUrl(url: string): string` helper using `new URL(url).hostname` in a try/catch.
     - Set text/image/file/code/unknown to null when link is detected.

2. In `src/components/clipboard/ClipboardItemRow.tsx`:
   - Update `getPreviewText` link case: change from `(item.content as ClipboardLinkItem).url` to `(item.content as ClipboardLinkItem).urls[0] ?? ''`.
   - After the preview text span, if link type and `urls.length > 1`, render a small badge: `<span className="text-xs text-muted-foreground bg-muted/50 px-1.5 py-0.5 rounded-full shrink-0">+{urls.length - 1}</span>`. This is the "+N more" badge per user decision.

3. In `src/components/clipboard/ClipboardPreview.tsx`:
   - Update the `case 'link'` in `renderContent`:
     - Get `linkItem = item.content as ClipboardLinkItem`
     - Show first URL as clickable link with ExternalLink icon (existing pattern, update to use `linkItem.urls[0]`).
     - If `linkItem.urls.length > 1`, show all remaining URLs below, each as a clickable link with its domain displayed in muted text.
   - Update `renderInformation` for link type:
     - Show domain(s): `linkItem.domains.filter(Boolean).join(', ')` or first domain.
     - Show URL count if > 1.
     - Show character count of first URL.
   - Import is not needed for ClipboardLinkItem -- it's already imported.
     </action>
     <verify>
     <automated>cd /Users/mark/conductor/workspaces/uniclipboard-desktop/budapest-v1 && bun run build</automated>
     </verify>
     <done>
   - ClipboardLinkItem has urls and domains arrays
   - transformProjectionToResponse detects link entries from both text/uri-list and plain-text URLs
   - List view shows first URL with +N more badge for multi-URL entries
   - Detail panel shows all URLs with domains
   - Frontend builds without TypeScript errors
     </done>
     </task>

<task type="auto">
  <name>Task 2: Activate link toggle in DeviceSettingsPanel</name>
  <files>src/components/device/DeviceSettingsPanel.tsx</files>
  <action>
1. In `src/components/device/DeviceSettingsPanel.tsx`:
   - In the `contentTypeEntries` array, change the link entry status from `'coming_soon'` to `'editable'`:
     ```
     { field: 'link', i18nKey: 'syncLink', status: 'editable' },
     ```
   - This single change makes the link toggle interactive (the existing rendering logic already handles editable vs coming_soon status — editable entries show a working Switch, coming_soon entries show a disabled Switch with "Coming Soon" badge).
   - All other unimplemented types (file, code_snippet, rich_text) remain 'coming_soon'.
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/uniclipboard-desktop/budapest-v1 && bun run build</automated>
  </verify>
  <done>
    - Link toggle in DeviceSettingsPanel is interactive (no "Coming Soon" badge)
    - File, code_snippet, rich_text toggles remain "Coming Soon"
    - Frontend builds without errors
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/mark/conductor/workspaces/uniclipboard-desktop/budapest-v1 && bun run build
```
Frontend builds with no TypeScript errors. Link types, display, and toggle are all wired.
</verification>

<success_criteria>

- ClipboardLinkItem uses urls/domains arrays (not single url string)
- Link entries detected and displayed correctly in both list and detail views
- Multi-URL entries show +N more badge in list, all URLs in detail
- Link sync toggle is interactive in DeviceSettingsPanel
- Frontend builds successfully
  </success_criteria>

<output>
After completion, create `.planning/phases/28-support-link-content-type-mime-link-and-url-detected-plain-text/28-02-SUMMARY.md`
</output>
