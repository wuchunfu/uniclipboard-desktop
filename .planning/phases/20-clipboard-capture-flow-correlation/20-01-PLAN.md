---
phase: 20-clipboard-capture-flow-correlation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/crates/uc-observability/Cargo.toml
  - src-tauri/crates/uc-observability/src/flow.rs
  - src-tauri/crates/uc-observability/src/stages.rs
  - src-tauri/crates/uc-observability/src/lib.rs
  - src-tauri/crates/uc-app/Cargo.toml
autonomous: true
requirements:
  - FLOW-01
  - FLOW-03

must_haves:
  truths:
    - 'FlowId::generate() produces a UUID v7 string usable as a tracing span field'
    - 'Stage constants are accessible from both uc-tauri and uc-app crates'
    - 'uc-observability compiles with uuid v7 feature enabled'
  artifacts:
    - path: 'src-tauri/crates/uc-observability/src/flow.rs'
      provides: 'FlowId newtype wrapping UUID v7'
      exports: ['FlowId']
    - path: 'src-tauri/crates/uc-observability/src/stages.rs'
      provides: 'Stage name constants for capture pipeline'
      exports:
        [
          'DETECT',
          'NORMALIZE',
          'PERSIST_EVENT',
          'CACHE_REPRESENTATIONS',
          'SELECT_POLICY',
          'PERSIST_ENTRY',
        ]
    - path: 'src-tauri/crates/uc-observability/src/lib.rs'
      provides: 'Public re-exports of flow and stages modules'
      contains: 'pub mod flow'
  key_links:
    - from: 'src-tauri/crates/uc-observability/src/flow.rs'
      to: 'uuid crate'
      via: 'Uuid::now_v7()'
      pattern: 'Uuid::now_v7'
    - from: 'src-tauri/crates/uc-app/Cargo.toml'
      to: 'uc-observability'
      via: 'dependency declaration'
      pattern: 'uc-observability'
---

<objective>
Add FlowId newtype and stage name constants to the uc-observability crate, establishing the shared vocabulary for clipboard capture flow correlation.

Purpose: Plan 02 instruments the actual capture pipeline with stage spans and flow_id fields. This plan creates the types and constants those spans reference, ensuring consistency across crates.
Output: FlowId newtype in uc-observability, stage constants module, dependency wiring for uc-app.
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-clipboard-capture-flow-correlation/20-CONTEXT.md
@.planning/phases/20-clipboard-capture-flow-correlation/20-RESEARCH.md

@src-tauri/crates/uc-observability/src/lib.rs
@src-tauri/crates/uc-observability/Cargo.toml
@src-tauri/crates/uc-app/Cargo.toml

<interfaces>
<!-- Existing uc-observability public API (from lib.rs) -->
From src-tauri/crates/uc-observability/src/lib.rs:
```rust
pub mod format;
mod init;
pub mod profile;

pub use init::{build_console_layer, build_json_layer, init_tracing_subscriber};
pub use profile::LogProfile;
pub use tracing_appender::non_blocking::WorkerGuard;

````
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FlowId newtype and stage constants in uc-observability</name>
  <files>
    src-tauri/crates/uc-observability/Cargo.toml,
    src-tauri/crates/uc-observability/src/flow.rs,
    src-tauri/crates/uc-observability/src/stages.rs,
    src-tauri/crates/uc-observability/src/lib.rs
  </files>
  <behavior>
    - FlowId::generate() returns a FlowId that displays as a UUID v7 string (36 chars with hyphens)
    - FlowId implements Display, Debug, Clone, PartialEq, Eq, Hash
    - FlowId can be used with tracing's %flow_id format directive (via Display)
    - Stage constants DETECT, NORMALIZE, PERSIST_EVENT, CACHE_REPRESENTATIONS, SELECT_POLICY, PERSIST_ENTRY are &str values matching their lowercase names
  </behavior>
  <action>
    1. Add `uuid = { version = "1", features = ["v7"] }` to uc-observability's `[dependencies]` in Cargo.toml.

    2. Create `src-tauri/crates/uc-observability/src/flow.rs`:
       - Define `pub struct FlowId(Uuid)` with derive Debug, Clone, PartialEq, Eq, Hash
       - Implement `FlowId::generate()` using `Uuid::now_v7()`
       - Implement `fmt::Display` delegating to inner Uuid's Display
       - No serde needed (this is a tracing field, not serialized)

    3. Create `src-tauri/crates/uc-observability/src/stages.rs`:
       - Define pub const for each stage: DETECT, NORMALIZE, PERSIST_EVENT, CACHE_REPRESENTATIONS, SELECT_POLICY, PERSIST_ENTRY
       - Values are lowercase snake_case strings matching the const name (e.g., `pub const DETECT: &str = "detect"`)
       - Add module-level doc comment explaining these are for clipboard capture flow correlation

    4. Update `src-tauri/crates/uc-observability/src/lib.rs`:
       - Add `pub mod flow;` and `pub mod stages;`
       - Add `pub use flow::FlowId;` for convenient top-level access
       - Keep all existing exports unchanged
  </action>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-observability --lib 2>&1 | tail -20</automated>
  </verify>
  <done>FlowId::generate() compiles and returns a displayable UUID v7. Stage constants are public. uc-observability builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add uc-observability dependency to uc-app</name>
  <files>src-tauri/crates/uc-app/Cargo.toml</files>
  <action>
    Add `uc-observability = { path = "../uc-observability" }` to uc-app's `[dependencies]` section. This enables CaptureClipboardUseCase (in uc-app) to reference stage constants from uc-observability in Plan 02.

    Verify the full workspace still compiles after this dependency addition.
  </action>
  <verify>
    <automated>cd src-tauri && cargo check -p uc-app 2>&1 | tail -5</automated>
  </verify>
  <done>uc-app depends on uc-observability and `cargo check -p uc-app` succeeds.</done>
</task>

</tasks>

<verification>
All uc-observability tests pass, uc-app compiles with new dependency, FlowId and stage constants are importable from both uc-tauri and uc-app.

```bash
cd src-tauri && cargo test -p uc-observability --lib && cargo check -p uc-app -p uc-tauri
````

</verification>

<success_criteria>

- FlowId newtype exists with generate(), Display, Debug, Clone
- Six stage constants exported from uc_observability::stages
- uc-app can import uc_observability::stages::\* and uc_observability::FlowId
- No compilation errors across affected crates
  </success_criteria>

<output>
After completion, create `.planning/phases/20-clipboard-capture-flow-correlation/20-01-SUMMARY.md`
</output>
