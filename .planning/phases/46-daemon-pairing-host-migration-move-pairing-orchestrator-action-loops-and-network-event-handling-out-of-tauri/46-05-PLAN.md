---
phase: 46
plan: 5
name: 'Gap Closure For Live GUI Pairing Bridge Activation'
type: execute
wave: 5
depends_on: ['46-04']
files_modified:
  - src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/pairing_bridge.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
  - src-tauri/src/main.rs
  - src-tauri/crates/uc-tauri/tests/pairing_bridge.rs
autonomous: true
gap_closure: true
requirements:
  - PH46-05
  - PH46-05A
  - PH46-06
must_haves:
  truths:
    - 'the GUI entrypoint constructs and passes a live PairingBridge so daemon pairing and discovery events actually reach Tauri compatibility events at runtime'
    - 'uc-tauri no longer retains the legacy pairing_action and pairing_events ownership loops as a fallback business host'
    - 'GUI startup keeps daemon discoverable by default, but participant-ready remains flow-scoped and bridge-controlled'
    - 'the Tauri setup pairing adapter implements the app-layer SetupPairingFacadePort without introducing a second daemon auth path'
  artifacts:
    - path: 'src-tauri/src/main.rs'
      provides: 'live bridge construction and injection into startup wiring'
      contains: 'Some(pairing_bridge)'
    - path: 'src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs'
      provides: 'bridge-only pairing startup path with no legacy host loops'
      contains: 'if let Some(mut bridge) = pairing_bridge'
    - path: 'src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs'
      provides: 'Tauri adapter implementing the app-layer setup pairing facade'
      contains: 'impl SetupPairingFacadePort for DaemonBackedSetupPairingFacade'
    - path: 'src-tauri/crates/uc-tauri/tests/pairing_bridge.rs'
      provides: 'runtime bridge activation and setup adapter regression coverage'
      contains: 'bridge_reports_lease_loss_without_dropping_active_session'
  key_links:
    - from: 'src-tauri/src/main.rs'
      to: 'src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs'
      via: 'main constructs PairingBridge and passes Some(pairing_bridge) into start_background_tasks'
    - from: 'src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs'
      to: 'src-tauri/crates/uc-app/src/usecases/setup/pairing_facade.rs'
      via: 'the Tauri adapter implements the app-layer SetupPairingFacadePort defined by the gap-closure extraction plan'
---

# Plan 46-05: Gap Closure For Live GUI Pairing Bridge Activation

## Goal

Close the remaining runtime-side Phase 46 verification gaps by activating the daemon pairing bridge in the GUI entrypoint, removing the hidden Tauri fallback loops, and binding the Tauri setup adapter to the app-layer setup pairing facade introduced in Plan 46-04.

## Context

@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-CONTEXT.md
@.planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-RESEARCH.md
@.planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-VERIFICATION.md
@.planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-03-SUMMARY.md
@.planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-04-PLAN.md

## Tasks

<task id="1" type="auto">
<name>Bind the Tauri setup pairing adapter to the app-layer facade contract</name>
<files>
- src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs
- src-tauri/crates/uc-tauri/tests/pairing_bridge.rs
</files>
<read_first>
- src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs
- src-tauri/crates/uc-tauri/tests/pairing_bridge.rs
- src-tauri/crates/uc-app/src/usecases/setup/pairing_facade.rs
- .planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-04-PLAN.md
</read_first>
<action>
Update `src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs` so `DaemonBackedSetupPairingFacade` implements the app-layer `uc_app::usecases::setup::SetupPairingFacadePort` contract introduced by Plan 46-04 rather than declaring or relying on a Tauri-local copy. Keep the adapter in `uc-tauri`, keep daemon command/auth usage on the existing `TauriDaemonPairingClient` path, and map daemon realtime updates into the setup-facing domain-event subset required by the setup flow.

Strengthen `src-tauri/crates/uc-tauri/tests/pairing_bridge.rs` so `bridge_feeds_setup_with_setup_pairing_facade` and `bridge_keeps_setup_flow_semantics` assert the real app-layer trait path and fail if the adapter stops implementing `SetupPairingFacadePort` or stops providing the setup-facing event progression semantics.
</action>
<acceptance_criteria>

- `src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs` contains `impl SetupPairingFacadePort for DaemonBackedSetupPairingFacade`
- `src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs` contains `subscribe`
- `src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs` contains `initiate_pairing`
- `src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs` contains `accept_pairing`
- `src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs` contains `reject_pairing`
- `src-tauri/crates/uc-tauri/tests/pairing_bridge.rs` contains `bridge_feeds_setup_with_setup_pairing_facade`
- `src-tauri/crates/uc-tauri/tests/pairing_bridge.rs` contains `bridge_keeps_setup_flow_semantics`
- `cd src-tauri && cargo test -p uc-tauri --test pairing_bridge -- --test-threads=1` exits 0
  </acceptance_criteria>
  <verify>
  <automated>cd src-tauri && cargo test -p uc-tauri --test pairing_bridge -- --test-threads=1</automated>
  </verify>
  <done>
  The Tauri setup pairing adapter implements the single app-layer setup facade contract and preserves the setup-facing event and command semantics without adding a second daemon auth path.
  </done>
  </task>

<task id="2" type="auto">
<name>Activate the GUI pairing bridge in main.rs and remove the legacy Tauri pairing host fallback</name>
<files>
- src-tauri/src/main.rs
- src-tauri/crates/uc-tauri/src/bootstrap/pairing_bridge.rs
- src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
- src-tauri/crates/uc-tauri/tests/pairing_bridge.rs
</files>
<read_first>
- src-tauri/src/main.rs
- src-tauri/crates/uc-tauri/src/bootstrap/pairing_bridge.rs
- src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
- src-tauri/crates/uc-tauri/tests/pairing_bridge.rs
- .planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-VERIFICATION.md
- .planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-VALIDATION.md
</read_first>
<action>
Update `src-tauri/src/main.rs` so the GUI entrypoint builds a real bridge instead of passing `pairing_bridge = None`. Instantiate `PairingBridge::new(app.handle().clone(), daemon_connection_state.clone())` in the startup path where `start_background_tasks(...)` is called, and pass `Some(pairing_bridge)` into that call.

Update `src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs` so `start_background_tasks(...)` no longer contains the `else` branch that spawns `.spawn("pairing_action", ...)` or `.spawn("pairing_events", ...)`. If bridge creation or startup fails, log the failure and surface degradation through the bridge event path, but do not resurrect Tauri-owned pairing action/event loops.

Tighten `src-tauri/crates/uc-tauri/src/bootstrap/pairing_bridge.rs` so runtime behavior matches the phase-locked GUI semantics:

- GUI startup sets daemon discoverability on by default
- participant-ready stays flow-scoped and is not enabled at raw process startup
- shutdown, disconnect, or lease loss revokes discoverability/readiness
- degraded bridge state emits `pairing-bridge-lease-lost` instead of overloading `p2p-pairing-verification`
- existing frontend event names remain unchanged

Expand `src-tauri/crates/uc-tauri/tests/pairing_bridge.rs` so the bridge regression suite covers runtime activation, discoverability lifecycle, and lease-loss handling:

- `bridge_registers_gui_client_as_discoverable_by_default`
- `bridge_sets_participant_ready_only_when_pairing_flow_is_active`
- `bridge_revokes_discoverability_and_ready_on_shutdown`
- `bridge_reports_lease_loss_without_dropping_active_session`
  </action>
  <acceptance_criteria>
- `src-tauri/src/main.rs` does not contain `let pairing_bridge = None`
- `src-tauri/src/main.rs` contains `PairingBridge::new`
- `src-tauri/src/main.rs` contains `Some(pairing_bridge)`
- `src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs` does not contain `.spawn("pairing_action"`
- `src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs` does not contain `.spawn("pairing_events"`
- `src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs` contains `if let Some(mut bridge) = pairing_bridge`
- `src-tauri/crates/uc-tauri/src/bootstrap/pairing_bridge.rs` contains `pairing-bridge-lease-lost`
- `src-tauri/crates/uc-tauri/tests/pairing_bridge.rs` contains `bridge_registers_gui_client_as_discoverable_by_default`
- `src-tauri/crates/uc-tauri/tests/pairing_bridge.rs` contains `bridge_sets_participant_ready_only_when_pairing_flow_is_active`
- `src-tauri/crates/uc-tauri/tests/pairing_bridge.rs` contains `bridge_revokes_discoverability_and_ready_on_shutdown`
- `src-tauri/crates/uc-tauri/tests/pairing_bridge.rs` contains `bridge_reports_lease_loss_without_dropping_active_session`
- `cd src-tauri && cargo test -p uc-tauri --test pairing_bridge -- --test-threads=1` exits 0
  </acceptance_criteria>
  <verify>
  <automated>cd src-tauri && cargo test -p uc-tauri --test pairing_bridge -- --test-threads=1</automated>
  </verify>
  <done>
  The desktop shell starts the daemon pairing bridge at runtime, GUI discoverability and ready lifecycle are bridge-owned, and Tauri no longer retains the old pairing action/event loops as a hidden host fallback.
  </done>
  </task>
