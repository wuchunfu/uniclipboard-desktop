---
phase: 46.3
slug: gui-daemon
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-21
---

# Phase 46.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property               | Value                                                                                 |
| ---------------------- | ------------------------------------------------------------------------------------- |
| **Framework**          | Rust `cargo test` + Vitest                                                            |
| **Config file**        | `src-tauri/Cargo.toml`, `package.json`                                                |
| **Quick run command**  | `cd src-tauri && cargo test -p uc-daemon http_api`                                    |
| **Full suite command** | `cd src-tauri && cargo test -p uc-tauri --lib && cargo test -p uc-daemon && bun test` |
| **Estimated runtime**  | ~180 seconds                                                                          |

---

## Sampling Rate

- **After every task commit:** Run the task-local command from the verification map below
- **After every plan wave:** Run `cd src-tauri && cargo test -p uc-tauri --lib`
- **Before `$gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 180 seconds

---

## Per-Task Verification Map

| Task ID    | Plan | Wave | Requirement                                            | Test Type       | Automated Command                                                                               | File Exists                  | Status     |
| ---------- | ---- | ---- | ------------------------------------------------------ | --------------- | ----------------------------------------------------------------------------------------------- | ---------------------------- | ---------- |
| 46.3-01-01 | 01   | 0    | GUI-DMN-05                                             | unit            | `cd src-tauri && cargo test -p uc-daemon api_query`                                             | ✅ partial                   | ⬜ pending |
| 46.3-01-02 | 01   | 0    | GUI-DMN-01                                             | unit            | `cd src-tauri && cargo test -p uc-tauri startup_helper_rejects_healthy_but_incompatible_daemon` | ✅ existing but red baseline | ⬜ pending |
| 46.3-02-01 | 02   | 1    | GUI-DMN-02                                             | integration     | `cd src-tauri && cargo test -p uc-tauri daemon_bootstrap_replace -- --nocapture`                | ❌ W0                        | ⬜ pending |
| 46.3-03-01 | 03   | 2    | GUI-DMN-03                                             | frontend/unit   | `bun test src/pages/__tests__/StartupErrorPage.test.tsx`                                        | ❌ W0                        | ⬜ pending |
| 46.3-03-02 | 03   | 2    | GUI-DMN-04                                             | integration     | `cd src-tauri && cargo test -p uc-tauri startup_recovery_flow -- --nocapture`                   | ❌ W0                        | ⬜ pending |
| 46.3-04-01 | 04   | 3    | GUI-DMN-01,GUI-DMN-02,GUI-DMN-03,GUI-DMN-04,GUI-DMN-05 | full regression | `cd src-tauri && cargo test -p uc-tauri --lib && cargo test -p uc-daemon && bun test`           | ✅ mixed                     | ⬜ pending |

_Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky_

---

## Wave 0 Requirements

- [ ] `src-tauri/crates/uc-tauri/tests/daemon_bootstrap_contract.rs` — 覆盖 incompatible / replace / bounded retry
- [ ] `src/pages/__tests__/StartupErrorPage.test.tsx` — 覆盖错误页持久展示、手动重试、自动恢复
- [ ] `src/api/__tests__/startup.test.ts` — 覆盖 startup status / retry IPC contract
- [ ] 修正 `src-tauri/crates/uc-tauri/src/bootstrap/run.rs` 当前测试与实现不一致问题，使 `IncompatibleDaemon` 基线先可编译

---

## Manual-Only Verifications

| Behavior                                                            | Requirement            | Why Manual                          | Test Instructions                                                                            |
| ------------------------------------------------------------------- | ---------------------- | ----------------------------------- | -------------------------------------------------------------------------------------------- |
| GUI 启动时发现旧 daemon 并静默替换，成功后直接进入主界面            | GUI-DMN-02             | 需要真实打包/本地 daemon 进程占端点 | 启动旧版本或伪造不兼容 daemon 占住预期端点，再启动 GUI，确认无额外弹窗、替换完成后进入主界面 |
| GUI 落入 startup error page 后，daemon 恢复健康兼容时自动回到主界面 | GUI-DMN-03, GUI-DMN-04 | 需要真实窗口生命周期和前端恢复观察  | 让 bootstrap 进入错误页，随后修复 daemon 并观察应用是否自动重试成功且切回正常界面            |
| 错误页提供明确、持久、可执行的恢复入口                              | GUI-DMN-03             | 需要人工判断交互与文案是否可执行    | 验证错误页存在明确错误原因、手动重试入口、自动恢复提示，且不是 toast 或 dashboard 内嵌告警   |

---

## Validation Sign-Off

- [ ] All tasks have `<acceptance_criteria>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 180s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
