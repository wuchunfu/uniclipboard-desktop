---
phase: 46.4-daemon-setup-gui-cli-setup-cli-gui-cli-daemon-peera-full-mode-peerb-passive-mode-peerb-peera-peera-b-a-b-a-a-b-a-b-a-a-b
plan: 03
type: execute
wave: 3
depends_on: ['46.4-01', '46.4-02']
files_modified:
  - src-tauri/crates/uc-daemon/src/api/routes.rs
  - src-tauri/crates/uc-daemon/src/api/types.rs
  - src-tauri/crates/uc-daemon/tests/setup_api.rs
  - src-tauri/crates/uc-cli/src/commands/setup.rs
  - src-tauri/crates/uc-cli/tests/setup_cli.rs
  - .planning/phases/46.4-daemon-setup-gui-cli-setup-cli-gui-cli-daemon-peera-full-mode-peerb-passive-mode-peerb-peera-peera-b-a-b-a-a-b-a-b-a-a-b/46.4-03-SUMMARY.md
autonomous: true
requirements: ['PH46-06', 'CLI-05', 'PH46-01A']
user_setup: []
must_haves:
  truths:
    - 'Phase 46.4 includes an explicit reset path for same-machine reruns; repeatability is a deliverable, not an undocumented cleanup trick.'
    - 'Validation proves both happy path and wrong-passphrase retry path through daemon-backed setup, not only individual route calls.'
    - 'Successful completion proves both peers can query paired-device state after setup and the flow can be rerun after reset.'
  artifacts:
    - path: 'src-tauri/crates/uc-daemon/src/api/routes.rs'
      provides: 'Reset route or equivalent daemon-side setup cleanup surface.'
      min_lines: 20
    - path: 'src-tauri/crates/uc-cli/src/commands/setup.rs'
      provides: 'CLI `setup reset` wiring and repeatability-focused messaging.'
      min_lines: 260
    - path: 'src-tauri/crates/uc-daemon/tests/setup_api.rs'
      provides: 'Daemon-side reset/retry regression coverage.'
      min_lines: 220
    - path: 'src-tauri/crates/uc-cli/tests/setup_cli.rs'
      provides: 'CLI-side command/rendering coverage for setup reset and status-driven retry behavior.'
      min_lines: 120
  key_links:
    - from: 'src-tauri/crates/uc-cli/src/commands/setup.rs'
      to: 'src-tauri/crates/uc-daemon/src/api/routes.rs'
      via: 'CLI `setup reset` delegates to daemon reset/cleanup transport instead of requiring manual file deletion.'
      pattern: 'setup reset|/setup/reset'
    - from: 'src-tauri/crates/uc-daemon/tests/setup_api.rs'
      to: 'src-tauri/crates/uc-app/src/usecases/setup/orchestrator.rs'
      via: 'Reset and wrong-passphrase retry tests bind daemon setup transport to existing setup-domain retry semantics.'
      pattern: 'PassphraseInvalidOrMismatch|JoinSpaceInputPassphrase|Completed'
---

<objective>
补齐 46.4 的 reset/repeatability 与 acceptance-proof 层，证明 CLI + daemon setup flow 不仅能跑通一次，而且能 wrong-passphrase retry、双端可见 paired devices，并在 reset 后重复成功。

Purpose: 把这次 urgent CLI setup work 从“能演示一次”收紧到“可重复验证、可回归”的交付。
Output: `setup reset` 路径、reset/retry regression、CLI setup tests、以及 46.4 operator walkthrough summary。
</objective>

<execution_context>
Use the standard execute-phase workflow already configured for this repository.
</execution_context>

<context>
@.planning/phases/46.4-daemon-setup-gui-cli-setup-cli-gui-cli-daemon-peera-full-mode-peerb-passive-mode-peerb-peera-peera-b-a-b-a-a-b-a-b-a-a-b/46.4-CONTEXT.md
@.planning/phases/46.4-daemon-setup-gui-cli-setup-cli-gui-cli-daemon-peera-full-mode-peerb-passive-mode-peerb-peera-peera-b-a-b-a-a-b-a-b-a-a-b/46.4-RESEARCH.md
@.planning/phases/46.4-daemon-setup-gui-cli-setup-cli-gui-cli-daemon-peera-full-mode-peerb-passive-mode-peerb-peera-peera-b-a-b-a-a-b-a-b-a-a-b/46.4-VALIDATION.md
@src-tauri/crates/uc-daemon/src/api/routes.rs
@src-tauri/crates/uc-daemon/tests/setup_api.rs
@src-tauri/crates/uc-cli/src/commands/setup.rs
@src-tauri/crates/uc-app/src/usecases/setup/orchestrator.rs
@src-tauri/crates/uc-cli/src/commands/devices.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增加 `setup reset` 路径并固定 repeatability contract</name>
  <files>
    src-tauri/crates/uc-daemon/src/api/routes.rs,
    src-tauri/crates/uc-daemon/src/api/types.rs,
    src-tauri/crates/uc-daemon/tests/setup_api.rs,
    src-tauri/crates/uc-cli/src/commands/setup.rs
  </files>
  <read_first>
    - src-tauri/crates/uc-daemon/src/api/routes.rs
    - src-tauri/crates/uc-daemon/src/api/types.rs
    - src-tauri/crates/uc-daemon/tests/setup_api.rs
    - src-tauri/crates/uc-cli/src/commands/setup.rs
    - src-tauri/crates/uc-cli/src/commands/devices.rs
    - src-tauri/crates/uc-app/src/usecases/setup/orchestrator.rs
    - .planning/phases/46.4-daemon-setup-gui-cli-setup-cli-gui-cli-daemon-peera-full-mode-peerb-passive-mode-peerb-peera-peera-b-a-b-a-a-b-a-b-a-a-b/46.4-CONTEXT.md
  </read_first>
  <action>
    1. 在 daemon transport 中新增 `POST /setup/reset`，把它定义为 phase-local repeatability cleanup surface，固定清理至少这些内容：
       - 当前 setup session / setup-in-progress state
       - 当前 pairing host lease / participant-ready lease
       - 本地 paired-device records 和与本 phase same-machine rerun 冲突的 setup-space join residue
       但不得通过粗暴删除整个 profile 数据目录来实现。
    2. 在 `src-tauri/crates/uc-cli/src/commands/setup.rs` 中新增 `setup reset` 子命令，调用 `/setup/reset` 并打印明确结果：
       - `Reset complete for profile <name>`
       - `Daemon kept running`
    3. 在 `src-tauri/crates/uc-daemon/tests/setup_api.rs` 中新增固定测试名：
       - `setup_reset_clears_active_setup_state`
       - `setup_reset_releases_pairing_host_leases`
       - `setup_reset_allows_second_host_start_without_manual_cleanup`
    4. 若 reset 实现需要清理 paired-device repository 或 setup-complete status，必须通过现有 repo/usecase/port 做显式 cleanup，不得在 route handler 里直接手工删文件。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-daemon setup_api -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg "\"/setup/reset\"" src-tauri/crates/uc-daemon/src/api/routes.rs -n` 返回 reset route。
    - `rg "Reset complete for profile|Daemon kept running|setup reset" src-tauri/crates/uc-cli/src/commands/setup.rs -n` 返回 CLI reset 输出。
    - `rg "setup_reset_clears_active_setup_state|setup_reset_releases_pairing_host_leases|setup_reset_allows_second_host_start_without_manual_cleanup" src-tauri/crates/uc-daemon/tests/setup_api.rs -n` 返回 3 个 reset 测试名。
    - `cd src-tauri && cargo test -p uc-daemon setup_api -- --test-threads=1` 退出码为 0。
  </acceptance_criteria>
  <done>
    - 46.4 获得显式 reset 路径，same-machine rerun 不再依赖手工清理。
  </done>
</task>

<task type="auto">
  <name>Task 2: 固定 wrong-passphrase retry、paired-device visibility 与 operator-proof</name>
  <files>
    src-tauri/crates/uc-cli/tests/setup_cli.rs,
    src-tauri/crates/uc-cli/src/commands/setup.rs,
    .planning/phases/46.4-daemon-setup-gui-cli-setup-cli-gui-cli-daemon-peera-full-mode-peerb-passive-mode-peerb-peera-peera-b-a-b-a-a-b-a-b-a-a-b/46.4-03-SUMMARY.md
  </files>
  <read_first>
    - src-tauri/crates/uc-cli/src/commands/setup.rs
    - src-tauri/crates/uc-cli/src/commands/devices.rs
    - src-tauri/crates/uc-app/src/usecases/setup/orchestrator.rs
    - src-tauri/crates/uc-daemon/tests/setup_api.rs
    - .planning/phases/46.4-daemon-setup-gui-cli-setup-cli-gui-cli-daemon-peera-full-mode-peerb-passive-mode-peerb-peera-peera-b-a-b-a-a-b-a-b-a-a-b/46.4-VALIDATION.md
  </read_first>
  <action>
    1. 新建 `src-tauri/crates/uc-cli/tests/setup_cli.rs` 或等价 CLI 测试文件，至少覆盖：
       - `setup_status_renders_next_step_and_session_identity`
       - `setup_join_reports_passphrase_retry_without_exiting`
       - `setup_reset_reports_daemon_kept_running`
    2. 在 CLI setup 实现中固定 wrong-passphrase retry 提示文案：
       - `Passphrase rejected; retrying current join session`
       该提示出现时命令不得立刻退出。
    3. 在 `.planning/.../46.4-03-SUMMARY.md` 中写出 operator walkthrough，固定包含以下绝对步骤：
       - Terminal A: `UC_PROFILE=peerA UC_CLIPBOARD_MODE=full uniclipboard-cli setup host`
       - Terminal B: `UC_PROFILE=peerB UC_CLIPBOARD_MODE=passive uniclipboard-cli setup join`
       - 在 B 侧错误输入一次 passphrase
       - 在 B 侧再次输入正确 passphrase
       - setup 完成后两侧都运行 `uniclipboard-cli devices`
       - 两侧执行 `uniclipboard-cli setup reset`
       - 再完整跑一轮 host/join 成功
    4. 在 summary 中明确记录成功判据：
       - A 和 B 的 `devices` 输出都能看到对方
       - B 的 `setup status` 或终态输出表明 encryption-ready/join-complete，而不是仅 pairing success
  </action>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-app setup_flow_integration_test -- --nocapture && cargo test -p uc-cli</automated>
  </verify>
  <acceptance_criteria>
    - `rg "setup_status_renders_next_step_and_session_identity|setup_join_reports_passphrase_retry_without_exiting|setup_reset_reports_daemon_kept_running" src-tauri/crates/uc-cli/tests/setup_cli.rs -n` 返回 3 个 CLI 测试名。
    - `rg "Passphrase rejected; retrying current join session" src-tauri/crates/uc-cli/src/commands/setup.rs -n` 返回固定 retry 提示。
    - `rg "UC_PROFILE=peerA UC_CLIPBOARD_MODE=full uniclipboard-cli setup host|UC_PROFILE=peerB UC_CLIPBOARD_MODE=passive uniclipboard-cli setup join|uniclipboard-cli devices|uniclipboard-cli setup reset" .planning/phases/46.4-daemon-setup-gui-cli-setup-cli-gui-cli-daemon-peera-full-mode-peerb-passive-mode-peerb-peera-peera-b-a-b-a-a-b-a-b-a-a-b/46.4-03-SUMMARY.md -n` 返回完整 walkthrough 命令。
    - `cd src-tauri && cargo test -p uc-app setup_flow_integration_test -- --nocapture` 退出码为 0。
    - `cd src-tauri && cargo test -p uc-cli` 退出码为 0。
  </acceptance_criteria>
  <done>
    - wrong-passphrase retry、paired-device visibility 和 reset 后 rerun 都有自动化或 operator-proof 约束。
  </done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo test -p uc-daemon setup_api -- --test-threads=1` 通过，证明 reset/repeatability transport contract 稳定。
- `cd src-tauri && cargo test -p uc-app setup_flow_integration_test -- --nocapture` 通过，证明 setup domain retry/completion contract 未回归。
- `cd src-tauri && cargo test -p uc-cli` 通过，证明 CLI status/retry/reset 交互逻辑稳定。
</verification>

<success_criteria>

- wrong-passphrase retry、paired-device visibility、reset rerun 都被固定成可回归的 46.4 交付。
- 46.4 最终不只是“CLI 能调用 daemon”，而是“CLI + daemon setup flow 可重复验证”。
  </success_criteria>

<output>
完成后创建 `.planning/phases/46.4-daemon-setup-gui-cli-setup-cli-gui-cli-daemon-peera-full-mode-peerb-passive-mode-peerb-peera-peera-b-a-b-a-a-b-a-b-a-a-b/46.4-03-SUMMARY.md`，写入完整 operator walkthrough 与 repeatability proof。
</output>
