---
phase: 12-lifecycle-governance-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src-tauri/crates/uc-app/src/usecases/pairing/staged_paired_device_store.rs
  - src-tauri/crates/uc-app/src/usecases/pairing/mod.rs
  - src-tauri/crates/uc-app/src/usecases/pairing/orchestrator.rs
  - src-tauri/crates/uc-app/src/usecases/space_access/persistence_adapter.rs
  - src-tauri/crates/uc-app/src/lib.rs
  - src-tauri/crates/uc-infra/src/security/encryption_session.rs
  - src-tauri/crates/uc-infra/src/security/mod.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
autonomous: true
requirements:
  - LIFE-03
  - LIFE-04

must_haves:
  truths:
    - 'Pairing staging state is injected via Arc, not accessed through a global static'
    - 'Only one EncryptionSessionPort implementation exists in non-test production code'
    - 'Staging state can be cleared during shutdown by the lifecycle owner'
  artifacts:
    - path: 'src-tauri/crates/uc-app/src/usecases/pairing/staged_paired_device_store.rs'
      provides: 'StagedPairedDeviceStore struct with injected Arc-based API replacing global OnceLock'
      contains: 'pub struct StagedPairedDeviceStore'
    - path: 'src-tauri/crates/uc-infra/src/security/encryption_session.rs'
      provides: 'File removed or gutted — no production EncryptionSessionPort impl'
  key_links:
    - from: 'src-tauri/crates/uc-app/src/usecases/pairing/orchestrator.rs'
      to: 'src-tauri/crates/uc-app/src/usecases/pairing/staged_paired_device_store.rs'
      via: 'Arc<StagedPairedDeviceStore> passed as constructor dependency'
      pattern: 'Arc<StagedPairedDeviceStore>'
    - from: 'src-tauri/crates/uc-app/src/usecases/space_access/persistence_adapter.rs'
      to: 'src-tauri/crates/uc-app/src/usecases/pairing/staged_paired_device_store.rs'
      via: 'Arc<StagedPairedDeviceStore> passed as constructor dependency'
      pattern: 'Arc<StagedPairedDeviceStore>'
    - from: 'src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs'
      to: 'src-tauri/crates/uc-app/src/usecases/pairing/staged_paired_device_store.rs'
      via: 'Creates Arc<StagedPairedDeviceStore> and passes to orchestrator + persistence adapter'
      pattern: 'StagedPairedDeviceStore::new'
---

<objective>
Replace the global static `staged_paired_device_store` with an injected `Arc<StagedPairedDeviceStore>` component, and remove the duplicate `InMemoryEncryptionSession` from uc-infra.

Purpose: Eliminates unmanaged global mutable state (LIFE-03) and consolidates encryption session to a single authoritative implementation (LIFE-04).
Output: Refactored staging store as injectable struct, removed uc-infra encryption session duplicate.
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-lifecycle-governance-baseline/12-RESEARCH.md
@.planning/phases/12-lifecycle-governance-baseline/12-01-SUMMARY.md

<interfaces>
<!-- Current staged_paired_device_store API (global static, to be replaced): -->

From src-tauri/crates/uc-app/src/usecases/pairing/staged_paired_device_store.rs:

```rust
// Current: module-level functions using OnceLock<Mutex<HashMap>>
pub(crate) fn stage(session_id: &str, device: PairedDevice) { ... }
pub(crate) fn take_by_peer_id(peer_id: &str) -> Option<PairedDevice> { ... }
pub(crate) fn get_by_peer_id(peer_id: &str) -> Option<PairedDevice> { ... }
pub(crate) fn clear() { ... }  // cfg(test) only
```

Callers of staged_paired_device_store:

- orchestrator.rs line 869: `staged_paired_device_store::stage(&session_id, device.clone())`
- orchestrator.rs tests: `staged_paired_device_store::clear()` and `staged_paired_device_store::take_by_peer_id()`
- persistence_adapter.rs line 35: `staged_paired_device_store::get_by_peer_id(peer_id)`
- persistence_adapter.rs line 38: `staged_paired_device_store::take_by_peer_id(peer_id)`
- persistence_adapter.rs tests: `staged_paired_device_store::clear()` and `staged_paired_device_store::stage()`

From src-tauri/crates/uc-infra/src/security/encryption_session.rs (to be removed):

```rust
pub struct InMemoryEncryptionSession { key: RwLock<Option<MasterKey>> }
impl EncryptionSessionPort for InMemoryEncryptionSession { ... }
```

Used in tests only: setup_flow_integration_test.rs imports it.

The KEPT implementation is in uc-platform:

```rust
// src-tauri/crates/uc-platform/src/adapters/encryption.rs
pub struct InMemoryEncryptionSessionPort { key: Arc<Mutex<Option<MasterKey>>> }
impl EncryptionSessionPort for InMemoryEncryptionSessionPort { ... }
```

This is the one wired in production (wiring.rs line 705).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert staged_paired_device_store from global static to injectable struct</name>
  <files>
    src-tauri/crates/uc-app/src/usecases/pairing/staged_paired_device_store.rs,
    src-tauri/crates/uc-app/src/usecases/pairing/mod.rs,
    src-tauri/crates/uc-app/src/usecases/pairing/orchestrator.rs,
    src-tauri/crates/uc-app/src/usecases/space_access/persistence_adapter.rs,
    src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
  </files>
  <behavior>
    - Test: StagedPairedDeviceStore::new() creates empty store, stage() inserts, get_by_peer_id() retrieves, take_by_peer_id() removes
    - Test: clear() empties the store
    - Test: Two separate StagedPairedDeviceStore instances do NOT share state (proving no global)
  </behavior>
  <action>
    1. **Rewrite `staged_paired_device_store.rs`** to export a `pub struct StagedPairedDeviceStore` instead of module-level functions:

       ```rust
       pub struct StagedPairedDeviceStore {
           devices: std::sync::Mutex<HashMap<String, PairedDevice>>,
       }
       impl StagedPairedDeviceStore {
           pub fn new() -> Self { ... }
           pub fn stage(&self, session_id: &str, device: PairedDevice) { ... }
           pub fn take_by_peer_id(&self, peer_id: &str) -> Option<PairedDevice> { ... }
           pub fn get_by_peer_id(&self, peer_id: &str) -> Option<PairedDevice> { ... }
           pub fn clear(&self) { ... }  // NOT cfg(test) — available for shutdown cleanup
       }
       ```

       Remove the `OnceLock` static entirely. Remove the `staged_devices()` helper. The `clear()` method must be public (not test-only) because the lifecycle owner needs to clear state during shutdown.

    2. **Update `mod.rs`**: Change `pub(crate) mod staged_paired_device_store` to `pub mod staged_paired_device_store` (or re-export the struct) so it can be accessed from uc-tauri wiring.

    3. **Update `orchestrator.rs`**:
       - Add `staged_store: Arc<StagedPairedDeviceStore>` field to `PairingOrchestrator`.
       - Update the constructor to accept it.
       - Replace `staged_paired_device_store::stage(...)` calls with `self.staged_store.stage(...)`.
       - Update tests: create a `StagedPairedDeviceStore::new()` per test instead of calling `clear()` on the global.

    4. **Update `persistence_adapter.rs`** (`SpaceAccessPersistenceAdapter`):
       - Add `staged_store: Arc<StagedPairedDeviceStore>` field.
       - Update `new()` to accept it.
       - Replace `staged_paired_device_store::get_by_peer_id(...)` and `take_by_peer_id(...)` calls with `self.staged_store.get_by_peer_id(...)` etc.
       - Update tests similarly.

    5. **Update `wiring.rs`**:
       - Create `let staged_store = Arc::new(StagedPairedDeviceStore::new());`.
       - Pass `staged_store.clone()` to `PairingOrchestrator::new()` and `SpaceAccessPersistenceAdapter::new()`.
       - If shutdown cleanup is desired, store a reference for calling `staged_store.clear()` during shutdown (optional — CancellationToken already stops tasks that use the store).

    6. Add unit tests in `staged_paired_device_store.rs` `#[cfg(test)] mod tests`.

    IMPORTANT: The struct uses `std::sync::Mutex` (not tokio), matching the current OnceLock<Mutex> pattern. All methods are synchronous.

  </action>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-app -- staged_paired_device_store::tests && cargo test -p uc-app -- pairing && cargo check</automated>
  </verify>
  <done>No OnceLock/global static in staged_paired_device_store.rs. StagedPairedDeviceStore is injected via Arc. All callers updated. All pairing and space_access tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate InMemoryEncryptionSession from uc-infra</name>
  <files>
    src-tauri/crates/uc-infra/src/security/encryption_session.rs,
    src-tauri/crates/uc-infra/src/security/mod.rs,
    src-tauri/crates/uc-app/tests/setup_flow_integration_test.rs
  </files>
  <action>
    1. **Delete the production code** from `src-tauri/crates/uc-infra/src/security/encryption_session.rs`. Either:
       - Remove the file entirely, OR
       - Keep the file but remove `pub struct InMemoryEncryptionSession` and its `impl EncryptionSessionPort` block. Keep only a comment explaining the implementation lives in `uc-platform::adapters::encryption::InMemoryEncryptionSessionPort`.

    2. **Update `src-tauri/crates/uc-infra/src/security/mod.rs`**: Remove `pub use encryption_session::InMemoryEncryptionSession;` re-export. If the file is deleted, remove the `mod encryption_session;` line too.

    3. **Update `setup_flow_integration_test.rs`** (in uc-app/tests/): Change the import from `uc_infra::security::InMemoryEncryptionSession` to `uc_platform::adapters::InMemoryEncryptionSessionPort`. Update all usages:
       - `Arc::new(InMemoryEncryptionSession::new())` → `Arc::new(InMemoryEncryptionSessionPort::new())`
       - This requires adding `uc-platform` as a dev-dependency of uc-app if not already present (check Cargo.toml). If adding the dependency creates a circular reference, instead create a simple test-only `InMemoryEncryptionSession` struct directly in the test file (copying the 4-method impl, ~20 lines).

    4. **Verify single implementation**: After changes, run:
       ```
       grep -r "impl EncryptionSessionPort" src-tauri/crates/ --include="*.rs" | grep -v test | grep -v mock
       ```
       Must return exactly 1 result (the uc-platform one).

    5. Ensure `cargo check` and `cargo test` pass for the workspace.

    IMPORTANT: The uc-infra tests for InMemoryEncryptionSession can be deleted — they test the exact same interface that uc-platform already tests.

  </action>
  <verify>
    <automated>cd src-tauri && cargo check && grep -rc "impl EncryptionSessionPort" crates/ --include="*.rs" | grep -v ":0$" | grep -v test | grep -v mock</automated>
  </verify>
  <done>Only one non-test EncryptionSessionPort implementation exists (uc-platform). uc-infra no longer exports InMemoryEncryptionSession. All tests pass.</done>
</task>

</tasks>

<verification>
1. `cd src-tauri && cargo test -p uc-app -- staged_paired_device_store` — injected store tests pass
2. `cd src-tauri && cargo test -p uc-app -- pairing` — orchestrator tests pass with injected store
3. `grep -rn "OnceLock\|static STAGED" src-tauri/crates/uc-app/` — no global statics remain
4. `grep -r "impl EncryptionSessionPort" src-tauri/crates/ --include="*.rs" | grep -v test | grep -v mock` — exactly 1 result
5. `cd src-tauri && cargo test` — full test suite passes
</verification>

<success_criteria>

- StagedPairedDeviceStore is a struct injected via Arc, no global OnceLock
- All callers (orchestrator.rs, persistence_adapter.rs) receive the store through constructors
- uc-infra InMemoryEncryptionSession is removed from production code
- Only uc-platform InMemoryEncryptionSessionPort remains as the single authoritative implementation
- Full workspace compiles and all tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/12-lifecycle-governance-baseline/12-02-SUMMARY.md`
</output>
