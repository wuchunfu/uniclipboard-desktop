---
phase: 46.2-daemon-tauri-daemon
plan: 03
subsystem: frontend
tags: [pairing, react, vitest, daemon, realtime]
requires:
  - phase: 46.2-daemon-tauri-daemon
    provides: daemon-backed shell pairing contract and unified realtime bridge from plans 01-02
provides:
  - initiator pairing UI that ignores cross-session realtime noise and preserves the existing stage progression
  - regression coverage for initiator, passive, and setup pairing flows after daemon cutover
affects: [47-frontend-daemon-cutover, pairing, setup, realtime]
tech-stack:
  added: []
  patterns:
    - frontend pairing components stay on the shared p2p shell API instead of binding directly to daemon transports
    - session-scoped realtime assertions lock pairing UX to the active session only
key-files:
  created: []
  modified:
    - src/components/PairingDialog.tsx
    - src/components/__tests__/PairingDialog.test.tsx
    - src/components/__tests__/PairingNotificationProvider.realtime.test.tsx
    - src/pages/__tests__/setup-peer-discovery-polling.test.tsx
key-decisions:
  - 'PairingDialog now tracks the active pairing session explicitly so unrelated daemon realtime events cannot move the initiator UI into another session state.'
  - 'Frontend parity is locked with vitest regressions across initiator, passive, and setup flows, while keeping transport access confined to `src/api/p2p.ts`.'
patterns-established:
  - 'Initiator realtime handlers ignore `verifying`/`complete`/`failed` events from non-active sessions.'
  - 'Setup regression coverage verifies daemon-backed peer discovery can still drive the join-space pairing progression.'
requirements-completed: [R46.2-3, R46.2-4, R46.2-8]
duration: 5min
completed: 2026-03-21
---

# Phase 46.2 Plan 03: Frontend Pairing Flow Parity And Regression Coverage Summary

**Frontend pairing parity is now locked to the daemon-backed shell contract without changing initiator, passive, or setup stage semantics.**

## Performance

- **Duration:** 5 min
- **Completed:** 2026-03-21T17:41:38+08:00
- **Tasks:** 1
- **Files modified:** 4

## Accomplishments

- Hardened `PairingDialog` so initiator-side realtime only advances the active pairing session and explicitly handles the `verifying` stage.
- Added initiator regressions covering same-session completion, cross-session event ignore behavior, and localized active-session failure handling.
- Added passive realtime regression coverage for `verification -> verifying -> success` and a setup regression proving daemon-backed peer discovery still advances the join pairing flow.

## Task Commits

Each task was committed atomically:

1. **Task 1a: 收紧 initiator session 边界** - `a5d89a0d` (`fix`)
2. **Task 1b: 固化 daemon-backed pairing parity 回归** - `6168290f` (`test`)

## Files Created/Modified

- `src/components/PairingDialog.tsx` - Added active-session tracking so initiator pairing ignores unrelated realtime events and preserves `verification -> verifying -> complete/failed`.
- `src/components/__tests__/PairingDialog.test.tsx` - Locked initiator same-session completion, cross-session ignore behavior, and localized failure assertions.
- `src/components/__tests__/PairingNotificationProvider.realtime.test.tsx` - Locked passive `verification -> verifying -> success` behavior to the active session only.
- `src/pages/__tests__/setup-peer-discovery-polling.test.tsx` - Added a daemon-realtime-driven join-flow regression proving setup pairing progression still advances after selecting a discovered device.

## Decisions Made

- Kept all frontend transport access behind `src/api/p2p.ts` and existing realtime listeners instead of introducing direct daemon clients into React components.
- Treated session identity as the boundary for parity correctness, because daemon realtime can carry events from multiple sessions while the UI must remain scoped to the user’s current pairing attempt.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Switched verification to the repository Vitest entrypoint**

- **Issue:** Plan text used `bun test ...`, but this repository requires the Vitest entrypoint with jsdom mocks; Bun's native runner is not the correct verification path.
- **Fix:** Ran `bun run test -- src/components/__tests__/PairingDialog.test.tsx src/components/__tests__/PairingNotificationProvider.realtime.test.tsx src/pages/__tests__/setup-peer-discovery-polling.test.tsx`.
- **Files modified:** None
- **Verification:** Command exited successfully with `3` test files and `18` tests passing.

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** No scope change. The deviation only corrected the test runner entrypoint to match the repository's established frontend testing pattern.

## Issues Encountered

- Existing console warnings from dialog composition and mocked failure paths still appear in Vitest output, but they are pre-existing and did not affect assertions or acceptance criteria.

## User Setup Required

None.

## Next Phase Readiness

- Phase 46.2 is ready for phase-level verification: daemon-backed pairing parity is covered across initiator, passive, and setup flows.
- Phase 47 can assume the desktop pairing UI already consumes the daemon-backed shell contract with regression coverage around session-scoped realtime behavior.

## Self-Check: PASSED

- Found task commits: `a5d89a0d`, `6168290f`
- Verified `bun run test -- src/components/__tests__/PairingDialog.test.tsx src/components/__tests__/PairingNotificationProvider.realtime.test.tsx src/pages/__tests__/setup-peer-discovery-polling.test.tsx` passed
