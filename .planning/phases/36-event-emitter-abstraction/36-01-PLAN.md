---
phase: 36-event-emitter-abstraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/crates/uc-core/src/ports/host_event_emitter.rs
  - src-tauri/crates/uc-core/src/ports/mod.rs
  - src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs
  - src-tauri/crates/uc-tauri/src/adapters/mod.rs
autonomous: true
requirements:
  - EVNT-01
  - EVNT-02
  - EVNT-03

must_haves:
  truths:
    - 'HostEventEmitterPort trait compiles in uc-core without any Tauri dependency'
    - 'TauriEventEmitter emits all 9 in-scope event types with correct Tauri event name strings and camelCase JSON payloads'
    - 'LoggingEventEmitter always returns Ok and logs events via tracing structured fields'
    - 'HostEvent enum uses pure Rust types with no serde annotations'
  artifacts:
    - path: 'src-tauri/crates/uc-core/src/ports/host_event_emitter.rs'
      provides: 'HostEventEmitterPort trait + HostEvent enum + sub-enums + EmitError'
      contains: 'pub trait HostEventEmitterPort'
    - path: 'src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs'
      provides: 'TauriEventEmitter + LoggingEventEmitter + internal payload DTOs + contract tests'
      contains: 'impl HostEventEmitterPort for TauriEventEmitter'
  key_links:
    - from: 'src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs'
      to: 'src-tauri/crates/uc-core/src/ports/host_event_emitter.rs'
      via: 'use uc_core::ports::host_event_emitter::{HostEvent, HostEventEmitterPort, EmitError}'
      pattern: 'use uc_core::ports::host_event_emitter'
    - from: 'src-tauri/crates/uc-core/src/ports/mod.rs'
      to: 'src-tauri/crates/uc-core/src/ports/host_event_emitter.rs'
      via: 'pub mod host_event_emitter + pub use re-exports'
      pattern: 'pub mod host_event_emitter'
---

<objective>
Define the HostEventEmitterPort trait with HostEvent type system in uc-core, then implement TauriEventEmitter and LoggingEventEmitter adapters in uc-tauri with full event contract tests.

Purpose: Create the abstract event delivery port that decouples background tasks from Tauri's AppHandle, enabling future CLI/daemon runtime modes. The adapters must preserve exact frontend event contracts (event name strings, camelCase JSON payloads).

Output: Port trait file in uc-core, adapter file in uc-tauri with both implementations and contract tests for all 9 in-scope event mappings.
</objective>

<execution_context>
@/home/wuy6/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuy6/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-event-emitter-abstraction/36-CONTEXT.md
@.planning/phases/36-event-emitter-abstraction/36-RESEARCH.md

<interfaces>
<!-- Existing port patterns the executor must follow -->

From src-tauri/crates/uc-core/src/ports/setup_event_port.rs:

```rust
// Pattern: single-method trait, Send + Sync, async_trait
#[async_trait::async_trait]
pub trait SetupEventPort: Send + Sync {
    async fn emit_setup_state_changed(&self, state: SetupState, session_id: Option<String>);
}
```

From src-tauri/crates/uc-core/src/ports/transfer_progress.rs:

```rust
// Pattern: TransferProgress struct already in uc-core (reuse for Transfer(Progress))
pub struct TransferProgress { /* fields */ }
```

From src-tauri/crates/uc-tauri/src/adapters/lifecycle.rs:

```rust
// Pattern: Tauri adapter wrapping AppHandle<R>
pub struct TauriSessionReadyEmitter<R: Runtime> {
    app_handle: Arc<std::sync::RwLock<Option<AppHandle<R>>>>,
}
// Pattern: Logging adapter (infallible)
pub struct LoggingLifecycleEventEmitter;
```

From src-tauri/crates/uc-tauri/src/events/p2p_peer.rs:

```rust
// Pattern: Existing DTOs with serde annotations (frontend contracts)
#[derive(Clone, Debug, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct P2PPeerDiscoveryEvent { pub peer_id: String, pub device_name: Option<String>, ... }
```

From src-tauri/crates/uc-core/src/ports/mod.rs:

```rust
// Existing re-export pattern — add host_event_emitter module and pub use
pub mod setup_event_port;
pub use setup_event_port::SetupEventPort;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define HostEventEmitterPort trait and HostEvent type system in uc-core</name>
  <files>
    src-tauri/crates/uc-core/src/ports/host_event_emitter.rs,
    src-tauri/crates/uc-core/src/ports/mod.rs
  </files>
  <read_first>
    src-tauri/crates/uc-core/src/ports/mod.rs,
    src-tauri/crates/uc-core/src/ports/setup_event_port.rs,
    src-tauri/crates/uc-core/src/ports/transfer_progress.rs,
    src-tauri/crates/uc-core/Cargo.toml,
    src-tauri/crates/uc-core/AGENTS.md,
    src-tauri/crates/uc-tauri/src/events/mod.rs,
    src-tauri/crates/uc-tauri/src/events/p2p_peer.rs,
    src-tauri/crates/uc-tauri/src/events/transfer_progress.rs,
    .planning/phases/36-event-emitter-abstraction/36-CONTEXT.md,
    .planning/phases/36-event-emitter-abstraction/36-RESEARCH.md
  </read_first>
  <action>
Create `src-tauri/crates/uc-core/src/ports/host_event_emitter.rs` with:

1. **HostEvent enum** — top-level enum with nested sub-enums per domain. Pure Rust types, NO serde annotations, NO camelCase rename. Only in-scope variants:
   - `HostEvent::Clipboard(ClipboardHostEvent)` — variants: `NewContent { entry_id: String, origin: ClipboardOriginKind }`, plus any inbound error/recovered variants if currently emitted at in-scope sites
   - `HostEvent::PeerDiscovery(PeerDiscoveryHostEvent)` — variants: `Discovered { peer_id: String, device_name: Option<String>, addresses: Vec<String> }`, `Lost { peer_id: String, device_name: Option<String> }`
   - `HostEvent::PeerConnection(PeerConnectionHostEvent)` — variants: `Ready`, `NotReady`, `Connected`, `Disconnected`, `NameUpdated` — each with `peer_id: String, device_name: Option<String>` (or `String` for Connected/NameUpdated)
   - `HostEvent::Transfer(TransferHostEvent)` — variants: `Progress(TransferProgress)` (reuse uc-core type), `Completed { transfer_id, filename, peer_id, file_size: u64, auto_pulled: bool, file_path: String }`, `StatusChanged { transfer_id, entry_id, status: String, reason: Option<String> }`

2. **ClipboardOriginKind enum** — `Local`, `Remote`

3. **EmitError** — `#[derive(Debug, thiserror::Error)]` with variant `#[error("emit failed: {0}")] Failed(String)`

4. **HostEventEmitterPort trait** — synchronous (NOT async_trait), `Send + Sync`:

   ```rust
   pub trait HostEventEmitterPort: Send + Sync {
       fn emit(&self, event: HostEvent) -> Result<(), EmitError>;
   }
   ```

5. Derive `Debug, Clone` on HostEvent and all sub-enums (needed for logging adapter). Do NOT add Serialize/Deserialize.

6. **Update `src-tauri/crates/uc-core/src/ports/mod.rs`**: Add `pub mod host_event_emitter;` and `pub use host_event_emitter::*;` following the existing pattern of other port modules.

7. Check existing in-scope emit sites to ensure all event data fields are covered:
   - wiring.rs:2336 (P2PPeerDiscoveryEvent with peer_id, device_name, addresses, discovered:true)
   - wiring.rs:2364 (same with discovered:false)
   - wiring.rs:2379, 2391, 2404 (P2PPeerConnectionEvent with peer_id, device_name, ready, connected)
   - wiring.rs:2418 (P2PPeerNameUpdatedEvent with peer_id, device_name)
   - wiring.rs:1376 (inbound clipboard subscribe recovered)
   - wiring.rs:1860 (inbound clipboard error)
   - wiring.rs:1774, file_transfer_wiring.rs:85 (file transfer status changed)
   - wiring.rs:2546 (file transfer completed)
   - transfer_progress.rs:42 (transfer progress)
   - runtime.rs:1189-1205 (clipboard new content)

Commit message: `arch(36-01): HostEventEmitterPort trait + HostEvent enums + EmitError in uc-core`
Verify: `cd src-tauri && cargo check -p uc-core`
</action>
<verify>
<automated>cd src-tauri && cargo check -p uc-core 2>&1 | tail -5</automated>
</verify>
<acceptance_criteria> - src-tauri/crates/uc-core/src/ports/host_event_emitter.rs exists and contains `pub trait HostEventEmitterPort: Send + Sync` - host_event_emitter.rs contains `pub enum HostEvent` with variants `Clipboard`, `PeerDiscovery`, `PeerConnection`, `Transfer` - host_event_emitter.rs contains `pub enum EmitError` with `#[derive(Debug, thiserror::Error)]` - host_event_emitter.rs does NOT contain `serde`, `Serialize`, `Deserialize`, `camelCase`, or `tauri` - host_event_emitter.rs contains `pub enum ClipboardOriginKind` with `Local` and `Remote` - src-tauri/crates/uc-core/src/ports/mod.rs contains `pub mod host_event_emitter` - `cd src-tauri && cargo check -p uc-core` exits 0
</acceptance_criteria>
<done>HostEventEmitterPort trait, HostEvent enum hierarchy, and EmitError are defined in uc-core/ports with zero Tauri dependency. cargo check -p uc-core passes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TauriEventEmitter and LoggingEventEmitter adapters with contract tests</name>
  <files>
    src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs,
    src-tauri/crates/uc-tauri/src/adapters/mod.rs
  </files>
  <read_first>
    src-tauri/crates/uc-core/src/ports/host_event_emitter.rs,
    src-tauri/crates/uc-tauri/src/adapters/mod.rs,
    src-tauri/crates/uc-tauri/src/adapters/lifecycle.rs,
    src-tauri/crates/uc-tauri/src/events/mod.rs,
    src-tauri/crates/uc-tauri/src/events/p2p_peer.rs,
    src-tauri/crates/uc-tauri/src/events/transfer_progress.rs,
    src-tauri/crates/uc-tauri/Cargo.toml,
    src-tauri/crates/uc-tauri/AGENTS.md,
    .planning/phases/36-event-emitter-abstraction/36-CONTEXT.md,
    .planning/phases/36-event-emitter-abstraction/36-RESEARCH.md
  </read_first>
  <action>
Create `src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs` containing:

**Part A: TauriEventEmitter (commit 2)**

1. `TauriEventEmitter<R: Runtime>` struct holding `app: AppHandle<R>` (NOT `Arc<RwLock<Option<...>>>` — direct ownership, constructed only when AppHandle exists).

2. `impl<R: Runtime> HostEventEmitterPort for TauriEventEmitter<R>` with `fn emit(&self, event: HostEvent) -> Result<(), EmitError>` that:
   - Calls internal `map_event()` method to convert HostEvent to `(event_name: &str, serializable_payload)`
   - Calls `self.app.emit(event_name, payload).map_err(|e| EmitError::Failed(e.to_string()))`

3. **Internal payload DTOs** (NOT exported, module-private) with `#[derive(Serialize)]` and `#[serde(rename_all = "camelCase")]`:
   - ClipboardNewContentPayload: `{ entry_id: String, preview: String, origin: String }` — match exact shape from current events/mod.rs ClipboardEvent
   - PeerDiscoveryChangedPayload: `{ peer_id: String, device_name: Option<String>, addresses: Vec<String>, discovered: bool }` — match P2PPeerDiscoveryEvent
   - PeerConnectionChangedPayload: `{ peer_id: String, device_name: Option<String>, ready: bool, connected: bool }` — match P2PPeerConnectionEvent
   - PeerNameUpdatedPayload: `{ peer_id: String, device_name: String }` — match P2PPeerNameUpdatedEvent
   - TransferProgressPayload: match existing TransferProgressEvent shape
   - TransferCompletedPayload: `{ transfer_id, filename, peer_id, file_size: u64, auto_pulled: bool, file_path: String }`
   - TransferStatusChangedPayload: `{ transfer_id, entry_id, status: String, reason: Option<String> }`
   - InboundClipboardErrorPayload, InboundClipboardSubscribeRecoveredPayload (if applicable)

4. **Event name mapping** (must preserve existing frontend contracts exactly):
   | HostEvent variant | Tauri event name string |
   |---|---|
   | Clipboard(NewContent) | `"clipboard://event"` |
   | Clipboard(InboundError) | `"inbound-clipboard-error"` |
   | Clipboard(InboundSubscribeRecovered) | `"inbound-clipboard-subscribe-recovered"` |
   | PeerDiscovery(Discovered) | `"p2p-peer-discovery-changed"` |
   | PeerDiscovery(Lost) | `"p2p-peer-discovery-changed"` |
   | PeerConnection(Ready/NotReady/Connected/Disconnected) | `"p2p-peer-connection-changed"` |
   | PeerConnection(NameUpdated) | `"p2p-peer-name-updated"` |
   | Transfer(Progress) | `"file-transfer://progress"` |
   | Transfer(Completed) | `"file-transfer://completed"` |
   | Transfer(StatusChanged) | `"file-transfer://status-changed"` |

   Verify these event name strings by reading the existing emit call sites in wiring.rs and events/\*.rs. The names MUST match exactly.

**Part B: LoggingEventEmitter (commit 3)**

5. `LoggingEventEmitter` unit struct (no fields).

6. `impl HostEventEmitterPort for LoggingEventEmitter` — match on all HostEvent variants, log structured tracing fields. Log levels:
   - `info!` for: Clipboard NewContent, Transfer Completed
   - `debug!` for: PeerDiscovery Discovered/Lost, PeerConnection variants, PeerConnection NameUpdated, Transfer StatusChanged
   - `warn!` for: Clipboard InboundError
   - `debug!` for: Transfer Progress (high frequency)
   - Always return `Ok(())`

7. Structured fields format: `tracing::info!(event_type = "clipboard.new_content", entry_id = %id, origin = %origin)` — no raw content, only IDs and summaries.

**Part C: Module registration**

8. Update `src-tauri/crates/uc-tauri/src/adapters/mod.rs`: Add `pub mod host_event_emitter;`

**Part D: Event contract tests (commit 2, in same file)**

9. Add `#[cfg(test)] mod tests` in host_event_emitter.rs with tests for EVERY event mapping. Use the pattern from `events/transfer_progress.rs`:
   - Create `tauri::test::mock_app()`
   - Listen on expected event name channel
   - Create `TauriEventEmitter` with mock app handle
   - Call `emitter.emit(HostEvent::Xxx(...))`
   - Assert received payload JSON has camelCase keys (e.g., `json["entryId"]` present, `json["entry_id"]` absent)
   - Assert event name string is correct

   Tests needed (one per event type at minimum):
   - `test_clipboard_new_content_event_contract`
   - `test_peer_discovery_changed_event_contract`
   - `test_peer_connection_changed_event_contract`
   - `test_peer_name_updated_event_contract`
   - `test_transfer_progress_event_contract`
   - `test_transfer_completed_event_contract`
   - `test_transfer_status_changed_event_contract`

10. Add test for LoggingEventEmitter: `test_logging_emitter_always_returns_ok` — call emit with each variant, assert all return Ok(()).

Commit 2: `impl(36-01): TauriEventEmitter adapter with event contract tests`
Verify commit 2: `cd src-tauri && cargo test -p uc-tauri host_event_emitter`

Commit 3: `impl(36-01): LoggingEventEmitter adapter`
Verify commit 3: `cd src-tauri && cargo test -p uc-tauri logging`
</action>
<verify>
<automated>cd src-tauri && cargo test -p uc-tauri host_event_emitter 2>&1 | tail -10</automated>
</verify>
<acceptance_criteria> - src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs exists - File contains `pub struct TauriEventEmitter` and `impl<R: Runtime> HostEventEmitterPort for TauriEventEmitter<R>` - File contains `pub struct LoggingEventEmitter` and `impl HostEventEmitterPort for LoggingEventEmitter` - File contains `#[serde(rename_all = "camelCase")]` on all internal payload DTOs - File contains event name strings: `"clipboard://event"`, `"p2p-peer-discovery-changed"`, `"p2p-peer-connection-changed"`, `"p2p-peer-name-updated"`, `"file-transfer://progress"`, `"file-transfer://completed"`, `"file-transfer://status-changed"` - File contains `#[cfg(test)]` module with at least 7 contract test functions - src-tauri/crates/uc-tauri/src/adapters/mod.rs contains `pub mod host_event_emitter` - `cd src-tauri && cargo test -p uc-tauri host_event_emitter` exits 0 with all tests passing - File does NOT export internal payload DTOs (they are module-private) - LoggingEventEmitter always returns Ok(()) — verified by test
</acceptance_criteria>
<done>TauriEventEmitter converts all 9 in-scope HostEvent variants to correct Tauri event names with camelCase JSON payloads. LoggingEventEmitter logs structured tracing output and always returns Ok. All contract tests pass.</done>
</task>

</tasks>

<verification>
1. `cd src-tauri && cargo check -p uc-core` — HostEventEmitterPort compiles without Tauri dependency
2. `cd src-tauri && cargo test -p uc-tauri host_event_emitter` — all event contract tests pass
3. `cd src-tauri && cargo check -p uc-tauri` — full uc-tauri crate compiles
4. Verify no `serde` or `tauri` imports in `uc-core/src/ports/host_event_emitter.rs`
</verification>

<success_criteria>

- HostEventEmitterPort trait exists in uc-core with zero Tauri dependency
- TauriEventEmitter maps all 9 in-scope events to correct event names and camelCase payloads
- LoggingEventEmitter logs all events with structured tracing and always returns Ok
- All event contract tests pass, verifying frontend compatibility
- No existing code is broken (this plan only adds new files)
  </success_criteria>

<output>
After completion, create `.planning/phases/36-event-emitter-abstraction/36-01-SUMMARY.md`
</output>
