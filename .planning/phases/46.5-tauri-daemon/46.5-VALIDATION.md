---
phase: 46.5
slug: tauri-daemon
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-22
---

# Phase 46.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property               | Value                                                                                                                       |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| **Framework**          | Rust `cargo test` per crate + frontend `vitest`                                                                             |
| **Config file**        | Cargo default crate test conventions; frontend uses `package.json` `test` script                                            |
| **Quick run command**  | `cd src-tauri && cargo test -p uc-platform pairing_runtime_disabled_does_not_register_pairing_protocol -- --test-threads=1` |
| **Full suite command** | `cd src-tauri && cargo test --workspace && cd .. && bun test`                                                               |
| **Estimated runtime**  | ~25 seconds                                                                                                                 |

---

## Sampling Rate

- **After every task commit:** Run the smallest matching targeted suite from the verification map; default quick gate is `cd src-tauri && cargo test -p uc-platform pairing_runtime_disabled_does_not_register_pairing_protocol -- --test-threads=1`
- **After every plan wave:** Run `cd src-tauri && cargo test --workspace && cd .. && bun test`
- **Before `$gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 25 seconds

---

## Per-Task Verification Map

| Task ID    | Plan | Wave | Requirement      | Test Type                       | Automated Command                                                                                                                                                                                                                                                                                                                                                                                                                    | File Exists | Status     |
| ---------- | ---- | ---- | ---------------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------- | ---------- |
| 46.5-01-01 | 01   | 3    | R46.2-1, R46.2-2 | unit/integration                | `cd src-tauri && cargo test -p uc-platform pairing_runtime -- --test-threads=1 && cargo test -p uc-platform disabled_pairing_transport_returns_fixed_error -- --test-threads=1`                                                                                                                                                                                                                                                      | ❌ W0       | ⬜ pending |
| 46.5-01-02 | 01   | 3    | R46.2-1, R46.2-2 | integration                     | `cd src-tauri && cargo test -p uc-bootstrap pairing_owner -- --test-threads=1 && cargo test -p uc-bootstrap gui_network_ports_pairing_is_disabled_transport -- --test-threads=1`                                                                                                                                                                                                                                                     | ❌ W0       | ⬜ pending |
| 46.5-02-01 | 02   | 0    | R46.2-4          | unit                            | `cd src-tauri && cargo test -p uc-tauri daemon_query_client_fetches_paired_devices_from_daemon_api -- --test-threads=1 && cargo test -p uc-tauri daemon_setup_client_fetches_setup_state_from_daemon_api -- --test-threads=1 && cargo test -p uc-tauri daemon_setup_client_posts_submit_passphrase_to_daemon_api -- --test-threads=1 && cargo test -p uc-tauri daemon_pairing_client_posts_unpair_to_daemon_api -- --test-threads=1` | ❌ W0       | ⬜ pending |
| 46.5-04-01 | 04   | 2    | R46.2-1, R46.2-4 | unit/integration                | `cd src-tauri && cargo test -p uc-tauri --test daemon_command_shell pairing_commands_use_daemon_queries_and_unpair_route -- --test-threads=1`                                                                                                                                                                                                                                                                                        | ❌ W0       | ⬜ pending |
| 46.5-04-02 | 04   | 2    | R46.2-1, R46.2-4 | integration                     | `cd src-tauri && cargo test -p uc-tauri --test daemon_command_shell -- --test-threads=1`                                                                                                                                                                                                                                                                                                                                             | ❌ W0       | ⬜ pending |
| 46.5-03-01 | 03   | 4    | R46.2-7          | integration + frontend contract | `cd src-tauri && cargo test -p uc-tauri --test daemon_ws_bridge -- --test-threads=1 && cd .. && bun test -- src/api/__tests__/p2p-realtime-contract.test.ts`                                                                                                                                                                                                                                                                         | ✅          | ⬜ pending |
| 46.5-03-02 | 03   | 4    | R46.2-8          | integration                     | `cd src-tauri && cargo test -p uc-daemon --test pairing_api pairing_unpair_route_unpairs_device_via_daemon_usecase -- --test-threads=1 && cargo test -p uc-daemon --test setup_api setup_confirm_peer -- --test-threads=1 && cargo test -p uc-daemon --test setup_api setup_submit_passphrase -- --test-threads=1`                                                                                                                   | ✅          | ⬜ pending |

_Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky_

---

## Wave 0 Requirements

- [ ] `src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs` — add tests proving GUI ownership disables pairing runtime creation, accept loop startup, pairing protocol registration, and disabled transport failure semantics
- [ ] `src-tauri/crates/uc-bootstrap/src/assembly.rs` / `builders.rs` — add ownership propagation tests including `gui_network_ports_pairing_is_disabled_transport`
- [ ] `src-tauri/crates/uc-tauri/src/daemon_client/` — add setup/query/action client coverage with decode tests
- [ ] `src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs` — add daemon-only routing tests and assert backend `p2p-*` emitter removal
- [ ] `src/api/p2p.ts` + `src/api/__tests__/p2p-realtime-contract.test.ts` — remove legacy `p2p-*` listeners and lock daemon realtime as sole event path

---

## Manual-Only Verifications

| Behavior                                                                                                               | Requirement               | Why Manual                                   | Test Instructions                                                                                                                                                |
| ---------------------------------------------------------------------------------------------------------------------- | ------------------------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PeerB 发起 join 后，PeerA 提示只来自 daemon realtime 而非 GUI 本地 pairing 事件                                        | R46.2-7, R46.2-8          | 需要双端交互确认运行时事件来源和用户可见行为 | 启动 daemon-backed GUI/CLI 双端，执行 join-space 流程，确认前端仅消费 `daemon://realtime` 事件且 GUI 日志不出现本地 pairing session started 痕迹                 |
| GUI 关闭本地 pairing runtime 后，daemon 仍能完成 accept/reject/verify 全流程，且前端不再依赖任何 `p2p-*` 兼容 listener | R46.2-1, R46.2-2, R46.2-8 | 需要端到端验证 ownership 切换后的整体行为    | 在两端执行完整 pairing + setup pairing 流，确认 GUI 不注册本地 pairing protocol，前端不再消费 `p2p-command-error` 之类兼容事件，daemon 仍能完成配对与 setup 收尾 |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
