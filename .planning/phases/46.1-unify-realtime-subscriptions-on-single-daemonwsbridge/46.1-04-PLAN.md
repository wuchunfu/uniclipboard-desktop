---
phase: 46.1
plan: 4
name: 'Frontend Contract Cutover To daemon://realtime'
type: execute
wave: 4
depends_on:
  - 46.1-02
  - 46.1-03
files_modified:
  - src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs
  - src/api/realtime.ts
  - src/api/p2p.ts
  - src/api/setup.ts
  - src/hooks/useDeviceDiscovery.ts
  - src/hooks/__tests__/useDeviceDiscovery.realtime.test.ts
  - src/components/PairingNotificationProvider.tsx
  - src/components/__tests__/PairingNotificationProvider.realtime.test.tsx
  - src/api/__tests__/p2p-realtime-contract.test.ts
autonomous: true
requirements:
  - PH461-04
must_haves:
  truths:
    - 'the frontend listens to one realtime Tauri event name, `daemon://realtime`, for pairing, peers, and setup updates'
    - 'payloads are camelCase and ordered through one typed envelope with `topic`, `type`, `ts`, and `payload`'
    - 'the old `p2p-*` event listeners stop being part of the active frontend runtime path'
  artifacts:
    - path: src/api/realtime.ts
      provides: shared frontend realtime envelope parser and listener helper
      contains: DAEMON_REALTIME_EVENT
    - path: src/api/__tests__/p2p-realtime-contract.test.ts
      provides: contract tests for the single realtime event name and camelCase payloads
      contains: daemon://realtime
    - path: src/components/__tests__/PairingNotificationProvider.realtime.test.tsx
      provides: session ordering and pairing dialog routing coverage for unified realtime envelopes
      contains: PairingNotificationProvider
  key_links:
    - from: src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs
      to: src/api/realtime.ts
      via: Tauri emitter serializes HostEvent::Realtime to `daemon://realtime`
      pattern: daemon://realtime
    - from: src/api/realtime.ts
      to: src/hooks/useDeviceDiscovery.ts
      via: hook consumes peer envelopes from shared listener helper
      pattern: onDaemonRealtimeEvent
---

<objective>
Cut the frontend and Tauri event emission over to the single realtime channel.

Purpose: land the breaking contract switch in one pass so no frontend or adapter path still depends on `p2p-*` runtime listeners.
Output: `daemon://realtime` listener helper, frontend migrations, adapter serialization update, and frontend contract tests.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-CONTEXT.md
@.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-RESEARCH.md
@.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-VALIDATION.md
@src/api/p2p.ts
@src/api/setup.ts
@src/hooks/useDeviceDiscovery.ts
@src/components/PairingNotificationProvider.tsx
@src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs

<interfaces>
Use this exact frontend envelope shape from research:

```typescript
type FrontendRealtimeEvent =
  | {
      topic: 'pairing'
      type: 'pairing.verificationRequired'
      ts: number
      payload: PairingVerificationPayload
    }
  | { topic: 'pairing'; type: 'pairing.updated'; ts: number; payload: PairingSessionPayload }
  | { topic: 'peers'; type: 'peers.changed'; ts: number; payload: PeerChangedPayload }
```

Lock the Tauri event name to:

```text
daemon://realtime
```

</interfaces>
</context>

<tasks>

<task id="46.1-04-01" type="auto">
  <name>Task 1: Create failing frontend contract and consumer tests for the unified realtime channel</name>
  <files>
src/api/__tests__/p2p-realtime-contract.test.ts
src/hooks/__tests__/useDeviceDiscovery.realtime.test.ts
src/components/__tests__/PairingNotificationProvider.realtime.test.tsx
  </files>
  <read_first>
src/api/p2p.ts
src/api/setup.ts
src/hooks/useDeviceDiscovery.ts
src/components/PairingNotificationProvider.tsx
.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-VALIDATION.md
  </read_first>
  <action>
Create the Wave 0 frontend tests referenced in `46.1-VALIDATION.md`:

- `src/api/__tests__/p2p-realtime-contract.test.ts`
- `src/hooks/__tests__/useDeviceDiscovery.realtime.test.ts`
- `src/components/__tests__/PairingNotificationProvider.realtime.test.tsx`

Add failing tests that assert:

- the only active listener name is `daemon://realtime`
- envelope payload keys are camelCase and old snake_case keys are absent
- `useDeviceDiscovery` updates from `peers.changed`, `peers.nameUpdated`, and `peers.connectionChanged` envelope types
- `PairingNotificationProvider` handles session ordering through unified pairing and setup envelopes and no longer relies on `p2p-pairing-verification`
  </action>
  <acceptance_criteria>
- `src/api/__tests__/p2p-realtime-contract.test.ts` exists
- `src/api/__tests__/p2p-realtime-contract.test.ts` contains `daemon://realtime`
- `src/hooks/__tests__/useDeviceDiscovery.realtime.test.ts` exists
- `src/hooks/__tests__/useDeviceDiscovery.realtime.test.ts` contains `useDeviceDiscovery`
- `src/components/__tests__/PairingNotificationProvider.realtime.test.tsx` exists
- `src/components/__tests__/PairingNotificationProvider.realtime.test.tsx` contains `PairingNotificationProvider`
  </acceptance_criteria>
  <verify>
  <automated>bun test src/api/**tests**/p2p-realtime-contract.test.ts src/hooks/**tests**/useDeviceDiscovery.realtime.test.ts src/components/**tests**/PairingNotificationProvider.realtime.test.tsx</automated>
  </verify>
  <done>
  The frontend cutover is specified in failing tests that lock the single event name, envelope fields, and session-ordering behavior.
  </done>
  </task>

<task id="46.1-04-02" type="auto">
  <name>Task 2: Switch Tauri emission and frontend listeners to the `daemon://realtime` envelope</name>
  <files>
src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs
src/api/realtime.ts
src/api/p2p.ts
src/api/setup.ts
src/hooks/useDeviceDiscovery.ts
src/hooks/__tests__/useDeviceDiscovery.realtime.test.ts
src/components/PairingNotificationProvider.tsx
src/components/__tests__/PairingNotificationProvider.realtime.test.tsx
src/api/__tests__/p2p-realtime-contract.test.ts
  </files>
  <read_first>
src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs
src/api/__tests__/p2p-realtime-contract.test.ts
src/api/p2p.ts
src/api/setup.ts
src/hooks/useDeviceDiscovery.ts
src/components/PairingNotificationProvider.tsx
src-tauri/crates/uc-core/src/ports/host_event_emitter.rs
  </read_first>
  <action>
Add `src/api/realtime.ts` with one shared helper:

- `export const DAEMON_REALTIME_EVENT = 'daemon://realtime'`
- `export type FrontendRealtimeEvent = ...`
- `export async function onDaemonRealtimeEvent(...)`

Update `src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs` so `HostEvent::Realtime(RealtimeFrontendEvent)` serializes to `daemon://realtime` with camelCase payloads and the exact envelope keys `topic`, `type`, `ts`, and `payload`.

Refactor `src/api/p2p.ts`, `src/api/setup.ts`, `src/hooks/useDeviceDiscovery.ts`, and `src/components/PairingNotificationProvider.tsx` to consume the shared listener helper and route on envelope `type` values instead of separate `listen('p2p-*')` or `listen('setup-state-changed')` calls.

Do not keep dual listeners, mirroring code, or fallback branches. The runtime listener path must switch directly to `daemon://realtime`.
</action>
<acceptance_criteria>

- `src/api/realtime.ts` contains `DAEMON_REALTIME_EVENT = 'daemon://realtime'`
- `src/api/realtime.ts` contains `onDaemonRealtimeEvent`
- `src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs` contains `daemon://realtime`
- `src/api/p2p.ts` contains `onDaemonRealtimeEvent`
- `src/api/setup.ts` contains `onDaemonRealtimeEvent`
- `src/hooks/useDeviceDiscovery.ts` contains `onDaemonRealtimeEvent`
- `src/components/PairingNotificationProvider.tsx` contains `onDaemonRealtimeEvent`
- `src/api/p2p.ts` does not contain `listen<P2PPairingVerificationEvent>('p2p-pairing-verification'`
- `src/api/setup.ts` does not contain `listen<SetupStateChangedEvent>('setup-state-changed'`
- `bun test src/api/__tests__/p2p-realtime-contract.test.ts src/hooks/__tests__/useDeviceDiscovery.realtime.test.ts src/components/__tests__/PairingNotificationProvider.realtime.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
  <automated>bun test src/api/**tests**/p2p-realtime-contract.test.ts src/hooks/**tests**/useDeviceDiscovery.realtime.test.ts src/components/**tests**/PairingNotificationProvider.realtime.test.tsx</automated>
  </verify>
  <done>
  The Tauri adapter and frontend now use one realtime event name and one typed envelope, with no remaining active `p2p-*` or `setup-state-changed` listener path.
  </done>
  </task>

</tasks>

<verification>
- `bun test src/api/__tests__/p2p-realtime-contract.test.ts src/hooks/__tests__/useDeviceDiscovery.realtime.test.ts src/components/__tests__/PairingNotificationProvider.realtime.test.tsx`
</verification>

<success_criteria>

- The frontend subscribes through one helper using `daemon://realtime`.
- Pairing, peers, and setup payloads arrive in camelCase envelopes with stable ordering fields.
- The adapter emits the new channel without keeping old event names alive in runtime code.
  </success_criteria>

<output>
After completion, create `.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-04-SUMMARY.md`
</output>
