---
phase: 56.1-eliminate-hardcoded-strings-in-pairing-setup-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 56.1-01
files_modified:
  - src-tauri/crates/uc-daemon/src/api/ws.rs
  - src-tauri/crates/uc-daemon/src/pairing/host.rs
  - src-tauri/crates/uc-daemon/src/api/server.rs
  - src-tauri/crates/uc-daemon/src/api/routes.rs
autonomous: true
requirements:
  - PH561-03
  - PH561-04

must_haves:
  truths:
    - 'uc-daemon/src/api/ws.rs has zero module-level TOPIC_* or *_EVENT const definitions'
    - 'uc-daemon/src/pairing/host.rs has zero inline string literals for topic names, event types, pairing stages, or busy reasons'
    - 'uc-daemon/src/api/server.rs uses pairing_error_code constants in map_daemon_pairing_error()'
    - 'uc-daemon/src/api/routes.rs uses pairing_stage and pairing_error_code constants in acknowledged() calls and error responses'
  artifacts:
    - path: 'src-tauri/crates/uc-daemon/src/api/ws.rs'
      provides: 'WS handler using shared constants'
      contains: 'use uc_core::network::daemon_api_strings'
    - path: 'src-tauri/crates/uc-daemon/src/pairing/host.rs'
      provides: 'Pairing host using shared constants'
      contains: 'use uc_core::network::daemon_api_strings'
    - path: 'src-tauri/crates/uc-daemon/src/api/server.rs'
      provides: 'Server error mapping using shared constants'
      contains: 'pairing_error_code::'
    - path: 'src-tauri/crates/uc-daemon/src/api/routes.rs'
      provides: 'Route handlers using shared constants'
      contains: 'pairing_stage::'
  key_links:
    - from: 'src-tauri/crates/uc-daemon/src/api/ws.rs'
      to: 'uc-core::network::daemon_api_strings'
      via: 'use import'
      pattern: 'use uc_core::network::daemon_api_strings'
    - from: 'src-tauri/crates/uc-daemon/src/pairing/host.rs'
      to: 'uc-core::network::daemon_api_strings'
      via: 'use import'
      pattern: 'use uc_core::network::daemon_api_strings'
---

<objective>
Replace all hardcoded string literals in uc-daemon (server side) with imports from uc-core::network::daemon_api_strings.

Purpose: Eliminate the server-side copy of all wire-protocol strings so any future change to topic names, event types, pairing stages, busy reasons, or error codes only needs to happen in one place.
Output: Four uc-daemon files using shared constants with zero remaining inline wire-protocol string literals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-CONTEXT.md
@.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-RESEARCH.md
@.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ws.rs module-level constants and server.rs error codes with uc-core imports</name>
  <files>src-tauri/crates/uc-daemon/src/api/ws.rs, src-tauri/crates/uc-daemon/src/api/server.rs</files>
  <read_first>
    - src-tauri/crates/uc-core/src/network/daemon_api_strings.rs (the constants to import)
    - src-tauri/crates/uc-daemon/src/api/ws.rs (full file — contains 25 module-level const definitions to delete and replace with imports)
    - src-tauri/crates/uc-daemon/src/api/server.rs (map_daemon_pairing_error function with 5 error code strings)
  </read_first>
  <action>
**ws.rs changes:**

1. Delete the entire module-level const block (lines 26-50, all `const TOPIC_*` and `*_EVENT` definitions).
2. Add import at the top: `use uc_core::network::daemon_api_strings::{ws_topic, ws_event};`
3. Replace every usage of the deleted constants with the corresponding `ws_topic::*` or `ws_event::*` constant. The mapping is 1:1:
   - `TOPIC_STATUS` -> `ws_topic::STATUS`
   - `TOPIC_PEERS` -> `ws_topic::PEERS`
   - `TOPIC_PAIRED_DEVICES` -> `ws_topic::PAIRED_DEVICES`
   - `TOPIC_PAIRING` -> `ws_topic::PAIRING`
   - `TOPIC_PAIRING_SESSION` -> `ws_topic::PAIRING_SESSION`
   - `TOPIC_PAIRING_VERIFICATION` -> `ws_topic::PAIRING_VERIFICATION`
   - `TOPIC_SETUP` -> `ws_topic::SETUP`
   - `TOPIC_SPACE_ACCESS` -> `ws_topic::SPACE_ACCESS`
   - `STATUS_SNAPSHOT_EVENT` -> `ws_event::STATUS_SNAPSHOT`
   - `PEERS_SNAPSHOT_EVENT` -> `ws_event::PEERS_SNAPSHOT`
   - `PAIRED_DEVICES_SNAPSHOT_EVENT` -> `ws_event::PAIRED_DEVICES_SNAPSHOT`
   - `PAIRING_SNAPSHOT_EVENT` -> `ws_event::PAIRING_SNAPSHOT`
   - `STATUS_UPDATED_EVENT` -> `ws_event::STATUS_UPDATED`
   - `PEERS_CHANGED_EVENT` -> `ws_event::PEERS_CHANGED`
   - `PEERS_NAME_UPDATED_EVENT` -> `ws_event::PEERS_NAME_UPDATED`
   - `PEERS_CONNECTION_CHANGED_EVENT` -> `ws_event::PEERS_CONNECTION_CHANGED`
   - `PAIRED_DEVICES_CHANGED_EVENT` -> `ws_event::PAIRED_DEVICES_CHANGED`
   - `PAIRING_UPDATED_EVENT` -> `ws_event::PAIRING_UPDATED`
   - `PAIRING_VERIFICATION_REQUIRED_EVENT` -> `ws_event::PAIRING_VERIFICATION_REQUIRED`
   - `PAIRING_COMPLETE_EVENT` -> `ws_event::PAIRING_COMPLETE`
   - `PAIRING_FAILED_EVENT` -> `ws_event::PAIRING_FAILED`
   - `SPACE_ACCESS_SNAPSHOT_EVENT` -> `ws_event::SPACE_ACCESS_SNAPSHOT`
   - `SPACE_ACCESS_STATE_CHANGED_EVENT` -> `ws_event::SPACE_ACCESS_STATE_CHANGED`
4. Delete the `pub const TOPIC_SPACE_ACCESS` and `pub const SPACE_ACCESS_SNAPSHOT_EVENT` re-exports — check if any other file in uc-daemon imports these pub consts from ws.rs and update those imports to use `uc_core::network::daemon_api_strings` instead.

**server.rs changes:**

1. Add import: `use uc_core::network::daemon_api_strings::pairing_error_code;`
2. Replace in `map_daemon_pairing_error()`:
   - `"active_session_exists".to_string()` -> `pairing_error_code::ACTIVE_SESSION_EXISTS.to_string()`
   - `"host_not_discoverable".to_string()` -> `pairing_error_code::HOST_NOT_DISCOVERABLE.to_string()`
   - `"no_local_participant".to_string()` -> `pairing_error_code::NO_LOCAL_PARTICIPANT.to_string()`
   - `"session_not_found".to_string()` -> `pairing_error_code::SESSION_NOT_FOUND.to_string()`
   - `"internal".to_string()` -> `pairing_error_code::INTERNAL.to_string()`
     </action>
     <verify>
     <automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo check -p uc-daemon 2>&1 | tail -10</automated>
     </verify>
     <acceptance_criteria>
   - ws.rs does NOT contain `const TOPIC_STATUS: &str`
   - ws.rs does NOT contain `const STATUS_SNAPSHOT_EVENT: &str`
   - ws.rs contains `use uc_core::network::daemon_api_strings::`
   - ws.rs contains `ws_topic::STATUS`
   - ws.rs contains `ws_event::STATUS_SNAPSHOT`
   - server.rs contains `pairing_error_code::ACTIVE_SESSION_EXISTS`
   - server.rs does NOT contain `"active_session_exists".to_string()`
   - `cd src-tauri && cargo check -p uc-daemon` exits 0
     </acceptance_criteria>
     <done>ws.rs uses imported constants for all topic and event type strings; server.rs uses imported constants for all error codes</done>
     </task>

<task type="auto">
  <name>Task 2: Replace host.rs inline strings and routes.rs state/error labels with uc-core imports</name>
  <files>src-tauri/crates/uc-daemon/src/pairing/host.rs, src-tauri/crates/uc-daemon/src/api/routes.rs</files>
  <read_first>
    - src-tauri/crates/uc-core/src/network/daemon_api_strings.rs (the constants to import)
    - src-tauri/crates/uc-daemon/src/pairing/host.rs (full file — contains inline topic, event_type, stage, and busy_reason strings throughout)
    - src-tauri/crates/uc-daemon/src/api/routes.rs (acknowledged() calls with stage strings, error code strings in handlers)
  </read_first>
  <action>
**host.rs changes:**

1. Add import at the top: `use uc_core::network::daemon_api_strings::{ws_topic, ws_event, pairing_stage, pairing_busy_reason};`
2. Replace ALL `emit_ws_event()` call inline topic/event strings:
   - `"pairing/session"` -> `ws_topic::PAIRING_SESSION`
   - `"pairing/verification"` -> `ws_topic::PAIRING_VERIFICATION`
   - `"space-access"` -> `ws_topic::SPACE_ACCESS`
   - `"pairing.updated"` -> `ws_event::PAIRING_UPDATED`
   - `"pairing.verification_required"` -> `ws_event::PAIRING_VERIFICATION_REQUIRED`
   - `"pairing.failed"` -> `ws_event::PAIRING_FAILED`
   - `"pairing.complete"` -> `ws_event::PAIRING_COMPLETE`
   - `"space_access.state_changed"` -> `ws_event::SPACE_ACCESS_STATE_CHANGED`
3. Replace ALL pairing stage inline strings:
   - `"request"` (when used as pairing stage label) -> `pairing_stage::REQUEST`
   - `"verification"` (when used as pairing stage) -> `pairing_stage::VERIFICATION`
   - `"verifying"` (when used as pairing stage) -> `pairing_stage::VERIFYING`
   - `"complete"` (when used as pairing stage) -> `pairing_stage::COMPLETE`
   - `"failed"` (when used as pairing stage) -> `pairing_stage::FAILED`
     NOTE: Be careful to only replace stage-context strings, not unrelated string uses of these words (e.g., log messages).
4. Replace ALL busy reason inline strings:
   - `"host_not_discoverable"` -> `pairing_busy_reason::HOST_NOT_DISCOVERABLE`
   - `"no_local_pairing_participant_ready"` -> `pairing_busy_reason::NO_LOCAL_PAIRING_PARTICIPANT_READY`
   - `"busy"` (when used as busy reason in reject_inbound_request context) -> `pairing_busy_reason::BUSY`
     NOTE: The same busy reason strings appear TWICE — once in `ensure_inbound_admitted()` struct method and once in standalone `handle_pairing_message()`. Replace BOTH occurrences.

**routes.rs changes:**

1. Add import: `use uc_core::network::daemon_api_strings::{pairing_stage, pairing_error_code};`
2. Replace `acknowledged()` call state labels:
   - `acknowledged(session_id, true, "request")` -> `acknowledged(session_id, true, pairing_stage::REQUEST)`
   - `acknowledged(session_id, true, "verifying")` -> `acknowledged(session_id, true, pairing_stage::VERIFYING)`
   - `acknowledged(session_id, true, "failed")` -> `acknowledged(session_id, true, pairing_stage::FAILED)`
3. Replace inline error code strings:
   - `"bad_request"` -> `pairing_error_code::BAD_REQUEST`
   - `"runtime_unavailable"` -> `pairing_error_code::RUNTIME_UNAVAILABLE`
   - `"internal"` -> `pairing_error_code::INTERNAL`
4. Do NOT change the `acknowledged()` function with `"participant_updated"` or `"current"` — these are NOT pairing stages.
5. After all replacements, grep the file for remaining hardcoded stage/error strings to verify completeness.
   </action>
   <verify>
   <automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo test -p uc-daemon 2>&1 | tail -20</automated>
   </verify>
   <acceptance_criteria> - host.rs contains `use uc_core::network::daemon_api_strings::` - host.rs contains `ws_topic::PAIRING_SESSION` - host.rs contains `pairing_stage::REQUEST` - host.rs contains `pairing_busy_reason::HOST_NOT_DISCOVERABLE` - host.rs does NOT contain inline `"pairing/session"` (grep returns 0 matches outside comments/docs) - host.rs does NOT contain inline `"host_not_discoverable"` (grep returns 0 matches outside comments/docs) - routes.rs contains `pairing_stage::REQUEST` - routes.rs contains `pairing_error_code::BAD_REQUEST` - routes.rs does NOT contain `"bad_request"` as a bare string (grep returns 0 in non-test code) - `cd src-tauri && cargo test -p uc-daemon` exits 0
   </acceptance_criteria>
   <done>All uc-daemon files use shared constants; zero hardcoded wire-protocol strings remain in production code paths</done>
   </task>

</tasks>

<verification>
- `cd src-tauri && cargo check -p uc-daemon` passes
- `cd src-tauri && cargo test -p uc-daemon` passes
- `grep -rn '"pairing/session"\|"pairing/verification"\|"host_not_discoverable"\|"no_local_pairing_participant_ready"\|"active_session_exists"' src-tauri/crates/uc-daemon/src/ --include='*.rs' | grep -v '//\|#\[' | wc -l` returns 0 (zero remaining bare string literals in non-comment lines)
</verification>

<success_criteria>

- All 4 uc-daemon source files import from daemon_api_strings
- Zero remaining hardcoded wire-protocol strings in uc-daemon production code
- All existing uc-daemon tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-02-SUMMARY.md`
</output>
