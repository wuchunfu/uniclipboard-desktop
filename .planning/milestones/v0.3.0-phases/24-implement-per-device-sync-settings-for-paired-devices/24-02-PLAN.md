---
phase: 24-implement-per-device-sync-settings-for-paired-devices
plan: 02
type: execute
wave: 2
depends_on: ['24-01']
files_modified:
  - src-tauri/crates/uc-app/src/usecases/pairing/get_device_sync_settings.rs
  - src-tauri/crates/uc-app/src/usecases/pairing/update_device_sync_settings.rs
  - src-tauri/crates/uc-app/src/usecases/pairing/mod.rs
  - src-tauri/crates/uc-tauri/src/commands/pairing.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs
  - src-tauri/src/main.rs
  - src-tauri/crates/uc-app/src/usecases/clipboard/sync_outbound.rs
autonomous: true
requirements:
  - DEVSYNC-04
  - DEVSYNC-05

must_haves:
  truths:
    - 'Frontend can fetch resolved sync settings for a specific device via Tauri command'
    - 'Frontend can update or clear per-device sync settings via Tauri command'
    - 'Outbound sync respects per-device auto_sync toggle -- skips peers with auto_sync disabled'
    - 'Settings changes take effect immediately without restart'
  artifacts:
    - path: 'src-tauri/crates/uc-app/src/usecases/pairing/get_device_sync_settings.rs'
      provides: 'GetDeviceSyncSettings use case'
      contains: 'resolve_sync_settings'
    - path: 'src-tauri/crates/uc-app/src/usecases/pairing/update_device_sync_settings.rs'
      provides: 'UpdateDeviceSyncSettings use case'
      contains: 'update_sync_settings'
    - path: 'src-tauri/crates/uc-tauri/src/commands/pairing.rs'
      provides: 'get_device_sync_settings and update_device_sync_settings commands'
      contains: 'get_device_sync_settings'
  key_links:
    - from: 'src-tauri/crates/uc-tauri/src/commands/pairing.rs'
      to: 'src-tauri/crates/uc-app/src/usecases/pairing/get_device_sync_settings.rs'
      via: 'runtime.usecases().get_device_sync_settings()'
      pattern: 'get_device_sync_settings'
    - from: 'src-tauri/crates/uc-app/src/usecases/clipboard/sync_outbound.rs'
      to: 'src-tauri/crates/uc-core/src/network/paired_device.rs'
      via: 'resolve_sync_settings for per-device auto_sync check'
      pattern: 'resolve_sync_settings'
    - from: 'src-tauri/src/main.rs'
      to: 'src-tauri/crates/uc-tauri/src/commands/pairing.rs'
      via: 'generate_handler! registration'
      pattern: 'get_device_sync_settings.*update_device_sync_settings'
---

<objective>
Create use cases, Tauri commands, and sync engine integration for per-device settings.

Purpose: Expose per-device sync settings to the frontend via Tauri commands and make the outbound sync engine respect per-device auto_sync toggle. This is the business logic layer connecting the data foundation (plan 01) to the UI (plan 03).
Output: Two new use cases, two new Tauri commands registered in main.rs, and sync engine filtered by per-device settings.
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-implement-per-device-sync-settings-for-paired-devices/24-CONTEXT.md
@.planning/phases/24-implement-per-device-sync-settings-for-paired-devices/24-RESEARCH.md
@.planning/phases/24-implement-per-device-sync-settings-for-paired-devices/24-01-SUMMARY.md

<interfaces>
<!-- Contracts from plan 01 that this plan depends on. -->

From src-tauri/crates/uc-core/src/network/paired_device.rs (after plan 01):

```rust
pub struct PairedDevice {
    pub peer_id: PeerId,
    pub pairing_state: PairingState,
    pub identity_fingerprint: String,
    pub paired_at: DateTime<Utc>,
    pub last_seen_at: Option<DateTime<Utc>>,
    pub device_name: String,
    pub sync_settings: Option<SyncSettings>,  // NEW
}

pub fn resolve_sync_settings<'a>(
    device: &'a PairedDevice,
    global: &'a SyncSettings,
) -> &'a SyncSettings;
```

From src-tauri/crates/uc-core/src/ports/paired_device_repository.rs (after plan 01):

```rust
// NEW method added to PairedDeviceRepositoryPort:
async fn update_sync_settings(
    &self,
    peer_id: &PeerId,
    settings: Option<SyncSettings>,
) -> Result<(), PairedDeviceRepositoryError>;
```

From src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs (UseCases pattern):

```rust
// Existing accessor pattern -- new use cases follow this:
pub fn sync_outbound_clipboard(&self) -> SyncOutboundClipboardUseCase { ... }
pub fn list_paired_devices(&self) -> ListPairedDevicesUseCase { ... }
```

From src-tauri/crates/uc-app/src/usecases/clipboard/sync_outbound.rs:

```rust
pub struct SyncOutboundClipboardUseCase {
    local_clipboard: Arc<dyn SystemClipboardPort>,
    clipboard_network: Arc<dyn ClipboardTransportPort>,
    peer_directory: Arc<dyn PeerDirectoryPort>,
    encryption_session: Arc<dyn EncryptionSessionPort>,
    device_identity: Arc<dyn DeviceIdentityPort>,
    settings: Arc<dyn SettingsPort>,
    transfer_encryptor: Arc<dyn TransferPayloadEncryptorPort>,
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create use cases and Tauri commands for device sync settings</name>
  <files>src-tauri/crates/uc-app/src/usecases/pairing/get_device_sync_settings.rs, src-tauri/crates/uc-app/src/usecases/pairing/update_device_sync_settings.rs, src-tauri/crates/uc-app/src/usecases/pairing/mod.rs, src-tauri/crates/uc-tauri/src/commands/pairing.rs, src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs, src-tauri/src/main.rs</files>
  <action>
1. Create `src-tauri/crates/uc-app/src/usecases/pairing/get_device_sync_settings.rs`:
   - Struct `GetDeviceSyncSettings` with fields: `paired_device_repo: Arc<dyn PairedDeviceRepositoryPort>`, `settings: Arc<dyn SettingsPort>`.
   - `from_ports(repo, settings)` constructor.
   - `execute(peer_id: &PeerId) -> Result<SyncSettings>` method:
     - Load device via `paired_device_repo.get_by_peer_id(peer_id)`.
     - If device not found, return error.
     - Load global settings via `settings.load()`.
     - Use `resolve_sync_settings(&device, &global.sync)` to get effective settings.
     - Return cloned effective settings.
   - Per user decision: settings are loaded from storage, not cached.

2. Create `src-tauri/crates/uc-app/src/usecases/pairing/update_device_sync_settings.rs`:
   - Struct `UpdateDeviceSyncSettings` with field: `paired_device_repo: Arc<dyn PairedDeviceRepositoryPort>`.
   - `from_ports(repo)` constructor.
   - `execute(peer_id: &PeerId, settings: Option<SyncSettings>) -> Result<()>` method:
     - Call `paired_device_repo.update_sync_settings(peer_id, settings)`.
     - Map PairedDeviceRepositoryError to anyhow.
   - Passing `None` resets to global defaults (per user decision).

3. Update `src-tauri/crates/uc-app/src/usecases/pairing/mod.rs`:
   - Add `pub mod get_device_sync_settings;` and `pub mod update_device_sync_settings;`

4. Add UseCases accessor methods in `src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs`:
   - `pub fn get_device_sync_settings(&self) -> GetDeviceSyncSettings` -- constructs with `paired_device_repo` and `settings` from deps.
   - `pub fn update_device_sync_settings(&self) -> UpdateDeviceSyncSettings` -- constructs with `paired_device_repo` from deps.

5. Add Tauri commands in `src-tauri/crates/uc-tauri/src/commands/pairing.rs`:
   - `get_device_sync_settings(runtime: State<'_, Arc<AppRuntime>>, peer_id: String, _trace: Option<TraceMetadata>) -> Result<SyncSettings, CommandError>`:
     - Create info_span "command.pairing.get_device_sync_settings".
     - Call `runtime.usecases().get_device_sync_settings().execute(&PeerId::from(peer_id.as_str()))`.
     - Map error to CommandError.
   - `update_device_sync_settings(runtime: State<'_, Arc<AppRuntime>>, peer_id: String, settings: Option<SyncSettings>, _trace: Option<TraceMetadata>) -> Result<(), CommandError>`:
     - Create info_span "command.pairing.update_device_sync_settings".
     - Call `runtime.usecases().update_device_sync_settings().execute(&PeerId::from(peer_id.as_str()), settings)`.
     - Map error to CommandError.
   - Note: SyncSettings already derives Serialize/Deserialize so it can be sent to/from the frontend directly.

6. Register commands in `src-tauri/src/main.rs`:
   - Add `uc_tauri::commands::pairing::get_device_sync_settings` and `uc_tauri::commands::pairing::update_device_sync_settings` to the `generate_handler![]` macro invocation, in the pairing commands section.
     </action>
     <verify>
     <automated>cd src-tauri && cargo check 2>&1 | tail -5</automated>
     </verify>
     <done>Two new use cases exist and are accessible via runtime.usecases(). Two new Tauri commands are registered in main.rs invoke_handler. cargo check passes for full workspace.</done>
     </task>

<task type="auto">
  <name>Task 2: Integrate per-device auto_sync check into outbound sync engine</name>
  <files>src-tauri/crates/uc-app/src/usecases/clipboard/sync_outbound.rs</files>
  <action>
1. Add `paired_device_repo: Arc<dyn PairedDeviceRepositoryPort>` as a new dependency to `SyncOutboundClipboardUseCase`:
   - Add field to struct.
   - Add parameter to `new()` constructor.
   - Import `PairedDeviceRepositoryPort` from `uc_core::ports`.
   - Import `resolve_sync_settings` from `uc_core::network::paired_device`.

2. Update the `sync_outbound_clipboard()` accessor in `src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs`:
   - Pass `self.runtime.deps.device.paired_device_repo.clone()` as the new parameter.

3. In `execute_async()`, add per-device settings check INSIDE the send loop (both the first peer block and the remaining_peers loop). Before calling `send_clipboard` for each peer:
   - Load the paired device: `self.paired_device_repo.get_by_peer_id(&PeerId::from(peer.peer_id.as_str())).await`.
   - If device found, load global settings via `self.settings.load().await` and use `resolve_sync_settings` to get effective settings.
   - If `!effective_settings.auto_sync`, log a debug message "Skipping sync for peer: auto_sync disabled" with peer_id and `continue` to the next peer.
   - If device not found in paired_device table, proceed with sync anyway (the peer is sendable but may not be in paired_device table yet -- this is a safety fallback).
   - Handle errors from settings/repo loads with `warn!` and continue (don't block sync on settings lookup failure).

4. Update the `build_usecase` helper in the test module:
   - Add a `TestPairedDeviceRepo` implementing `PairedDeviceRepositoryPort` that returns `Ok(None)` for all queries (no per-device overrides in existing tests -- they should continue to work unchanged).
   - Pass it to the `SyncOutboundClipboardUseCase::new()` call.

5. Per user decision: settings are loaded from storage on every sync operation, not cached in memory. This is acceptable because SQLite + WAL is fast for the small number of paired devices (2-5).
   </action>
   <verify>
   <automated>cd src-tauri && cargo test -p uc-app -- sync_outbound 2>&1 | tail -10</automated>
   </verify>
   <done>SyncOutboundClipboardUseCase checks per-device auto_sync before sending. Peers with auto_sync disabled are skipped. All existing sync_outbound tests pass. cargo test passes.</done>
   </task>

</tasks>

<verification>
- `cd src-tauri && cargo test -p uc-app -- sync_outbound` -- all existing outbound sync tests pass
- `cd src-tauri && cargo check` -- full workspace compiles
- New commands are visible in `main.rs` generate_handler
</verification>

<success_criteria>

- GetDeviceSyncSettings use case returns resolved settings (per-device or global fallback)
- UpdateDeviceSyncSettings use case updates or clears per-device settings
- Both commands registered in main.rs and callable from frontend
- Outbound sync engine skips peers whose effective auto_sync is false
- Settings loaded from storage each time (not cached) per user decision
- All existing tests pass unchanged
  </success_criteria>

<output>
After completion, create `.planning/phases/24-implement-per-device-sync-settings-for-paired-devices/24-02-SUMMARY.md`
</output>
