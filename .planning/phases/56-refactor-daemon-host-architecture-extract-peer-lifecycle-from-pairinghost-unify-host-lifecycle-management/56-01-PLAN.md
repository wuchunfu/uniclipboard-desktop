---
phase: 56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/crates/uc-daemon/src/service.rs
  - src-tauri/crates/uc-daemon/src/lib.rs
  - src-tauri/crates/uc-daemon/src/app.rs
  - src-tauri/crates/uc-daemon/src/main.rs
  - src-tauri/crates/uc-daemon/src/state.rs
  - src-tauri/crates/uc-daemon/src/workers/clipboard_watcher.rs
  - src-tauri/crates/uc-daemon/src/workers/peer_discovery.rs
  - src-tauri/crates/uc-daemon/src/rpc/handler.rs
  - src-tauri/crates/uc-daemon/src/rpc/types.rs
  - src-tauri/crates/uc-daemon/src/api/query.rs
  - src-tauri/crates/uc-daemon/tests/api_query.rs
autonomous: true
requirements:
  - PH56-02
must_haves:
  truths:
    - "Daemon long-lived components share one lifecycle trait named DaemonService."
    - "Daemon runtime snapshots still expose health data without changing the existing transport field names."
    - "The daemon crate builds after the worker-to-service rename with no leftover DaemonWorker references."
  artifacts:
    - path: "src-tauri/crates/uc-daemon/src/service.rs"
      provides: "Unified daemon lifecycle trait and service health enum"
    - path: "src-tauri/crates/uc-daemon/src/state.rs"
      provides: "Service-based runtime snapshot types"
    - path: "src-tauri/crates/uc-daemon/src/api/query.rs"
      provides: "HTTP status mapping from service snapshots while keeping the workers wire field"
  key_links:
    - from: "src-tauri/crates/uc-daemon/src/lib.rs"
      to: "src-tauri/crates/uc-daemon/src/service.rs"
      via: "pub mod service"
      pattern: "pub mod service"
    - from: "src-tauri/crates/uc-daemon/src/state.rs"
      to: "src-tauri/crates/uc-daemon/src/api/query.rs"
      via: "DaemonServiceSnapshot mapping"
      pattern: "DaemonServiceSnapshot"
    - from: "src-tauri/crates/uc-daemon/src/main.rs"
      to: "src-tauri/crates/uc-daemon/src/service.rs"
      via: "Vec<Arc<dyn DaemonService>> imports"
      pattern: "DaemonService"
---

<objective>
Rename the daemon lifecycle abstraction from worker-oriented naming to service-oriented naming without changing behavior.

Purpose: Establish the common lifecycle contract required before `PeerMonitor` extraction and `DaemonApp` service unification.
Output: `service.rs`, renamed snapshot/status vocabulary, and a compiling daemon crate with stable transport payload keys.
</objective>

<execution_context>
Use the repository GSD execution workflow already loaded by the orchestrator.
Validation contract: @.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-VALIDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-CONTEXT.md
@.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-RESEARCH.md
@.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-VALIDATION.md
@.planning/phases/41-daemon-and-cli-skeletons/41-01-SUMMARY.md
@src-tauri/crates/uc-daemon/src/app.rs
@src-tauri/crates/uc-daemon/src/worker.rs
@src-tauri/crates/uc-daemon/src/state.rs
@src-tauri/crates/uc-daemon/src/rpc/handler.rs
@src-tauri/crates/uc-daemon/src/rpc/types.rs
@src-tauri/crates/uc-daemon/src/api/query.rs
@src-tauri/crates/uc-daemon/src/main.rs

<interfaces>
From `src-tauri/crates/uc-daemon/src/worker.rs`:

```rust
#[async_trait]
pub trait DaemonWorker: Send + Sync {
    fn name(&self) -> &str;
    async fn start(&self, cancel: CancellationToken) -> anyhow::Result<()>;
    async fn stop(&self) -> anyhow::Result<()>;
    fn health_check(&self) -> WorkerHealth;
}
```

From `src-tauri/crates/uc-daemon/src/state.rs`:

```rust
pub struct DaemonWorkerSnapshot {
    pub name: String,
    pub health: WorkerHealth,
}

pub struct RuntimeState {
    worker_statuses: Vec<DaemonWorkerSnapshot>,
}
```

From `src-tauri/crates/uc-daemon/src/api/query.rs`:

```rust
pub async fn status(&self) -> Result<StatusResponse>;
fn worker_statuses(snapshots: &[DaemonWorkerSnapshot]) -> Vec<WorkerStatusDto>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the daemon lifecycle trait with DaemonService</name>
  <files>
src-tauri/crates/uc-daemon/src/service.rs
src-tauri/crates/uc-daemon/src/lib.rs
src-tauri/crates/uc-daemon/src/app.rs
src-tauri/crates/uc-daemon/src/main.rs
src-tauri/crates/uc-daemon/src/workers/clipboard_watcher.rs
src-tauri/crates/uc-daemon/src/workers/peer_discovery.rs
  </files>
  <read_first>
- src-tauri/crates/uc-daemon/src/worker.rs
- src-tauri/crates/uc-daemon/src/lib.rs
- src-tauri/crates/uc-daemon/src/app.rs
- src-tauri/crates/uc-daemon/src/main.rs
- src-tauri/crates/uc-daemon/src/workers/clipboard_watcher.rs
- src-tauri/crates/uc-daemon/src/workers/peer_discovery.rs
- .planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-CONTEXT.md
  </read_first>
  <behavior>
- Test 1: the daemon crate exposes `pub trait DaemonService` with the same lifecycle method signatures as the old trait.
- Test 2: `ClipboardWatcherWorker` and `PeerDiscoveryWorker` compile against `DaemonService` and still return healthy placeholder status.
- Test 3: the crate root exports `pub mod service` and no source file imports `crate::worker::DaemonWorker`.
  </behavior>
  <action>Create `src-tauri/crates/uc-daemon/src/service.rs` by moving the lifecycle contract out of `worker.rs`, renaming the trait to `DaemonService`, and renaming the health enum to `ServiceHealth`. Update `lib.rs`, `app.rs`, `main.rs`, `workers/clipboard_watcher.rs`, and `workers/peer_discovery.rs` to import `crate::service::{DaemonService, ServiceHealth}`. Delete `worker.rs` from the active module graph and remove all `DaemonWorker` imports/usages from the daemon crate. Keep the lifecycle surface exactly `name()`, `start(CancellationToken)`, `stop()`, `health_check()` per decision D-04. Do not change runtime behavior in this task.</action>
  <acceptance_criteria>
- `rg -n "pub trait DaemonService" src-tauri/crates/uc-daemon/src/service.rs` returns one match.
- `rg -n "pub enum ServiceHealth" src-tauri/crates/uc-daemon/src/service.rs` returns one match.
- `rg -n "impl DaemonService for ClipboardWatcherWorker|impl DaemonService for PeerDiscoveryWorker" src-tauri/crates/uc-daemon/src/workers` returns two matches.
- `rg -n "DaemonWorker|crate::worker|pub mod worker" src-tauri/crates/uc-daemon/src` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo check -p uc-daemon</automated>
  </verify>
  <done>`uc-daemon` builds with `DaemonService` as the only lifecycle trait and existing placeholder services still compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename runtime snapshot code to service vocabulary without changing wire keys</name>
  <files>
src-tauri/crates/uc-daemon/src/state.rs
src-tauri/crates/uc-daemon/src/rpc/handler.rs
src-tauri/crates/uc-daemon/src/rpc/types.rs
src-tauri/crates/uc-daemon/src/api/query.rs
src-tauri/crates/uc-daemon/tests/api_query.rs
  </files>
  <read_first>
- src-tauri/crates/uc-daemon/src/state.rs
- src-tauri/crates/uc-daemon/src/rpc/handler.rs
- src-tauri/crates/uc-daemon/src/rpc/types.rs
- src-tauri/crates/uc-daemon/src/api/query.rs
- src-tauri/crates/uc-daemon/tests/api_query.rs
- .planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-RESEARCH.md
  </read_first>
  <behavior>
- Test 1: `RuntimeState` stores `DaemonServiceSnapshot` items instead of worker snapshots.
- Test 2: HTTP and RPC status builders read service snapshots from runtime state and still serialize the top-level `workers` field.
- Test 3: existing API query tests still prove that the transport contract keeps `workers` while internal code uses service terminology.
  </behavior>
  <action>Rename `DaemonWorkerSnapshot` to `DaemonServiceSnapshot` and rename helper functions from `worker_statuses` to `service_statuses` across `state.rs`, `rpc/handler.rs`, `rpc/types.rs`, `api/query.rs`, and `tests/api_query.rs`. Keep the serialized response field name `workers` unchanged in `StatusResponse` and `WorkerStatusDto`-equivalent payload structs so Phase 56 stays outside frontend/API contract changes. Update tests to assert the unchanged `workers` key while importing the new internal snapshot and health types.</action>
  <acceptance_criteria>
- `rg -n "pub struct DaemonServiceSnapshot" src-tauri/crates/uc-daemon/src/state.rs` returns one match.
- `rg -n "service_statuses" src-tauri/crates/uc-daemon/src/rpc/handler.rs src-tauri/crates/uc-daemon/src/api/query.rs` returns matches in both files.
- `rg -n "DaemonWorkerSnapshot|worker_statuses\\(" src-tauri/crates/uc-daemon/src src-tauri/crates/uc-daemon/tests` returns no matches.
- `rg -n "workers: Vec<" src-tauri/crates/uc-daemon/src/rpc/types.rs src-tauri/crates/uc-daemon/src/api/types.rs` still returns matches, proving the wire field name stayed stable.
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-daemon --test api_query -- --nocapture && cargo test -p uc-daemon rpc::handler --lib -- --nocapture</automated>
  </verify>
  <done>Internal daemon code uses service terminology while RPC and HTTP status payloads remain backward-compatible.</done>
</task>

</tasks>

<verification>
Run the task-level commands, then confirm `rg -n "DaemonWorker|crate::worker|DaemonWorkerSnapshot" src-tauri/crates/uc-daemon/src src-tauri/crates/uc-daemon/tests` returns no matches.
</verification>

<success_criteria>
`DaemonService` exists as the only daemon lifecycle trait, runtime snapshots are service-based, and the daemon crate compiles without changing the existing `workers` transport field.
</success_criteria>

<output>
After completion, create `.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-01-SUMMARY.md`
</output>
