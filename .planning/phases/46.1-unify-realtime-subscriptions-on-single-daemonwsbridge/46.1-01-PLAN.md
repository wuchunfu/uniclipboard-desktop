---
phase: 46.1
plan: 1
name: 'Core Realtime Port And Envelope Model'
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/crates/uc-core/src/ports/realtime.rs
  - src-tauri/crates/uc-core/src/ports/mod.rs
  - src-tauri/crates/uc-core/src/ports/host_event_emitter.rs
  - src-tauri/crates/uc-core/tests/realtime_port.rs
autonomous: true
requirements:
  - PH461-02
must_haves:
  truths:
    - 'uc-core exposes one typed realtime subscription contract that app code can depend on without importing daemon websocket DTOs'
    - 'the unified frontend realtime envelope is modeled before any uc-tauri adapter code is added'
    - 'the core contract is covered by an isolated cargo test target before downstream bridge and consumer work begins'
  artifacts:
    - path: src-tauri/crates/uc-core/src/ports/realtime.rs
      provides: Realtime topic subscription contract and typed daemon-agnostic event model
      contains: pub trait RealtimeTopicPort
    - path: src-tauri/crates/uc-core/src/ports/host_event_emitter.rs
      provides: unified frontend realtime host event model
      contains: RealtimeHostEvent
    - path: src-tauri/crates/uc-core/tests/realtime_port.rs
      provides: contract tests for topic and envelope semantics
      contains: realtime_port_topic_set_is_stable
  key_links:
    - from: src-tauri/crates/uc-core/src/ports/mod.rs
      to: src-tauri/crates/uc-core/src/ports/realtime.rs
      via: pub mod realtime export
      pattern: pub mod realtime;
    - from: src-tauri/crates/uc-core/src/ports/host_event_emitter.rs
      to: src-tauri/crates/uc-core/src/ports/realtime.rs
      via: HostEvent::Realtime envelope references typed topics and payload variants
      pattern: HostEvent::Realtime
---

<objective>
Define the core realtime contract for Phase 46.1.

Purpose: lock the daemon-agnostic subscription interface and frontend envelope model before any uc-app or uc-tauri wiring touches it.
Output: typed `RealtimeTopicPort`, `RealtimeEvent`, `RealtimeFrontendEvent`, and a dedicated core test file.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-CONTEXT.md
@.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-RESEARCH.md
@.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-VALIDATION.md
@src-tauri/crates/uc-core/src/ports/host_event_emitter.rs

<interfaces>
From `src-tauri/crates/uc-core/src/ports/host_event_emitter.rs`:

```rust
pub enum HostEvent {
    Clipboard(ClipboardHostEvent),
    PeerDiscovery(PeerDiscoveryHostEvent),
    PeerConnection(PeerConnectionHostEvent),
    Transfer(TransferHostEvent),
    Pairing(PairingHostEvent),
    Setup(SetupHostEvent),
    SpaceAccess(SpaceAccessHostEvent),
}

pub trait HostEventEmitterPort: Send + Sync {
    fn emit(&self, event: HostEvent) -> Result<(), EmitError>;
}
```

Use the existing `HostEventEmitterPort` for outbound frontend delivery. Do not add any uc-tauri or daemon DTO dependency to `uc-core`.
</interfaces>
</context>

<tasks>

<task id="46.1-01-01" type="auto">
  <name>Task 1: Create failing core contract tests and Wave 0 realtime stubs</name>
  <files>
src-tauri/crates/uc-core/src/ports/realtime.rs
src-tauri/crates/uc-core/src/ports/mod.rs
src-tauri/crates/uc-core/tests/realtime_port.rs
  </files>
  <read_first>
src-tauri/crates/uc-core/src/ports/mod.rs
src-tauri/crates/uc-core/src/ports/host_event_emitter.rs
.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-CONTEXT.md
.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-RESEARCH.md
.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-VALIDATION.md
  </read_first>
  <action>
Create `src-tauri/crates/uc-core/tests/realtime_port.rs` as the Wave 0 contract test file referenced by `46.1-VALIDATION.md`. Add failing tests for these exact behaviors:

- `realtime_port_topic_set_is_stable` asserts `RealtimeTopic::{Pairing,Peers,PairedDevices}` exist.
- `realtime_event_variants_cover_pairing_peers_and_paired_devices` asserts `RealtimeEvent` includes typed variants for pairing lifecycle, peer updates, and paired-device changes instead of `serde_json::Value`.
- `realtime_frontend_event_uses_daemon_realtime_contract` asserts the outbound envelope uses the exact frontend channel name `daemon://realtime`, carries `topic`, `type`, `ts`, and `payload`, and keeps `type` strings like `pairing.verificationRequired` and `peers.changed`.

Add the minimal Wave 0 stub file `src-tauri/crates/uc-core/src/ports/realtime.rs` and `pub mod realtime;` export so the test target compiles. Keep the stub intentionally skeletal; do not implement uc-tauri details in this task.
</action>
<acceptance_criteria>

- `src-tauri/crates/uc-core/tests/realtime_port.rs` exists
- `src-tauri/crates/uc-core/tests/realtime_port.rs` contains `realtime_port_topic_set_is_stable`
- `src-tauri/crates/uc-core/tests/realtime_port.rs` contains `realtime_event_variants_cover_pairing_peers_and_paired_devices`
- `src-tauri/crates/uc-core/tests/realtime_port.rs` contains `daemon://realtime`
- `src-tauri/crates/uc-core/src/ports/realtime.rs` exists
- `src-tauri/crates/uc-core/src/ports/mod.rs` contains `pub mod realtime;`
  </acceptance_criteria>
  <verify>
  <automated>cd src-tauri && cargo test -p uc-core realtime_port -- --test-threads=1</automated>
  </verify>
  <done>
  The core test target and source stub exist, and the failing coverage describes the exact typed realtime contract Phase 46.1 will implement.
  </done>
  </task>

<task id="46.1-01-02" type="auto">
  <name>Task 2: Implement typed realtime topics, inbound events, and outbound unified envelope in uc-core</name>
  <files>
src-tauri/crates/uc-core/src/ports/realtime.rs
src-tauri/crates/uc-core/src/ports/mod.rs
src-tauri/crates/uc-core/src/ports/host_event_emitter.rs
src-tauri/crates/uc-core/tests/realtime_port.rs
  </files>
  <read_first>
src-tauri/crates/uc-core/src/ports/realtime.rs
src-tauri/crates/uc-core/src/ports/mod.rs
src-tauri/crates/uc-core/src/ports/host_event_emitter.rs
src-tauri/crates/uc-core/tests/realtime_port.rs
.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-RESEARCH.md
  </read_first>
  <action>
Implement the Phase 46.1 core contract with these exact items:

- `pub enum RealtimeTopic { Pairing, Peers, PairedDevices }`
- `pub enum RealtimeEvent` with typed variants for `pairing.updated`, `pairing.verification_required`, `pairing.failed`, `pairing.complete`, `peers.changed`, `peers.name_updated`, `peers.connection_changed`, and `paired-devices.changed`
- `pub trait RealtimeTopicPort: Send + Sync` with `async fn subscribe(&self, consumer: &'static str, topics: &[RealtimeTopic]) -> anyhow::Result<tokio::sync::mpsc::Receiver<RealtimeEvent>>`
- `pub const FRONTEND_REALTIME_EVENT: &str = "daemon://realtime"`
- `pub struct RealtimeFrontendEvent` and typed payload enums/structs needed for app consumers to emit the single frontend contract

Extend `HostEvent` with one new `Realtime(RealtimeFrontendEvent)` variant so uc-app consumers can emit the unified contract without depending on any tauri-specific serializer. Keep the old pairing/peer/setup variants in place for now; Phase 46.1-05 removes them after the frontend cutover lands.
</action>
<acceptance_criteria>

- `src-tauri/crates/uc-core/src/ports/realtime.rs` contains `pub enum RealtimeTopic`
- `src-tauri/crates/uc-core/src/ports/realtime.rs` contains `pub enum RealtimeEvent`
- `src-tauri/crates/uc-core/src/ports/realtime.rs` contains `pub trait RealtimeTopicPort`
- `src-tauri/crates/uc-core/src/ports/realtime.rs` contains `async fn subscribe`
- `src-tauri/crates/uc-core/src/ports/realtime.rs` contains `pub const FRONTEND_REALTIME_EVENT: &str = "daemon://realtime"`
- `src-tauri/crates/uc-core/src/ports/host_event_emitter.rs` contains `Realtime(RealtimeFrontendEvent)`
- `src-tauri/crates/uc-core/tests/realtime_port.rs` contains `pairing.verificationRequired`
- `cd src-tauri && cargo test -p uc-core realtime_port -- --test-threads=1` exits 0
  </acceptance_criteria>
  <verify>
  <automated>cd src-tauri && cargo test -p uc-core realtime_port -- --test-threads=1</automated>
  </verify>
  <done>
  `uc-core` now owns the typed subscription contract and unified frontend envelope model, and downstream work can compile against those exports without importing daemon transport DTOs.
  </done>
  </task>

</tasks>

<verification>
- `cd src-tauri && cargo test -p uc-core realtime_port -- --test-threads=1`
</verification>

<success_criteria>

- `RealtimeTopicPort` exists in `uc-core` with typed topics and typed events.
- The new frontend realtime envelope name is locked to `daemon://realtime`.
- No `uc-core` realtime type references `DaemonWsEvent`, `serde_json::Value`, or tauri types.
  </success_criteria>

<output>
After completion, create `.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-01-SUMMARY.md`
</output>
