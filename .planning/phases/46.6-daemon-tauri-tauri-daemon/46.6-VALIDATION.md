---
phase: 46.6
slug: daemon-tauri-tauri-daemon
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-22
---

# Phase 46.6 - Validation Strategy

## Test Infrastructure

| Property               | Value                                                                                                                                                                  |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Framework**          | Rust `cargo test`                                                                                                                                                      |
| **Config file**        | `src-tauri/Cargo.toml`                                                                                                                                                 |
| **Quick run command**  | `cd src-tauri && cargo test -p uc-tauri --test daemon_exit_cleanup -- --test-threads=1`                                                                                |
| **Full suite command** | `cd src-tauri && cargo test -p uc-tauri --test daemon_exit_cleanup -- --test-threads=1 && cargo test -p uc-tauri --test daemon_bootstrap_contract -- --test-threads=1` |
| **Estimated runtime**  | ~45 seconds                                                                                                                                                            |

## Sampling Rate

- After every task commit: run the targeted `uc-tauri` lifecycle regression test
- After every plan: run the full suite listed above
- Before phase verification: full suite must pass

## Per-Task Verification Map

| Task ID    | Plan | Wave | Requirement                                         | Test Type   | Automated Command                                                                       | Status  |
| ---------- | ---- | ---- | --------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------- | ------- |
| 46.6-01-01 | 01   | 0    | Wave 0 regression target exists and is executable   | integration | `cd src-tauri && cargo test -p uc-tauri --test daemon_exit_cleanup -- --test-threads=1` | pending |
| 46.6-01-02 | 01   | 0    | managed daemon ownership survives bootstrap to exit | integration | `cd src-tauri && cargo test -p uc-tauri --test daemon_exit_cleanup -- --test-threads=1` | pending |
| 46.6-02-01 | 02   | 1    | owned daemon cleanup is bounded and clears state    | integration | `cd src-tauri && cargo test -p uc-tauri --test daemon_exit_cleanup -- --test-threads=1` | pending |
| 46.6-02-02 | 02   | 1    | non-owned daemon and tray-hide remain unaffected    | integration | `cd src-tauri && cargo test -p uc-tauri --test daemon_exit_cleanup -- --test-threads=1` | pending |

## Wave 0 Requirements

- [x] `46.6-01` is Wave 0 and creates the dedicated `uc-tauri` regression target for daemon exit cleanup before later implementation tasks depend on it
- [x] `daemon_exit_cleanup` is the single canonical task-level regression target for the phase; `daemon_bootstrap_contract` remains supplemental plan-level regression only

## Manual-Only Verifications

| Behavior                                                 | Why Manual                                         | Test Instructions                                                                           |
| -------------------------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| GUI close button still hides to tray instead of quitting | UX semantics depend on live Tauri window lifecycle | Launch app, click main window close, verify app hides and daemon is still available         |
| Real quit tears down GUI-owned daemon                    | Needs actual app shutdown timing                   | Launch GUI with no daemon present, quit app from tray/app menu, verify daemon process exits |
| Independently started daemon survives GUI quit           | Must prove no overreach                            | Start daemon manually first, launch GUI, quit GUI, verify daemon remains running            |

## Validation Sign-Off

- [ ] Dedicated exit-cleanup regression test exists
- [ ] No plan relies on manual-only verification for correctness of core ownership logic
- [ ] Full targeted suite passes from `src-tauri/`
