---
phase: 46.5-tauri-daemon
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/crates/uc-daemon/src/api/pairing.rs
  - src-tauri/crates/uc-daemon/src/api/routes.rs
  - src-tauri/crates/uc-tauri/src/daemon_client/mod.rs
  - src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs
  - src-tauri/crates/uc-tauri/src/daemon_client/query.rs
  - src-tauri/crates/uc-tauri/src/daemon_client/query_tests.rs
  - src-tauri/crates/uc-tauri/src/daemon_client/setup.rs
  - src-tauri/crates/uc-tauri/src/commands/pairing.rs
  - src-tauri/crates/uc-tauri/src/commands/setup.rs
  - src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs
autonomous: true
requirements: ['PH46-05', 'PH461-06', 'R46.2-1', 'R46.2-4']
user_setup: []
must_haves:
  truths:
    - 'Tauri pairing/setup command surface 只做daemon HTTP client转发和错误映射，不再直接驱动本地pairing/setup业务对象。'
    - '前端继续调用原有Tauri command名，并继续收到原有`SetupState`/`PairedPeer`形状。'
    - 'unpair、setup host/join/select/confirm/passphrase/cancel 全部经由daemon route完成。'
  artifacts:
    - path: 'src-tauri/crates/uc-tauri/src/daemon_client/setup.rs'
      provides: 'setup shell专用daemon client，覆盖 `/setup/state|host|join|select-peer|confirm-peer|submit-passphrase|cancel`。'
      min_lines: 160
    - path: 'src-tauri/crates/uc-tauri/src/commands/setup.rs'
      provides: '保持原Tauri command名不变的daemon-backed setup shell。'
      min_lines: 220
    - path: 'src-tauri/crates/uc-tauri/src/commands/pairing.rs'
      provides: 'daemon-backed paired-device read/unpair shell。'
      min_lines: 500
    - path: 'src-tauri/crates/uc-daemon/src/api/routes.rs'
      provides: 'daemon-owned `/pairing/unpair` route，以及setup route继续作为唯一backend真相。'
      min_lines: 320
    - path: 'src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs'
      provides: 'pairing/setup command shell回归测试。'
      min_lines: 160
  key_links:
    - from: 'src-tauri/crates/uc-tauri/src/commands/setup.rs'
      to: 'src-tauri/crates/uc-tauri/src/daemon_client/setup.rs'
      via: '原有setup command名映射到daemon `/setup/*` 路径，并把响应的 `state` 字段反序列化回 `SetupState`。'
      pattern: 'get_setup_state|start_new_space|start_join_space|select_device|submit_passphrase|verify_passphrase|confirm_peer_trust|cancel_setup'
    - from: 'src-tauri/crates/uc-tauri/src/commands/pairing.rs'
      to: 'src-tauri/crates/uc-tauri/src/daemon_client/query.rs'
      via: 'paired-device读面改走 `/paired-devices`，不再调用本地use case。'
      pattern: 'get_paired_devices|list_paired_devices|get_paired_peers_with_status'
    - from: 'src-tauri/crates/uc-tauri/src/commands/pairing.rs'
      to: 'src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs'
      via: 'unpair 改走 `/pairing/unpair`，而不是 `runtime.usecases().unpair_device()`。'
      pattern: 'unpair_device|/pairing/unpair|unpair_p2p_device'
---

<objective>
把 `uc-tauri` 的 pairing/setup command surface 收口成 daemon-only shell，同时保持现有前端 Tauri command contract 不变。

Purpose: 在 Phase 47 前不动前端 transport，但先把 Tauri backend 的业务 ownership 清空，只留下 daemon adapter 责任。
Output: daemon-backed setup client、unpair route 与 Tauri pairing/setup command shell 回归测试。
</objective>

<execution_context>
Use the standard execute-phase workflow already configured for this repository.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46.5-tauri-daemon/46.5-CONTEXT.md
@.planning/phases/46.5-tauri-daemon/46.5-RESEARCH.md
@src-tauri/crates/uc-daemon/src/api/pairing.rs
@src-tauri/crates/uc-daemon/src/api/routes.rs
@src-tauri/crates/uc-daemon/src/api/types.rs
@src-tauri/crates/uc-daemon/src/api/query.rs
@src-tauri/crates/uc-tauri/src/daemon_client/mod.rs
@src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs
@src-tauri/crates/uc-tauri/src/daemon_client/query.rs
@src-tauri/crates/uc-tauri/src/commands/pairing.rs
@src-tauri/crates/uc-tauri/src/commands/setup.rs
@src/api/setup.ts
@src/api/p2p.ts

<interfaces>
From `src-tauri/crates/uc-daemon/src/api/types.rs`:
```rust
pub struct SetupStateResponse {
    pub state: Value,
    pub session_id: Option<String>,
    pub next_step_hint: String,
    pub profile: String,
    pub clipboard_mode: String,
    pub device_name: String,
    pub peer_id: String,
    pub selected_peer_id: Option<String>,
    pub selected_peer_name: Option<String>,
    pub has_completed: bool,
}

pub struct SetupActionAckResponse {
pub state: Value,
pub session_id: Option<String>,
pub next_step_hint: String,
}

pub struct PairedDeviceDto {
pub peer_id: String,
pub device_name: String,
pub pairing_state: String,
pub last_seen_at_ms: Option<i64>,
pub connected: bool,
}

````

From `src-tauri/crates/uc-core/src/setup/state.rs`:
```rust
pub enum SetupState {
    Welcome,
    CreateSpaceInputPassphrase { error: Option<SetupError> },
    JoinSpaceSelectDevice { error: Option<SetupError> },
    JoinSpaceConfirmPeer { short_code: String, peer_fingerprint: Option<String>, error: Option<SetupError> },
    JoinSpaceInputPassphrase { error: Option<SetupError> },
    ProcessingCreateSpace { message: Option<String> },
    ProcessingJoinSpace { message: Option<String> },
    Completed,
}
````

</interfaces>
</context>

<atomic_commit_boundaries>

- Commit 1: `impl: add daemon pairing and setup shell transport foundation`
  Scope: `src-tauri/crates/uc-daemon/src/api/pairing.rs`, `src-tauri/crates/uc-daemon/src/api/routes.rs`, `src-tauri/crates/uc-tauri/src/daemon_client/mod.rs`, `src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs`, `src-tauri/crates/uc-tauri/src/daemon_client/query.rs`, `src-tauri/crates/uc-tauri/src/daemon_client/query_tests.rs`, `src-tauri/crates/uc-tauri/src/daemon_client/setup.rs`
- Commit 2: `impl: cut tauri pairing and setup commands to daemon shell`
  Scope: `src-tauri/crates/uc-tauri/src/commands/pairing.rs`, `src-tauri/crates/uc-tauri/src/commands/setup.rs`, `src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs`
- Rule: 执行时不得把 route/client foundation 和 command-shell cutover 混进同一个提交；若 Task 1 与 Task 2 同时完成，也必须按上述边界拆成两个原子提交。
  </atomic_commit_boundaries>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补齐daemon pairing/setup shell所需的HTTP client与unpair route</name>
  <files>
    src-tauri/crates/uc-daemon/src/api/pairing.rs,
    src-tauri/crates/uc-daemon/src/api/routes.rs,
    src-tauri/crates/uc-tauri/src/daemon_client/mod.rs,
    src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs,
    src-tauri/crates/uc-tauri/src/daemon_client/query.rs,
    src-tauri/crates/uc-tauri/src/daemon_client/query_tests.rs,
    src-tauri/crates/uc-tauri/src/daemon_client/setup.rs
  </files>
  <read_first>
    - src-tauri/crates/uc-daemon/src/api/pairing.rs
    - src-tauri/crates/uc-daemon/src/api/routes.rs
    - src-tauri/crates/uc-daemon/src/api/types.rs
    - src-tauri/crates/uc-daemon/src/api/query.rs
    - src-tauri/crates/uc-tauri/src/daemon_client/mod.rs
    - src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs
    - src-tauri/crates/uc-tauri/src/daemon_client/query.rs
    - src-tauri/crates/uc-tauri/src/daemon_client/query_tests.rs
    - .planning/phases/46.5-tauri-daemon/46.5-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `TauriDaemonSetupClient` 能 decode `/setup/state` 的 `SetupStateResponse` 与 `/setup/*` action ack 的 `SetupActionAckResponse`。
    - Test 2: `TauriDaemonQueryClient` 新增 `get_paired_devices()` 并正确 decode `Vec<PairedDeviceDto>`。
    - Test 3: daemon 新增 `POST /pairing/unpair`，`TauriDaemonPairingClient::unpair_device(peer_id)` 能走通成功与错误 decode。
  </behavior>
  <action>
    1. 在 `src-tauri/crates/uc-daemon/src/api/pairing.rs` 中新增 `UnpairDeviceRequest { peer_id: String }`，保持 `#[serde(rename_all = "camelCase")]`。
    2. 在 `src-tauri/crates/uc-daemon/src/api/routes.rs` 中新增 `POST /pairing/unpair`，固定行为：
       - 认证方式与现有 `/pairing/*` 完全一致。
       - 请求体使用 `UnpairDeviceRequest`。
       - 通过 daemon runtime 的 use case 执行 `unpair_device(peer_id)`。
       - 成功返回 `204 No Content`。
       - 失败继续走现有 pairing error mapping，不引入第二套错误格式。
    3. 在 `src-tauri/crates/uc-tauri/src/daemon_client/setup.rs` 中新增 `TauriDaemonSetupClient`，固定方法名与路径：
       - `get_setup_state()` -> `GET /setup/state`
       - `start_new_space()` -> `POST /setup/host`
       - `start_join_space()` -> `POST /setup/join`
       - `select_device(peer_id: String)` -> `POST /setup/select-peer`
       - `confirm_peer_trust()` -> `POST /setup/confirm-peer`
       - `submit_passphrase(passphrase: String)` -> `POST /setup/submit-passphrase`
       - `cancel_setup()` -> `POST /setup/cancel`
       所有请求复用与现有 daemon client 相同的 bearer token/base_url 解析逻辑，不复制另一套连接状态代码。
    4. 在 `src-tauri/crates/uc-tauri/src/daemon_client/query.rs` 中新增 `get_paired_devices()` -> `GET /paired-devices`。
    5. 在 `src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs` 中新增 `unpair_device(peer_id: String)` -> `POST /pairing/unpair`，并继续沿用 `DaemonPairingRequestError`。
    6. 在 `src-tauri/crates/uc-tauri/src/daemon_client/mod.rs` 导出 `TauriDaemonSetupClient`。
    7. 在 `src-tauri/crates/uc-tauri/src/daemon_client/query_tests.rs` 与新建的 `setup.rs` 内测试区中补充明确测试：
       - `daemon_query_client_fetches_paired_devices_from_daemon_api`
       - `daemon_setup_client_fetches_setup_state_from_daemon_api`
       - `daemon_setup_client_posts_submit_passphrase_to_daemon_api`
       - `daemon_pairing_client_posts_unpair_to_daemon_api`
  </action>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-tauri daemon_client -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg "struct UnpairDeviceRequest|peer_id: String" src-tauri/crates/uc-daemon/src/api/pairing.rs -n` 返回 unpair 请求 DTO。
    - `rg "/pairing/unpair|async fn handle_.*unpair|unpair_device" src-tauri/crates/uc-daemon/src/api/routes.rs src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs -n` 返回 daemon route 与 client 方法。
    - `rg "struct TauriDaemonSetupClient|get_setup_state|start_new_space|start_join_space|select_device|confirm_peer_trust|submit_passphrase|cancel_setup" src-tauri/crates/uc-tauri/src/daemon_client/setup.rs -n` 返回 7 个 setup helper。
    - `rg "get_paired_devices" src-tauri/crates/uc-tauri/src/daemon_client/query.rs src-tauri/crates/uc-tauri/src/daemon_client/query_tests.rs -n` 返回 paired-device query helper 与测试。
    - `cd src-tauri && cargo test -p uc-tauri daemon_client -- --test-threads=1` 退出码为 0。
  </acceptance_criteria>
  <done>
    - daemon shell 所需的 setup/pairing HTTP client 面齐备。
    - unpair 不再依赖 Tauri 进程内本地pairing transport。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把Tauri pairing/setup commands收口成daemon-only shell并锁定回归</name>
  <files>
    src-tauri/crates/uc-tauri/src/commands/pairing.rs,
    src-tauri/crates/uc-tauri/src/commands/setup.rs,
    src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs
  </files>
  <read_first>
    - src-tauri/crates/uc-tauri/src/commands/pairing.rs
    - src-tauri/crates/uc-tauri/src/commands/setup.rs
    - src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs
    - src-tauri/crates/uc-tauri/src/daemon_client/query.rs
    - src-tauri/crates/uc-tauri/src/daemon_client/setup.rs
    - src-tauri/crates/uc-daemon/src/api/types.rs
    - src-tauri/crates/uc-core/src/setup/error.rs
    - src-tauri/crates/uc-core/src/setup/state.rs
    - src/api/setup.ts
    - src/api/p2p.ts
  </read_first>
  <behavior>
    - Test 1: setup commands继续保留 `get_setup_state`、`start_new_space`、`start_join_space`、`select_device`、`submit_passphrase`、`verify_passphrase`、`confirm_peer_trust`、`cancel_setup` 这些Tauri command名，但内部只调 daemon client。
    - Test 2: `submit_passphrase(passphrase1, passphrase2)` 当两个口令不相等时，不发daemon请求，直接返回 `SetupState::CreateSpaceInputPassphrase { error: Some(SetupError::PassphraseMismatch) }`。
    - Test 3: pairing commands 的 `list_paired_devices`、`get_paired_peers_with_status`、`unpair_p2p_device` 不再调用 `runtime.usecases().list_paired_devices()`、`get_p2p_peers_snapshot()`、`unpair_device()`。
  </behavior>
  <action>
    1. 在 `src-tauri/crates/uc-tauri/src/commands/setup.rs` 中给全部 setup commands 增加 `daemon_connection: State<'_, DaemonConnectionState>`，并改成固定daemon shell映射：
       - `get_setup_state` -> `TauriDaemonSetupClient::get_setup_state()`
       - `start_new_space` -> `TauriDaemonSetupClient::start_new_space()`
       - `start_join_space` -> `TauriDaemonSetupClient::start_join_space()`
       - `select_device(peer_id)` -> `TauriDaemonSetupClient::select_device(peer_id)`
       - `verify_passphrase(passphrase)` -> `TauriDaemonSetupClient::submit_passphrase(passphrase)`
       - `confirm_peer_trust` -> `TauriDaemonSetupClient::confirm_peer_trust()`
       - `cancel_setup` -> `TauriDaemonSetupClient::cancel_setup()`
       并把响应里的 `state: serde_json::Value` 用 `serde_json::from_value::<SetupState>(...)` 反序列化回原有返回类型。
    2. `submit_passphrase(passphrase1, passphrase2)` 保持现有前端 contract：
       - 若 `passphrase1 != passphrase2`，直接返回 `Ok(SetupState::CreateSpaceInputPassphrase { error: Some(uc_core::setup::SetupError::PassphraseMismatch) })`
       - 若相等，则只把 `passphrase1` 传给 `TauriDaemonSetupClient::submit_passphrase(passphrase1)`，再把 ack.state 反序列化成 `SetupState`
    3. 在 `src-tauri/crates/uc-tauri/src/commands/pairing.rs` 中执行以下明确收口：
       - `get_p2p_peers` 继续使用 `TauriDaemonQueryClient::get_peers()`
       - `list_paired_devices` 改为 `TauriDaemonQueryClient::get_paired_devices()`
       - `get_paired_peers_with_status` 改为 `TauriDaemonQueryClient::get_paired_devices()`
       - `unpair_p2p_device` 改为 `TauriDaemonPairingClient::unpair_device(peer_id.clone())`
       - `get_local_peer_id` 与 `get_local_device_info` 保持本地读取
       - 删除 active command path 中对 `runtime.usecases().list_paired_devices()`、`runtime.usecases().get_p2p_peers_snapshot()`、`runtime.usecases().unpair_device()` 的直接调用
    4. `PairedPeer` 映射固定为：
       - `peer_id <- dto.peer_id`
       - `device_name <- dto.device_name`
       - `shared_secret <- vec![]`
       - `paired_at <- String::new()`
       - `last_seen <- dto.last_seen_at_ms.map(|ms| chrono::DateTime::from_timestamp_millis(ms).unwrap_or_default().to_rfc3339())` 的等价安全实现，禁止 `unwrap()`
       - `last_known_addresses <- vec![]`
       - `connected <- dto.connected`
    5. 在 `src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs` 新增明确测试名：
       - `setup_commands_forward_to_daemon_and_deserialize_setup_state`
       - `setup_submit_passphrase_preserves_local_mismatch_contract`
       - `pairing_commands_use_daemon_queries_and_unpair_route`
       测试要通过假 HTTP server 或受控 connection state 断言命令shell只打daemon route，不接触本地 orchestrator/usecase。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-tauri daemon_command_shell -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg "State<'_, DaemonConnectionState>" src-tauri/crates/uc-tauri/src/commands/setup.rs -n` 返回 setup commands 的 daemon connection 依赖。
    - `rg "TauriDaemonSetupClient::new|serde_json::from_value::<SetupState>|PassphraseMismatch" src-tauri/crates/uc-tauri/src/commands/setup.rs -n` 返回 daemon shell 映射与本地 mismatch 保底逻辑。
    - `rg "get_paired_devices|unpair_device\\(|runtime.usecases\\(\\)\\.list_paired_devices|runtime.usecases\\(\\)\\.unpair_device" src-tauri/crates/uc-tauri/src/commands/pairing.rs -n` 显示 daemon query/client 路径存在，且本地 usecase 调用已从 active path 删除。
    - `rg "setup_commands_forward_to_daemon_and_deserialize_setup_state|setup_submit_passphrase_preserves_local_mismatch_contract|pairing_commands_use_daemon_queries_and_unpair_route" src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs -n` 返回3个测试名。
    - `cd src-tauri && cargo test -p uc-tauri daemon_command_shell -- --test-threads=1` 退出码为 0。
  </acceptance_criteria>
  <done>
    - Tauri pairing/setup command surface 只剩 daemon adapter 责任。
    - 前端在不改 command 名与返回形状的前提下，后端已经完成 daemon-only 收口。
  </done>
</task>

</tasks>

<verification>
`cd src-tauri && cargo test -p uc-tauri daemon_client -- --test-threads=1`
`cd src-tauri && cargo test -p uc-tauri daemon_command_shell -- --test-threads=1`
</verification>

<success_criteria>
`commands/pairing.rs` 与 `commands/setup.rs` 的活跃业务路径全部变成 daemon-backed shell；GUI local pairing transport 就算被 plan 01 切断，也不会再让 setup/join/unpair 回落到 Tauri 本地业务对象。
</success_criteria>

<output>
完成后创建 `.planning/phases/46.5-tauri-daemon/46.5-02-SUMMARY.md`，总结daemon setup client、unpair route 与 Tauri command shell cutover 的最终形态。
</output>
