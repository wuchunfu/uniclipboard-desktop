---
phase: 33-fix-file-sync-eventual-consistency-ensure-atomic-sync-with-metadata-and-blob-together
plan: 04
type: execute
wave: 4
depends_on:
  - 33-03
files_modified:
  - src/api/clipboardItems.ts
  - src/store/slices/fileTransferSlice.ts
  - src/hooks/useTransferProgress.ts
  - src/api/__tests__/clipboardItems.test.ts
  - src/store/slices/__tests__/clipboardSlice.test.ts
autonomous: true
requirements:
  - FSYNC-CONSISTENCY

must_haves:
  truths:
    - 'Initial clipboard list responses hydrate file entry transfer state without waiting for live events'
    - 'Live `file-transfer://status-changed` events update entry-level transfer state independently of progress animation state'
    - 'Completed progress cleanup does not erase durable completed or failed entry state'
  artifacts:
    - path: 'src/api/clipboardItems.ts'
      provides: 'Frontend response types include persisted file transfer status and reason'
      contains: 'file_transfer_status'
    - path: 'src/store/slices/fileTransferSlice.ts'
      provides: 'Redux state distinguishes live progress from durable entry-level transfer status'
      contains: 'entryStatusById'
    - path: 'src/hooks/useTransferProgress.ts'
      provides: 'Listener for `file-transfer://status-changed` alongside renamed `file-transfer://progress` and `file-transfer://error` events'
      contains: 'file-transfer://status-changed'
  key_links:
    - from: 'src/api/clipboardItems.ts'
      to: 'src/store/slices/fileTransferSlice.ts'
      via: 'initial query payload seeds durable entry-level transfer state'
      pattern: 'file_transfer_status'
    - from: 'src/hooks/useTransferProgress.ts'
      to: 'src/store/slices/fileTransferSlice.ts'
      via: 'live status-changed events update durable state and progress remains separate'
      pattern: 'status-changed|entryStatusById'
---

<objective>
Hydrate and maintain durable file transfer state on the frontend data layer.

Purpose: make restart-safe transfer truth available to UI components before any rendering or action gating work begins.
Output: API types, Redux state, and event listeners that preserve backend transfer status.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-fix-file-sync-eventual-consistency-ensure-atomic-sync-with-metadata-and-blob-together/32-CONTEXT.md
@.planning/phases/33-fix-file-sync-eventual-consistency-ensure-atomic-sync-with-metadata-and-blob-together/32-RESEARCH.md
@.planning/phases/33-fix-file-sync-eventual-consistency-ensure-atomic-sync-with-metadata-and-blob-together/33-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend API types, slice state, and status listeners for durable entry tracking</name>
  <files>
    src/api/clipboardItems.ts
    src/store/slices/fileTransferSlice.ts
    src/hooks/useTransferProgress.ts
    src/api/__tests__/clipboardItems.test.ts
    src/store/slices/__tests__/clipboardSlice.test.ts
  </files>
  <action>
    1. Extend frontend clipboard response types and transformation logic to preserve backend `file_transfer_status` and `file_transfer_reason` fields for file entries.

    2. Update `fileTransferSlice` so it separately tracks:
       - live transfer progress keyed by `transferId`
       - durable entry status keyed by `entryId`
       - optional failure reason keyed by `entryId`

    3. In `useTransferProgress.ts`:
       - Update existing progress listener channel from `transfer://progress` to `file-transfer://progress` (renamed in Plan 03).
       - Update existing error listener channel from `transfer://error` to `file-transfer://error` (renamed in Plan 03).
       - Add a new listener for `file-transfer://status-changed`. This listener must update durable entry state even when no local progress event was seen.

    4. Keep progress auto-clear behavior for completed transfers, but do not let that remove the durable entry-level status seeded from commands or status events.

    5. Add tests for:
       - initial API hydration of failed/pending status
       - live status event updating an existing entry to `transferring`
       - completion clearing only progress state while leaving entry status intact

  </action>
  <verify>
    <automated>bun run test -- --run -t "file transfer status"</automated>
  </verify>
  <done>
    - file entry transfer state survives reload because it is hydrated from command responses
    - live status events update durable entry state independently of progress cleanup
  </done>
</task>

</tasks>

<verification>
1. Initial query tests prove file transfer state is present before any event arrives
2. Status event tests prove durable entry state updates without reloading the list
3. Progress cleanup no longer erases durable state
</verification>

<success_criteria>

- Frontend data flow has a durable source for file transfer truth
- UI plans can now render and gate actions without inventing their own state model
  </success_criteria>

<output>
After completion, create `.planning/phases/33-fix-file-sync-eventual-consistency-ensure-atomic-sync-with-metadata-and-blob-together/33-04-SUMMARY.md`
</output>
