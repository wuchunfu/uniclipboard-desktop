---
phase: 56.1-eliminate-hardcoded-strings-in-pairing-setup-flow
plan: 02
type: execute
wave: 2
depends_on: ['56.1-01']
files_modified:
  - src-tauri/crates/uc-daemon/src/api/ws.rs
  - src-tauri/crates/uc-daemon/src/api/server.rs
  - src-tauri/crates/uc-daemon/src/api/routes.rs
  - src-tauri/crates/uc-daemon/src/pairing/host.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - 'uc-daemon has zero hardcoded WS topic/event type strings — all come from uc-core constants'
    - 'uc-daemon has zero hardcoded pairing stage/busy reason/error code strings — all come from uc-core constants'
    - 'uc-daemon HTTP route definitions use shared path constants from uc-core'
    - 'Existing daemon tests still pass with no behavioral change'
  artifacts:
    - path: 'src-tauri/crates/uc-daemon/src/api/ws.rs'
      provides: 'WS handler using imported constants instead of module-level const definitions'
      contains: 'use uc_core::network::daemon_api_strings'
    - path: 'src-tauri/crates/uc-daemon/src/pairing/host.rs'
      provides: 'Pairing host using imported constants for stage labels, topic names, event types, busy reasons'
      contains: 'use uc_core::network::daemon_api_strings'
    - path: 'src-tauri/crates/uc-daemon/src/api/server.rs'
      provides: 'Error mapper using imported error code constants'
      contains: 'use uc_core::network::daemon_api_strings'
    - path: 'src-tauri/crates/uc-daemon/src/api/routes.rs'
      provides: 'Router using imported HTTP path constants and pairing stage constants'
      contains: 'use uc_core::network::daemon_api_strings'
  key_links:
    - from: 'src-tauri/crates/uc-daemon/src/api/ws.rs'
      to: 'uc-core daemon_api_strings'
      via: 'use uc_core::network::daemon_api_strings::{ws_topic, ws_event}'
      pattern: 'ws_topic::|ws_event::'
    - from: 'src-tauri/crates/uc-daemon/src/pairing/host.rs'
      to: 'uc-core daemon_api_strings'
      via: 'use uc_core::network::daemon_api_strings::{ws_topic, ws_event, pairing_stage, pairing_busy_reason}'
      pattern: 'pairing_stage::|pairing_busy_reason::'
---

<objective>
Replace all hardcoded string literals in the uc-daemon crate (server side) with imports from the shared uc-core daemon_api_strings constants module.

Purpose: Eliminate the server-side half of the duplicated string problem. After this plan, uc-daemon no longer defines its own WS topic/event constants and instead imports them from uc-core.

Output: Four modified uc-daemon source files with zero remaining hardcoded daemon wire-protocol strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-RESEARCH.md
@.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-01-SUMMARY.md
@src-tauri/crates/uc-core/src/network/daemon_api_strings.rs

<interfaces>
<!-- Constants created by Plan 01 that this plan imports -->
From src-tauri/crates/uc-core/src/network/daemon_api_strings.rs:

pub mod ws_topic {
pub const STATUS: &str = "status";
pub const PEERS: &str = "peers";
pub const PAIRED_DEVICES: &str = "paired-devices";
pub const PAIRING: &str = "pairing";
pub const PAIRING_SESSION: &str = "pairing/session";
pub const PAIRING_VERIFICATION: &str = "pairing/verification";
pub const SETUP: &str = "setup";
pub const SPACE_ACCESS: &str = "space-access";
}

pub mod ws_event { /_ 17 event type constants _/ }
pub mod pairing_stage { REQUEST, VERIFICATION, VERIFYING, COMPLETE, FAILED }
pub mod pairing_busy_reason { HOST_NOT_DISCOVERABLE, NO_LOCAL_PAIRING_PARTICIPANT_READY, BUSY }
pub mod pairing_error_code { ACTIVE_SESSION_EXISTS, HOST_NOT_DISCOVERABLE, NO_LOCAL_PARTICIPANT, SESSION_NOT_FOUND, INTERNAL }
pub mod http_path { /_ 22 path constants _/ }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded strings in ws.rs and server.rs with uc-core constants</name>
  <files>src-tauri/crates/uc-daemon/src/api/ws.rs, src-tauri/crates/uc-daemon/src/api/server.rs</files>
  <read_first>
    src-tauri/crates/uc-daemon/src/api/ws.rs
    src-tauri/crates/uc-daemon/src/api/server.rs
    src-tauri/crates/uc-core/src/network/daemon_api_strings.rs
  </read_first>
  <action>
**ws.rs changes:**

1. Add import: `use uc_core::network::daemon_api_strings::{ws_topic, ws_event};`

2. Delete the 8 module-level `const TOPIC_*` definitions (lines 26-33) and replace all usages:
   - `TOPIC_STATUS` -> `ws_topic::STATUS`
   - `TOPIC_PEERS` -> `ws_topic::PEERS`
   - `TOPIC_PAIRED_DEVICES` -> `ws_topic::PAIRED_DEVICES`
   - `TOPIC_PAIRING` -> `ws_topic::PAIRING`
   - `TOPIC_PAIRING_SESSION` -> `ws_topic::PAIRING_SESSION`
   - `TOPIC_PAIRING_VERIFICATION` -> `ws_topic::PAIRING_VERIFICATION`
   - `TOPIC_SETUP` -> `ws_topic::SETUP`
   - `TOPIC_SPACE_ACCESS` -> `ws_topic::SPACE_ACCESS` (this was `pub const` — keep the re-export if other files use it, or replace those files' imports too)

3. Delete the `pub const SPACE_ACCESS_SNAPSHOT_EVENT` definition and replace usages with `ws_event::SPACE_ACCESS_SNAPSHOT`.

4. Delete the 13 module-level `const *_EVENT` definitions (lines 37-50) and replace all usages:
   - `STATUS_SNAPSHOT_EVENT` -> `ws_event::STATUS_SNAPSHOT`
   - `PEERS_SNAPSHOT_EVENT` -> `ws_event::PEERS_SNAPSHOT`
   - `PAIRED_DEVICES_SNAPSHOT_EVENT` -> `ws_event::PAIRED_DEVICES_SNAPSHOT`
   - `PAIRING_SNAPSHOT_EVENT` -> `ws_event::PAIRING_SNAPSHOT`
   - `STATUS_UPDATED_EVENT` -> `ws_event::STATUS_UPDATED`
   - `PEERS_CHANGED_EVENT` -> `ws_event::PEERS_CHANGED`
   - `PEERS_NAME_UPDATED_EVENT` -> `ws_event::PEERS_NAME_UPDATED`
   - `PEERS_CONNECTION_CHANGED_EVENT` -> `ws_event::PEERS_CONNECTION_CHANGED`
   - `PAIRED_DEVICES_CHANGED_EVENT` -> `ws_event::PAIRED_DEVICES_CHANGED`
   - `PAIRING_UPDATED_EVENT` -> `ws_event::PAIRING_UPDATED`
   - `PAIRING_VERIFICATION_REQUIRED_EVENT` -> `ws_event::PAIRING_VERIFICATION_REQUIRED`
   - `PAIRING_COMPLETE_EVENT` -> `ws_event::PAIRING_COMPLETE`
   - `PAIRING_FAILED_EVENT` -> `ws_event::PAIRING_FAILED`

5. Check if `TOPIC_SPACE_ACCESS` or `SPACE_ACCESS_SNAPSHOT_EVENT` are used from other files (grep for `ws::TOPIC_SPACE_ACCESS` and `ws::SPACE_ACCESS_SNAPSHOT_EVENT`). If so, update those imports to use `uc_core::network::daemon_api_strings::ws_topic::SPACE_ACCESS` and `ws_event::SPACE_ACCESS_SNAPSHOT` directly.

**server.rs changes:**

1. Add import: `use uc_core::network::daemon_api_strings::pairing_error_code;`

2. In `map_daemon_pairing_error()`, replace all inline error code strings:
   - `"active_session_exists".to_string()` -> `pairing_error_code::ACTIVE_SESSION_EXISTS.to_string()`
   - `"host_not_discoverable".to_string()` -> `pairing_error_code::HOST_NOT_DISCOVERABLE.to_string()`
   - `"no_local_participant".to_string()` -> `pairing_error_code::NO_LOCAL_PARTICIPANT.to_string()`
   - `"session_not_found".to_string()` -> `pairing_error_code::SESSION_NOT_FOUND.to_string()`
   - `"internal".to_string()` -> `pairing_error_code::INTERNAL.to_string()`

3. Run `cd src-tauri && cargo check -p uc-daemon` to verify compilation.
   </action>
   <verify>
   <automated>cd /Volumes/ExternalSSD/myprojects/uniclipboard-desktop/src-tauri && cargo check -p uc-daemon 2>&1 | tail -10</automated>
   </verify>
   <acceptance_criteria> - ws.rs contains `use uc_core::network::daemon_api_strings` - ws.rs does NOT contain `const TOPIC_STATUS: &str` - ws.rs does NOT contain `const STATUS_SNAPSHOT_EVENT: &str` - ws.rs contains `ws_topic::STATUS` and `ws_event::STATUS_SNAPSHOT` - server.rs contains `use uc_core::network::daemon_api_strings::pairing_error_code` - server.rs does NOT contain `"active_session_exists".to_string()` - server.rs contains `pairing_error_code::ACTIVE_SESSION_EXISTS.to_string()` - `cargo check -p uc-daemon` exits 0
   </acceptance_criteria>
   <done>ws.rs and server.rs have zero hardcoded daemon wire-protocol string literals — all use uc-core constants.</done>
   </task>

<task type="auto">
  <name>Task 2: Replace hardcoded strings in host.rs and routes.rs with uc-core constants</name>
  <files>src-tauri/crates/uc-daemon/src/pairing/host.rs, src-tauri/crates/uc-daemon/src/api/routes.rs</files>
  <read_first>
    src-tauri/crates/uc-daemon/src/pairing/host.rs
    src-tauri/crates/uc-daemon/src/api/routes.rs
    src-tauri/crates/uc-core/src/network/daemon_api_strings.rs
  </read_first>
  <action>
**host.rs changes:**

1. Add import: `use uc_core::network::daemon_api_strings::{ws_topic, ws_event, pairing_stage, pairing_busy_reason};`

2. Replace ALL pairing stage string literals (research identifies ~20 occurrences across lines 237-969):
   - `"request"` (used as stage label) -> `pairing_stage::REQUEST`
   - `"verification"` (used as stage label) -> `pairing_stage::VERIFICATION`
   - `"verifying"` (used as stage label) -> `pairing_stage::VERIFYING`
   - `"complete"` (used as stage label) -> `pairing_stage::COMPLETE`
   - `"failed"` (used as stage label) -> `pairing_stage::FAILED`

   IMPORTANT: Only replace stage label usages — NOT log messages, error strings, or other unrelated uses of these words. The targets are arguments to `upsert_pairing_snapshot()`, `emit_pairing_session_changed()`, `emit_pairing_verification()`, `emit_pairing_failure()`, and state label fields.

3. Replace ALL WS topic/event inline strings in emit helper functions:
   - `emit_pairing_session_changed()`: `"pairing/session"` -> `ws_topic::PAIRING_SESSION`, `"pairing.updated"` -> `ws_event::PAIRING_UPDATED`
   - `emit_pairing_verification()`: `"pairing/verification"` -> `ws_topic::PAIRING_VERIFICATION`, `"pairing.verification_required"` -> `ws_event::PAIRING_VERIFICATION_REQUIRED`
   - `emit_pairing_failure()`: `"pairing/verification"` -> `ws_topic::PAIRING_VERIFICATION`, `"pairing.failed"` -> `ws_event::PAIRING_FAILED`
   - `broadcast_space_access_state_changed()`: `"space-access"` -> `ws_topic::SPACE_ACCESS`, `"space_access.state_changed"` -> `ws_event::SPACE_ACCESS_STATE_CHANGED`
   - All inline `emit_ws_event()` calls at lines ~824-827, ~856-858, ~941-945, ~1154-1158: replace topic and event_type string arguments with constants

4. Replace ALL busy reason string literals (research identifies 5 occurrences):
   - In `ensure_inbound_admitted()`: `"host_not_discoverable"` -> `pairing_busy_reason::HOST_NOT_DISCOVERABLE`, `"no_local_pairing_participant_ready"` -> `pairing_busy_reason::NO_LOCAL_PAIRING_PARTICIPANT_READY`
   - In `handle_pairing_message()`: same replacements plus `"busy"` -> `pairing_busy_reason::BUSY`

**routes.rs changes:**

1. Add import: `use uc_core::network::daemon_api_strings::{http_path, pairing_stage};`

2. In `router()` function, replace all 22+ inline route path strings with `http_path::*` constants. For dynamic-segment routes (`/pairing/sessions/:session_id`, `/pairing/sessions/:session_id/accept`, etc.), keep the `format!()` or inline string since they contain dynamic segments that cannot be expressed as simple constants.

3. In `acknowledged()` calls throughout routes.rs, replace stage label strings:
   - `"request"` -> `pairing_stage::REQUEST`
   - `"verifying"` -> `pairing_stage::VERIFYING`
   - `"failed"` -> `pairing_stage::FAILED`
   - `"participant_updated"` — leave as-is (not a pairing stage)

4. Run `cd src-tauri && cargo test -p uc-daemon` to verify all tests pass.
   </action>
   <verify>
   <automated>cd /Volumes/ExternalSSD/myprojects/uniclipboard-desktop/src-tauri && cargo test -p uc-daemon 2>&1 | tail -20</automated>
   </verify>
   <acceptance_criteria> - host.rs contains `use uc_core::network::daemon_api_strings` - host.rs does NOT contain the literal string `"request"` as a pairing stage argument (grep: `upsert_pairing_snapshot.*"request"` returns 0 matches) - host.rs contains `pairing_stage::REQUEST` instead - host.rs does NOT contain `"pairing/session"` as a string literal (grep returns 0 matches) - host.rs does NOT contain `"host_not_discoverable"` as a string literal - routes.rs contains `use uc_core::network::daemon_api_strings` - routes.rs contains `http_path::HEALTH` instead of `"/health"` - `cargo test -p uc-daemon` exits 0
   </acceptance_criteria>
   <done>host.rs and routes.rs have zero hardcoded daemon wire-protocol string literals — all pairing stages, WS topics/events, busy reasons, and HTTP paths use uc-core constants.</done>
   </task>

</tasks>

<verification>
- `cd src-tauri && cargo test -p uc-daemon` passes
- grep for remaining hardcoded strings in uc-daemon/src/:
  - `grep -rn '"pairing/session"' src-tauri/crates/uc-daemon/src/` returns 0 results
  - `grep -rn '"pairing.updated"' src-tauri/crates/uc-daemon/src/` returns 0 results
  - `grep -rn '"host_not_discoverable"' src-tauri/crates/uc-daemon/src/` returns 0 results
  - `grep -rn '"active_session_exists"' src-tauri/crates/uc-daemon/src/` returns 0 results
</verification>

<success_criteria>

- All four uc-daemon source files import from uc_core::network::daemon_api_strings
- Zero remaining hardcoded daemon wire-protocol string literals in uc-daemon
- All existing uc-daemon tests pass unchanged
  </success_criteria>

<output>
After completion, create `.planning/phases/56.1-eliminate-hardcoded-strings-in-pairing-setup-flow/56.1-02-SUMMARY.md`
</output>
