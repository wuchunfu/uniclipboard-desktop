---
phase: 46.5
slug: tauri-daemon
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-22
---

# Phase 46.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property               | Value                                                                                                                                              |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Framework**          | Rust `cargo test` per crate + frontend `vitest`                                                                                                    |
| **Config file**        | Cargo default crate test conventions; frontend uses `package.json` `test` script                                                                   |
| **Quick run command**  | `cd src-tauri && cargo test -p uc-platform pairing_runtime -- --test-threads=1 && cargo test -p uc-tauri daemon_command_shell -- --test-threads=1` |
| **Full suite command** | `cd src-tauri && cargo test --workspace && cd .. && bun test`                                                                                      |
| **Estimated runtime**  | ~120 seconds                                                                                                                                       |

---

## Sampling Rate

- **After every task commit:** Run the smallest matching targeted suite from the verification map; default quick gate is `cd src-tauri && cargo test -p uc-platform pairing_runtime -- --test-threads=1 && cargo test -p uc-tauri daemon_command_shell -- --test-threads=1`
- **After every plan wave:** Run `cd src-tauri && cargo test --workspace && cd .. && bun test`
- **Before `$gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID    | Plan | Wave | Requirement       | Test Type                       | Automated Command                                                                                                                                     | File Exists | Status     |
| ---------- | ---- | ---- | ----------------- | ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------- |
| 46.5-01-01 | 01   | 0    | R46.2-1, R46.2-2  | unit/integration                | `cd src-tauri && cargo test -p uc-platform pairing_runtime -- --test-threads=1`                                                                       | ❌ W0       | ⬜ pending |
| 46.5-02-01 | 02   | 0    | R46.2-4           | unit                            | `cd src-tauri && cargo test -p uc-tauri daemon_command_shell -x`                                                                                      | ❌ W0       | ⬜ pending |
| 46.5-03-01 | 03   | 1    | R46.2-7, PH461-06 | integration + frontend contract | `cd src-tauri && cargo test -p uc-tauri daemon_ws_bridge -- --test-threads=1 && cd .. && bun test -- src/api/__tests__/p2p-realtime-contract.test.ts` | ✅          | ⬜ pending |
| 46.5-03-02 | 03   | 1    | PH46-05, R46.2-8  | integration                     | `cd src-tauri && cargo test -p uc-daemon --test pairing_host --test pairing_api --test setup_api -x`                                                  | ✅          | ⬜ pending |

_Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky_

---

## Wave 0 Requirements

- [ ] `src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs` — add tests proving GUI ownership disables pairing runtime creation, accept loop startup, and pairing protocol registration
- [ ] `src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs` — add daemon-only routing and error-mapping tests for pairing/setup command shells
- [ ] `src-tauri/crates/uc-tauri/src/daemon_client/` — add or extract setup query/action client coverage with decode tests
- [ ] `src-tauri/crates/uc-daemon/tests/pairing_api.rs` — extend unpair route coverage if planner includes daemon-side unpair in phase scope

---

## Manual-Only Verifications

| Behavior                                                                        | Requirement               | Why Manual                                   | Test Instructions                                                                                                                                |
| ------------------------------------------------------------------------------- | ------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| PeerB 发起 join 后，PeerA 提示只来自 daemon realtime 而非 GUI 本地 pairing 事件 | PH46-05, R46.2-7, R46.2-8 | 需要双端交互确认运行时事件来源和用户可见行为 | 启动 daemon-backed GUI/CLI 双端，执行 join-space 流程，确认前端仅消费 `daemon://realtime` 事件且 GUI 日志不出现本地 pairing session started 痕迹 |
| GUI 关闭本地 pairing runtime 后，daemon 仍能完成 accept/reject/verify 全流程    | R46.2-1, R46.2-2, R46.2-8 | 需要端到端验证 ownership 切换后的整体行为    | 在两端执行完整 pairing + setup pairing 流，确认 GUI 不注册本地 pairing protocol，daemon 仍能完成配对与 setup 收尾                                |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
