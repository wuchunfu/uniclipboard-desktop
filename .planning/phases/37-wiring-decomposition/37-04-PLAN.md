---
phase: 37-wiring-decomposition
plan: 04
type: execute
wave: 4
depends_on:
  - 37-03
  - 37-UAT
files_modified:
  - src/components/PairingNotificationProvider.tsx
  - src/api/p2p.ts
  - src/components/__tests__/PairingDialog.test.tsx
autonomous: true
requirements:
  - RNTM-02
must_haves:
  truths:
    - 'peerA 在点击 Accept 后，即使 verification 事件立刻到达，也不会丢失当前 session'
    - 'p2p-pairing-verification(kind=verification) 到达后会稳定显示 PIN 弹窗'
    - '前端 session 过滤不再依赖异步 state -> ref 同步才能处理首个 verification 事件'
    - '新增前端回归测试覆盖 request -> accept -> immediate verification 时序'
  artifacts:
    - path: 'src/components/PairingNotificationProvider.tsx'
      provides: '同步建立 active session，消除 accept 后 verification 事件丢失竞态'
      contains: 'activeSessionIdRef'
    - path: 'src/components/__tests__/PairingDialog.test.tsx'
      provides: '覆盖 accept 后立刻收到 verification 的 UI 回归测试'
  key_links:
    - from: 'src/components/PairingNotificationProvider.tsx'
      to: 'src/api/p2p.ts'
      via: 'acceptP2PPairing + onP2PPairingVerification'
      pattern: 'acceptP2PPairing'
---

<objective>
修复 peerA 侧 PairingNotificationProvider 的 accept->verification 竞态，确保点击 Accept 后立即到达的 verification 事件不会被 session guard 丢弃，并补齐前端时序回归测试。

Purpose: 关闭 UAT Test 3 的主根因，恢复 PIN 确认弹窗显示能力。
Output: 稳定显示 verification PIN 弹窗的前端实现 + 对应测试。
</objective>

<context>
@.planning/phases/37-wiring-decomposition/37-UAT.md
@src/components/PairingNotificationProvider.tsx
@src/api/p2p.ts
@src/components/PairingPinDialog.tsx
@src/components/__tests__/PairingDialog.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 同步建立 active session，消除 accept 时序竞态</name>
  <files>
    src/components/PairingNotificationProvider.tsx
  </files>
  <action>
在 Accept 按钮回调里，不要只依赖 `setActiveSessionId(event.sessionId)` 等待下一轮 render/useEffect 再更新 `activeSessionIdRef.current`。

要求：

- 在调用 `acceptP2PPairing(event.sessionId)` 之前，同步写入当前 session ref
- 保持现有 session 过滤语义，避免其他 session 的 verification/failed/completed 污染当前弹窗
- 出错回滚时同时清理 state 和 ref
- 只改前端通知链路，不改后端 pairing/setup 行为
  </action>
  <done> - Accept 回调在发起 acceptP2PPairing 前已同步建立当前 session，首个 verification 不再依赖下一轮 useEffect 才能匹配 - accept 失败回滚会同时清理 active session 的 state 与 ref，不留下脏 session - verification 事件到达后，PIN 弹窗可稳定进入 display/verifying/success 或 failed 分支
  </done>
  <verify>
  <command>npm test -- PairingDialog</command>
  </verify>
  </task>

<task type="auto">
  <name>Task 2: 新增 accept 后 immediate verification 回归测试</name>
  <files>
    src/components/__tests__/PairingDialog.test.tsx
  </files>
  <action>
补一条时序测试，覆盖：
1. 前端收到 `request`
2. 用户点击 Accept
3. 后端立即推送同 session 的 `verification`
4. PIN 弹窗出现，且 session 不被误判为无效

如果现有测试文件不适合承载该 provider 级别测试，可在同目录新增更贴近 PairingNotificationProvider 的测试，但必须保持单一意图。
</action>
<done> - 新增测试能稳定复现 request -> 用户 Accept -> immediate verification 的时序 - 测试断言 verification 不会被 session guard 丢弃，且会显示 PIN 弹窗 - 测试覆盖 accept 失败或 cleanup 后不会遗留错误 session
</done>
<verify>
<command>npm test -- PairingDialog</command>
</verify>
</task>

</tasks>
