---
phase: 26-implement-global-sync-master-toggle-and-improve-sync-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/crates/uc-app/src/usecases/clipboard/sync_outbound.rs
  - src/i18n/locales/en-US.json
  - src/i18n/locales/zh-CN.json
  - src/components/setting/SyncSection.tsx
autonomous: true
requirements:
  - GSYNC-01
  - GSYNC-02
  - GSYNC-05

must_haves:
  truths:
    - 'Global auto_sync=false causes apply_sync_policy to return empty peer list before any per-device evaluation'
    - 'Per-device settings in storage are never modified by the global toggle'
    - 'Auto sync toggle description in Settings displays master switch copy in both EN and ZH'
    - 'New i18n keys for banner text and navigation link exist in both locale files'
  artifacts:
    - path: 'src-tauri/crates/uc-app/src/usecases/clipboard/sync_outbound.rs'
      provides: 'Global auto_sync early-return guard in apply_sync_policy'
      contains: 'Global auto_sync disabled'
    - path: 'src/i18n/locales/en-US.json'
      provides: 'EN locale with syncPaused banner keys and updated autoSync description'
      contains: 'syncPaused'
    - path: 'src/i18n/locales/zh-CN.json'
      provides: 'ZH locale with syncPaused banner keys and updated autoSync description'
      contains: 'syncPaused'
    - path: 'src/components/setting/SyncSection.tsx'
      provides: 'SyncSection with i18n-driven auto sync description'
  key_links:
    - from: 'src-tauri/crates/uc-app/src/usecases/clipboard/sync_outbound.rs'
      to: 'global_settings.sync.auto_sync'
      via: 'early return before per-device loop'
      pattern: "!gs\\.sync\\.auto_sync"
---

<objective>
Add global auto_sync master switch enforcement in the backend sync engine and prepare all i18n strings for the frontend UI changes.

Purpose: The backend guard ensures no outbound sync occurs when global auto_sync is off, regardless of per-device settings. The i18n keys and description copy update provide the foundation for the frontend banner and cascade disable UI in Plan 02.

Output: Modified sync_outbound.rs with global guard, updated locale files with new keys, updated SyncSection description.
</objective>

<execution_context>
@/home/wuy6/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuy6/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-implement-global-sync-master-toggle-and-improve-sync-ux/26-CONTEXT.md
@.planning/phases/26-implement-global-sync-master-toggle-and-improve-sync-ux/26-RESEARCH.md

@src-tauri/crates/uc-app/src/usecases/clipboard/sync_outbound.rs
@src/i18n/locales/en-US.json
@src/i18n/locales/zh-CN.json
@src/components/setting/SyncSection.tsx

<interfaces>
From src-tauri/crates/uc-app/src/usecases/clipboard/sync_outbound.rs (lines 60-121):
```rust
async fn apply_sync_policy(
    &self,
    peers: &[uc_core::network::DiscoveredPeer],
    snapshot: &SystemClipboardSnapshot,
) -> Vec<uc_core::network::DiscoveredPeer>
// Line 67-76: loads global_settings via self.settings.load()
// Line 79: classify_snapshot(snapshot)
// Line 82-119: per-device peer loop
```

From src/components/setting/SyncSection.tsx:

- Uses `useTranslation()` hook with `t()` function
- Description key: `settings.sections.sync.autoSync.description`
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add global auto_sync guard in apply_sync_policy</name>
  <files>src-tauri/crates/uc-app/src/usecases/clipboard/sync_outbound.rs</files>
  <action>
In the `apply_sync_policy` method, after the `global_settings` load block (after line 76, before the `classify_snapshot` call at line 79), add an early-return guard:

```rust
// Global master toggle: if auto_sync is off, skip ALL outbound sync
if let Some(ref gs) = global_settings {
    if !gs.sync.auto_sync {
        info!("Global auto_sync disabled; returning empty peer list");
        return vec![];
    }
}
```

This must be BEFORE the `classify_snapshot` call and the per-device loop. This ensures:

- When global auto_sync is false, no sync happens to any device (hard override per user decision)
- Per-device settings are never touched or modified (they stay in storage unchanged)
- Settings load failure fallback is preserved (if global_settings is None, all peers proceed)
- When global is re-enabled, per-device filtering resumes as before (no code change needed -- the guard simply stops short-circuiting)

Do NOT modify any per-device settings storage when global toggle changes. The global toggle is an overlay/override, not an erase.
</action>
<verify>
<automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo check -p uc-app 2>&1 | tail -5</automated>
</verify>
<done>apply_sync_policy returns empty vec when global auto_sync is false, before any per-device evaluation. Cargo check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n keys and update auto sync description</name>
  <files>src/i18n/locales/en-US.json, src/i18n/locales/zh-CN.json, src/components/setting/SyncSection.tsx</files>
  <action>
1. In `en-US.json`, update the existing `settings.sections.sync.autoSync.description` value to:
   "Control clipboard sync across all devices. When disabled, no content will be synced to any device."

2. In `en-US.json`, add a `syncPaused` object under the `devices` key (sibling to `settings`, `list`, etc.):

   ```json
   "syncPaused": {
     "message": "Clipboard sync is paused. All devices are currently not syncing.",
     "goToSettings": "Go to Settings"
   }
   ```

3. In `zh-CN.json`, update the existing `settings.sections.sync.autoSync.description` value to:
   "控制所有设备的剪贴板同步。关闭后停止向所有设备同步内容。"

4. In `zh-CN.json`, add the matching `syncPaused` object under the `devices` key:

   ```json
   "syncPaused": {
     "message": "剪贴板同步已暂停，所有设备当前均不会同步内容。",
     "goToSettings": "前往设置"
   }
   ```

5. In `SyncSection.tsx`, no code changes needed -- the component already uses `t('settings.sections.sync.autoSync.description')` which will pick up the updated string automatically.

Verify the JSON is valid after editing (no trailing commas, proper nesting).
</action>
<verify>
<automated>cd /home/wuy6/myprojects/UniClipboard && node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en-US.json','utf8')); JSON.parse(require('fs').readFileSync('src/i18n/locales/zh-CN.json','utf8')); console.log('Both locale files are valid JSON')"</automated>
</verify>
<done>Both locale files contain updated autoSync description and new syncPaused keys. JSON is valid. EN and ZH strings match the user-specified copy exactly.</done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo check -p uc-app` passes (backend guard compiles)
- Both locale JSON files parse without error
- `settings.sections.sync.autoSync.description` in en-US.json matches "Control clipboard sync across all devices. When disabled, no content will be synced to any device."
- `devices.syncPaused.message` exists in both locale files
</verification>

<success_criteria>

1. Backend apply_sync_policy short-circuits with empty vec when global auto_sync is false
2. Per-device settings storage is never modified by this change
3. Both locale files contain new syncPaused keys and updated autoSync description
4. All strings match the exact copy specified in CONTEXT.md
   </success_criteria>

<output>
After completion, create `.planning/phases/26-implement-global-sync-master-toggle-and-improve-sync-ux/26-01-SUMMARY.md`
</output>
