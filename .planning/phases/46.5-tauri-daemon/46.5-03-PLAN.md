---
phase: 46.5-tauri-daemon
plan: 03
type: execute
wave: 3
depends_on: ['46.5-01', '46.5-04']
files_modified:
  - src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
  - src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs
  - src-tauri/crates/uc-daemon/tests/pairing_api.rs
  - src-tauri/crates/uc-daemon/tests/setup_api.rs
  - src/api/p2p.ts
  - src/api/__tests__/setup.test.ts
  - src/api/__tests__/p2p-realtime-contract.test.ts
  - src/pages/__tests__/setup-peer-discovery-polling.test.tsx
autonomous: true
requirements: ['PH46-05', 'PH461-06', 'R46.2-7', 'R46.2-8']
user_setup: []
must_haves:
  truths:
    - 'GUI runtime 中不再保留任何“忽略本地 pairing 事件”的兜底路径，daemon realtime 是唯一前端 pairing 事件源。'
    - 'daemon pairing/setup API 回归覆盖包含 unpair、host confirm、join passphrase 等关键路径。'
    - '前端对 `daemon://realtime`、原有 setup command 名、join-flow 进展、以及 legacy `p2p-*` listener 删除都被回归测试锁定。'
  artifacts:
    - path: 'src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs'
      provides: '移除本地 pairing fallback 痕迹后的 GUI wiring。'
      min_lines: 1100
    - path: 'src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs'
      provides: 'daemon realtime 仍是唯一 pairing topic 入口的桥接回归。'
      min_lines: 380
    - path: 'src-tauri/crates/uc-daemon/tests/pairing_api.rs'
      provides: 'daemon-owned pairing API 包含 unpair 路径回归。'
      min_lines: 220
    - path: 'src-tauri/crates/uc-daemon/tests/setup_api.rs'
      provides: 'daemon-owned setup API 在 GUI local pairing runtime 关闭后仍可完成 host/join 关键转换。'
      min_lines: 260
    - path: 'src/api/setup.ts'
      provides: '前端 setup command 名与参数契约回归。'
      min_lines: 60
    - path: 'src/api/p2p.ts'
      provides: '前端移除 legacy `p2p-*` listener，仅保留 daemon realtime 契约。'
      min_lines: 120
  key_links:
    - from: 'src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs'
      to: 'src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs'
      via: 'GUI wiring 不再声明本地 pairing fallback，pairing topic 唯一实时入口仍由 DaemonWsBridge 覆盖。'
      pattern: 'daemon://realtime|DaemonWsBridge|PairingMessageReceived|PairingFailed'
    - from: 'src-tauri/crates/uc-daemon/tests/pairing_api.rs'
      to: 'src-tauri/crates/uc-daemon/src/api/routes.rs'
      via: 'pairing API 回归直接锁定 `/pairing/unpair` 和现有 session mutation surface。'
      pattern: '/pairing/unpair|/pairing/sessions'
    - from: 'src/api/p2p.ts'
      to: 'src/api/__tests__/p2p-realtime-contract.test.ts'
      via: '前端删除 legacy `p2p-*` listener，只保留 `daemon://realtime` 订阅和 typed command return path。'
      pattern: 'p2p-command-error|daemon://realtime|pairing.verificationRequired'
---

<objective>
删除剩余的 GUI 本地 pairing fallback 痕迹和前端 legacy `p2p-*` listener，并用 daemon API、daemon realtime、frontend contract 三层回归证明 46.5 的结构性切口已闭环。

Purpose: 防止“代码看起来切了，运行时却还有本地 pairing 残留”这种假完成。
Output: wiring cleanup、daemon API regression、frontend contract regression、以及跨层 targeted suite。
</objective>

<execution_context>
Use the standard execute-phase workflow already configured for this repository.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/46.5-tauri-daemon/46.5-CONTEXT.md
@.planning/phases/46.5-tauri-daemon/46.5-RESEARCH.md
@.planning/phases/46.5-tauri-daemon/46.5-VALIDATION.md
@.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-05-SUMMARY.md
@.planning/phases/46.2-daemon-tauri-daemon/46.2-03-SUMMARY.md
@.planning/phases/46.4-daemon-setup-gui-cli-setup-cli-gui-cli-daemon-peera-full-mode-peerb-passive-mode-peerb-peera-peera-b-a-b-a-a-b-a-b-a-a-b/46.4-03-SUMMARY.md
@src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
@src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs
@src-tauri/crates/uc-daemon/tests/pairing_api.rs
@src-tauri/crates/uc-daemon/tests/setup_api.rs
@src/api/p2p.ts
@src/api/__tests__/setup.test.ts
@src/api/__tests__/p2p-realtime-contract.test.ts
@src/pages/__tests__/setup-peer-discovery-polling.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 删除 wiring 中的本地 pairing fallback 痕迹、移除前端 legacy `p2p-*` listener，并锁定 bridge/contract 契约</name>
  <files>
    src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs,
    src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs,
    src/api/p2p.ts,
    src/api/__tests__/setup.test.ts,
    src/api/__tests__/p2p-realtime-contract.test.ts,
    src/pages/__tests__/setup-peer-discovery-polling.test.tsx
  </files>
  <read_first>
    - src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
    - src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs
    - src/api/p2p.ts
    - src/api/__tests__/setup.test.ts
    - src/api/__tests__/p2p-realtime-contract.test.ts
    - src/pages/__tests__/setup-peer-discovery-polling.test.tsx
    - src/api/setup.ts
    - .planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-05-SUMMARY.md
    - .planning/phases/46.2-daemon-tauri-daemon/46.2-03-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `wiring.rs` 不再保留 `NetworkEvent::PairingMessageReceived` / `NetworkEvent::PairingFailed` 的“ignore local pairing event”分支或日志字符串。
    - Test 2: `daemon_ws_bridge` 回归继续证明 `daemon://realtime` 是唯一 pairing topic 入口。
    - Test 3: 前端 `p2p.ts` 不再注册 `p2p-command-error` 等 legacy `p2p-*` listener，`setup.ts` / `p2p.ts` 继续通过 typed command return + daemon realtime 推进 join-flow。
  </behavior>
  <action>
    1. 在 `src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs` 中删除当前针对
       - `NetworkEvent::PairingMessageReceived { .. }`
       - `NetworkEvent::PairingFailed { .. }`
       的 match 分支、debug 日志和注释。
       46.5 完成后，GUI runtime 不应再显式“忽略”这类事件，因为 GUI 根本不该再收到它们。
    2. 在 `src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs` 中补或收紧断言，固定 `DaemonWsBridge` 仍是唯一 pairing realtime owner，不引入第二条 websocket 或本地事件桥。
    3. 在 `src/api/p2p.ts` 中删除剩余 legacy `p2p-*` listener，尤其是 `p2p-command-error` 订阅与对应兼容分支；命令失败只通过 typed command return 处理，不再维持第二条前端事件通道。
    4. 在 `src/api/__tests__/setup.test.ts` 中把 setup API contract 覆盖扩展到全部 8 个 command：
       - `get_setup_state`
       - `start_new_space`
       - `start_join_space`
       - `select_device`
       - `submit_passphrase`
       - `verify_passphrase`
       - `confirm_peer_trust`
       - `cancel_setup`
    5. 在 `src/api/__tests__/p2p-realtime-contract.test.ts` 中继续锁定 `daemon://realtime` 是唯一监听名，并断言 pairing payload 使用 camelCase 键名 `sessionId`、`peerId`、`deviceName`、`code`，同时验证没有 `p2p-command-error` 监听注册。
    6. 在 `src/pages/__tests__/setup-peer-discovery-polling.test.tsx` 中保留 daemon realtime 驱动 join-flow 前进的回归，确保“选设备 -> ProcessingJoinSpace”仍成立。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-tauri daemon_ws_bridge -- --test-threads=1 && cd .. && bun run test -- src/api/__tests__/setup.test.ts src/api/__tests__/p2p-realtime-contract.test.ts src/pages/__tests__/setup-peer-discovery-polling.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg "PairingMessageReceived|PairingFailed|Ignoring local pairing network event; daemon owns pairing runtime" src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs -n` 返回 0 行。
    - `rg "p2p-command-error|listen\\(\"p2p-|listen\\('p2p-" src/api/p2p.ts -n` 返回 0 行。
    - `rg "daemon://realtime|pairing.verificationRequired|pairing.complete|pairing.failed" src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs src/api/__tests__/p2p-realtime-contract.test.ts -n` 返回 daemon realtime 契约断言。
    - `rg "get_setup_state|start_new_space|start_join_space|select_device|submit_passphrase|verify_passphrase|confirm_peer_trust|cancel_setup" src/api/__tests__/setup.test.ts -n` 返回 8 个 setup command 名断言。
    - `cd src-tauri && cargo test -p uc-tauri daemon_ws_bridge -- --test-threads=1 && cd .. && bun run test -- src/api/__tests__/setup.test.ts src/api/__tests__/p2p-realtime-contract.test.ts src/pages/__tests__/setup-peer-discovery-polling.test.tsx` 退出码为 0。
  </acceptance_criteria>
  <done>
    - GUI runtime 的本地 pairing fallback 痕迹被删除。
    - 前端 legacy `p2p-*` listener 被删除，contract 继续稳定地建立在 `daemon://realtime` 和既有 Tauri command 名之上。
  </done>
</task>

<task type="auto">
  <name>Task 2: 扩展 daemon pairing/setup API 回归，证明 cutover 后的全流程仍由 daemon 独占</name>
  <files>
    src-tauri/crates/uc-daemon/tests/pairing_api.rs,
    src-tauri/crates/uc-daemon/tests/setup_api.rs
  </files>
  <read_first>
    - src-tauri/crates/uc-daemon/tests/pairing_api.rs
    - src-tauri/crates/uc-daemon/tests/setup_api.rs
    - src-tauri/crates/uc-daemon/src/api/routes.rs
    - src-tauri/crates/uc-daemon/src/api/query.rs
    - src-tauri/crates/uc-daemon/src/pairing/host.rs
    - .planning/phases/46.4-daemon-setup-gui-cli-setup-cli-gui-cli-daemon-peera-full-mode-peerb-passive-mode-peerb-peera-peera-b-a-b-a-a-b-a-b-a-a-b/46.4-03-SUMMARY.md
  </read_first>
  <action>
    1. 在 `src-tauri/crates/uc-daemon/tests/pairing_api.rs` 中新增明确测试：
       - `pairing_unpair_route_unpairs_device_via_daemon_usecase`
       该测试必须对 `POST /pairing/unpair` 发请求，并断言 paired-device 状态由 daemon route 改变，而不是依赖 Tauri 本地 use case。
    2. 在 `src-tauri/crates/uc-daemon/tests/setup_api.rs` 中补一条 host/join 关键路径测试，固定断言：
       - `setup_confirm_peer` 在 host confirm 阶段委托给 `DaemonPairingHost::accept_pairing(...)`
       - `setup_submit_passphrase` 在 join passphrase 阶段委托给 daemon-owned `verify_passphrase(...)`
       - 测试 fixture 不依赖 GUI 本地 pairing runtime
    3. 保持现有 reset/repeatability fixture；不要为测试临时恢复 GUI 本地 pairing transport。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-daemon --test pairing_api pairing_unpair_route_unpairs_device_via_daemon_usecase -- --test-threads=1 && cargo test -p uc-daemon --test setup_api setup_submit_passphrase -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg "pairing_unpair_route_unpairs_device_via_daemon_usecase" src-tauri/crates/uc-daemon/tests/pairing_api.rs -n` 返回 unpair API 回归测试。
    - `rg "accept_pairing|verify_passphrase|setup_confirm_peer|setup_submit_passphrase" src-tauri/crates/uc-daemon/tests/setup_api.rs -n` 返回 daemon-owned setup path 断言。
    - `cd src-tauri && cargo test -p uc-daemon --test pairing_api pairing_unpair_route_unpairs_device_via_daemon_usecase -- --test-threads=1 && cargo test -p uc-daemon --test setup_api setup_submit_passphrase -- --test-threads=1` 退出码为 0。
  </acceptance_criteria>
  <done>
    - daemon API regression 证明 unpair 与 setup 关键路径都由 daemon 独占。
    - 46.5 的 cutover 不再依赖任何 GUI 本地 pairing runtime 残留。
  </done>
</task>

</tasks>

<verification>
`cd src-tauri && cargo test -p uc-daemon --test pairing_api pairing_unpair_route_unpairs_device_via_daemon_usecase -- --test-threads=1`
`cd src-tauri && cargo test -p uc-daemon --test setup_api setup_submit_passphrase -- --test-threads=1`
`cd src-tauri && cargo test -p uc-tauri daemon_ws_bridge -- --test-threads=1`
`cd .. && bun run test -- src/api/__tests__/setup.test.ts src/api/__tests__/p2p-realtime-contract.test.ts src/pages/__tests__/setup-peer-discovery-polling.test.tsx`
</verification>

<success_criteria>
代码和回归测试都证明：GUI 不再拥有本地 pairing runtime，不再保留本地 pairing fallback；daemon HTTP + daemon realtime 成为 pairing/setup 的唯一真实来源，而前端 command/realtime contract 仍保持不变。
</success_criteria>

<output>
完成后创建 `.planning/phases/46.5-tauri-daemon/46.5-03-SUMMARY.md`，总结 wiring cleanup、daemon API regression 与 frontend contract regression 的最终形态。
</output>
