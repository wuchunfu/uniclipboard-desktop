---
phase: 32.1-inbound-file-sync-clipboard-integration
plan: 03
subsystem: clipboard-sync
tags: [file-sync, clipboard-race, gap-closure]
dependency_graph:
  requires: [32.1-01]
  provides: [FCLIP-03-race-detection]
  affects: [wiring]
tech_stack:
  patterns: [consume-before-set origin guard]
key_files:
  modified:
    - src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
decisions:
  - Pre-write race check uses consume_origin_or_default(LocalCapture) to detect if clipboard was touched during transfer
metrics:
  duration: 1min
  completed: '2026-03-14T01:20:22Z'
---

# Phase 32.1 Plan 03: Clipboard Race Detection Gap Closure Summary

Race detection guard in write_file_to_clipboard_after_transfer using consume_origin_or_default to skip auto-write when user copies during file transfer.

## What Was Done

### Task 1: Add clipboard race detection before auto-write in wiring.rs

**Commit:** `8da20c69`

Added FCLIP-03 race detection between entry persistence and clipboard write in `write_file_to_clipboard_after_transfer`:

1. After persisting the clipboard entry (always unconditional), call `consume_origin_or_default(LocalCapture)`
2. If returned origin is NOT `LocalCapture`, another operation set an origin during transfer -- skip clipboard write
3. If no race detected, proceed with existing `set_next_origin(RemotePush)` + `write_snapshot` flow
4. Updated doc comment to accurately describe the race detection behavior

**Key behavior preserved:** Entry persistence via `execute_with_origin(RemotePush)` always runs before the race check, so file entries appear in Dashboard history regardless of race outcome.

## Deviations from Plan

None - plan executed exactly as written.

## Verification

- `cargo check --package uc-tauri` passes
- `consume_origin_or_default` call appears at line 2079, before `set_next_origin` at line 2092
- Entry persistence (`execute_with_origin`) at line 2057 remains before race check

## Self-Check: PASSED
