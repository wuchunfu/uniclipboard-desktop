# Deferred Items

- 2026-03-06: `cargo check -p uc-tauri` blocked by pre-existing compile errors in `src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs` (`self` used outside method, plus type inference failure around `transfer_decryptor`). Out of scope for plan 10-01 file set.
