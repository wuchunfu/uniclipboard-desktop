---
phase: 10-boundary-repair-baseline
plan: 03
type: execute
wave: 2
depends_on:
  - '10-01'
files_modified:
  - src-tauri/crates/uc-core/src/ports/mod.rs
  - src-tauri/crates/uc-core/src/ports/autostart.rs
  - src-tauri/crates/uc-core/src/ports/ui_port.rs
  - src-tauri/crates/uc-core/src/ports/app_dirs.rs
  - src-tauri/crates/uc-core/src/ports/watcher_control.rs
  - src-tauri/crates/uc-core/src/ports/identity_store.rs
  - src-tauri/crates/uc-core/src/ports/observability.rs
  - src-tauri/crates/uc-platform/src/ports/mod.rs
  - src-tauri/crates/uc-platform/src/ports/autostart.rs
  - src-tauri/crates/uc-platform/src/ports/ui_port.rs
  - src-tauri/crates/uc-platform/src/ports/app_dirs.rs
  - src-tauri/crates/uc-platform/src/ports/watcher_control.rs
  - src-tauri/crates/uc-platform/src/ports/identity_store.rs
  - src-tauri/crates/uc-platform/src/ports/observability.rs
  - src-tauri/crates/uc-app/src/deps.rs
  - src-tauri/crates/uc-app/src/lib.rs
  - src-tauri/crates/uc-app/src/usecases/settings/apply_autostart.rs
  - src-tauri/crates/uc-app/src/usecases/start_clipboard_watcher.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
autonomous: true
requirements:
  - BOUND-04

must_haves:
  truths:
    - 'uc-core/src/ports/ contains only domain-relevant port contracts'
    - 'Non-domain ports (AutostartPort, UiPort, AppDirsPort, WatcherControlPort, IdentityStorePort, observability) live in uc-platform/src/ports/'
    - 'Use cases that depend on evicted ports are relocated to uc-platform or their imports updated'
    - 'AppDeps no longer imports evicted ports from uc-core (fields removed or re-imported from uc-platform)'
  artifacts:
    - path: 'src-tauri/crates/uc-platform/src/ports/autostart.rs'
      provides: 'AutostartPort trait definition'
    - path: 'src-tauri/crates/uc-platform/src/ports/ui_port.rs'
      provides: 'UiPort trait definition'
    - path: 'src-tauri/crates/uc-platform/src/ports/app_dirs.rs'
      provides: 'AppDirsPort trait definition'
    - path: 'src-tauri/crates/uc-platform/src/ports/watcher_control.rs'
      provides: 'WatcherControlPort trait definition'
    - path: 'src-tauri/crates/uc-platform/src/ports/identity_store.rs'
      provides: 'IdentityStorePort trait definition'
    - path: 'src-tauri/crates/uc-core/src/ports/mod.rs'
      provides: 'Only domain ports remain'
  key_links:
    - from: 'src-tauri/crates/uc-platform/src/adapters/'
      to: 'src-tauri/crates/uc-platform/src/ports/'
      via: 'Local crate imports for evicted ports'
      pattern: 'use crate::ports::'
    - from: 'src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs'
      to: 'src-tauri/crates/uc-platform/src/ports/'
      via: 'Imports evicted ports from uc-platform for bootstrap wiring'
      pattern: 'use uc_platform::ports::'
---

<objective>
Move non-domain ports out of uc-core into uc-platform and update all dependents.

Purpose: Enforce BOUND-04 so uc-core remains focused on domain contracts. Non-domain ports (platform-facing interface contracts) belong in uc-platform. Use cases that depend on evicted ports are relocated to maintain proper dependency direction.

Output: 6 port modules moved from uc-core to uc-platform, affected use cases relocated, AppDeps updated, all imports fixed.
</objective>

<execution_context>
@/home/wuy6/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuy6/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-boundary-repair-baseline/10-CONTEXT.md
@.planning/phases/10-boundary-repair-baseline/10-RESEARCH.md
@.planning/phases/10-boundary-repair-baseline/10-01-SUMMARY.md

<interfaces>
<!-- Key types, current port locations, and dependency chains. -->

Ports to evict from uc-core/src/ports/:

```
autostart.rs      -> AutostartPort trait
ui_port.rs        -> UiPort trait
app_dirs.rs       -> AppDirsPort trait (returns AppDirs data type from uc-core -- type stays in core)
watcher_control.rs -> WatcherControlPort trait, WatcherControlError
identity_store.rs -> IdentityStorePort trait, IdentityStoreError
observability.rs  -> TraceMetadata, OptionalTrace, extract_trace (no trait -- data types/utility)
```

Current consumers of evicted ports:

```
uc-app/src/deps.rs:
  - watcher_control: Arc<dyn WatcherControlPort>   (field must be removed -- can't import from uc-platform)
  - ui_port: Arc<dyn UiPort>                       (field must be removed)
  - autostart: Arc<dyn AutostartPort>               (field must be removed)

uc-app/src/lib.rs (App struct):
  - autostart: Arc<dyn AutostartPort>
  - ui_port: Arc<dyn UiPort>

uc-app/src/usecases/settings/apply_autostart.rs:
  - imports AutostartPort from uc_core::ports
  - USE CASE MUST MOVE to uc-platform (per user decision)

uc-app/src/usecases/start_clipboard_watcher.rs:
  - imports WatcherControlPort from uc_core::ports
  - USE CASE MUST MOVE to uc-platform (per user decision)

uc-platform/src/adapters/ (multiple files):
  - IdentityStorePort -- already used in uc-platform, imports change to local

uc-tauri/src/commands/ (multiple files):
  - TraceMetadata from observability -- imports change to uc-platform path
  - IdentityStorePort -- used in bootstrap wiring

uc-tauri/src/bootstrap/runtime.rs:
  - UseCases accessor methods that reference evicted ports need import updates
```

Existing uc-platform/src/ports/mod.rs:

```rust
pub mod app_event_handler;
pub mod clipboard_runtime;
pub mod command_executor;

pub use clipboard_runtime::ClipboardRuntimePort;
pub use command_executor::PlatformCommandExecutorPort;
```

Dependency direction (MUST preserve):
uc-app -> uc-core (allowed)
uc-platform -> uc-core (allowed)
uc-tauri -> uc-app, uc-core, uc-platform, uc-infra (allowed -- wiring crate)
uc-app -> uc-platform (FORBIDDEN)
uc-core -> anything (FORBIDDEN)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move port files from uc-core to uc-platform and update uc-core exports</name>
  <files>src-tauri/crates/uc-core/src/ports/mod.rs, src-tauri/crates/uc-core/src/ports/autostart.rs, src-tauri/crates/uc-core/src/ports/ui_port.rs, src-tauri/crates/uc-core/src/ports/app_dirs.rs, src-tauri/crates/uc-core/src/ports/watcher_control.rs, src-tauri/crates/uc-core/src/ports/identity_store.rs, src-tauri/crates/uc-core/src/ports/observability.rs, src-tauri/crates/uc-platform/src/ports/mod.rs, src-tauri/crates/uc-platform/src/ports/autostart.rs, src-tauri/crates/uc-platform/src/ports/ui_port.rs, src-tauri/crates/uc-platform/src/ports/app_dirs.rs, src-tauri/crates/uc-platform/src/ports/watcher_control.rs, src-tauri/crates/uc-platform/src/ports/identity_store.rs, src-tauri/crates/uc-platform/src/ports/observability.rs</files>
  <action>
**Step 1: Evaluate observability module before moving.**
Run `grep -rn "observability\|TraceMetadata\|extract_trace\|OptionalTrace" src-tauri/crates/uc-app/src/` to check if any uc-app code depends on observability types. Per research, no uc-app usage is expected. If confirmed, proceed with eviction. If uc-app does depend on it, keep observability in uc-core (it would be a domain cross-cutting concern).

**Step 2: Copy the 6 port files to uc-platform/src/ports/.**
For each file (autostart.rs, ui_port.rs, app_dirs.rs, watcher_control.rs, identity_store.rs, observability.rs):

- Copy from `uc-core/src/ports/{file}` to `uc-platform/src/ports/{file}`
- Update internal imports: change `use crate::` references to use `uc_core::` where they reference core domain types (e.g., `AppDirs` data type stays in uc-core, so `app_dirs.rs` port file will import `uc_core::app_dirs::AppDirs`)
- For observability.rs: this contains data types not traits. Adjust imports accordingly.

**Step 3: Update uc-platform/src/ports/mod.rs.**
Add module declarations and pub use re-exports for all 6 moved ports:

```rust
pub mod autostart;
pub mod ui_port;
pub mod app_dirs;
pub mod watcher_control;
pub mod identity_store;
pub mod observability;

pub use autostart::AutostartPort;
pub use ui_port::UiPort;
pub use app_dirs::AppDirsPort;
pub use watcher_control::{WatcherControlError, WatcherControlPort};
pub use identity_store::{IdentityStoreError, IdentityStorePort};
pub use observability::{extract_trace, OptionalTrace, TraceMetadata, TraceParseError};
```

**Step 4: Remove the 6 port files from uc-core/src/ports/.**
Delete the original files and remove their `pub mod` and `pub use` entries from `uc-core/src/ports/mod.rs`.

**Step 5: Do NOT re-export evicted ports from uc-core.** This is an explicit anti-pattern from RESEARCH.md. All downstream imports must change.

At this point, `cargo check` will fail with many import errors. That's expected -- Task 2 fixes all consumers.
</action>
<verify>
<automated>ls /home/wuy6/myprojects/UniClipboard/src-tauri/crates/uc-platform/src/ports/autostart.rs /home/wuy6/myprojects/UniClipboard/src-tauri/crates/uc-platform/src/ports/ui_port.rs /home/wuy6/myprojects/UniClipboard/src-tauri/crates/uc-platform/src/ports/watcher_control.rs /home/wuy6/myprojects/UniClipboard/src-tauri/crates/uc-platform/src/ports/identity_store.rs /home/wuy6/myprojects/UniClipboard/src-tauri/crates/uc-platform/src/ports/app_dirs.rs /home/wuy6/myprojects/UniClipboard/src-tauri/crates/uc-platform/src/ports/observability.rs && echo "ALL PORT FILES EXIST" && grep -c "AutostartPort\|UiPort\|AppDirsPort\|WatcherControlPort\|IdentityStorePort\|observability" /home/wuy6/myprojects/UniClipboard/src-tauri/crates/uc-core/src/ports/mod.rs || echo "REMOVED FROM CORE"</automated>
</verify>
<done>

- All 6 port files exist in uc-platform/src/ports/
- uc-platform/src/ports/mod.rs re-exports all evicted ports
- uc-core/src/ports/mod.rs has zero references to evicted ports
- Original files deleted from uc-core/src/ports/
  </done>
  </task>

<task type="auto">
  <name>Task 2: Update all consumers -- deps, use cases, commands, bootstrap, adapters</name>
  <files>src-tauri/crates/uc-app/src/deps.rs, src-tauri/crates/uc-app/src/lib.rs, src-tauri/crates/uc-app/src/usecases/settings/apply_autostart.rs, src-tauri/crates/uc-app/src/usecases/start_clipboard_watcher.rs, src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs, src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs</files>
  <action>
This task fixes all compilation errors caused by port eviction. Work through each crate systematically:

**A. uc-app changes (CANNOT import from uc-platform):**

1. **deps.rs**: Remove these fields from `AppDeps`:
   - `pub watcher_control: Arc<dyn WatcherControlPort>`
   - `pub ui_port: Arc<dyn UiPort>`
   - `pub autostart: Arc<dyn AutostartPort>`
     Remove the corresponding imports from `use uc_core::ports::*` that pull these types. The `use uc_core::ports::*` glob import will automatically stop importing them since they're gone from core.

2. **lib.rs**: The `App` struct has `autostart: Arc<dyn AutostartPort>` and `ui_port: Arc<dyn UiPort>`. Remove these fields and the `use uc_core::ports::{AutostartPort, UiPort}` import. Update `App::new()` to not clone/store these. If `App` is used elsewhere, trace and update callers.

3. **usecases/settings/apply_autostart.rs**: This use case imports `AutostartPort` from `uc_core::ports`. Per user decision, MOVE this entire file to `uc-platform`. Create `src-tauri/crates/uc-platform/src/usecases/` directory if it doesn't exist, and `src-tauri/crates/uc-platform/src/usecases/apply_autostart.rs`. Update imports to use `crate::ports::AutostartPort`. Also update `SettingsPort` import -- it stays in uc-core, so import as `uc_core::ports::SettingsPort`.

4. **usecases/start_clipboard_watcher.rs**: Same treatment -- MOVE to `uc-platform/src/usecases/start_clipboard_watcher.rs`. Update `WatcherControlPort` import to `crate::ports::WatcherControlPort`. Other core ports remain imported from `uc_core`.

5. Update `uc-app/src/usecases/mod.rs` and `uc-app/src/usecases/settings/mod.rs` to remove the moved use case modules.

**B. uc-platform changes:**

1. **adapters/ files** that import evicted ports from uc-core: Change `use uc_core::ports::IdentityStorePort` (and similar) to `use crate::ports::IdentityStorePort`. Run `grep -rn "uc_core::ports::IdentityStorePort\|uc_core::ports::WatcherControlPort\|uc_core::ports::AutostartPort\|uc_core::ports::UiPort\|uc_core::ports::AppDirsPort\|uc_core::ports::observability" src-tauri/crates/uc-platform/src/` to find all sites.

2. Add `pub mod usecases;` to uc-platform/src/lib.rs (or wherever the crate root is) if creating a new usecases directory.

**C. uc-tauri changes:**

1. **bootstrap/runtime.rs**: The `UseCases` accessor has methods that construct `ApplyAutostartSetting` and `StartClipboardWatcher`. These now live in uc-platform. Update imports from `uc_app::usecases::...` to `uc_platform::usecases::...`. The facade methods added in Plan 01 may reference ports that moved -- update imports.

2. **bootstrap/wiring.rs**: Update any imports of evicted ports to come from `uc_platform::ports::` instead of `uc_core::ports::`. Update `AppDeps` construction to not include removed fields.

3. **commands/\*.rs**: Update `TraceMetadata` imports from `uc_core::ports::observability::TraceMetadata` to `uc_platform::ports::observability::TraceMetadata`. Run `grep -rn "uc_core::ports::observability" src-tauri/crates/uc-tauri/src/` to find all sites.

**D. Final sweep:**
Run `grep -rn "uc_core::ports::AutostartPort\|uc_core::ports::UiPort\|uc_core::ports::AppDirsPort\|uc_core::ports::WatcherControlPort\|uc_core::ports::IdentityStorePort\|uc_core::ports::observability" src-tauri/crates/` to confirm zero remaining references to evicted ports via uc-core path.

**E. Compilation gate:**
`cd src-tauri && cargo check` must pass. Then `cd src-tauri && cargo test` must pass.
</action>
<verify>
<automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo check 2>&1 | tail -10 && echo "---" && cargo test 2>&1 | tail -10 && echo "---" && grep -rn "uc_core::ports::AutostartPort\|uc_core::ports::UiPort\|uc_core::ports::AppDirsPort\|uc_core::ports::WatcherControlPort\|uc_core::ports::IdentityStorePort\|uc_core::ports::observability" crates/ || echo "NO STALE IMPORTS"</automated>
</verify>
<done>

- All consumers updated to import evicted ports from uc-platform
- apply_autostart.rs and start_clipboard_watcher.rs relocated to uc-platform/src/usecases/
- AppDeps no longer contains watcher_control, ui_port, autostart fields
- App struct in lib.rs updated (evicted port fields removed)
- Full workspace compiles and all tests pass
- Zero references to evicted ports via uc_core path
  </done>
  </task>

</tasks>

<verification>
1. `cd src-tauri && cargo check` -- full workspace compiles
2. `cd src-tauri && cargo test` -- all tests pass
3. `grep -rn 'AutostartPort\|UiPort\|AppDirsPort\|WatcherControlPort\|IdentityStorePort\|ObservabilityPort' src-tauri/crates/uc-core/src/ports/mod.rs` -- returns empty (evicted from core)
4. `grep -rn "uc_core::ports::AutostartPort\|uc_core::ports::UiPort\|uc_core::ports::AppDirsPort\|uc_core::ports::WatcherControlPort\|uc_core::ports::IdentityStorePort\|uc_core::ports::observability" src-tauri/crates/` -- returns empty (no stale imports)
5. `ls src-tauri/crates/uc-platform/src/ports/autostart.rs src-tauri/crates/uc-platform/src/ports/ui_port.rs src-tauri/crates/uc-platform/src/ports/watcher_control.rs src-tauri/crates/uc-platform/src/ports/identity_store.rs src-tauri/crates/uc-platform/src/ports/app_dirs.rs src-tauri/crates/uc-platform/src/ports/observability.rs` -- all exist
</verification>

<success_criteria>

- uc-core/src/ports/ contains only domain-relevant port contracts (6 non-domain ports removed)
- All 6 evicted ports live in uc-platform/src/ports/ with proper re-exports
- apply_autostart and start_clipboard_watcher use cases relocated to uc-platform
- AppDeps fields for evicted ports removed
- Full workspace compiles and all tests pass
- No stale imports referencing evicted ports via uc-core path
  </success_criteria>

<output>
After completion, create `.planning/phases/10-boundary-repair-baseline/10-03-SUMMARY.md`
</output>
