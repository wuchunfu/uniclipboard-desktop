## 0.2.0-alpha.6 - 2026-03-09

### Features

- Add in-app release notes rendering with bilingual update notes support based on the user's language
- Add circular reveal and collapse animations when entering or leaving Settings

### Fixes

- Fix empty update release notes by populating the updater manifest from the changelog files
- Fix page navigation skeleton flashes so cached content stays visible during background refresh
- Fix slow macOS image sync by converting clipboard TIFF images to PNG before transfer
