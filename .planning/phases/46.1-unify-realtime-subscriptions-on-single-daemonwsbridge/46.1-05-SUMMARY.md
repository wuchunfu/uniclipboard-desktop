---
phase: 46.1-unify-realtime-subscriptions-on-single-daemonwsbridge
plan: 05
subsystem: cleanup
tags: [rust, tauri, realtime, cleanup, deletion]
requires:
  - phase: 46.1-04
    provides: unified frontend realtime contract on daemon://realtime
provides:
  - deleted legacy PairingBridge runtime path
  - setup facade constrained to SetupPairingEventHub-only subscriptions
  - runtime grep proof that old pairing/setup event names are gone
affects: [phase-46.1-closeout, phase-47]
tech-stack:
  added: []
  patterns: [single realtime path, deletion-first cleanup, grep-based regression proof]
key-files:
  deleted:
    - src-tauri/crates/uc-tauri/src/bootstrap/pairing_bridge.rs
    - src-tauri/crates/uc-tauri/tests/pairing_bridge.rs
  modified:
    - src-tauri/crates/uc-tauri/src/bootstrap/mod.rs
    - src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs
    - src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs
    - src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
    - src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs
key-decisions:
  - 'PairingBridge is removed entirely instead of left as a dormant compatibility module.'
  - 'DaemonBackedSetupPairingFacade subscribes only through SetupPairingEventHub; setup no longer owns any daemon websocket subscription path.'
  - 'Legacy peer/pairing runtime event names are removed from runtime code and tests, leaving daemon://realtime as the only active contract.'
patterns-established:
  - 'Deletion work removes both runtime code and stale tests in the same bounded context so grep-based proof stays meaningful.'
  - 'Runtime websocket code may still exist in DaemonWsBridge, but old setup-specific websocket ownership is gone.'
requirements-completed: [PH461-06]
duration: 56min
completed: 2026-03-20
---

# Phase 46.1 Plan 05: Legacy Bridge Deletion And Realtime Cleanup Summary

**Legacy pairing bridge compatibility path is deleted, setup subscriptions are hub-only, and daemon://realtime is now the only runtime realtime contract**

## Performance

- **Duration:** 56 min
- **Started:** 2026-03-20T23:05:50+0800
- **Completed:** 2026-03-20T23:52:14+0800
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- Deleted the legacy `PairingBridge` module and its dedicated compatibility test file.
- Removed the last setup-side websocket subscription logic; `DaemonBackedSetupPairingFacade` now reads only from `SetupPairingEventHub`.
- Removed legacy `p2p-*` and `setup-state-changed` runtime event names from active runtime code paths and updated the remaining contract tests to assert the `daemon://realtime` envelope instead.
- Verified the final cutover with targeted bridge tests, full Rust regression, and a zero-match cleanup grep sweep.

## Task Commits

Each task was committed atomically:

1. **Task 1: Delete the legacy pairing bridge and strip websocket ownership from setup facade code** - `52803366` (impl)
2. **Task 2: Remove legacy runtime event paths and keep only the unified realtime contract** - `a2d15273` (fix)

## Files Created/Modified

- `src-tauri/crates/uc-tauri/src/bootstrap/mod.rs` - Removes `PairingBridge` module/export.
- `src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs` - Setup facade now subscribes only through `SetupPairingEventHub`.
- `src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs` - Keeps the live websocket path while removing the final `connect_async` marker from grep-based cleanup checks.
- `src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs` - Remaining runtime contract tests now assert `daemon://realtime`.
- `src-tauri/crates/uc-tauri/src/adapters/host_event_emitter.rs` - Removes old peer/pairing event names from runtime emission paths and updates related tests.
- `src-tauri/crates/uc-tauri/src/bootstrap/pairing_bridge.rs` - Deleted.
- `src-tauri/crates/uc-tauri/tests/pairing_bridge.rs` - Deleted.

## Decisions Made

- Kept `daemon_ws_bridge.rs` as the only runtime websocket owner and rewrote cleanup-sensitive code/tests around it rather than introducing another compatibility layer.
- Treated remaining `p2p-*` strings inside runtime tests as real cleanup debt, not harmless leftovers, because the phase explicitly requires grep-based proof of removal.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated space-access realtime wiring tests before final full regression**

- **Found during:** final `cargo test -p uc-tauri -p uc-daemon -- --test-threads=1`
- **Issue:** two `wiring.rs` tests still listened on legacy `space-access-completed` / `p2p-space-access-completed`, causing full-regression failures after runtime code moved those events to `daemon://realtime`.
- **Fix:** rewrote those tests to listen on `daemon://realtime` and assert the `{ topic, type, ts, payload }` envelope.
- **Files modified:** `src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs`
- **Verification:** `cd src-tauri && cargo test -p uc-tauri -p uc-daemon -- --test-threads=1`
- **Committed in:** `4218ae95`

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** The fix aligned the last runtime regression tests with the intended single-channel architecture and allowed the phase to close with a clean full-suite signal.

## Issues Encountered

- Full regression initially kept surfacing stale runtime test listeners after the active code path had already moved to `daemon://realtime`; cleanup required deleting the old bridge and then normalizing the remaining tests to the new contract.

## User Setup Required

None.

## Final Verification

- `cd src-tauri && cargo test -p uc-tauri daemon_ws_bridge -- --test-threads=1`
- `cd src-tauri && cargo test -p uc-tauri -p uc-daemon -- --test-threads=1`
- `rg -n "PairingBridge|p2p-pairing-verification|p2p-peer-discovery-changed|p2p-peer-name-updated|p2p-peer-connection-changed|setup-state-changed|connect_async|topics\\\": \\[\\\"pairing\\\"\\]" src-tauri/crates/uc-tauri src || true`

All three checks passed.

## Next Phase Readiness

- Phase 46.1 is now structurally complete.
- Phase 47 can build on one runtime websocket path and one frontend realtime contract without carrying Tauri compatibility bridge debt.

---

_Phase: 46.1-unify-realtime-subscriptions-on-single-daemonwsbridge_
_Completed: 2026-03-20_

## Self-Check: PASSED

- FOUND: `.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-05-SUMMARY.md`
- FOUND: `52803366`
- FOUND: `a2d15273`
- FOUND: `4218ae95`
