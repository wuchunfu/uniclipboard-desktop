---
phase: 37-wiring-decomposition
plan: 05
type: execute
wave: 4
depends_on:
  - 37-03
  - 37-UAT
files_modified:
  - src-tauri/crates/uc-app/src/usecases/setup/action_executor.rs
  - src-tauri/crates/uc-app/src/usecases/pairing/orchestrator.rs
  - src-tauri/crates/uc-platform/src/adapters/pairing_stream/service.rs
  - src-tauri/crates/uc-app/src/usecases/setup/orchestrator.rs
autonomous: true
requirements:
  - RNTM-02
must_haves:
  truths:
    - 'join-space 不会因为 setup 对 pairing 事件订阅过晚而错过 PairingVerificationRequired / PairingFailed'
    - 'peerA 拒绝初始请求后，peerB 能稳定退回 JoinSpaceSelectDevice 并显示 PairingRejected'
    - 'clean close / EOF 不再让 active pairing session 静默卡死'
    - '新增后端测试覆盖低延迟 accept/reject 和初始请求 reject 的失败传播'
    - 'Test 5 不单独修，作为 Test 2/4 修复后的下游验证结果关闭'
  artifacts:
    - path: 'src-tauri/crates/uc-app/src/usecases/setup/action_executor.rs'
      provides: 'setup 对 pairing 事件的可靠订阅时序'
      contains: 'start_pairing_verification_listener'
    - path: 'src-tauri/crates/uc-platform/src/adapters/pairing_stream/service.rs'
      provides: 'active pairing session 在 clean close / EOF 下的失败传播兜底'
      contains: 'StreamClosedByPeer'
    - path: 'src-tauri/crates/uc-app/src/usecases/setup/orchestrator.rs'
      provides: '覆盖 join-space accept/reject 低延迟场景的集成测试'
  key_links:
    - from: 'src-tauri/crates/uc-app/src/usecases/setup/action_executor.rs'
      to: 'src-tauri/crates/uc-app/src/usecases/pairing/orchestrator.rs'
      via: 'setup pairing listener subscription'
      pattern: 'start_pairing_verification_listener'
    - from: 'src-tauri/crates/uc-platform/src/adapters/pairing_stream/service.rs'
      to: 'src-tauri/crates/uc-app/src/usecases/setup/action_executor.rs'
      via: 'PairingFailed fallback on clean close / EOF'
      pattern: 'PairingFailed'
---

<objective>
修复 join-space 链路的事件可靠性问题：消除 “先 initiate_pairing 后订阅” 的竞态窗口，并为初始请求阶段的 reject / clean close 提供稳定的失败传播，使 peerB 不再无提示卡在 ProcessingJoinSpace。

Purpose: 关闭 UAT Test 2、4，并间接恢复 Test 5 的完成态可达性。
Output: 稳定推进 join-space 状态的后端实现 + 覆盖 accept/reject 低延迟场景的测试。
</objective>

<context>
@.planning/phases/37-wiring-decomposition/37-UAT.md
@src-tauri/crates/uc-app/src/usecases/setup/action_executor.rs
@src-tauri/crates/uc-app/src/usecases/pairing/orchestrator.rs
@src-tauri/crates/uc-app/src/usecases/pairing/protocol_handler.rs
@src-tauri/crates/uc-platform/src/adapters/pairing_stream/service.rs
@src-tauri/crates/uc-app/src/usecases/setup/orchestrator.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 前移 setup pairing 订阅，消除事件先发后订阅窗口</name>
  <files>
    src-tauri/crates/uc-app/src/usecases/setup/action_executor.rs
    src-tauri/crates/uc-app/src/usecases/pairing/orchestrator.rs
  </files>
  <action>
调整 join-space ensure pairing 的顺序，确保 setup 对 pairing 领域事件的监听在可能发出 `PairingVerificationRequired` / `PairingFailed` 之前已经建立。

要求：

- 不引入跨层边界破坏
- 若现有 subscribe 语义无法保证可靠性，则补最小必要能力，但不要把修复扩散成大规模重构
- 明确保证 accept 和 reject 两条低延迟路径都能驱动 setup 状态变化
  </action>
  <done> - join-space 不再存在 initiate_pairing 后才开始监听 pairing 事件的窗口 - PairingVerificationRequired 与 PairingFailed 在低延迟 accept/reject 下都能稳定推进 setup 状态 - ProcessingJoinSpace 不再因为错过首个 pairing 事件而无限停留
  </done>
  <verify>
  <command>cd src-tauri && cargo test -p uc-app join_space</command>
  </verify>
  </task>

<task type="auto">
  <name>Task 2: 为 pairing stream 的初始请求 reject / clean close 增加失败传播兜底</name>
  <files>
    src-tauri/crates/uc-platform/src/adapters/pairing_stream/service.rs
  </files>
  <action>
给 active pairing session 的 reject 失败链补可靠传播保证。

可接受方向：

- 让 reject 在 close 前具备上层可观察的送达保障
- 或在 clean close / EOF 且 session 仍处于活跃失败上下文时，桥接成 pairing failure

要求：

- 不把正常完成流误判成失败
- 这一 task 只处理 `uc-platform` 的 failure propagation，不夹带 `uc-app` 行为验证或跨层测试改动
  </action>
  <done> - active pairing session 在 reject 终止帧丢失但发生 clean close / EOF 时，仍能向上游暴露明确失败 - 正常成功完成流不会被新的 failure bridge 误判 - `uc-platform` 变更可单独提交，不依赖 `uc-app` 测试文件同 commit 落地
  </done>
  <verify>
  <command>cd src-tauri && cargo test -p uc-platform pairing_stream</command>
  </verify>
  </task>

<task type="auto">
  <name>Task 3: 在 uc-app 验证 reject 回退与 join-space 下游恢复</name>
  <files>
    src-tauri/crates/uc-app/src/usecases/setup/orchestrator.rs
    src-tauri/crates/uc-app/src/usecases/setup/action_executor.rs
  </files>
  <action>
基于 Task 1 和 Task 2 的修复结果，补齐 `uc-app` 层的行为验证与下游闭环。

要求：

- 补一条覆盖 “A reject initial request -> B 回到 JoinSpaceSelectDevice 且 error=PairingRejected” 的测试
- 补一条低延迟 accept/reject 下 join-space 不再卡住的测试
- 保持 Test 5 不单独修，但把它作为下游验收条件：修复后 join-space 至少能推进到 JoinSpaceConfirmPeer，完整 happy path 应恢复到 Completed
  </action>
  <done> - uc-app 层测试可证明 reject 路径会把 B 端退回 JoinSpaceSelectDevice 且 error=PairingRejected - accept 低延迟路径至少推进到 JoinSpaceConfirmPeer，完整 join happy path 可再次到达 Completed - UAT Test 5 的下游阻断条件被解除，不再因前序 pairing/setup 事件链断裂而失败
  </done>
  <verify>
  <command>cd src-tauri && cargo test -p uc-app join_space</command>
  </verify>
  </task>

</tasks>
