# Phase 56.1: Eliminate Hardcoded Strings in Pairing/Setup Flow - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-25
**Phase:** 56.1-eliminate-hardcoded-strings-in-pairing-setup-flow
**Areas discussed:** Module naming & structure, HTTP path extraction scope, Frontend TypeScript constants, Validation strategy

---

## Module Naming & Structure

| Option                        | Description                                                                 | Selected |
| ----------------------------- | --------------------------------------------------------------------------- | -------- |
| daemon_api_strings.rs（推荐） | 单文件 + pub mod 子模块（ws_topic, ws_event 等），符合 protocol_ids.rs 先例 | ✓        |
| daemon_wire.rs                | 更短的名字，强调跨线协议语义                                                |          |
| 你决定                        | Claude 自行选择最合适的方案                                                 |          |

**User's choice:** daemon_api_strings.rs（推荐）
**Notes:** None

---

## HTTP Path Extraction Scope

| Option                 | Description                                          | Selected |
| ---------------------- | ---------------------------------------------------- | -------- |
| 只提取静态路径（推荐） | 约18个纯静态路由提取为常量，动态段路径保持 format!() |          |
| 全部提取               | 包括动态路径的基础部分                               |          |
| 跳过 HTTP 路径         | 本次只处理 WS 主题/事件/状态字符串                   | ✓        |

**User's choice:** 跳过，不提取
**Notes:** User asked "HTTP 路径可复用吗，不能复用的话，为什么要提取". After explanation that paths are duplicated server↔client but change infrequently, user decided the risk is acceptable without constants.

---

## Frontend TypeScript Constants

| Option         | Description                                                        | Selected |
| -------------- | ------------------------------------------------------------------ | -------- |
| 不纳入（推荐） | 前端 camelCase 与 Rust snake_case 属不同层，不能共享常量，延后处理 | ✓        |
| 纳入           | 同步提取 TS 常量文件（如 src/constants/daemon-events.ts）          |          |

**User's choice:** 不纳入（推荐）
**Notes:** None

---

## Validation Strategy

| Option               | Description                                                   | Selected |
| -------------------- | ------------------------------------------------------------- | -------- |
| 写值断言测试（推荐） | 在 uc-core 中加少量测试，断言每个常量的字符串值正确，防止手误 | ✓        |
| 只靠 cargo check     | 不加额外测试，依赖编译检查和现有集成测试                      |          |
| 你决定               | Claude 根据实际情况判断是否需要                               |          |

**User's choice:** 写值断言测试（推荐）
**Notes:** None

---

## Claude's Discretion

- Exact ordering of constants within each submodule
- Whether to add doc comments on individual constants
- Whether snapshot and incremental event constants go in same submodule or separate sections

## Deferred Ideas

- Frontend TypeScript constant extraction — different casing layer, defer to future phase
- HTTP route path constant extraction — low risk, could be a future cleanup
