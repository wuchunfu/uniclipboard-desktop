---
phase: 17-dynamic-theme-switching-with-shadcn-preset-import-and-enhanced-color-preview
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants/theme.ts
  - src/lib/theme-engine.ts
  - src/contexts/SettingContext.tsx
  - src/styles/globals.css
  - src/styles/themes/zinc.css
  - src/styles/themes/catppuccin.css
  - src/styles/themes/t3chat.css
  - src/styles/themes/claude.css
  - src/lib/__tests__/theme-engine.test.ts
  - src/contexts/__tests__/SettingContext.theme.test.tsx
  - src/styles/__tests__/theme-migration.test.ts
autonomous: true
requirements:
  - P17-01
  - P17-02
  - P17-03
  - P17-05
  - P17-06
must_haves:
  truths:
    - 'Theme presets are defined in TS with explicit light/dark token maps and preview metadata.'
    - 'SettingProvider applies mode (light/dark/system) and injects theme CSS variables from preset definition at runtime.'
    - 'globals.css no longer imports static per-theme CSS files for color ownership.'
    - 'Persisted theme_color is applied on startup with default fallback when unset/unknown.'
    - 'Theme color switch has smooth CSS transition (~200ms) for key color properties.'
  artifacts:
    - path: 'src/lib/theme-engine.ts'
      provides: 'Theme preset registry + runtime CSS variable injection helper'
      contains: 'applyThemePreset'
    - path: 'src/constants/theme.ts'
      provides: 'Theme option exports sourced from TS preset definitions'
      contains: 'THEME_COLORS'
    - path: 'src/contexts/SettingContext.tsx'
      provides: 'Mode and preset application integration in existing theme effect'
      contains: 'applyThemePreset'
    - path: 'src/styles/globals.css'
      provides: 'Theme transition and minimal fallback ownership after migration'
      contains: 'transition'
    - path: 'src/lib/__tests__/theme-engine.test.ts'
      provides: 'Runtime injection + fallback tests'
      contains: 'applyThemePreset'
  key_links:
    - from: 'src/contexts/SettingContext.tsx'
      to: 'src/lib/theme-engine.ts'
      via: 'theme application from settings.general.theme_color + effective mode'
      pattern: 'applyThemePreset'
    - from: 'src/constants/theme.ts'
      to: 'src/lib/theme-engine.ts'
      via: 'THEME_COLORS derived from preset registry'
      pattern: 'THEME_COLORS'
---

<objective>
Build the runtime theme preset engine and migrate color token ownership from static CSS theme files to TS objects + root CSS variable injection.

Purpose: Establish a single, testable source of truth for theme colors while preserving existing mode behavior and settings persistence.
Output: Runtime theme engine utility, SettingProvider integration, static theme CSS import cleanup, and foundational tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dynamic-theme-switching-with-shadcn-preset-import-and-enhanced-color-preview/17-CONTEXT.md
@.planning/phases/17-dynamic-theme-switching-with-shadcn-preset-import-and-enhanced-color-preview/17-RESEARCH.md
@.planning/phases/17-dynamic-theme-switching-with-shadcn-preset-import-and-enhanced-color-preview/17-VALIDATION.md

<interfaces>
From `src/contexts/SettingContext.tsx` (current behavior):

```tsx
const theme = setting?.general.theme
const themeColor = setting?.general.theme_color || DEFAULT_THEME_COLOR

root.classList.remove('light', 'dark')
if (theme === 'system' || !theme) {
  const systemTheme = systemThemeMedia.matches ? 'dark' : 'light'
  root.classList.add(systemTheme)
} else {
  root.classList.add(theme)
}
root.setAttribute('data-theme', themeColor)
```

From `src/constants/theme.ts` (current shape):

```ts
export interface ThemeColorOption {
  name: string
  color: string
}

export const THEME_COLORS: ThemeColorOption[] = [
  { name: 'zinc', color: '#52525b' },
  { name: 'catppuccin', color: '#cba6f7' },
  { name: 't3chat', color: '#a3004c' },
  { name: 'claude', color: '#d97757' },
]
```

From `src/types/setting.ts` / backend model:

```ts
export interface GeneralSettings {
  theme: 'light' | 'dark' | 'system'
  theme_color?: string | null
}
```

```rust
pub struct GeneralSettings {
    pub theme: Theme,
    pub theme_color: Option<String>,
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce TS theme preset registry and runtime application helper</name>
  <files>
    src/lib/theme-engine.ts
    src/constants/theme.ts
    src/lib/__tests__/theme-engine.test.ts
  </files>
  <behavior>
    - Theme presets are defined as TS objects with required token keys for both `light` and `dark`.
    - Existing presets (`zinc`, `catppuccin`, `t3chat`, `claude`) are migrated from CSS files into TS definitions.
    - Preset export provides metadata needed by UI swatches (3-4 preview colors) without separate hardcoded color tables.
    - `applyThemePreset(themeName, mode, root)` injects token values into root CSS vars and gracefully falls back to default preset when unknown.
  </behavior>
  <action>
    1. Create `src/lib/theme-engine.ts` with:
       - Preset token type definitions.
       - Curated built-in preset registry (including migrated existing 4 presets; optional additional curated presets allowed).
       - `DEFAULT_THEME_COLOR` export.
       - `applyThemePreset(themeName, mode, root)` helper that writes CSS variables onto `root.style`.
       - `getThemePreviewDots(themeName, mode)` helper used by appearance UI.

    2. Update `src/constants/theme.ts` to become a thin adapter over the preset registry:
       - Keep `THEME_COLORS` export for compatibility.
       - Include preview dots (or enough metadata for dot preview rendering).
       - Remove standalone hardcoded hex values that duplicate registry data.

    3. Add tests in `src/lib/__tests__/theme-engine.test.ts`:
       - Applies known preset in light mode and sets representative CSS variables (`--background`, `--primary`, `--border`).
       - Applies known preset in dark mode and values differ from light where expected.
       - Unknown preset name falls back to default preset.
       - Preview dot helper returns 3-4 values.

  </action>
  <verify>
    <automated>bun run test --run src/lib/__tests__/theme-engine.test.ts</automated>
  </verify>
  <done>
    - Runtime theme engine utility exists and is covered by unit tests.
    - Existing themes are migrated to TS registry.
    - Theme metadata is centralized (no duplicated hardcoded preview values in UI).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate runtime injection in SettingProvider and migrate global CSS ownership</name>
  <files>
    src/contexts/SettingContext.tsx
    src/styles/globals.css
    src/styles/themes/zinc.css
    src/styles/themes/catppuccin.css
    src/styles/themes/t3chat.css
    src/styles/themes/claude.css
    src/contexts/__tests__/SettingContext.theme.test.tsx
    src/styles/__tests__/theme-migration.test.ts
  </files>
  <behavior>
    - Existing mode behavior (`light` / `dark` / `system`) remains unchanged.
    - Theme token application runs after mode resolution and uses persisted `theme_color` fallback.
    - `globals.css` no longer imports per-theme CSS files for active color ownership.
    - Color transition is applied with ~200ms timing for key color-related properties.
  </behavior>
  <action>
    1. In `SettingContext.tsx`:
       - Import and call `applyThemePreset` inside current theme `useEffect`.
       - Keep class + `matchMedia` logic unchanged except necessary integration.
       - Ensure fallback to default preset when `theme_color` is null/unknown.

    2. In `globals.css`:
       - Remove `@import './themes/*.css'` lines.
       - Remove/strip static per-theme variable blocks that conflict with runtime ownership.
       - Keep minimal startup fallback tokens required to avoid startup flash.
       - Add transition rule for color-driven surfaces (approx `200ms`, easing).

    3. Legacy theme CSS files:
       - Either remove color blocks or keep as non-authoritative references with no active imports.
       - Ensure no runtime path still depends on them.

    4. Add `SettingContext` tests (`src/contexts/__tests__/SettingContext.theme.test.tsx`):
       - Persisted `theme_color` is applied on mount.
       - Unknown theme color falls back to default without throw.
       - `system` mode change listener still updates mode class.

    5. Add CSS migration assertion test (`src/styles/__tests__/theme-migration.test.ts`):
       - `globals.css` does not contain `@import './themes/`.
       - Transition rule is present for expected properties.

  </action>
  <verify>
    <automated>bun run test --run src/contexts/__tests__/SettingContext.theme.test.tsx src/styles/__tests__/theme-migration.test.ts</automated>
  </verify>
  <done>
    - SettingProvider performs runtime preset injection while preserving mode semantics.
    - Static theme import ownership is removed from globals.css.
    - Transition and fallback behavior are test-covered.
  </done>
</task>

</tasks>

<verification>
- `bun run test --run src/lib/__tests__/theme-engine.test.ts src/contexts/__tests__/SettingContext.theme.test.tsx src/styles/__tests__/theme-migration.test.ts` passes.
- `bun run build` passes.
</verification>
