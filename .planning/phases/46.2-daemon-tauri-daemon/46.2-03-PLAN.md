---
phase: 46.2-daemon-tauri-daemon
plan: 03
type: execute
wave: 3
depends_on: ['46.2-02']
files_modified:
  - src/api/p2p.ts
  - src/components/PairingDialog.tsx
  - src/components/PairingNotificationProvider.tsx
  - src/components/__tests__/PairingDialog.test.tsx
  - src/components/__tests__/PairingNotificationProvider.realtime.test.tsx
  - src/pages/__tests__/setup-peer-discovery-polling.test.tsx
autonomous: true
requirements: ['R46.2-3', 'R46.2-4', 'R46.2-8']
user_setup: []
must_haves:
  truths:
    - 'Frontend pairing flows preserve the existing request -> verification -> verifying -> complete/failed UX semantics while consuming daemon-backed commands and realtime events.'
    - 'PairingDialog, PairingNotificationProvider, and setup-related pairing progression continue to depend only on src/api/p2p.ts and existing shell contracts, not on direct daemon transport clients.'
    - 'Frontend regression tests prove initiator flow, passive flow, and setup pairing progression remain behaviorally equivalent after the daemon cutover.'
  artifacts:
    - path: 'src/api/p2p.ts'
      provides: 'Stable frontend pairing API surface and event listener contract on top of daemon-backed shell commands/events.'
      min_lines: 220
    - path: 'src/components/PairingDialog.tsx'
      provides: 'Initiator-side pairing dialog that preserves existing stage semantics.'
      min_lines: 220
    - path: 'src/components/PairingNotificationProvider.tsx'
      provides: 'Passive-side pairing notifications and verification handling using the existing pairing contract.'
      min_lines: 180
    - path: 'src/components/__tests__/PairingDialog.test.tsx'
      provides: 'Initiator flow regression coverage for daemon-backed pairing.'
      min_lines: 120
    - path: 'src/components/__tests__/PairingNotificationProvider.realtime.test.tsx'
      provides: 'Passive flow regression coverage for daemon-backed pairing realtime.'
      min_lines: 120
    - path: 'src/pages/__tests__/setup-peer-discovery-polling.test.tsx'
      provides: 'Setup pairing progression regression coverage after the shell cutover.'
      min_lines: 120
  key_links:
    - from: 'src/api/p2p.ts'
      to: 'src/components/PairingDialog.tsx'
      via: 'Initiator flow continues to use the existing initiate/verify pairing API and listener contract.'
      pattern: 'initiateP2PPairing|verifyP2PPairingPin|onP2PPairingVerification'
    - from: 'src/api/p2p.ts'
      to: 'src/components/PairingNotificationProvider.tsx'
      via: 'Passive flow continues to consume pairing verification events through the shared pairing API contract.'
      pattern: 'acceptP2PPairing|rejectP2PPairing|onP2PPairingVerification'
    - from: 'src/api/p2p.ts'
      to: 'src/pages/__tests__/setup-peer-discovery-polling.test.tsx'
      via: 'Setup progression continues to rely on the same pairing-facing shell contracts after cutover.'
      pattern: 'pairing|setup|peer'
---

<objective>
在 shell cutover 完成后，单独验证并收口前端 pairing flow parity：保持 initiator、passive、setup 三条用户路径的阶段语义不变，同时用前端 regression tests 固化这一行为。

Purpose: 把 UI/parity 风险和 shell cutover 分离，避免同一计划横跨 bridge 与 frontend 两个大面。
Output: 保持原契约的 `src/api/p2p.ts`、daemon-backed pairing UI flow、以及覆盖 initiator/passive/setup 的前端回归。
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46.2-daemon-tauri-daemon/46.2-CONTEXT.md
@.planning/phases/46.2-daemon-tauri-daemon/46.2-VALIDATION.md
@.planning/phases/46.2-daemon-tauri-daemon/46.2-02-PLAN.md

@src/api/p2p.ts
@src/components/PairingDialog.tsx
@src/components/PairingNotificationProvider.tsx
@src/components/**tests**/PairingDialog.test.tsx
@src/components/**tests**/PairingNotificationProvider.realtime.test.tsx
@src/pages/**tests**/setup-peer-discovery-polling.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 保持前端 pairing UX 阶段语义不变并补齐界面回归</name>
  <files>
    src/api/p2p.ts,
    src/components/PairingDialog.tsx,
    src/components/PairingNotificationProvider.tsx,
    src/components/__tests__/PairingDialog.test.tsx,
    src/components/__tests__/PairingNotificationProvider.realtime.test.tsx,
    src/pages/__tests__/setup-peer-discovery-polling.test.tsx
  </files>
  <read_first>
    - src/api/p2p.ts
    - src/components/PairingDialog.tsx
    - src/components/PairingNotificationProvider.tsx
    - src/components/__tests__/PairingDialog.test.tsx
    - src/components/__tests__/PairingNotificationProvider.realtime.test.tsx
    - src/pages/__tests__/setup-peer-discovery-polling.test.tsx
    - .planning/phases/46.2-daemon-tauri-daemon/46.2-CONTEXT.md
    - .planning/phases/46.2-daemon-tauri-daemon/46.2-VALIDATION.md
    - .planning/phases/46.2-daemon-tauri-daemon/46.2-02-PLAN.md
  </read_first>
  <action>
    1. 在 `src/api/p2p.ts` 中保持 pairing command/event contract 稳定：`P2PPairingResponse` 仍是 `{ sessionId, success, error? }`，pairing verification listener 继续覆盖 `request`、`verification`、`verifying`、`complete`、`failed` 这组既有阶段。
    2. 在 `src/components/PairingDialog.tsx` 与 `src/components/PairingNotificationProvider.tsx` 中，只做 daemon-backed 适配，不改变用户可见流程：
       - initiator 仍走 request/verification/verifying/complete/failed。
       - passive notification 仍围绕 request/verification/verifying/complete/failed 处理。
       - 组件不得直接访问 daemon HTTP client 或 websocket client，只依赖 `src/api/p2p.ts` 暴露的 API/listener。
    3. 在 `src/components/__tests__/PairingDialog.test.tsx`、`src/components/__tests__/PairingNotificationProvider.realtime.test.tsx` 与 `src/pages/__tests__/setup-peer-discovery-polling.test.tsx` 中增加或调整 daemon-backed regression，覆盖 initiator、passive flow、setup pairing progression 三条主要用户路径。
  </action>
  <verify>
    <automated>
      bun test src/components/__tests__/PairingDialog.test.tsx src/components/__tests__/PairingNotificationProvider.realtime.test.tsx src/pages/__tests__/setup-peer-discovery-polling.test.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - `rg "request|verification|verifying|complete|failed" src/api/p2p.ts src/components/PairingDialog.tsx src/components/PairingNotificationProvider.tsx -n` 返回既有 pairing 阶段语义的使用位置。
    - `rg "onP2PPairingVerification|initiateP2PPairing|acceptP2PPairing|rejectP2PPairing|verifyP2PPairingPin" src/components/PairingDialog.tsx src/components/PairingNotificationProvider.tsx -n` 返回组件继续通过 `src/api/p2p.ts` 暴露接口消费 pairing 流程。
    - `! rg "new WebSocket|fetch\\(|reqwest|createDaemonWsClient" src/components/PairingDialog.tsx src/components/PairingNotificationProvider.tsx -n` 退出码为 0。
    - `bun test src/components/__tests__/PairingDialog.test.tsx src/components/__tests__/PairingNotificationProvider.realtime.test.tsx src/pages/__tests__/setup-peer-discovery-polling.test.tsx` 退出码为 0。
  </acceptance_criteria>
  <done>
    - 用户可见 pairing flow 仍然维持既有阶段语义，只是内部来源切换为 daemon-backed shell contract。
    - 前端 pairing 组件保持对 shell 契约的依赖，不引入新的 transport 耦合。
  </done>
</task>

</tasks>

<verification>
- `bun test src/components/__tests__/PairingDialog.test.tsx src/components/__tests__/PairingNotificationProvider.realtime.test.tsx src/pages/__tests__/setup-peer-discovery-polling.test.tsx` 通过，证明 initiator、passive、setup 三条 pairing 用户路径在 cutover 后仍然成立。
- `! rg "new WebSocket|fetch\\(|reqwest|createDaemonWsClient" src/components/PairingDialog.tsx src/components/PairingNotificationProvider.tsx -n` 通过，证明前端组件没有直接耦合 daemon transport。
</verification>

<success_criteria>

- 前端 pairing flow 在用户视角保持原有阶段语义。
- 前端 regression 可以自动验证 daemon-backed cutover 后的 initiator/passive/setup 行为。
  </success_criteria>

<output>
完成后创建 `.planning/phases/46.2-daemon-tauri-daemon/46.2-03-SUMMARY.md`，总结 pairing UI parity 与 frontend regression 结果。
</output>
