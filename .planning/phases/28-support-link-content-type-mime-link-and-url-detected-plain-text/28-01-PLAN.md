---
phase: 28-support-link-content-type-mime-link-and-url-detected-plain-text
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/crates/uc-core/Cargo.toml
  - src-tauri/crates/uc-core/src/clipboard/link_utils.rs
  - src-tauri/crates/uc-core/src/clipboard/mod.rs
  - src-tauri/crates/uc-core/src/settings/content_type_filter.rs
  - src-tauri/crates/uc-tauri/src/models/mod.rs
  - src-tauri/crates/uc-tauri/src/commands/clipboard.rs
autonomous: true
requirements:
  - LINK-01
  - LINK-02
  - LINK-03
  - LINK-04

must_haves:
  truths:
    - 'text/uri-list MIME snapshots are classified as Link'
    - 'text/plain content that is a single valid URL (after trim, no whitespace) is classified as Link'
    - "Mixed text like 'see https://example.com' stays classified as Text"
    - 'Link category respects ct.link toggle in is_content_type_allowed'
    - 'get_clipboard_item returns populated ClipboardLinkItemDto for link entries with urls and domains'
    - 'text/uri-list with multiple URLs returns all parsed URLs and their domains'
  artifacts:
    - path: 'src-tauri/crates/uc-core/src/clipboard/link_utils.rs'
      provides: 'URL parsing utilities: parse_uri_list, is_single_url, extract_domain'
      min_lines: 20
    - path: 'src-tauri/crates/uc-core/src/settings/content_type_filter.rs'
      provides: 'Extended classify_snapshot with plain text URL detection; updated is_content_type_allowed'
      contains: 'ContentTypeCategory::Link => ct.link'
    - path: 'src-tauri/crates/uc-tauri/src/models/mod.rs'
      provides: 'ClipboardLinkItemDto struct'
      contains: 'ClipboardLinkItemDto'
    - path: 'src-tauri/crates/uc-tauri/src/commands/clipboard.rs'
      provides: 'Link-aware DTO mapping in get_clipboard_item'
      contains: 'ClipboardLinkItemDto'
  key_links:
    - from: 'src-tauri/crates/uc-core/src/settings/content_type_filter.rs'
      to: 'src-tauri/crates/uc-core/src/clipboard/link_utils.rs'
      via: 'is_single_url call in classify_snapshot text/plain branch'
      pattern: 'is_single_url'
    - from: 'src-tauri/crates/uc-tauri/src/commands/clipboard.rs'
      to: 'src-tauri/crates/uc-core/src/clipboard/link_utils.rs'
      via: 'parse_uri_list and extract_domain in DTO mapping'
      pattern: 'parse_uri_list|extract_domain'
---

<objective>
Backend link detection, classification, and DTO population for the link content type.

Purpose: Enable the backend to detect links (both MIME text/uri-list and plain text URLs), classify them correctly for sync filtering, and return structured link data (urls + domains) to the frontend via ClipboardItemDto.

Output: Extended classify_snapshot, updated is_content_type_allowed, link_utils module, ClipboardLinkItemDto, and link-aware get_clipboard_item command.
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-support-link-content-type-mime-link-and-url-detected-plain-text/28-CONTEXT.md
@.planning/phases/28-support-link-content-type-mime-link-and-url-detected-plain-text/28-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src-tauri/crates/uc-core/src/settings/content_type_filter.rs:

```rust
pub enum ContentTypeCategory { Text, Image, RichText, Link, File, CodeSnippet, Unknown }
pub fn classify_snapshot(snapshot: &SystemClipboardSnapshot) -> ContentTypeCategory
pub fn is_content_type_allowed(category: ContentTypeCategory, ct: &ContentTypes) -> bool
```

From src-tauri/crates/uc-core/src/clipboard/mod.rs:

```rust
// SystemClipboardSnapshot and ObservedClipboardRepresentation with .mime and .bytes fields
```

From src-tauri/crates/uc-tauri/src/models/mod.rs:

```rust
pub struct ClipboardItemDto {
    pub text: Option<ClipboardTextItemDto>,
    pub image: Option<ClipboardImageItemDto>,
    pub file: Option<serde_json::Value>,
    pub link: Option<serde_json::Value>,  // Currently always None - needs to become Option<ClipboardLinkItemDto>
    pub code: Option<serde_json::Value>,
    pub unknown: Option<serde_json::Value>,
}
```

From src-tauri/crates/uc-tauri/src/commands/clipboard.rs (get_clipboard_item):

```rust
// Currently has binary is_image check, builds ClipboardItemDto with link: None always
// proj.content_type is a MIME string (e.g. "text/plain", "text/uri-list", "image/png")
// proj.preview contains the text content for text entries
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create link_utils module and extend classify_snapshot</name>
  <files>src-tauri/crates/uc-core/Cargo.toml, src-tauri/crates/uc-core/src/clipboard/link_utils.rs, src-tauri/crates/uc-core/src/clipboard/mod.rs, src-tauri/crates/uc-core/src/settings/content_type_filter.rs</files>
  <behavior>
    - is_single_url("https://github.com") => true
    - is_single_url("https://github.com/repo?q=1#section") => true
    - is_single_url("ftp://files.example.com/doc.pdf") => true
    - is_single_url("mailto:user@example.com") => true
    - is_single_url("  https://github.com  ") => true (trimmed)
    - is_single_url("see https://github.com") => false (mixed content)
    - is_single_url("not a url") => false
    - is_single_url("") => false
    - parse_uri_list("https://a.com\nhttps://b.com") => ["https://a.com", "https://b.com"]
    - parse_uri_list("# comment\nhttps://a.com\n\nhttps://b.com") => ["https://a.com", "https://b.com"]
    - extract_domain("https://github.com/repo") => Some("github.com")
    - extract_domain("mailto:user@example.com") => None (host_str is None for mailto)
    - classify_snapshot with text/plain "https://github.com" => Link
    - classify_snapshot with text/plain "hello world" => Text
    - classify_snapshot with text/plain "see https://github.com" => Text
    - classify_snapshot with text/uri-list => Link (existing behavior preserved)
    - is_content_type_allowed(Link, ct{link: false}) => false
    - is_content_type_allowed(Link, ct{link: true}) => true
  </behavior>
  <action>
1. Add `url = "2"` to `src-tauri/crates/uc-core/Cargo.toml` dependencies.

2. Create `src-tauri/crates/uc-core/src/clipboard/link_utils.rs` with:
   - `pub fn is_single_url(text: &str) -> bool` — trim, check not empty, check no whitespace, validate with `url::Url::parse`. Per user decision: entire text (after trim) must be a single valid URL.
   - `pub fn parse_uri_list(content: &str) -> Vec<String>` — line-by-line, trim each line, skip empty and `#`-prefixed comment lines, collect remaining as strings (per RFC 2483).
   - `pub fn extract_domain(url_str: &str) -> Option<String>` — parse with `url::Url::parse`, return `host_str()` as owned String.

3. Add `pub mod link_utils;` to `src-tauri/crates/uc-core/src/clipboard/mod.rs`.

4. Update `classify_snapshot` in `content_type_filter.rs`:
   - In the `"text/plain"` match arm, before returning `Text`, check if the content bytes decode to UTF-8 and pass `is_single_url` check. If so, return `ContentTypeCategory::Link`.
   - Import `link_utils::is_single_url` from the clipboard module.
   - The function already has access to `rep.bytes` via `SystemClipboardSnapshot`.

5. Update `is_content_type_allowed`:
   - Move `ContentTypeCategory::Link` out of the "always true" group.
   - Add: `ContentTypeCategory::Link => ct.link`

6. Update existing tests:
   - Fix `unimplemented_types_always_allowed` test: remove `Link` from the assertion group (Link is now filterable).
   - Update `make_snapshot` helper to accept bytes parameter for URL detection tests.
   - Add new tests for: plain text URL classified as Link, mixed text stays as Text, Link toggle filtering.
     </action>
     <verify>
     <automated>cd src-tauri && cargo test -p uc-core --lib -- settings::content_type_filter::tests && cargo test -p uc-core --lib -- clipboard::link_utils</automated>
     </verify>
     <done>
   - link_utils module exists with is_single_url, parse_uri_list, extract_domain
   - classify_snapshot returns Link for text/plain containing a single URL
   - classify_snapshot returns Text for text/plain with mixed content
   - is_content_type_allowed returns ct.link for Link category
   - All existing tests pass, new tests cover URL detection edge cases
     </done>
     </task>

<task type="auto">
  <name>Task 2: Add ClipboardLinkItemDto and populate link field in get_clipboard_item</name>
  <files>src-tauri/crates/uc-tauri/src/models/mod.rs, src-tauri/crates/uc-tauri/src/commands/clipboard.rs</files>
  <action>
1. In `src-tauri/crates/uc-tauri/src/models/mod.rs`:
   - Add `ClipboardLinkItemDto` struct with `pub urls: Vec<String>` and `pub domains: Vec<String>`, deriving Debug, Clone, Serialize, Deserialize.
   - Change `ClipboardItemDto.link` from `Option<serde_json::Value>` to `Option<ClipboardLinkItemDto>`.
   - Add `ClipboardLinkItemDto` to the import in `clipboard.rs`.

2. In `src-tauri/crates/uc-tauri/src/commands/clipboard.rs`, update the `get_clipboard_item` command:
   - After finding the projection, detect if it's a link entry. Two conditions:
     a. `proj.content_type == "text/uri-list"` — parse URLs with `parse_uri_list(&proj.preview)`
     b. `proj.content_type` starts with "text/plain" AND `is_single_url(&proj.preview)` — single URL wrapped in vec
   - For link entries: extract domains using `extract_domain` for each URL. Build `ClipboardLinkItemDto { urls, domains }`.
   - Build `ClipboardItemDto` with `link: Some(link_dto)`, text/image/file/code/unknown all None.
   - For non-link entries: keep existing image/text logic unchanged.
   - Import `uc_core::clipboard::link_utils::{parse_uri_list, is_single_url, extract_domain}`.

3. The `transformProjectionToResponse` in the frontend also builds ClipboardItemDto — that will be handled in Plan 02 (frontend). The backend `get_clipboard_item` is the authoritative path for item detail.

4. Ensure `cargo check -p uc-tauri` passes with no warnings.
   </action>
   <verify>
   <automated>cd src-tauri && cargo check -p uc-tauri && cargo test -p uc-tauri</automated>
   </verify>
   <done> - ClipboardLinkItemDto struct exists with urls and domains fields - ClipboardItemDto.link is typed as Option<ClipboardLinkItemDto> - get_clipboard_item returns populated link data for text/uri-list and plain-text URL entries - Existing text and image entries continue to work unchanged
   </done>
   </task>

</tasks>

<verification>
```bash
cd src-tauri && cargo test -p uc-core && cargo test -p uc-tauri && cargo check
```
All tests pass. Link classification, utility functions, DTO structure, and command mapping all work correctly.
</verification>

<success_criteria>

- classify_snapshot detects both text/uri-list and plain-text single URLs as Link
- is_content_type_allowed respects ct.link toggle for Link category
- get_clipboard_item returns ClipboardLinkItemDto with urls and domains for link entries
- All existing tests continue to pass
  </success_criteria>

<output>
After completion, create `.planning/phases/28-support-link-content-type-mime-link-and-url-detected-plain-text/28-01-SUMMARY.md`
</output>
