---
phase: 42.1
slug: refactor-cli-bootstrap-to-extract-shared-runtime-initialization
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-19
---

# Phase 42.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property               | Value                                                                |
| ---------------------- | -------------------------------------------------------------------- |
| **Framework**          | cargo test (Rust)                                                    |
| **Config file**        | `src-tauri/Cargo.toml`                                               |
| **Quick run command**  | `cd src-tauri && cargo test -p uc-cli --test cli_smoke`              |
| **Full suite command** | `cd src-tauri && cargo test -p uc-bootstrap && cargo test -p uc-cli` |
| **Estimated runtime**  | ~20 seconds                                                          |

---

## Sampling Rate

- **After every task commit:** Run `cd src-tauri && cargo test -p uc-cli --test cli_smoke`
- **After every plan wave:** Run `cd src-tauri && cargo test -p uc-bootstrap && cargo test -p uc-cli`
- **Before `$gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID                     | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status     |
| --------------------------- | ---- | ---- | ----------- | --------- | ----------------- | ----------- | ---------- |
| _Populated during planning_ |      |      |             |           |                   |             | ⬜ pending |

_Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky_

---

## Wave 0 Requirements

- [ ] Add helper-focused `uc-bootstrap` tests if current coverage does not lock down direct-runtime bootstrap behavior
- [ ] Add grep-verifiable checks proving command modules no longer inline raw bootstrap calls

_Existing Cargo test infrastructure covers all framework requirements._

---

## Manual-Only Verifications

| Behavior                                                            | Requirement          | Why Manual                                                                                                          | Test Instructions                                                                                                                                  |
| ------------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| CLI human-readable output remains unchanged after helper extraction | REFACTOR-CLI-BOOT-05 | Existing automated tests may focus on exit codes and JSON; human formatting regressions are easier to spot manually | Run `uniclipboard-cli devices`, `space-status`, and `clipboard list` against a dev profile and compare output shape with pre-refactor expectations |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
