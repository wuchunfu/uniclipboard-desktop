//! Space status command — shows encryption state via direct bootstrap (no daemon required).

use crate::exit_codes;

/// Placeholder for Task 2 implementation.
pub async fn run(_json: bool) -> i32 {
    eprintln!("Error: space-status command not yet implemented");
    exit_codes::EXIT_ERROR
}
