---
phase: 56.1
slug: eliminate-hardcoded-strings-in-pairing-setup-flow
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-25
---

# Phase 56.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property               | Value                                                                    |
| ---------------------- | ------------------------------------------------------------------------ |
| **Framework**          | cargo test (Rust), vitest (TypeScript)                                   |
| **Config file**        | `src-tauri/Cargo.toml`, `vitest.config.ts`                               |
| **Quick run command**  | `cd src-tauri && cargo test -p uc-core --lib daemon_api`                 |
| **Full suite command** | `cd src-tauri && cargo test -p uc-core -p uc-daemon -p uc-daemon-client` |
| **Estimated runtime**  | ~30 seconds                                                              |

---

## Sampling Rate

- **After every task commit:** Run `cd src-tauri && cargo test -p uc-core --lib daemon_api`
- **After every plan wave:** Run `cd src-tauri && cargo test -p uc-core -p uc-daemon -p uc-daemon-client`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID    | Plan | Wave | Requirement | Test Type    | Automated Command                                 | File Exists | Status     |
| ---------- | ---- | ---- | ----------- | ------------ | ------------------------------------------------- | ----------- | ---------- |
| 56.1-01-01 | 01   | 1    | N/A         | compile      | `cd src-tauri && cargo check -p uc-core`          | N/A         | ⬜ pending |
| 56.1-01-02 | 01   | 1    | N/A         | compile+grep | `cd src-tauri && cargo check -p uc-daemon`        | N/A         | ⬜ pending |
| 56.1-02-01 | 02   | 1    | N/A         | compile+grep | `cd src-tauri && cargo check -p uc-daemon`        | N/A         | ⬜ pending |
| 56.1-03-01 | 03   | 2    | N/A         | compile+grep | `cd src-tauri && cargo check -p uc-daemon-client` | N/A         | ⬜ pending |

_Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky_

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. This phase is a pure refactor (extracting strings to constants) — no new test framework needed.

---

## Manual-Only Verifications

| Behavior                          | Requirement | Why Manual                  | Test Instructions                                               |
| --------------------------------- | ----------- | --------------------------- | --------------------------------------------------------------- |
| WS topic subscription still works | N/A         | Requires running daemon+GUI | Start daemon, connect GUI, verify WS topics subscribe correctly |
| Pairing flow end-to-end           | N/A         | Requires two devices        | Run peerA + peerB, complete pairing flow                        |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
