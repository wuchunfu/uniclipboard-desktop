---
phase: 46.3-gui-daemon
plan: 01
type: execute
wave: 1
depends_on: ['45-03', '46.1-03', '46.2-03']
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/crates/uc-daemon/Cargo.toml
  - src-tauri/crates/uc-tauri/Cargo.toml
  - src-tauri/crates/uc-daemon/src/lib.rs
  - src-tauri/crates/uc-daemon/src/api/types.rs
  - src-tauri/crates/uc-daemon/src/api/query.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/run.rs
autonomous: true
requirements: ['GUI-DMN-01', 'GUI-DMN-05']
user_setup: []
must_haves:
  truths:
    - 'The daemon health contract exposes a shared packageVersion and apiRevision that identify the exact local UniClipboard build.'
    - 'GUI bootstrap can classify the expected local daemon endpoint as absent, compatible, or incompatible before any replacement logic runs.'
    - 'Malformed or legacy /health responses are treated as incompatible, not as temporarily healthy.'
  artifacts:
    - path: 'src-tauri/Cargo.toml'
      provides: 'Workspace-level release version source shared by GUI and daemon crates.'
      min_lines: 40
    - path: 'src-tauri/crates/uc-daemon/src/api/types.rs'
      provides: 'Health/status DTOs with packageVersion/apiRevision fields.'
      min_lines: 20
    - path: 'src-tauri/crates/uc-daemon/src/api/query.rs'
      provides: 'Daemon query responses populated from the shared release contract.'
      min_lines: 40
    - path: 'src-tauri/crates/uc-tauri/src/bootstrap/run.rs'
      provides: 'Compatibility probe classification for the expected local daemon endpoint.'
      min_lines: 180
  key_links:
    - from: 'src-tauri/Cargo.toml'
      to: 'src-tauri/crates/uc-daemon/Cargo.toml'
      via: 'workspace.package version inheritance keeps GUI and daemon on one release identifier.'
      pattern: 'workspace.package|version.workspace'
    - from: 'src-tauri/crates/uc-daemon/src/api/query.rs'
      to: 'src-tauri/crates/uc-daemon/src/api/types.rs'
      via: 'HealthResponse and StatusResponse serialize packageVersion/apiRevision for bootstrap compatibility checks.'
      pattern: 'package_version|api_revision'
    - from: 'src-tauri/crates/uc-tauri/src/bootstrap/run.rs'
      to: 'src-tauri/crates/uc-daemon/src/api/types.rs'
      via: 'run.rs decodes the daemon health contract and maps it into an explicit compatibility outcome.'
      pattern: 'HealthResponse|ProbeOutcome|IncompatibleDaemon'
---

<objective>
建立 46.3 的兼容判定根基：先把 GUI/daemon 的共享发布版本与 daemon health 契约收紧，再把 bootstrap probe 从布尔健康检查升级成明确的兼容分类。

Purpose: 先修正“版本源不一致 + health 只会返回 ok”的结构性问题，避免后续 replacement 与 startup gating 建在错误前提上。
Output: 共享 release version 来源、带 `packageVersion`/`apiRevision` 的 daemon health/status DTO、以及明确的 probe compatibility outcome。
</objective>

<execution_context>
Use the standard execute-phase workflow already configured for this repository.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46.3-gui-daemon/46.3-CONTEXT.md
@.planning/phases/46.3-gui-daemon/46.3-RESEARCH.md
@.planning/phases/46.3-gui-daemon/46.3-VALIDATION.md
@.planning/phases/45-daemon-api-foundation-add-local-http-and-websocket-transport-with-read-only-runtime-queries/45-03-SUMMARY.md
@src-tauri/Cargo.toml
@src-tauri/crates/uc-daemon/Cargo.toml
@src-tauri/crates/uc-tauri/Cargo.toml
@src-tauri/crates/uc-daemon/src/lib.rs
@src-tauri/crates/uc-daemon/src/api/types.rs
@src-tauri/crates/uc-daemon/src/api/query.rs
@src-tauri/crates/uc-tauri/src/bootstrap/run.rs

<interfaces>
From `src-tauri/crates/uc-daemon/src/lib.rs`:
```rust
pub const DAEMON_VERSION: &str = env!("CARGO_PKG_VERSION");
pub const DAEMON_API_REVISION: &str = "setup-pairing-http-routes-v1";
```

From `src-tauri/crates/uc-daemon/src/api/types.rs`:

```rust
pub struct HealthResponse {
    pub status: String,
    pub version: String,
}

pub struct StatusResponse {
    pub version: String,
    pub uptime_seconds: u64,
    pub workers: Vec<WorkerStatusDto>,
    pub connected_peers: u32,
}
```

From `src-tauri/crates/uc-tauri/src/bootstrap/run.rs`:

```rust
pub async fn bootstrap_daemon_connection(
    state: &DaemonConnectionState,
) -> Result<DaemonConnectionInfo, DaemonBootstrapError>;

pub async fn probe_daemon_health(
    client: &reqwest::Client,
) -> Result<bool, DaemonBootstrapError>;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 收敛共享 release version 与 daemon health/status 兼容元数据</name>
  <files>
    src-tauri/Cargo.toml,
    src-tauri/crates/uc-daemon/Cargo.toml,
    src-tauri/crates/uc-tauri/Cargo.toml,
    src-tauri/crates/uc-daemon/src/lib.rs,
    src-tauri/crates/uc-daemon/src/api/types.rs,
    src-tauri/crates/uc-daemon/src/api/query.rs
  </files>
  <read_first>
    - src-tauri/Cargo.toml
    - src-tauri/crates/uc-daemon/Cargo.toml
    - src-tauri/crates/uc-tauri/Cargo.toml
    - src-tauri/crates/uc-daemon/src/lib.rs
    - src-tauri/crates/uc-daemon/src/api/types.rs
    - src-tauri/crates/uc-daemon/src/api/query.rs
    - .planning/phases/46.3-gui-daemon/46.3-RESEARCH.md
    - .planning/phases/46.3-gui-daemon/46.3-VALIDATION.md
  </read_first>
  <action>
    1. 先运行 `cd src-tauri && cargo test -p uc-tauri startup_helper_treats_timeout_as_error -- --nocapture` 作为 46.3 的 bootstrap 基线检查；如果此命令失败，先修复当前 worktree 的 bootstrap 漂移，再继续本任务。
    2. 在 `src-tauri/Cargo.toml` 中新增 `[workspace.package] version = "0.4.0-alpha.1"`，然后把 `src-tauri/crates/uc-daemon/Cargo.toml` 与 `src-tauri/crates/uc-tauri/Cargo.toml` 的版本声明改成 `version.workspace = true`，消除 GUI crate / daemon crate 各自维持 `0.1.0` 的分叉版本源。
    3. 保留 `src-tauri/crates/uc-daemon/src/lib.rs` 中的 `DAEMON_VERSION` 与 `DAEMON_API_REVISION` 常量，但让 `DAEMON_VERSION` 现在从共享 workspace version 得到 `0.4.0-alpha.1`，不再把 crate 私有版本当成 GUI/daemon 兼容依据。
    4. 在 `src-tauri/crates/uc-daemon/src/api/types.rs` 中，把 `HealthResponse` 与 `StatusResponse` 从单一 `version` 字段改成显式 `package_version` 与 `api_revision` 字段；保持 `#[serde(rename_all = "camelCase")]`，让 JSON 输出为 `packageVersion` / `apiRevision`。
    5. 在 `src-tauri/crates/uc-daemon/src/api/query.rs` 中，统一用 `DAEMON_VERSION` 和 `DAEMON_API_REVISION` 填充 `/health` 与 `/status`，不要再直接内联 `env!("CARGO_PKG_VERSION")`。
    6. 添加或扩展 daemon 侧测试，明确断言 `/health` 与 `/status` JSON 中存在 `packageVersion` 与 `apiRevision`，且不再出现旧的顶层 `version` 字段。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-daemon api_query</automated>
  </verify>
  <acceptance_criteria>
    - `rg "workspace.package" src-tauri/Cargo.toml -n` 返回新增的 workspace package 段。
    - `rg "version\\.workspace = true" src-tauri/crates/uc-daemon/Cargo.toml src-tauri/crates/uc-tauri/Cargo.toml -n` 返回 2 处 workspace 版本继承。
    - `rg "package_version|api_revision" src-tauri/crates/uc-daemon/src/api/types.rs src-tauri/crates/uc-daemon/src/api/query.rs -n` 返回 health/status DTO 与查询填充代码。
    - `! rg "pub version: String" src-tauri/crates/uc-daemon/src/api/types.rs -n` 退出码为 0。
    - `cd src-tauri && cargo test -p uc-daemon api_query` 退出码为 0。
    - `cd src-tauri && cargo test -p uc-tauri startup_helper_treats_timeout_as_error -- --nocapture` 退出码为 0。
  </acceptance_criteria>
  <done>
    - daemon `/health` 与 `/status` 暴露可信的 `packageVersion` 与 `apiRevision`。
    - GUI 与 daemon 使用同一个发布版本源，后续“同包一致优先”的兼容判定具备结构基础。
  </done>
</task>

<task type="auto">
  <name>Task 2: 把 bootstrap probe 从 bool 提升为显式 compatibility outcome</name>
  <files>
    src-tauri/crates/uc-tauri/src/bootstrap/run.rs
  </files>
  <read_first>
    - src-tauri/crates/uc-tauri/src/bootstrap/run.rs
    - src-tauri/crates/uc-daemon/src/api/types.rs
    - src-tauri/crates/uc-daemon/src/api/query.rs
    - src-tauri/crates/uc-daemon/src/lib.rs
    - .planning/phases/46.3-gui-daemon/46.3-CONTEXT.md
    - .planning/phases/46.3-gui-daemon/46.3-RESEARCH.md
  </read_first>
  <action>
    1. 在 `src-tauri/crates/uc-tauri/src/bootstrap/run.rs` 中新增 `ProbeOutcome` 枚举，固定三种分类：`Absent`、`Compatible(HealthResponse)`、`Incompatible { details: String, observed_package_version: Option<String>, observed_api_revision: Option<String> }`。
    2. 把 `probe_daemon_health` / `probe_daemon_health_at` 的职责从“返回 bool”改成“读取 `/health` 后给出 `ProbeOutcome`”，具体规则固定为：
       - 请求连接失败或超时：`Absent`
       - `/health` 非 2xx：`Absent`
       - `/health` JSON 解码失败、缺少 `packageVersion`/`apiRevision`、状态不是 `ok`、`packageVersion` 与 GUI 当前 `env!("CARGO_PKG_VERSION")` 不一致、`apiRevision` 与 `uc_daemon::DAEMON_API_REVISION` 不一致：`Incompatible`
       - 只有全部满足时才返回 `Compatible(...)`
    3. 保留 `DaemonBootstrapError::IncompatibleDaemon`，但让它来自 `ProbeOutcome::Incompatible` 的细化信息，而不是把所有探测异常粗暴折叠成 `Probe`。
    4. 更新 `run.rs` 现有测试，并补一个新的兼容探测测试，明确覆盖“健康但版本不一致”“健康但 api revision 不一致”“响应格式损坏”都落入 `Incompatible`，而不是 ready。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-tauri startup_helper_rejects_healthy_but_incompatible_daemon -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg "enum ProbeOutcome" src-tauri/crates/uc-tauri/src/bootstrap/run.rs -n` 返回新枚举定义。
    - `rg "observed_package_version|observed_api_revision" src-tauri/crates/uc-tauri/src/bootstrap/run.rs -n` 返回不兼容探测细节字段。
    - `rg "CARGO_PKG_VERSION|DAEMON_API_REVISION" src-tauri/crates/uc-tauri/src/bootstrap/run.rs -n` 返回版本与契约修订比较逻辑。
    - `! rg "Future<Output = Result<bool, DaemonBootstrapError>>" src-tauri/crates/uc-tauri/src/bootstrap/run.rs -n` 退出码为 0。
    - `cd src-tauri && cargo test -p uc-tauri startup_helper_rejects_healthy_but_incompatible_daemon -- --nocapture` 退出码为 0。
  </acceptance_criteria>
  <done>
    - GUI bootstrap 在 replacement 之前就能区分“没 daemon”和“有 daemon 但不兼容”。
    - legacy / malformed health 响应不再误导启动链路进入 ready。
  </done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo test -p uc-daemon api_query` 通过，证明 health/status 契约包含 `packageVersion` 与 `apiRevision`。
- `cd src-tauri && cargo test -p uc-tauri startup_helper_rejects_healthy_but_incompatible_daemon -- --nocapture` 通过，证明 bootstrap probe 已能判定 incompatible daemon。
</verification>

<success_criteria>

- `GUI-DMN-05` 达成：daemon health/status 具备共享 packageVersion/apiRevision 契约。
- `GUI-DMN-01` 的前半段达成：bootstrap probe 能在 replacement 前做出明确兼容结论。
  </success_criteria>

<output>
完成后创建 `.planning/phases/46.3-gui-daemon/46.3-01-SUMMARY.md`，总结共享版本契约与 compatibility probe 的最终形态。
</output>
