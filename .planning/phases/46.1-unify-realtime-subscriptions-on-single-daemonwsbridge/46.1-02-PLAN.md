---
phase: 46.1
plan: 2
name: 'App Realtime Consumers And Setup Event Hub'
type: execute
wave: 2
depends_on:
  - 46.1-01
files_modified:
  - src-tauri/crates/uc-app/src/realtime/mod.rs
  - src-tauri/crates/uc-app/src/realtime/pairing_consumer.rs
  - src-tauri/crates/uc-app/src/realtime/peers_consumer.rs
  - src-tauri/crates/uc-app/src/realtime/setup_consumer.rs
  - src-tauri/crates/uc-app/src/lib.rs
  - src-tauri/crates/uc-app/src/usecases/setup/pairing_facade.rs
  - src-tauri/crates/uc-app/tests/realtime_consumers.rs
autonomous: true
requirements:
  - PH461-03
must_haves:
  truths:
    - 'uc-app owns the pairing, peers, and setup consumers that subscribe through `RealtimeTopicPort` instead of opening websocket clients'
    - 'setup flow can receive pairing-domain updates from a shared realtime-fed hub instead of a facade-owned websocket'
    - 'frontend envelope mapping logic lives in app consumers rather than in the bridge transport loop'
  artifacts:
    - path: src-tauri/crates/uc-app/src/realtime/pairing_consumer.rs
      provides: pairing topic consumer producing unified frontend realtime envelopes
      contains: run_pairing_realtime_consumer
    - path: src-tauri/crates/uc-app/src/realtime/setup_consumer.rs
      provides: setup pairing event hub fed from the shared realtime port
      contains: SetupPairingEventHub
    - path: src-tauri/crates/uc-app/tests/realtime_consumers.rs
      provides: unit coverage for consumer fanout and setup subscription behavior
      contains: setup_consumer_reuses_shared_realtime_hub
  key_links:
    - from: src-tauri/crates/uc-app/src/realtime/setup_consumer.rs
      to: src-tauri/crates/uc-app/src/usecases/setup/pairing_facade.rs
      via: setup facade subscribe path reads from shared event hub instead of websocket ownership
      pattern: SetupPairingEventHub
    - from: src-tauri/crates/uc-app/src/realtime/pairing_consumer.rs
      to: src-tauri/crates/uc-core/src/ports/host_event_emitter.rs
      via: consumer emits HostEvent::Realtime envelopes
      pattern: HostEvent::Realtime
---

<objective>
Move realtime business consumption into `uc-app`.

Purpose: pairing, peers, and setup flows should consume one shared typed port and stop owning transport lifecycle or daemon DTO parsing.
Output: realtime consumer modules, a setup pairing event hub, and uc-app tests covering shared subscription behavior.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-CONTEXT.md
@.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-RESEARCH.md
@.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-VALIDATION.md
@.planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-04-SUMMARY.md
@src-tauri/crates/uc-app/src/usecases/setup/pairing_facade.rs
@src-tauri/crates/uc-core/src/ports/realtime.rs
@src-tauri/crates/uc-core/src/ports/host_event_emitter.rs

<interfaces>
From `src-tauri/crates/uc-app/src/usecases/setup/pairing_facade.rs`:

```rust
pub trait SetupPairingFacadePort: Send + Sync {
    async fn subscribe(&self) -> Result<tokio::sync::mpsc::Receiver<PairingDomainEvent>>;
    async fn initiate_pairing(&self, peer_id: String) -> Result<String>;
    async fn accept_pairing(&self, session_id: &str) -> Result<()>;
    async fn reject_pairing(&self, session_id: &str) -> Result<()>;
    async fn cancel_pairing(&self, session_id: &str) -> Result<()>;
    async fn verify_pairing(&self, session_id: &str, pin_matches: bool) -> Result<()>;
}
```

Do not change command semantics in this plan. Only replace the websocket-owning subscription path with a shared realtime-fed app abstraction.
</interfaces>
</context>

<tasks>

<task id="46.1-02-01" type="auto">
  <name>Task 1: Create failing uc-app consumer tests for pairing, peers, and setup hub behavior</name>
  <files>
src-tauri/crates/uc-app/tests/realtime_consumers.rs
src-tauri/crates/uc-app/src/realtime/mod.rs
  </files>
  <read_first>
src-tauri/crates/uc-app/src/usecases/setup/pairing_facade.rs
src-tauri/crates/uc-core/src/ports/realtime.rs
src-tauri/crates/uc-core/src/ports/host_event_emitter.rs
.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-RESEARCH.md
.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-VALIDATION.md
  </read_first>
  <action>
Create `src-tauri/crates/uc-app/tests/realtime_consumers.rs` as the Wave 0 uc-app test target referenced in `46.1-VALIDATION.md`. Add failing tests for these exact cases:

- `pairing_consumer_maps_typed_realtime_events_to_daemon_realtime_envelope`
- `peers_consumer_emits_peer_delta_envelopes_without_transport_dto_leakage`
- `setup_consumer_reuses_shared_realtime_hub`
- `setup_consumer_filters_session_ordering_before_forwarding`

Add `src-tauri/crates/uc-app/src/realtime/mod.rs` with placeholder module exports so the test target can compile against the planned module names.
</action>
<acceptance_criteria>

- `src-tauri/crates/uc-app/tests/realtime_consumers.rs` exists
- `src-tauri/crates/uc-app/tests/realtime_consumers.rs` contains `pairing_consumer_maps_typed_realtime_events_to_daemon_realtime_envelope`
- `src-tauri/crates/uc-app/tests/realtime_consumers.rs` contains `setup_consumer_reuses_shared_realtime_hub`
- `src-tauri/crates/uc-app/src/realtime/mod.rs` exists
  </acceptance_criteria>
  <verify>
  <automated>cd src-tauri && cargo test -p uc-app realtime_consumer -- --test-threads=1</automated>
  </verify>
  <done>
  The app-layer consumer contract and expected setup-hub behavior are captured in a dedicated failing test target before implementation starts.
  </done>
  </task>

<task id="46.1-02-02" type="auto">
  <name>Task 2: Implement uc-app realtime consumers and a shared setup pairing event hub</name>
  <files>
src-tauri/crates/uc-app/src/realtime/mod.rs
src-tauri/crates/uc-app/src/realtime/pairing_consumer.rs
src-tauri/crates/uc-app/src/realtime/peers_consumer.rs
src-tauri/crates/uc-app/src/realtime/setup_consumer.rs
src-tauri/crates/uc-app/src/lib.rs
src-tauri/crates/uc-app/src/usecases/setup/pairing_facade.rs
src-tauri/crates/uc-app/tests/realtime_consumers.rs
  </files>
  <read_first>
src-tauri/crates/uc-app/src/realtime/mod.rs
src-tauri/crates/uc-app/tests/realtime_consumers.rs
src-tauri/crates/uc-app/src/usecases/setup/pairing_facade.rs
src-tauri/crates/uc-core/src/ports/realtime.rs
src-tauri/crates/uc-core/src/ports/host_event_emitter.rs
  </read_first>
  <action>
Implement the app consumer layer with these exact exports:

- `run_pairing_realtime_consumer(...)` in `pairing_consumer.rs`
- `run_peers_realtime_consumer(...)` in `peers_consumer.rs`
- `pub struct SetupPairingEventHub` plus `run_setup_realtime_consumer(...)` in `setup_consumer.rs`

Use `Arc<dyn RealtimeTopicPort>` as the only inbound subscription dependency. Pairing and peers consumers must map `RealtimeEvent` into `HostEvent::Realtime(RealtimeFrontendEvent)` using the exact contract name `daemon://realtime`.

`SetupPairingEventHub` must expose a shared subscribe surface for `PairingDomainEvent` so `SetupPairingFacadePort::subscribe()` can be implemented without facade-owned websocket code. Modify `src-tauri/crates/uc-app/src/usecases/setup/pairing_facade.rs` only as needed to support hub-backed subscription semantics; do not add uc-tauri or websocket imports there.
</action>
<acceptance_criteria>

- `src-tauri/crates/uc-app/src/realtime/pairing_consumer.rs` contains `run_pairing_realtime_consumer`
- `src-tauri/crates/uc-app/src/realtime/peers_consumer.rs` contains `run_peers_realtime_consumer`
- `src-tauri/crates/uc-app/src/realtime/setup_consumer.rs` contains `pub struct SetupPairingEventHub`
- `src-tauri/crates/uc-app/src/realtime/setup_consumer.rs` contains `run_setup_realtime_consumer`
- `src-tauri/crates/uc-app/src/realtime/setup_consumer.rs` contains `PairingDomainEvent`
- `src-tauri/crates/uc-app/src/usecases/setup/pairing_facade.rs` contains `SetupPairingEventHub`
- `src-tauri/crates/uc-app/tests/realtime_consumers.rs` contains `daemon://realtime`
- `cd src-tauri && cargo test -p uc-app realtime_consumer -- --test-threads=1` exits 0
  </acceptance_criteria>
  <verify>
  <automated>cd src-tauri && cargo test -p uc-app realtime_consumer -- --test-threads=1</automated>
  </verify>
  <done>
  `uc-app` owns typed realtime consumption for pairing, peers, and setup, and the setup flow now has a shared app-layer subscription hub instead of a websocket-owned facade path.
  </done>
  </task>

</tasks>

<verification>
- `cd src-tauri && cargo test -p uc-app realtime_consumer -- --test-threads=1`
</verification>

<success_criteria>

- Pairing, peers, and setup each have explicit app-layer consumer modules.
- The setup subscription path is compatible with shared realtime delivery instead of direct websocket ownership.
- No uc-app realtime code imports `uc_daemon::api::types::DaemonWsEvent`.
  </success_criteria>

<output>
After completion, create `.planning/phases/46.1-unify-realtime-subscriptions-on-single-daemonwsbridge/46.1-02-SUMMARY.md`
</output>
