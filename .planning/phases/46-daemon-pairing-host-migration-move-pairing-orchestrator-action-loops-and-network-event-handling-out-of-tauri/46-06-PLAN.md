---
phase: 46
plan: 6
name: 'Gap Closure For Daemon Pairing Regression Tests And PH46 Traceability'
type: execute
wave: 6
depends_on: ['46-05']
files_modified:
  - src-tauri/crates/uc-daemon/tests/pairing_host.rs
  - src-tauri/crates/uc-daemon/tests/pairing_api.rs
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - PH46-01
  - PH46-01A
  - PH46-01B
  - PH46-02
  - PH46-03
  - PH46-03A
  - PH46-04
  - PH46-05
  - PH46-05A
  - PH46-06
must_haves:
  truths:
    - 'Phase 46 daemon regression tests compile and pass with the current DaemonPairingHost constructor contract'
    - 'Phase 46 requirement IDs are present in REQUIREMENTS.md and mapped to phase 46 in traceability'
    - 'Requirement audit can trace all PH46 IDs from plans and roadmap into REQUIREMENTS.md'
  artifacts:
    - path: 'src-tauri/crates/uc-daemon/tests/pairing_host.rs'
      provides: 'daemon host continuity regression coverage against the new constructor signature'
      contains: 'broadcast::channel::<DaemonWsEvent>'
    - path: 'src-tauri/crates/uc-daemon/tests/pairing_api.rs'
      provides: 'daemon API regression coverage against the new constructor signature'
      contains: 'broadcast::channel::<DaemonWsEvent>'
    - path: '.planning/REQUIREMENTS.md'
      provides: 'phase 46 requirement definitions and traceability rows'
      contains: 'PH46-06'
  key_links:
    - from: 'src-tauri/crates/uc-daemon/tests/pairing_host.rs'
      to: 'src-tauri/crates/uc-daemon/src/pairing/host.rs'
      via: 'DaemonPairingHost::new call includes broadcast::Sender<DaemonWsEvent> in the 7th constructor position'
    - from: 'src-tauri/crates/uc-daemon/tests/pairing_api.rs'
      to: 'src-tauri/crates/uc-daemon/src/pairing/host.rs'
      via: 'API fixture host wiring matches current constructor dependency set'
    - from: '.planning/REQUIREMENTS.md'
      to: '.planning/ROADMAP.md'
      via: 'PH46 IDs listed in roadmap/plans are explicitly mapped in requirements traceability'
---

# Plan 46-06: Gap Closure For Daemon Pairing Regression Tests And PH46 Traceability

## Goal

Close the two remaining Phase 46 verification gaps by restoring compile/runtime verification for daemon-owned pairing host continuity tests and adding complete PH46 requirement traceability entries.

## Context

@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-CONTEXT.md
@.planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-VERIFICATION.md
@src-tauri/crates/uc-daemon/src/pairing/host.rs
@src-tauri/crates/uc-daemon/tests/pairing_host.rs
@src-tauri/crates/uc-daemon/tests/pairing_api.rs

## Tasks

<task id="1" type="auto">
<name>Repair daemon pairing regression fixtures for the updated DaemonPairingHost constructor</name>
<files>
- src-tauri/crates/uc-daemon/tests/pairing_host.rs
- src-tauri/crates/uc-daemon/tests/pairing_api.rs
</files>
<read_first>
- src-tauri/crates/uc-daemon/src/pairing/host.rs
- src-tauri/crates/uc-daemon/tests/pairing_host.rs
- src-tauri/crates/uc-daemon/tests/pairing_api.rs
- .planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-VERIFICATION.md
</read_first>
<action>
Update both test fixtures to satisfy the current `DaemonPairingHost::new(...)` signature that now requires seven arguments, with `event_tx: broadcast::Sender<DaemonWsEvent>` as argument 7.

In `src-tauri/crates/uc-daemon/tests/pairing_host.rs`:

- add `use tokio::sync::broadcast;`
- add `use uc_daemon::api::types::DaemonWsEvent;`
- create the sender with `let (event_tx, _event_rx) = broadcast::channel::<DaemonWsEvent>(128);`
- pass `event_tx` as the final argument in the existing `DaemonPairingHost::new(...)` call.

In `src-tauri/crates/uc-daemon/tests/pairing_api.rs`:

- add `use tokio::sync::broadcast;`
- add `use uc_daemon::api::types::DaemonWsEvent;`
- in `build_api_router()`, create `let (event_tx, _event_rx) = broadcast::channel::<DaemonWsEvent>(128);`
- pass `event_tx` as the final argument in `DaemonPairingHost::new(...)`.

Do not change daemon host business behavior or API route semantics in this task; only repair fixture wiring so regression suites compile and execute.
</action>
<acceptance_criteria>

- `src-tauri/crates/uc-daemon/tests/pairing_host.rs` contains `use tokio::sync::broadcast`
- `src-tauri/crates/uc-daemon/tests/pairing_host.rs` contains `use uc_daemon::api::types::DaemonWsEvent`
- `src-tauri/crates/uc-daemon/tests/pairing_host.rs` contains `broadcast::channel::<DaemonWsEvent>(128)`
- `src-tauri/crates/uc-daemon/tests/pairing_api.rs` contains `use tokio::sync::broadcast`
- `src-tauri/crates/uc-daemon/tests/pairing_api.rs` contains `use uc_daemon::api::types::DaemonWsEvent`
- `src-tauri/crates/uc-daemon/tests/pairing_api.rs` contains `broadcast::channel::<DaemonWsEvent>(128)`
- `cd src-tauri && cargo test -p uc-daemon --test pairing_host -- --test-threads=1` exits 0
- `cd src-tauri && cargo test -p uc-daemon --test pairing_api -- --test-threads=1` exits 0
  </acceptance_criteria>
  <verify>
  <automated>cd src-tauri && cargo test -p uc-daemon --test pairing_host -- --test-threads=1 && cargo test -p uc-daemon --test pairing_api -- --test-threads=1</automated>
  </verify>
  <done>
  The daemon host/API regression suites compile and pass against the current constructor contract, restoring automated evidence for daemon-owned session continuity and API wiring.
  </done>
  </task>

<task id="2" type="auto">
<name>Add Phase 46 requirement definitions and phase traceability rows to REQUIREMENTS.md</name>
<files>
- .planning/REQUIREMENTS.md
</files>
<read_first>
- .planning/REQUIREMENTS.md
- .planning/ROADMAP.md
- .planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-01-PLAN.md
- .planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-02-PLAN.md
- .planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-03-PLAN.md
- .planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-04-PLAN.md
- .planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-05-PLAN.md
- .planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-VERIFICATION.md
</read_first>
<action>
Update `.planning/REQUIREMENTS.md` to add explicit Phase 46 requirement entries and traceability mapping.

Add a new subsection named `### Daemon Pairing Host Migration` under runtime-mode requirements and define these exact IDs:

- `PH46-01`: daemon owns pairing orchestrator/session lifecycle
- `PH46-01A`: discoverability and participant readiness are separately controlled
- `PH46-01B`: headless/CLI daemon remains non-discoverable until explicit opt-in
- `PH46-02`: pairing action execution/event handling run daemon-side and survive Tauri/webview disconnects
- `PH46-03`: daemon exposes pairing mutation surface (initiate/accept/reject/cancel/verify) with sessionId follow-up semantics
- `PH46-03A`: discoverability/readiness mutation APIs support explicit lease semantics
- `PH46-04`: read models remain metadata-only; verification secrets remain authenticated realtime-only
- `PH46-05`: Tauri remains a compatibility bridge that forwards daemon pairing/peer updates into existing frontend event contract
- `PH46-05A`: GUI-hosted daemon is discoverable by default while readiness stays flow-scoped
- `PH46-06`: regression tests validate daemon continuity and setup compatibility

Then extend the `## Traceability` table with one row per PH46 ID, each mapped to phase `46` with status `Complete`.

Do not delete or rewrite unrelated requirement sections.
</action>
<acceptance_criteria>

- `.planning/REQUIREMENTS.md` contains `### Daemon Pairing Host Migration`
- `.planning/REQUIREMENTS.md` contains `PH46-01`
- `.planning/REQUIREMENTS.md` contains `PH46-01A`
- `.planning/REQUIREMENTS.md` contains `PH46-01B`
- `.planning/REQUIREMENTS.md` contains `PH46-02`
- `.planning/REQUIREMENTS.md` contains `PH46-03`
- `.planning/REQUIREMENTS.md` contains `PH46-03A`
- `.planning/REQUIREMENTS.md` contains `PH46-04`
- `.planning/REQUIREMENTS.md` contains `PH46-05`
- `.planning/REQUIREMENTS.md` contains `PH46-05A`
- `.planning/REQUIREMENTS.md` contains `PH46-06`
- `.planning/REQUIREMENTS.md` contains `| PH46-01 | 46 | Complete |`
- `.planning/REQUIREMENTS.md` contains `| PH46-06 | 46 | Complete |`
- `rg -n "PH46-01|PH46-01A|PH46-01B|PH46-02|PH46-03|PH46-03A|PH46-04|PH46-05|PH46-05A|PH46-06" .planning/REQUIREMENTS.md` returns 10+ matches
  </acceptance_criteria>
  <verify>
  <automated>rg -n "PH46-01|PH46-01A|PH46-01B|PH46-02|PH46-03|PH46-03A|PH46-04|PH46-05|PH46-05A|PH46-06" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
  All Phase 46 requirement IDs referenced by roadmap/plans are explicitly defined in REQUIREMENTS.md and traceable to phase 46.
  </done>
  </task>

## Verification

- `cd src-tauri && cargo test -p uc-daemon --test pairing_host -- --test-threads=1 && cargo test -p uc-daemon --test pairing_api -- --test-threads=1`
- `rg -n "PH46-01|PH46-01A|PH46-01B|PH46-02|PH46-03|PH46-03A|PH46-04|PH46-05|PH46-05A|PH46-06" .planning/REQUIREMENTS.md`

## Success Criteria

- Both daemon pairing regression suites compile and pass with the current `DaemonPairingHost::new` dependency list.
- `.planning/REQUIREMENTS.md` contains full PH46 requirement definitions and a complete phase-46 traceability mapping.
- Phase 46 verification has no remaining blocker for missing PH46 requirements or stale daemon pairing test constructors.

## Output

After completion, create `.planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-06-SUMMARY.md`.
