---
phase: 07-redesign-setup-flow-ux-for-cross-platform-consistency
plan: 02
type: execute
wave: 2
depends_on: ['07-01']
files_modified:
  - src/pages/SetupPage.tsx
  - src/pages/setup/WelcomeStep.tsx
  - src/pages/setup/CreatePassphraseStep.tsx
  - src/pages/setup/JoinPickDeviceStep.tsx
  - src/pages/setup/JoinVerifyPassphraseStep.tsx
  - src/pages/setup/PairingConfirmStep.tsx
  - src/pages/setup/SetupDoneStep.tsx
  - src/pages/__tests__/SetupFlow.test.tsx
autonomous: false
requirements:
  - UX-03
  - UX-05
  - UX-06
  - UX-08

must_haves:
  truths:
    - 'All non-Welcome steps render inside StepLayout (no per-step motion.div)'
    - 'WelcomeStep uses vertical card layout (flex-col, no grid-cols-2)'
    - 'Animation direction changes based on forward/backward navigation'
    - 'Step dot indicator shows current progress'
    - 'Security badges use sm: breakpoint instead of lg:'
    - 'No lg: breakpoints remain in any setup flow file'
    - 'All existing tests still pass after migration'
    - 'SetupPage main element uses overflow-hidden for Welcome, overflow-y-auto for other steps'
  artifacts:
    - path: 'src/pages/setup/WelcomeStep.tsx'
      provides: 'Vertical card layout welcome step'
      contains: 'flex-col'
    - path: 'src/pages/setup/CreatePassphraseStep.tsx'
      provides: 'Migrated to StepLayout'
      contains: 'StepLayout'
    - path: 'src/pages/setup/JoinPickDeviceStep.tsx'
      provides: 'Migrated to StepLayout'
      contains: 'StepLayout'
    - path: 'src/pages/setup/JoinVerifyPassphraseStep.tsx'
      provides: 'Migrated to StepLayout'
      contains: 'StepLayout'
    - path: 'src/pages/setup/PairingConfirmStep.tsx'
      provides: "Migrated to StepLayout with variant='centered'"
      contains: 'variant.*centered'
    - path: 'src/pages/setup/SetupDoneStep.tsx'
      provides: "Migrated to StepLayout with variant='centered'"
      contains: 'variant.*centered'
    - path: 'src/pages/SetupPage.tsx'
      provides: 'Direction tracking, dot indicator, overflow handling, badges fix'
      contains: 'StepDotIndicator'
  key_links:
    - from: 'src/pages/setup/CreatePassphraseStep.tsx'
      to: 'src/pages/setup/StepLayout.tsx'
      via: 'import and render StepLayout with slots'
      pattern: 'import.*StepLayout'
    - from: 'src/pages/SetupPage.tsx'
      to: 'src/pages/setup/StepDotIndicator.tsx'
      via: 'renders StepDotIndicator with computed step info'
      pattern: 'StepDotIndicator'
    - from: 'src/pages/SetupPage.tsx'
      to: 'src/pages/setup/ProcessingJoinStep.tsx'
      via: 'replaces inline ProcessingJoinSpace JSX'
      pattern: 'ProcessingJoinStep'
---

<objective>
Migrate all existing step components to use StepLayout, refactor WelcomeStep to vertical layout, update SetupPage with animation direction tracking + dot indicator + overflow handling, and ensure all tests pass.

Purpose: Complete the UX redesign by wiring all components through the new StepLayout and updating the orchestrator.
Output: All setup flow components refactored, SetupPage updated, all tests green.
</objective>

<execution_context>
@/home/wuy6/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuy6/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-redesign-setup-flow-ux-for-cross-platform-consistency/07-CONTEXT.md
@.planning/phases/07-redesign-setup-flow-ux-for-cross-platform-consistency/07-RESEARCH.md
@.planning/phases/07-redesign-setup-flow-ux-for-cross-platform-consistency/07-01-SUMMARY.md

@src/pages/setup/types.ts
@src/pages/SetupPage.tsx
@src/pages/setup/WelcomeStep.tsx
@src/pages/setup/CreatePassphraseStep.tsx
@src/pages/setup/JoinPickDeviceStep.tsx
@src/pages/setup/JoinVerifyPassphraseStep.tsx
@src/pages/setup/PairingConfirmStep.tsx
@src/pages/setup/SetupDoneStep.tsx
@src/pages/**tests**/SetupFlow.test.tsx

<interfaces>
<!-- From Plan 01 outputs (StepLayout, StepDotIndicator, ProcessingJoinStep) -->

From src/pages/setup/types.ts (after Plan 01):

```typescript
interface StepLayoutProps {
  headerLeft?: ReactNode
  headerRight?: ReactNode
  title: string
  subtitle?: string
  children: ReactNode
  footer?: ReactNode
  hint?: string
  error?: string | null
  variant?: 'default' | 'centered'
  direction?: 'forward' | 'backward'
}

interface ProcessingJoinStepProps {
  onCancel: () => void
  loading?: boolean
}
```

From src/pages/setup/StepLayout.tsx:

```typescript
export default function StepLayout(props: StepLayoutProps): JSX.Element
// Wraps content in motion.div with directional slide animation
// Renders: header row, title section, children, error display, footer, hint
```

From src/pages/setup/StepDotIndicator.tsx:

```typescript
export default function StepDotIndicator(props: {
  totalSteps: number
  currentStep: number
}): JSX.Element
// Renders filled dot for currentStep, hollow dots for others
```

From src/pages/setup/ProcessingJoinStep.tsx:

```typescript
export default function ProcessingJoinStep(props: ProcessingJoinStepProps): JSX.Element
// Uses StepLayout variant='centered', shows spinner + device hint + cancel button
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all step components to StepLayout and refactor WelcomeStep</name>
  <files>src/pages/setup/WelcomeStep.tsx, src/pages/setup/CreatePassphraseStep.tsx, src/pages/setup/JoinPickDeviceStep.tsx, src/pages/setup/JoinVerifyPassphraseStep.tsx, src/pages/setup/PairingConfirmStep.tsx, src/pages/setup/SetupDoneStep.tsx</files>
  <action>
    **WelcomeStep.tsx** - Stays independent (no StepLayout per user decision), but change layout:
    - Keep its own motion.div but change animation to match the unified x-axis slide pattern (initial: { opacity: 0, x: 20 }, animate: { opacity: 1, x: 0 }, exit: { opacity: 0, x: -20 }) with transition={{ duration: 0.2, ease: 'easeOut' }}.
    - Change card layout from `grid grid-cols-1 sm:grid-cols-2` to `flex flex-col gap-4` (always vertical)
    - Make cards larger and more prominent: increase padding to p-7 everywhere
    - Replace ALL `lg:` prefixed classes with `sm:` equivalents or remove them:
      - `lg:mb-10` -> `sm:mb-10`
      - `lg:text-4xl` -> `sm:text-4xl`
      - `lg:text-left` -> remove (keep text-center always for vertical layout)
      - `lg:p-7` -> just p-7 (no breakpoint needed)
      - `lg:mt-10` -> `sm:mt-10`
      - `lg:text-left` -> remove
    - Keep the title centered since cards are vertical: remove `lg:text-left`, keep `text-center`
    - Accept `direction` prop from StepProps and use it in motion.div custom variant

    **CreatePassphraseStep.tsx** - Migrate to StepLayout:
    - Remove outer `motion.div` wrapper
    - Remove the back button element (move to StepLayout's headerLeft slot)
    - Remove the title/subtitle section (pass as StepLayout title/subtitle props)
    - Remove the error display section (pass localError as StepLayout's error prop)
    - Remove the footer/hint section (pass as StepLayout's footer/hint props)
    - Keep the form fields (passphrase inputs) as children
    - Create a BackButton inline: `<button onClick={onBack} className="flex items-center gap-1 text-sm text-muted-foreground transition-colors hover:text-foreground"><ArrowLeft className="h-4 w-4" />{tCommon('back')}</button>`
    - Pass to StepLayout: headerLeft={backButton}, title={t('title')}, subtitle={t('subtitle')}, error={localError}, footer={submitButton}, hint={t('hint')}, direction={direction}
    - Remove `AlertCircle` import (StepLayout handles it)
    - Remove `motion` import from framer-motion (no longer needed)

    **JoinPickDeviceStep.tsx** - Migrate to StepLayout:
    - Remove outer `motion.div` wrapper
    - Move back button to headerLeft, refresh button to headerRight
    - Pass title/subtitle to StepLayout
    - Keep error resolution logic, pass resolved errorMessage as StepLayout's error prop
    - Keep peer list / scanning / empty states as children
    - Remove `AlertCircle` import, remove `motion` import
    - Pass direction={direction} to StepLayout

    **JoinVerifyPassphraseStep.tsx** - Migrate to StepLayout:
    - This component has TWO render paths: normal form and mismatchHelp view
    - Both paths should use StepLayout
    - Normal form: headerLeft=backButton, title, subtitle (with peerId display), error=localError, footer=verifyButton, children=passphrase input
    - MismatchHelp view: headerLeft=backButton, title=mismatchHelp.title, subtitle=mismatchHelp.subtitle, children=help text + buttons
    - Remove `motion.div` wrappers from both paths
    - Remove `motion` and `AlertCircle` imports
    - Pass direction={direction} to StepLayout

    **PairingConfirmStep.tsx** - Migrate to StepLayout with variant='centered':
    - Remove outer `motion.div` wrapper
    - Use StepLayout with variant='centered', title, subtitle
    - Children: short code display + peer fingerprint
    - Resolve error to i18n string before passing to StepLayout's error prop: `error === 'PairingRejected' ? t('errors.rejected') : error ? t('errors.generic') : null`
    - Footer: confirm + cancel buttons in flex gap-4
    - Remove `AlertCircle` and `motion` imports
    - Pass direction={direction} to StepLayout

    **SetupDoneStep.tsx** - Migrate to StepLayout with variant='centered':
    - Remove outer `motion.div` wrapper
    - Use StepLayout with variant='centered', title, subtitle
    - Children: checkmark icon
    - Footer: enter button
    - Remove `motion` import
    - Pass direction={direction} to StepLayout

    **Critical anti-patterns to avoid:**
    - Do NOT add `motion.div` in any migrated step component -- StepLayout owns animation
    - Do NOT use `lg:` breakpoints anywhere -- `sm:` only
    - Do NOT use fixed px bracket values -- use rem or Tailwind utilities

  </action>
  <verify>
    <automated>cd /home/wuy6/myprojects/UniClipboard && bunx vitest run src/pages/setup/__tests__/ --reporter=verbose && grep -r "lg:" src/pages/setup/*.tsx | grep -v node_modules | wc -l</automated>
  </verify>
  <done>
    - All 5 non-Welcome steps use StepLayout (import present, no motion.div wrapper)
    - WelcomeStep uses flex-col for vertical card layout, no grid-cols-2, no lg: classes
    - Zero lg: breakpoint usage across all setup step files
    - Existing step-level tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SetupPage with direction tracking, dot indicator, overflow, badges, and wiring</name>
  <files>src/pages/SetupPage.tsx, src/pages/setup/types.ts, src/pages/__tests__/SetupFlow.test.tsx</files>
  <action>
    **types.ts change:**
    - Add `direction?: 'forward' | 'backward'` to base `StepProps` interface so all step prop interfaces inherit it

    **SetupPage.tsx changes:**

    1. **Animation direction tracking** - Add state ordinal system:
       - Create `getStateOrdinal(state: SetupState | null): number` function: Welcome=0, CreateSpaceInputPassphrase=1, ProcessingCreateSpace=2, JoinSpaceSelectDevice=1, JoinSpaceInputPassphrase=2, JoinSpaceConfirmPeer=3, ProcessingJoinSpace=4, Completed=99, null=-1
       - Add `prevStateRef` useRef (can reuse existing setupStateRef or add new one for direction tracking)
       - Compute direction: `const direction = getStateOrdinal(setupState) >= getStateOrdinal(prevStateRef.current) ? 'forward' : 'backward'`
       - Add useEffect to update prevStateRef after each render
       - Pass `direction={direction}` to every step render call in renderStep()

    2. **Replace inline ProcessingJoinSpace JSX** (lines 283-319):
       - Import ProcessingJoinStep from '@/pages/setup/ProcessingJoinStep'
       - Replace the entire `if ('ProcessingJoinSpace' in setupState)` block with: `<ProcessingJoinStep onCancel={() => runAction(() => cancelSetup())} loading={loading} direction={direction} />`
       - Keep ProcessingCreateSpace inline (simple 8-line spinner + message)

    3. **Step dot indicator** - Add StepDotIndicator below the content area:
       - Import StepDotIndicator
       - Compute totalSteps and currentStep based on the flow:
         - If Welcome or loading: don't show dots
         - Create flow: totalSteps=3 (InputPassphrase=0, ProcessingCreate=1, Done=2)
         - Join flow: totalSteps=5 (SelectDevice=0, InputPassphrase=1, ConfirmPeer=2, ProcessingJoin=3, Done=4)
       - Create helper function `getStepInfo(state: SetupState | null): { total: number; current: number } | null`
       - Place the indicator between </main> and the badges div, centered at bottom

    4. **Overflow handling on main**:
       - Add conditional class: when stepKey === 'Welcome' use `overflow-hidden`, otherwise `overflow-y-auto`
       - The existing test (`uses non-scrollable main layout on welcome step`) checks for this

    5. **Security badges breakpoint fix**:
       - Change `lg:flex` to `sm:flex` on the badges container
       - This makes badges visible on screens >= 640px instead of >= 1024px

    6. **Remove lg: breakpoints from SetupPage**:
       - `lg:px-12 lg:py-8` on main -> remove (keep sm:px-6 sm:py-6)
       - `lg:flex` on badges -> `sm:flex`
       - Ensure zero `lg:` classes remain

    7. **Unused imports cleanup in SetupPage.tsx:**
       - Remove `Monitor`, `Smartphone` imports (moved to ProcessingJoinStep)
       - Keep other icons that are still used (Loader2 for ProcessingCreateSpace inline, Shield, Key, Wifi for badges)

    **SetupFlow.test.tsx changes:**
    - Existing tests should still pass since framer-motion is mocked (motion.div -> div)
    - The `overflow-hidden` test for Welcome should pass with the new conditional overflow logic
    - Add a test verifying that step dot indicator is NOT shown on Welcome step
    - Verify the existing 5 tests still pass
    - If any test breaks due to structural changes (e.g., new wrapper divs), fix the selectors

  </action>
  <verify>
    <automated>cd /home/wuy6/myprojects/UniClipboard && bunx vitest run src/pages/ --reporter=verbose && grep -rc "lg:" src/pages/SetupPage.tsx src/pages/setup/*.tsx</automated>
  </verify>
  <done>
    - Direction tracking computes forward/backward based on state ordinal comparison
    - StepDotIndicator rendered with correct total/current for create and join flows
    - No dots shown on Welcome step
    - Main element uses overflow-hidden for Welcome, overflow-y-auto for other steps
    - Security badges visible at sm: breakpoint
    - Zero lg: breakpoints in all setup flow files
    - ProcessingJoinSpace inline JSX replaced with ProcessingJoinStep component
    - All existing + new tests pass: `bunx vitest run src/pages/ --reporter=verbose`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete setup flow UX redesign</name>
  <files>src/pages/SetupPage.tsx</files>
  <action>User verifies the complete setup flow visually in the running application.</action>
  <verify>
    <automated>cd /home/wuy6/myprojects/UniClipboard && bunx vitest run src/pages/ --reporter=verbose</automated>
  </verify>
  <done>User confirms the setup flow looks and behaves correctly across the entire flow.</done>
  <what-built>Complete setup flow UX redesign: StepLayout component, vertical WelcomeStep cards, unified directional animations, step dot indicator, and cross-platform breakpoint consistency.</what-built>
  <how-to-verify>
    1. Run `bun tauri dev` and navigate to the setup flow
    2. On the Welcome page: verify cards are vertically stacked (not side-by-side), centered title
    3. Click "Create new space" -- verify the step slides in from the right
    4. Click "Back" -- verify the step slides in from the left (backward direction)
    5. Verify step dot indicator appears at the bottom (not on Welcome page)
    6. Verify security badges (E2EE, Local Keys, LAN Discovery) are visible in bottom-right
    7. Try the Join flow: Welcome -> Select Device -> Enter Passphrase -- verify forward slide animation
    8. Resize window to test responsive behavior at different widths
    9. Verify consistent look across the entire flow -- no visual glitches
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/UX issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `bunx vitest run src/pages/ --reporter=verbose` -- all tests pass (existing + new)
- `grep -r "lg:" src/pages/setup/*.tsx src/pages/SetupPage.tsx` -- returns 0 matches
- `grep -r "StepLayout" src/pages/setup/CreatePassphraseStep.tsx src/pages/setup/JoinPickDeviceStep.tsx src/pages/setup/JoinVerifyPassphraseStep.tsx src/pages/setup/PairingConfirmStep.tsx src/pages/setup/SetupDoneStep.tsx` -- all 5 files contain StepLayout import
- `grep "flex-col" src/pages/setup/WelcomeStep.tsx` -- vertical layout confirmed
- `grep "grid-cols-2" src/pages/setup/WelcomeStep.tsx` -- no horizontal grid
- `grep "StepDotIndicator" src/pages/SetupPage.tsx` -- dot indicator wired
- `grep "ProcessingJoinStep" src/pages/SetupPage.tsx` -- extracted component used
</verification>

<success_criteria>

- All step components (except WelcomeStep) use StepLayout for layout and animation
- WelcomeStep uses vertical card layout with unified animation
- SetupPage tracks animation direction and passes to all steps
- Step dot indicator shows progress in create and join flows
- Security badges use sm: breakpoint
- Zero lg: breakpoints in entire setup flow
- Overflow handling correct per step type
- All tests pass (existing regression + new)
- Visual verification confirms cross-platform consistent UX
  </success_criteria>

<output>
After completion, create `.planning/phases/07-redesign-setup-flow-ux-for-cross-platform-consistency/07-02-SUMMARY.md`
</output>
