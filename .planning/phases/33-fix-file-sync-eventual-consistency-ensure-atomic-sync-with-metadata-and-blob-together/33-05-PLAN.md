---
phase: 33-fix-file-sync-eventual-consistency-ensure-atomic-sync-with-metadata-and-blob-together
plan: 05
type: execute
wave: 5
depends_on:
  - 33-04
files_modified:
  - src/components/clipboard/ClipboardContent.tsx
  - src/components/clipboard/ClipboardItemRow.tsx
  - src/components/clipboard/ClipboardPreview.tsx
  - src/components/clipboard/FileContextMenu.tsx
  - src/i18n/locales/en-US.json
  - src/i18n/locales/zh-CN.json
autonomous: true
requirements:
  - FSYNC-CONSISTENCY

must_haves:
  truths:
    - 'File entries visibly distinguish pending, transferring, completed, and failed'
    - 'Copy is disabled for pending, transferring, and failed entries; Delete stays available'
    - 'A failed entry after restart still renders as failed because the state comes from command data'
  artifacts:
    - path: 'src/components/clipboard/ClipboardItemRow.tsx'
      provides: 'State-aware list rendering for file entries'
      contains: 'pending|transferring|failed'
    - path: 'src/components/clipboard/ClipboardPreview.tsx'
      provides: 'Preview-side status and failure reason rendering'
      contains: 'transfer'
    - path: 'src/components/clipboard/FileContextMenu.tsx'
      provides: 'State-aware Copy disable behavior for non-completed file entries'
      contains: 'disabled'
  key_links:
    - from: 'src/components/clipboard/FileContextMenu.tsx'
      to: 'src/store/slices/fileTransferSlice.ts'
      via: 'Copy action gates on durable entry transfer status'
      pattern: 'entryStatus|transfer status'
    - from: 'src/components/clipboard/ClipboardItemRow.tsx'
      to: 'src/store/slices/fileTransferSlice.ts'
      via: 'row badge and icon follow durable entry status'
      pattern: 'entryStatus'
---

<objective>
Render the durable backend transfer contract in the clipboard UI and gate actions on it.

Purpose: remove the phantom-ready UX where an entry looks usable before the blob lifecycle has actually completed.
Output: stateful file list/preview/context-menu behavior for pending, transferring, completed, and failed entries.

**Scope note:** Retry functionality (re-request failed transfers from sender) is explicitly NOT in Phase 33 scope. Failed entries show status only; retry is deferred to a future phase.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-fix-file-sync-eventual-consistency-ensure-atomic-sync-with-metadata-and-blob-together/32-CONTEXT.md
@.planning/phases/33-fix-file-sync-eventual-consistency-ensure-atomic-sync-with-metadata-and-blob-together/32-RESEARCH.md
@.planning/phases/33-fix-file-sync-eventual-consistency-ensure-atomic-sync-with-metadata-and-blob-together/33-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render file transfer states in list and preview</name>
  <files>
    src/components/clipboard/ClipboardContent.tsx
    src/components/clipboard/ClipboardItemRow.tsx
    src/components/clipboard/ClipboardPreview.tsx
    src/i18n/locales/en-US.json
    src/i18n/locales/zh-CN.json
  </files>
  <action>
    1. Render explicit file status UI for:
       - `pending`
       - `transferring`
       - `completed`
       - `failed`

    2. Use durable entry status, not just transient progress, to decide badges, helper text, and failure detail rendering.

    3. Ensure a restarted receiver showing a reconciled failed entry renders correctly without any new event arriving.

    4. Add `aria-label` to status badges so screen readers can announce the transfer state (e.g., "File transfer pending", "File transfer failed").

  </action>
  <verify>
    <automated>bun run test -- --run -t "Clipboard file state"</automated>
  </verify>
  <done>
    - list and preview reflect durable transfer truth
    - failed/orphaned entries remain visible and understandable after restart
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate Copy on durable status while preserving Delete</name>
  <files>
    src/components/clipboard/FileContextMenu.tsx
    src/components/clipboard/ClipboardContent.tsx
  </files>
  <action>
    1. Disable Copy for `pending`, `transferring`, and `failed` file entries.

    2. Keep Delete available for every transfer state.

    3. Show state-aware disabled text or tooltip so the user understands why Copy is blocked (e.g., "File transfer in progress" / "File transfer failed").

    4. Add `aria-disabled` to the disabled Copy button for accessibility compliance.

  </action>
  <verify>
    <automated>bun run test -- --run -t "Clipboard file actions"</automated>
  </verify>
  <done>
    - Copy is blocked until the entry is truly completed
    - Delete remains available for cleanup and recovery flows
  </done>
</task>

</tasks>

<verification>
1. UI tests prove failed status is visible from initial command data alone
2. Action tests prove Copy is disabled for non-completed states and Delete remains enabled
</verification>

<success_criteria>

- Users never see a phantom file entry that looks ready when the transfer actually failed
- File actions match the durable backend state machine
  </success_criteria>

<output>
After completion, create `.planning/phases/33-fix-file-sync-eventual-consistency-ensure-atomic-sync-with-metadata-and-blob-together/33-05-SUMMARY.md`
</output>
