---
phase: 23-distributed-tracing-with-trace-view-visualization-for-cross-device-observability
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - docs/seq/signals/flow-timeline.json
  - docs/seq/signals/cross-device-flow.json
  - docs/architecture/logging-architecture.md
  - src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
autonomous: false
requirements: []

must_haves:
  truths:
    - 'Seq saved searches exist for flow timeline and cross-device queries'
    - 'Older peer messages without origin_flow_id produce warning logs'
    - 'Documentation covers cross-device tracing capabilities'
    - 'Human verification confirms device_id appears in Seq events'
  artifacts:
    - path: 'docs/seq/signals/flow-timeline.json'
      provides: 'Saved search for flow timeline queries'
      contains: 'flow_id'
    - path: 'docs/seq/signals/cross-device-flow.json'
      provides: 'Saved search for cross-device flow queries'
      contains: 'origin_flow_id'
    - path: 'docs/architecture/logging-architecture.md'
      provides: 'Cross-device tracing documentation'
      contains: 'Cross-Device'
    - path: 'src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs'
      provides: 'Warning log for missing origin_flow_id'
      contains: 'origin_flow_id.*older version'
  key_links:
    - from: 'wiring.rs'
      to: 'Seq events'
      via: 'warn! macro'
      pattern: 'origin_flow_id.*warn'
---

<objective>
Create Seq saved searches/signals for cross-device flow queries, add graceful degradation warning for older peers, and extend documentation.

Purpose: Enable developers to query cross-device flows in Seq UI, handle older peers gracefully, and document the new capabilities.
Output: Seq signal JSON configs, warning logs for missing origin_flow_id, documentation extended.
</objective>

<context>
@src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs
@docs/architecture/logging-architecture.md
@.planning/phases/23-distributed-tracing-with-trace-view-visualization-for-cross-device-observability/23-01-SUMMARY.md

# Key reference from prior plan

# origin_flow_id already captured in span at wiring.rs:loop.clipboard.receive_message

</context>

<tasks>

<task type="auto">
<name>Task 1: Create Seq signal JSON configs for cross-device queries</name>
<files>docs/seq/signals/flow-timeline.json, docs/seq/signals/cross-device-flow.json</files>
  <action>
    1. Create docs/seq/signals/ directory
    2. Create flow-timeline.json - saved search for viewing all stages of a flow_id:
       - Title: "Flow Timeline"
       - Query: filter by flow_id field
       - Show: flow_id, stage, device_id, timestamp, message
    3. Create cross-device-flow.json - saved search for viewing sender+receiver flows:
       - Title: "Cross-Device Flow"
       - Query: filter by flow_id OR origin_flow_id
       - Show: flow_id, origin_flow_id, device_id, stage, timestamp, message
    4. Include human-readable signal expressions as comments in JSON or separate .md files
  </action>
  <verify>
    <automated>ls -la docs/seq/signals/</automated>
  </verify>
  <done>Two signal JSON files created in docs/seq/signals/</done>
</task>

<task type="auto">
<name>Task 2: Add warning log for missing origin_flow_id on inbound messages</name>
<files>src-tauri/crates/uc-tauri/src/bootstrap/wiring.rs</files>
  <action>
    1. In the clipboard receive loop (look for where origin_flow_id_display is set), add warning log:
       ```rust
       if message.origin_flow_id.is_none() {
           warn!(
               message_id = %message.id,
               origin_device_id = %message.origin_device_id,
               "Inbound message has no origin_flow_id (sender may be an older version)"
           );
       }
       ```
    2. Place this check after receiving the message but before processing
  </action>
  <verify>
    <automated>cd /home/wuy6/myprojects/UniClipboard/src-tauri && cargo check -p uc-tauri 2>&1 | head -30</automated>
  </verify>
  <done>Warning logged when inbound message has no origin_flow_id</done>
</task>

<task type="auto">
<name>Task 3: Extend logging-architecture.md documentation</name>
<files>docs/architecture/logging-architecture.md</files>
  <action>
    1. Add new section: "Cross-Device Tracing"
    2. Document device_id injection: every CLEF event includes device_id field
    3. Document origin_flow_id linking: sender flow_id preserved as origin_flow_id on receiver
    4. Document Seq signal queries: how to use the saved searches in docs/seq/signals/
    5. Document graceful degradation: older peers log warnings, no crashes
    6. Document docker-compose.seq.yml changes for LAN access
  </action>
  <verify>
    <automated>grep -n "Cross-Device" docs/architecture/logging-architecture.md</automated>
  </verify>
  <done>Documentation extended with cross-device tracing section</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Complete cross-device tracing setup: device_id in events, Seq signals, documentation</what-built>
  <how-to-verify>
    1. Start Seq: docker compose -f docker-compose.seq.yml up -d
    2. Set UC_SEQ_URL=http://localhost:5341
    3. Run app and generate some clipboard events
    4. In Seq UI: verify device_id field appears in events
    5. Test cross-device query: filter by device_id or origin_flow_id
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Signal files exist, warning log compiles, documentation updated, human verifies Seq queries work.
</verification>

<success_criteria>

- Seq signal JSON configs created in docs/seq/signals/
- Warning logged for missing origin_flow_id on older peer messages
- Documentation extended covering cross-device tracing
- Human verification: device_id appears in Seq events, queries work
  </success_criteria>

<output>
After completion, create `.planning/phases/23-distributed-tracing-with-trace-view-visualization-for-cross-device-observability/23-02-SUMMARY.md`
</output>
