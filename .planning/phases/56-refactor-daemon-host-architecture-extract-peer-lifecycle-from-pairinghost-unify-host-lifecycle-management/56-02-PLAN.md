---
phase: 56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management
plan: 02
type: execute
wave: 2
depends_on:
  - 56-01
files_modified:
  - src-tauri/crates/uc-daemon/src/peers/mod.rs
  - src-tauri/crates/uc-daemon/src/peers/monitor.rs
  - src-tauri/crates/uc-daemon/src/lib.rs
  - src-tauri/crates/uc-daemon/src/pairing/host.rs
autonomous: true
requirements:
  - PH56-01
must_haves:
  truths:
    - "Peer lifecycle websocket updates are produced by PeerMonitor instead of PairingHost."
    - "PairingHost network processing is limited to pairing protocol messages and transport failures."
    - "Peer-discovery behavior stays unchanged while PeerMonitor uses its own network-event subscription."
  artifacts:
    - path: "src-tauri/crates/uc-daemon/src/peers/monitor.rs"
      provides: "PeerMonitor service owning peer lifecycle event handling"
    - path: "src-tauri/crates/uc-daemon/src/pairing/host.rs"
      provides: "Pairing-only protocol loop with renamed responsibility"
  key_links:
    - from: "src-tauri/crates/uc-daemon/src/peers/monitor.rs"
      to: "runtime.wiring_deps().network_ports.events"
      via: "subscribe_events"
      pattern: "subscribe_events"
    - from: "src-tauri/crates/uc-daemon/src/peers/monitor.rs"
      to: "CoreUseCases::new(runtime.as_ref()).get_p2p_peers_snapshot()"
      via: "full peer snapshot emission"
      pattern: "get_p2p_peers_snapshot"
    - from: "src-tauri/crates/uc-daemon/src/pairing/host.rs"
      to: "NetworkEvent::PairingMessageReceived|NetworkEvent::PairingFailed"
      via: "run_pairing_protocol_loop"
      pattern: "PairingMessageReceived|PairingFailed"
---

<objective>
Extract peer lifecycle handling into `PeerMonitor` and leave `DaemonPairingHost` responsible only for pairing protocol work.

Purpose: Remove mixed responsibilities from the host before the daemon app lifecycle is unified.
Output: `PeerMonitor`, renamed pairing protocol loop, and preserved peer websocket payload behavior.
</objective>

<execution_context>
Use the repository GSD execution workflow already loaded by the orchestrator.
Validation contract: @.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-VALIDATION.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-CONTEXT.md
@.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-RESEARCH.md
@.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-VALIDATION.md
@.planning/phases/46-daemon-pairing-host-migration-move-pairing-orchestrator-action-loops-and-network-event-handling-out-of-tauri/46-01-SUMMARY.md
@.planning/phases/52-daemon-space-access-ssot/52-01-SUMMARY.md
@src-tauri/crates/uc-daemon/src/pairing/host.rs
@src-tauri/crates/uc-daemon/src/workers/peer_discovery.rs
@src-tauri/crates/uc-daemon/src/api/types.rs

<interfaces>
From `src-tauri/crates/uc-daemon/src/pairing/host.rs`:

```rust
async fn run_pairing_network_event_loop(
    runtime: Arc<CoreRuntime>,
    setup_orchestrator: Arc<SetupOrchestrator>,
    space_access_orchestrator: Arc<SpaceAccessOrchestrator>,
    pairing_orchestrator: Arc<PairingOrchestrator>,
    state: Arc<RwLock<RuntimeState>>,
    active_session_id: Arc<RwLock<Option<String>>>,
    discoverability: Arc<LeaseRegistry>,
    participant_readiness: Arc<LeaseRegistry>,
    event_tx: broadcast::Sender<DaemonWsEvent>,
    cancel: CancellationToken,
) -> anyhow::Result<()>;
```

Current peer event branches to move:

```rust
NetworkEvent::PeerDiscovered(_peer)
NetworkEvent::PeerLost(_peer_id)
NetworkEvent::PeerNameUpdated { peer_id, device_name }
NetworkEvent::PeerConnected(peer)
NetworkEvent::PeerDisconnected(peer_id)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PeerMonitor as the dedicated peer lifecycle service</name>
  <files>
src-tauri/crates/uc-daemon/src/peers/mod.rs
src-tauri/crates/uc-daemon/src/peers/monitor.rs
src-tauri/crates/uc-daemon/src/lib.rs
  </files>
  <read_first>
- src-tauri/crates/uc-daemon/src/pairing/host.rs
- src-tauri/crates/uc-daemon/src/workers/peer_discovery.rs
- src-tauri/crates/uc-daemon/src/api/types.rs
- src-tauri/crates/uc-daemon/src/service.rs
- .planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-CONTEXT.md
  </read_first>
  <behavior>
- Test 1: `PeerMonitor` subscribes to `NetworkEventPort` independently and handles only peer lifecycle events.
- Test 2: `PeerDiscovered` and `PeerLost` still emit `peers.changed` with a full peer snapshot payload.
- Test 3: `PeerNameUpdated`, `PeerConnected`, and `PeerDisconnected` still emit the same websocket payload types as before.
  </behavior>
  <action>Create a new peer-level module at `src-tauri/crates/uc-daemon/src/peers/monitor.rs` with `pub struct PeerMonitor`. Implement `DaemonService` there. `PeerMonitor` must own the peer-only `NetworkEvent` arms from D-01: `PeerDiscovered`, `PeerLost`, `PeerConnected`, `PeerDisconnected`, and `PeerNameUpdated`. Keep D-02 by calling `network_events.subscribe_events()` inside `PeerMonitor`; do not introduce a central router or shared dispatcher. Reuse the existing `DaemonWsEvent` payload types and `CoreUseCases::new(runtime.as_ref()).get_p2p_peers_snapshot().execute()` for `peers.changed` full-snapshot emission. Export the new module from `lib.rs`. Move or recreate the peer websocket unit tests beside `PeerMonitor` so the ownership change is explicit.</action>
  <acceptance_criteria>
- `rg -n "pub struct PeerMonitor" src-tauri/crates/uc-daemon/src/peers/monitor.rs` returns one match.
- `rg -n "impl DaemonService for PeerMonitor" src-tauri/crates/uc-daemon/src/peers/monitor.rs` returns one match.
- `rg -n "NetworkEvent::PeerDiscovered|NetworkEvent::PeerLost|NetworkEvent::PeerConnected|NetworkEvent::PeerDisconnected|NetworkEvent::PeerNameUpdated" src-tauri/crates/uc-daemon/src/peers/monitor.rs` returns five matches.
- `rg -n "peer_discovered_emits_peers_changed_full_payload_with_peer_list|peer_lost_can_emit_peers_changed_with_empty_list" src-tauri/crates/uc-daemon/src/peers/monitor.rs` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-daemon peer_discovered_emits_peers_changed_full_payload_with_peer_list -- --exact && cargo test -p uc-daemon peer_lost_can_emit_peers_changed_with_empty_list -- --exact</automated>
  </verify>
  <done>`PeerMonitor` exists as a first-class daemon component and owns all peer websocket emission logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove peer lifecycle branches from PairingHost and rename the protocol loop</name>
  <files>
src-tauri/crates/uc-daemon/src/pairing/host.rs
  </files>
  <read_first>
- src-tauri/crates/uc-daemon/src/pairing/host.rs
- src-tauri/crates/uc-daemon/src/peers/monitor.rs
- .planning/phases/52-daemon-space-access-ssot/52-01-SUMMARY.md
  </read_first>
  <behavior>
- Test 1: `DaemonPairingHost` no longer matches peer lifecycle events in its network subscription loop.
- Test 2: the renamed loop only handles `PairingMessageReceived` and `PairingFailed` plus existing space-access follow-up helpers.
- Test 3: space-access and transport-failure behavior still lives with the pairing host and keeps existing helper functions.
  </behavior>
  <action>In `src-tauri/crates/uc-daemon/src/pairing/host.rs`, rename `run_pairing_network_event_loop` to `run_pairing_protocol_loop` per D-08 and update the internal spawn call to use the new name. Remove the peer lifecycle `NetworkEvent` arms from the host loop entirely; those events must now be owned only by `PeerMonitor`. Keep the pairing protocol arms (`PairingMessageReceived`, `PairingFailed`) and the existing space-access side effects in place. Do not merge `PeerDiscoveryWorker` into `PeerMonitor`; leave `PeerDiscoveryWorker` unchanged beyond imports or minor compile fixes, per D-09 and the research guidance.</action>
  <acceptance_criteria>
- `rg -n "run_pairing_protocol_loop" src-tauri/crates/uc-daemon/src/pairing/host.rs` returns matches.
- `rg -n "run_pairing_network_event_loop" src-tauri/crates/uc-daemon/src/pairing/host.rs` returns no matches.
- `rg -n "NetworkEvent::PeerDiscovered|NetworkEvent::PeerLost|NetworkEvent::PeerConnected|NetworkEvent::PeerDisconnected|NetworkEvent::PeerNameUpdated" src-tauri/crates/uc-daemon/src/pairing/host.rs` returns no matches.
- `rg -n "NetworkEvent::PairingMessageReceived|NetworkEvent::PairingFailed" src-tauri/crates/uc-daemon/src/pairing/host.rs` still returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo check -p uc-daemon && cargo test -p uc-daemon peer_discovery_worker_starts_network_and_announces_device_name -- --exact</automated>
  </verify>
  <done>`DaemonPairingHost` keeps only pairing protocol responsibilities and all peer lifecycle handling has moved out.</done>
</task>

</tasks>

<verification>
Run the task-level commands, then confirm `rg -n "PeerDiscovered|PeerLost|PeerConnected|PeerDisconnected|PeerNameUpdated" src-tauri/crates/uc-daemon/src/pairing/host.rs` returns no matches.
</verification>

<success_criteria>
Peer websocket emissions come from `PeerMonitor`, `DaemonPairingHost` owns only pairing protocol work, and no new event-router layer is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/56-refactor-daemon-host-architecture-extract-peer-lifecycle-from-pairinghost-unify-host-lifecycle-management/56-02-SUMMARY.md`
</output>
