---
phase: 54-extract-daemon-client-and-realtime-infrastructure-from-uc-tauri
plan: '02'
type: execute
wave: 2
depends_on:
  - '01'
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/crates/uc-tauri/Cargo.toml
  - src-tauri/crates/uc-tauri/src/lib.rs
  - src-tauri/src/main.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/mod.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/run.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs
  - src-tauri/crates/uc-tauri/src/commands/pairing.rs
  - src-tauri/crates/uc-tauri/src/commands/setup.rs
  - src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs
  - src-tauri/crates/uc-tauri/tests/daemon_bootstrap_contract.rs
  - src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs
  - src-tauri/crates/uc-tauri/src/daemon_client/mod.rs
  - src-tauri/crates/uc-tauri/src/daemon_client/pairing.rs
  - src-tauri/crates/uc-tauri/src/daemon_client/query.rs
  - src-tauri/crates/uc-tauri/src/daemon_client/query_tests.rs
  - src-tauri/crates/uc-tauri/src/daemon_client/setup.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs
  - src-tauri/crates/uc-tauri/src/bootstrap/realtime_runtime.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - 'uc-daemon-client registered as workspace member'
    - 'uc-tauri directly depends on uc-daemon-client'
    - 'uc-tauri/src/lib.rs has no daemon_client module'
    - 'bootstrap/mod.rs has no re-exports from uc_daemon_client (D-10: no stub)'
    - 'main.rs imports DaemonConnectionState from uc_daemon_client'
    - 'runtime.rs has no DaemonConnectionState'
    - 'run.rs imports DaemonConnectionState from uc_daemon_client'
    - 'setup_pairing_bridge.rs uses DaemonPairingClient from uc_daemon_client'
    - 'commands/pairing.rs uses DaemonPairingClient and DaemonQueryClient from uc_daemon_client'
    - 'commands/setup.rs uses DaemonSetupClient from uc_daemon_client'
    - 'All 3 test files use uc_daemon_client imports'
    - 'Old daemon_client/ directory deleted from uc-tauri'
    - 'Old daemon_ws_bridge.rs and realtime_runtime.rs deleted from uc-tauri bootstrap/'
    - 'uc-tauri compiles with zero errors after extraction'
  artifacts:
    - path: 'src-tauri/Cargo.toml'
      provides: 'uc-daemon-client listed in workspace.members'
      min_lines: 1
    - path: 'src-tauri/crates/uc-tauri/Cargo.toml'
      provides: 'uc-daemon-client in [dependencies], direct reqwest/tokio-tungstenite/tokio-util/futures-util removed'
      min_lines: 1
    - path: 'src-tauri/crates/uc-tauri/src/lib.rs'
      provides: 'no pub mod daemon_client declaration'
      min_lines: 1
    - path: 'src-tauri/src/main.rs'
      provides: 'use uc_daemon_client::DaemonConnectionState import present'
      min_lines: 1
    - path: 'src-tauri/crates/uc-tauri/src/bootstrap/mod.rs'
      provides: 'no re-exports from uc_daemon_client (all call sites import directly per D-10)'
      min_lines: 1
    - path: 'src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs'
      provides: 'DaemonConnectionState struct and impl absent'
      min_lines: 1
    - path: 'src-tauri/crates/uc-tauri/src/bootstrap/run.rs'
      provides: 'DaemonConnectionState imported from uc_daemon_client'
      min_lines: 1
    - path: 'src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs'
      provides: 'DaemonPairingClient imported from uc_daemon_client'
      min_lines: 1
    - path: 'src-tauri/crates/uc-tauri/src/commands/pairing.rs'
      provides: 'DaemonPairingClient and DaemonQueryClient imported from uc_daemon_client'
      min_lines: 1
    - path: 'src-tauri/crates/uc-tauri/src/commands/setup.rs'
      provides: 'DaemonSetupClient imported from uc_daemon_client'
      min_lines: 1
    - path: 'src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs'
      provides: 'DaemonWsBridge imported from uc_daemon_client'
      min_lines: 1
    - path: 'src-tauri/crates/uc-tauri/tests/daemon_bootstrap_contract.rs'
      provides: 'DaemonConnectionState imported from uc_daemon_client'
      min_lines: 1
    - path: 'src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs'
      provides: 'DaemonConnectionState imported from uc_daemon_client'
      min_lines: 1
    - path: 'src-tauri/crates/uc-tauri/src/daemon_client/'
      provides: 'Directory deleted'
      min_lines: 0
    - path: 'src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs'
      provides: 'File deleted'
      min_lines: 0
    - path: 'src-tauri/crates/uc-tauri/src/bootstrap/realtime_runtime.rs'
      provides: 'File deleted'
      min_lines: 0
  key_links:
    - from: 'src-tauri/src/main.rs'
      to: 'src-tauri/crates/uc-daemon-client/src/connection.rs'
      via: 'use uc_daemon_client::DaemonConnectionState'
      pattern: 'use uc_daemon_client::DaemonConnectionState'
    - from: 'src-tauri/crates/uc-tauri/src/commands/pairing.rs'
      to: 'src-tauri/crates/uc-daemon-client/src/http/pairing.rs'
      via: 'use uc_daemon_client::http::DaemonPairingClient'
      pattern: 'use uc_daemon_client::http::DaemonPairingClient'
    - from: 'src-tauri/crates/uc-tauri/src/commands/setup.rs'
      to: 'src-tauri/crates/uc-daemon-client/src/http/setup.rs'
      via: 'use uc_daemon_client::http::DaemonSetupClient'
      pattern: 'use uc_daemon_client::http::DaemonSetupClient'
    - from: 'src-tauri/crates/uc-tauri/src/bootstrap/run.rs'
      to: 'src-tauri/crates/uc-daemon-client/src/connection.rs'
      via: 'use uc_daemon_client::DaemonConnectionState'
      pattern: 'use uc_daemon_client::DaemonConnectionState'
---

<objective>
Update all uc-tauri call sites to import from uc-daemon-client, delete the old daemon_client/ module and old bootstrap files, register the new crate in the workspace, and verify the full uc-tauri crate compiles.
</objective>

<context>
@src-tauri/crates/uc-daemon-client/src/lib.rs
@src-tauri/Cargo.toml
@src-tauri/crates/uc-tauri/Cargo.toml
@src-tauri/crates/uc-tauri/src/lib.rs
@src-tauri/src/main.rs
@src-tauri/crates/uc-tauri/src/bootstrap/mod.rs
@src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs
@src-tauri/crates/uc-tauri/src/bootstrap/run.rs
@src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs
@src-tauri/crates/uc-tauri/src/commands/pairing.rs
@src-tauri/crates/uc-tauri/src/commands/setup.rs
@src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs
@src-tauri/crates/uc-tauri/tests/daemon_bootstrap_contract.rs
@src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register uc-daemon-client in workspace and uc-tauri deps</name>
  <files>
    - src-tauri/Cargo.toml
    - src-tauri/crates/uc-tauri/Cargo.toml
  </files>
  <read_first>
    - src-tauri/Cargo.toml
    - src-tauri/crates/uc-tauri/Cargo.toml
  </read_first>
  <action>
In `src-tauri/Cargo.toml`:
- Add `"crates/uc-daemon-client"` to the `workspace.members` array (at the end, before the closing `]`)

In `src-tauri/crates/uc-tauri/Cargo.toml`:

- Add `uc-daemon-client = { path = "../uc-daemon-client" }` to the `[dependencies]` section (after the uc-daemon line)
- Remove `reqwest` from direct dependencies (uc-daemon-client now provides it)
- Remove `tokio-tungstenite = "0.24"` (uc-daemon-client provides it)
- Remove `tokio-util = "0.7"` (uc-daemon-client provides it)
- Remove `futures-util = "0.3"` (uc-daemon-client provides it)
- Keep `async-trait = "0.1"` (still needed for uc-tauri's own async trait usage)

Before removing those deps, verify no other uc-tauri source files use them directly (excluding uc-daemon-client imports). Run:
`grep -rn "tokio_tungstenite\|tokio_util\|futures_util\|reqwest::" src-tauri/crates/uc-tauri/src/ | grep -v "uc_daemon_client"`
If nothing is found (or only in the old daemon_client/ files being deleted), safely remove those deps.
</action>
<verify>grep 'uc-daemon-client' src-tauri/Cargo.toml && grep 'uc-daemon-client' src-tauri/crates/uc-tauri/Cargo.toml</verify>
<done>uc-daemon-client registered as workspace member and uc-tauri dependency</done>
</task>

<task type="auto">
  <name>Task 2: Update uc-tauri/lib.rs, main.rs, bootstrap/mod.rs</name>
  <files>
    - src-tauri/crates/uc-tauri/src/lib.rs
    - src-tauri/src/main.rs
    - src-tauri/crates/uc-tauri/src/bootstrap/mod.rs
  </files>
  <read_first>
    - src-tauri/crates/uc-tauri/src/lib.rs
    - src-tauri/src/main.rs
    - src-tauri/crates/uc-tauri/src/bootstrap/mod.rs
    - src-tauri/crates/uc-daemon-client/src/lib.rs
  </read_first>
  <action>
**src-tauri/crates/uc-tauri/src/lib.rs:**
- Remove `pub mod daemon_client;` (the module no longer exists in uc-tauri)
- Keep all other module declarations unchanged

**src-tauri/src/main.rs:**

- Change `use uc_tauri::bootstrap::{..., DaemonConnectionState, ...};` to remove `DaemonConnectionState` from this import (it is no longer re-exported from uc_tauri::bootstrap)
- Add `use uc_daemon_client::DaemonConnectionState;` as a separate import line

**src-tauri/crates/uc-tauri/src/bootstrap/mod.rs:**

- Remove `pub mod daemon_client;`
- Remove `pub mod daemon_ws_bridge;`
- Remove `pub mod realtime_runtime;`
- Remove `pub use daemon_ws_bridge::DaemonWsBridge;` (line 27)
- Remove `pub use realtime_runtime::{install_daemon_setup_pairing_facade, start_realtime_runtime};` (line 29)
- Remove `pub use runtime::DaemonConnectionState;` (part of line 33)
- Change `pub use runtime::{create_app, create_runtime, AppRuntime, AppUseCases, DaemonConnectionState};` to `pub use runtime::{create_app, create_runtime, AppRuntime, AppUseCases};`
- Do NOT add any re-exports from uc_daemon_client (D-10: no re-export stubs — all call sites import directly)
- Keep `pub mod daemon_lifecycle;` (Phase 55 scope)
- Keep `pub mod setup_pairing_bridge;` (Phase 55 scope)
- Keep `pub use setup_pairing_bridge::{build_setup_pairing_facade, DaemonBackedSetupPairingFacade};` (Phase 55)
  </action>
  <verify>cargo check -p uc-tauri 2>&1 | head -30</verify>
  <done>uc-tauri/lib.rs has no daemon_client module; bootstrap/mod.rs has no re-exports from uc_daemon_client (all call sites import directly); main.rs imports DaemonConnectionState from uc_daemon_client</done>
  </task>

<task type="auto">
  <name>Task 3: Update bootstrap/runtime.rs (remove DaemonConnectionState, keep AppRuntime)</name>
  <files>src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs</files>
  <read_first>
    - src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs
  </read_first>
  <action>
In `src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs`:
- Delete the entire `DaemonConnectionState` struct definition (the `#[derive(Clone, Default)] pub struct DaemonConnectionState...` block and its impl — approximately lines 48-78)
- Delete the entire `#[cfg(test)]` module containing `daemon_connection_state_stores_connection_info_in_memory` (this test was moved to uc-daemon-client)
- Keep `DaemonBootstrapOwnershipState` and `DaemonBootstrapOwnershipSnapshot` (these remain in uc-tauri, not extracted)
- Keep everything else (AppRuntime, UseCases, create_app, create_runtime, AppUseCases, etc.)
- Remove any `use` imports that are now unused after removing DaemonConnectionState (e.g., if `uc_daemon::api::auth::DaemonConnectionInfo` was imported but is no longer used)
</action>
<verify>cargo check -p uc-tauri 2>&1 | grep -E "^error" | head -10</verify>
<done>runtime.rs compiles without DaemonConnectionState; DaemonBootstrapOwnershipState remains</done>
</task>

<task type="auto">
  <name>Task 4: Update bootstrap/run.rs imports</name>
  <files>src-tauri/crates/uc-tauri/src/bootstrap/run.rs</files>
  <read_first>
    - src-tauri/crates/uc-tauri/src/bootstrap/run.rs
  </read_first>
  <action>
In `src-tauri/crates/uc-tauri/src/bootstrap/run.rs`:
- Change `use super::runtime::{DaemonBootstrapOwnershipState, DaemonConnectionState};` to `use super::runtime::DaemonBootstrapOwnershipState;`
- Add `use uc_daemon_client::DaemonConnectionState;`
- All other occurrences of `DaemonConnectionState` in the file remain (the type is still used in function signatures and function bodies) — only the import source changes
</action>
<verify>cargo check -p uc-tauri 2>&1 | grep "^error" | head -10</verify>
<done>run.rs compiles; DaemonConnectionState used but imported from uc_daemon_client</done>
</task>

<task type="auto">
  <name>Task 5: Update bootstrap/setup_pairing_bridge.rs</name>
  <files>src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs</files>
  <read_first>
    - src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs
  </read_first>
  <action>
In `src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs`:
- Change `use crate::bootstrap::DaemonConnectionState;` to `use uc_daemon_client::DaemonConnectionState;`
- Change `use crate::daemon_client::TauriDaemonPairingClient;` to `use uc_daemon_client::http::DaemonPairingClient;`
- Change all `TauriDaemonPairingClient::new` to `DaemonPairingClient::new`
- All other code remains unchanged (the struct field `connection_state: DaemonConnectionState` stays the same type, only the import source changes)
</action>
<verify>cargo check -p uc-tauri 2>&1 | grep "^error" | head -10</verify>
<done>setup_pairing_bridge.rs compiles; all TauriDaemonPairingClient references replaced with DaemonPairingClient</done>
</task>

<task type="auto">
  <name>Task 6: Update commands/pairing.rs and commands/setup.rs</name>
  <files>
    - src-tauri/crates/uc-tauri/src/commands/pairing.rs
    - src-tauri/crates/uc-tauri/src/commands/setup.rs
  </files>
  <read_first>
    - src-tauri/crates/uc-tauri/src/commands/pairing.rs
    - src-tauri/crates/uc-tauri/src/commands/setup.rs
  </read_first>
  <action>
**src-tauri/crates/uc-tauri/src/commands/pairing.rs:**
- Change `use crate::bootstrap::DaemonConnectionState;` to `use uc_daemon_client::DaemonConnectionState;`
- Change `use crate::daemon_client::{DaemonPairingRequestError, TauriDaemonPairingClient, TauriDaemonQueryClient,};` to `use uc_daemon_client::http::{DaemonPairingClient, DaemonPairingRequestError, DaemonQueryClient};`
- Change all `TauriDaemonPairingClient` to `DaemonPairingClient`
- Change all `TauriDaemonQueryClient` to `DaemonQueryClient`
- Keep `DaemonPairingRequestError` as-is

**src-tauri/crates/uc-tauri/src/commands/setup.rs:**

- Change `use crate::bootstrap::{AppRuntime, DaemonConnectionState};` to two lines: `use crate::bootstrap::AppRuntime;` and `use uc_daemon_client::DaemonConnectionState;`
- Change `use crate::daemon_client::TauriDaemonSetupClient;` to `use uc_daemon_client::http::DaemonSetupClient;`
- Change all `TauriDaemonSetupClient` to `DaemonSetupClient`
  </action>
  <verify>cargo check -p uc-tauri 2>&1 | grep "^error" | head -10</verify>
  <done>commands/pairing.rs and commands/setup.rs compile; all TauriDaemon\*Client replaced</done>
  </task>

<task type="auto">
  <name>Task 7: Update test files</name>
  <files>
    - src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs
    - src-tauri/crates/uc-tauri/tests/daemon_bootstrap_contract.rs
    - src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs
  </files>
  <read_first>
    - src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs
    - src-tauri/crates/uc-tauri/tests/daemon_bootstrap_contract.rs
    - src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs
  </read_first>
  <action>
**src-tauri/crates/uc-tauri/tests/daemon_ws_bridge.rs:**
- Change `use uc_tauri::bootstrap::daemon_ws_bridge::{BridgeState, DaemonWsBridge, DaemonWsBridgeConfig, ScriptedDaemonWsConnector};` to `use uc_daemon_client::ws_bridge::{BridgeState, DaemonWsBridge, DaemonWsBridgeConfig, ScriptedDaemonWsConnector};`
- Split the `use uc_tauri::bootstrap::{install_daemon_setup_pairing_facade, DaemonConnectionState, SetupAssemblyPorts, SetupPairingFacadePort};` line into two imports:
  - Add `use uc_daemon_client::{DaemonConnectionState, realtime::install_daemon_setup_pairing_facade};` (DaemonConnectionState from uc-daemon-client, install_daemon_setup_pairing_facade from uc-daemon-client's realtime submodule)
  - Keep `use uc_tauri::bootstrap::{SetupAssemblyPorts, SetupPairingFacadePort};` (these types remain defined in uc-tauri and are used in test assertions on lines 537, 636)
- Change `use uc_tauri::bootstrap::runtime::DaemonBootstrapOwnershipState;` — keep this import (still in uc-tauri)
- Change `use crate::daemon_client::TauriDaemonPairingClient;` to `use uc_daemon_client::http::DaemonPairingClient;`
- Change all `TauriDaemonPairingClient` to `DaemonPairingClient` in the test code

**src-tauri/crates/uc-tauri/tests/daemon_bootstrap_contract.rs:**

- Change `use uc_tauri::bootstrap::runtime::{DaemonBootstrapOwnershipState, DaemonConnectionState};` to two lines:
  - `use uc_tauri::bootstrap::runtime::DaemonBootstrapOwnershipState;`
  - `use uc_daemon_client::DaemonConnectionState;`

**src-tauri/crates/uc-tauri/tests/daemon_command_shell.rs:**

- Change `use uc_tauri::bootstrap::DaemonConnectionState;` to `use uc_daemon_client::DaemonConnectionState;`
  </action>
  <verify>cargo test -p uc-tauri --test daemon_ws_bridge 2>&1 | tail -5; cargo test -p uc-tauri --test daemon_bootstrap_contract 2>&1 | tail -5; cargo test -p uc-tauri --test daemon_command_shell 2>&1 | tail -5</verify>
  <done>All 3 test files compile and tests pass</done>
  </task>

<task type="auto">
  <name>Task 8: Delete old source files from uc-tauri</name>
  <files>
    - DELETE: src-tauri/crates/uc-tauri/src/daemon_client/
    - DELETE: src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs
    - DELETE: src-tauri/crates/uc-tauri/src/bootstrap/realtime_runtime.rs
  </files>
  <read_first>
    - src-tauri/crates/uc-tauri/src/daemon_client/
    - src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs
    - src-tauri/crates/uc-tauri/src/bootstrap/realtime_runtime.rs
  </read_first>
  <action>
Delete the following files and directories using `rm -rf` (bash command):
1. `src-tauri/crates/uc-tauri/src/daemon_client/` (entire directory — contains mod.rs, pairing.rs, query.rs, setup.rs, query_tests.rs)
2. `src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs`
3. `src-tauri/crates/uc-tauri/src/bootstrap/realtime_runtime.rs`

Confirm deletion:

- `ls src-tauri/crates/uc-tauri/src/daemon_client/ 2>&1` must show: No such file or directory
- `ls src-tauri/crates/uc-tauri/src/bootstrap/daemon_ws_bridge.rs 2>&1` must show: No such file or directory
- `ls src-tauri/crates/uc-tauri/src/bootstrap/realtime_runtime.rs 2>&1` must show: No such file or directory
  </action>
  <verify>cargo check -p uc-tauri 2>&1 | grep "^error" | head -10</verify>
  <done>Old source files deleted; uc-tauri compiles cleanly with zero errors</done>
  </task>

</tasks>

<verification>
After all tasks: `cd src-tauri && cargo check -p uc-daemon-client && cargo check -p uc-tauri && cargo test -p uc-daemon-client && cargo test -p uc-tauri --test daemon_ws_bridge --test daemon_bootstrap_contract --test daemon_command_shell`
</verification>

<success_criteria>

- `src-tauri/Cargo.toml` has `"crates/uc-daemon-client"` in workspace members
- `src-tauri/crates/uc-tauri/Cargo.toml` has `uc-daemon-client` in dependencies
- `src-tauri/crates/uc-tauri/src/lib.rs` has no `daemon_client` module
- `src-tauri/crates/uc-tauri/src/bootstrap/mod.rs` has no re-exports from `uc_daemon_client` (all call sites import directly; D-10 no re-export stubs)
- `src-tauri/src/main.rs` imports `DaemonConnectionState` from `uc_daemon_client`
- `src-tauri/crates/uc-tauri/src/bootstrap/runtime.rs` has no `DaemonConnectionState`
- `src-tauri/crates/uc-tauri/src/bootstrap/run.rs` imports `DaemonConnectionState` from `uc_daemon_client`
- `src-tauri/crates/uc-tauri/src/bootstrap/setup_pairing_bridge.rs` uses `DaemonPairingClient`
- `src-tauri/crates/uc-tauri/src/commands/pairing.rs` uses `DaemonPairingClient`, `DaemonQueryClient`, `DaemonConnectionState` from `uc_daemon_client`
- `src-tauri/crates/uc-tauri/src/commands/setup.rs` uses `DaemonSetupClient`, `DaemonConnectionState` from `uc_daemon_client`
- All 3 test files use `uc_daemon_client` imports
- Old daemon_client/ directory deleted from uc-tauri
- Old daemon_ws_bridge.rs and realtime_runtime.rs deleted from uc-tauri bootstrap/
- `cargo check -p uc-tauri` passes with zero errors
- `cargo check -p uc-daemon-client` passes with zero errors
  </success_criteria>

<output>
After completion, create `.planning/phases/54-extract-daemon-client-and-realtime-infrastructure-from-uc-tauri/54-02-SUMMARY.md`
</output>
